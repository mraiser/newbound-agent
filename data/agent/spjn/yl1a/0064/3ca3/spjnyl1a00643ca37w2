{"data":{"attachmentkeynames":["rs"],"desc":"Start the journal tailer (explicit - never autostarts). Sweeps every ~2s: mtime-gated scan of all _patches journals, one store_change perception per new entry, claims bound by source pointer with staleness by hash compare, delivered to agent-executive-perceive. The executive's own journaled acts return through here: the self-model. Killable via stop. The cursor persists at runtime/agent/store_sense.json (the git sensor's pattern): a restart resumes from it, so entries journaled while the sensor was down are perceived; a box with no persisted state starts at now - a fresh start never replays history.","import":"use ndata::dataobject::DataObject;\nuse ndata::dataarray::DataArray;\nuse flowlang::datastore::DataStore;\nuse flowlang::command::Command;\nuse flowlang::flowlang::system::time::time;","params":[],"returntype":"JSONObject","type":"rust"},"id":"spjnyl1a00643ca37w2","readers":[],"time":1787338221506,"username":"system","writers":[]}