[
{"claim": "The store sensor cursor is runtime state: it resets to now on start, so restarts never replay journal history. Sensor-bound claims ride the perception envelope (claims: [{lib, ctl, claim, stale}]) and surface in executive status.last_context as bound/bound_top next to recall's fuzzy matched count - the precise join is the primary signal for store_change perceptions.", "tags": "phase2", "confidence": "high", "time": 1786811602356},
{"claim": "H4 landed across three repos: the system sensor (agent-sensor-system_sense, band-crossing emissions off the grown resource-map probe - one probe two consumers, wired into the tailer loop every ~30s), acoustic consolidation (agent-executive-consolidate_room - quiet gate + cursor, claims to kb.environment via adjudicate incl. voiceprint bindings, tried first on every drive tick), the hollis vocabulary (fork branch claude/harvest-h4-sensor-vocabulary: emit path widened with extra-JSON payload fields; transient/continuous/ambience_shift/state_change/cadence/micro_transient wired with per-event gates and coalescing per the section 6 rubric), and the contract amendment (system_state row + the sensor-quality rubric in section 6)", "detail": "Overlaying hollis into a checkout works exactly like agent/kb/scratch (two symlinks); its crate needs newbound rebuild to generate src (store-only repo convention - generated src is NOT committed there), libasound2-dev for cpal, and ort needs ORT_STRATEGY=system ORT_LIB_LOCATION pointing at a dir containing libonnxruntime.so (the PyPI onnxruntime wheel supplies it) with ORT_PREFER_DYNAMIC_LINK=1 - plus a libonnxruntime.so.1 soname link on LD_LIBRARY_PATH at dylib-load time. api.rs churn from a hollis-bearing rebuild must be reverted in the agent repo before committing.", "tags": "harvest,h4,sensors", "confidence": "high", "time": 1787145334528},
{"claim": "Brick 4 landed (overlay commit 421d7cd, claude/git-first-class): agent-sensor-git_sense is the git sensor - one porcelain-v2 status per registered repo THROUGH dev.git.read (one git engine, no second path), emitting git_state perceptions only on edges (HEAD moved, branch switch, clean<->dirty crossing, ahead/behind change, merge/rebase/cherry-pick markers appearing or clearing).", "detail": "State persists at runtime/agent/git_sense.json, so a restart announces a standing dirty/mid-merge condition ONCE instead of silently re-seeding - the system sensor's silent-seed flaw fixed by design. Deliberately NO .git mtime gate: worktree edits never touch .git, so a gate would miss exactly the clean->dirty crossing; the ~30s porcelain probe is cheap. Envelopes bind claims whose {repo,path} sources name the repo (brick 3 stamps), stale by working-tree byte compare. Wired into the tailer's ~30s loop, but the sensor was left STOPPED as found - starting it is bound to the executive-start plan item (idle initiative spends frontier calls). En route fix: Command::execute wraps a FLAT return under an \"a\" key, which the sensor unwraps.", "tags": "agent.sensor,dev.git,git_state,perception,edge-trigger,unreviewed", "confidence": "high", "time": 1787317796126},
{"end": true}]
