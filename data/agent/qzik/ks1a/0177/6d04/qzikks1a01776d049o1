{"data":{"attachmentkeynames":["rs"],"desc":"The resource map (spectrum S1): per-GPU total/free memory and free disk where the model subsystem lives. Reads the service's /status resources block when the service answers (torch's view); falls back to nvidia-smi directly when it doesn't, so the map is never absent. Empty gpus on a CPU-only box is an answer, not an error; a GPU whose memory nvidia-smi reports [N/A] (unified-memory parts like GB10) is listed with unmeasurable:true instead of dropped. Disk is probed at the model dir or its first existing ancestor, so the disk fact survives the model subsystem being off. Customers: the posture solver, ring byte-budget warnings, birth-run sizing.","import":"use ndata::dataobject::DataObject;\nuse ndata::dataarray::DataArray;\nuse flowlang::datastore::DataStore;\nuse flowlang::flowlang::system::system_call::system_call;","params":[],"returntype":"JSONObject","type":"rust"},"id":"qzikks1a01776d049o1","readers":[],"time":1787338224918,"username":"system","writers":[]}