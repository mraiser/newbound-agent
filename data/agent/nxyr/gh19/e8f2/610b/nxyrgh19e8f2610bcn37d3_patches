{"data":{"list":[{"author":"migration","cmd":"","facet":"memory","label":"memory: Every LLM provider behind agent.llm's resolver accepts exactl...","new":"[\n  {\n    \"claim\": \"Every LLM provider behind agent.llm's resolver accepts exactly ONE system message, and it must be at index 0.\",\n    \"detail\": \"vLLM's chat template answers a system message at any later index — including index 1, ahead of every user turn — with a hard 400: BadRequestError \\\"System message must be at the beginning.\\\" So client-side context injection (recall layer 4's mode pack) must be APPENDED to the existing system message, never inserted beside it. Appending rather than prepending is also the cache-friendly choice: the curriculum stays a stable prefix, so server-side prefix caching still hits. agent.agentloop's chatTurn does this in convo(), which builds a merged COPY — the venue's own messages array is never mutated.\",\n    \"tags\": \"llm,agent,platform-api,wire\",\n    \"source\": {\n      \"lib\": \"agent\",\n      \"ctl\": \"agentloop\",\n      \"facet\": \"js\",\n      \"hash\": \"6569fbeed2ace744\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1785964641067\n  }\n]\n","old":"","patch_id":"p1","time":1786733933867},{"author":"claude","cmd":"","facet":"memory","label":"memory: __probe__ delete me","new":"[\n  {\n    \"claim\": \"Every LLM provider behind agent.llm's resolver accepts exactly ONE system message, and it must be at index 0.\",\n    \"detail\": \"vLLM's chat template answers a system message at any later index — including index 1, ahead of every user turn — with a hard 400: BadRequestError \\\"System message must be at the beginning.\\\" So client-side context injection (recall layer 4's mode pack) must be APPENDED to the existing system message, never inserted beside it. Appending rather than prepending is also the cache-friendly choice: the curriculum stays a stable prefix, so server-side prefix caching still hits. agent.agentloop's chatTurn does this in convo(), which builds a merged COPY — the venue's own messages array is never mutated.\",\n    \"tags\": \"llm,agent,platform-api,wire\",\n    \"source\": {\n      \"lib\": \"agent\",\n      \"ctl\": \"agentloop\",\n      \"facet\": \"js\",\n      \"hash\": \"6569fbeed2ace744\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1785964641067\n  },\n  {\n    \"claim\": \"__probe__ delete me\",\n    \"detail\": \"probe\",\n    \"tags\": \"probe\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"low\",\n    \"time\": 1786878844003\n  }\n]\n","old":"[\n  {\n    \"claim\": \"Every LLM provider behind agent.llm's resolver accepts exactly ONE system message, and it must be at index 0.\",\n    \"detail\": \"vLLM's chat template answers a system message at any later index — including index 1, ahead of every user turn — with a hard 400: BadRequestError \\\"System message must be at the beginning.\\\" So client-side context injection (recall layer 4's mode pack) must be APPENDED to the existing system message, never inserted beside it. Appending rather than prepending is also the cache-friendly choice: the curriculum stays a stable prefix, so server-side prefix caching still hits. agent.agentloop's chatTurn does this in convo(), which builds a merged COPY — the venue's own messages array is never mutated.\",\n    \"tags\": \"llm,agent,platform-api,wire\",\n    \"source\": {\n      \"lib\": \"agent\",\n      \"ctl\": \"agentloop\",\n      \"facet\": \"js\",\n      \"hash\": \"6569fbeed2ace744\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1785964641067\n  }\n]\n","patch_id":"p2","time":1786878844004},{"author":"claude","facet":"memory","label":"remove probe claim","new":"}","old":"},\n  {\n    \"claim\": \"__probe__ delete me\",\n    \"detail\": \"probe\",\n    \"tags\": \"probe\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"low\",\n    \"time\": 1786878844003\n  }","patch_id":"p3","time":1786878877408},{"author":"claude","cmd":"","facet":"memory","label":"memory: Anthropic's Messages API cannot be reached through the OPENAI...","new":"[\n  {\n    \"claim\": \"Every LLM provider behind agent.llm's resolver accepts exactly ONE system message, and it must be at index 0.\",\n    \"detail\": \"vLLM's chat template answers a system message at any later index — including index 1, ahead of every user turn — with a hard 400: BadRequestError \\\"System message must be at the beginning.\\\" So client-side context injection (recall layer 4's mode pack) must be APPENDED to the existing system message, never inserted beside it. Appending rather than prepending is also the cache-friendly choice: the curriculum stays a stable prefix, so server-side prefix caching still hits. agent.agentloop's chatTurn does this in convo(), which builds a merged COPY — the venue's own messages array is never mutated.\",\n    \"tags\": \"llm,agent,platform-api,wire\",\n    \"source\": {\n      \"lib\": \"agent\",\n      \"ctl\": \"agentloop\",\n      \"facet\": \"js\",\n      \"hash\": \"6569fbeed2ace744\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1785964641067\n  },\n  {\n    \"claim\": \"Anthropic's Messages API cannot be reached through the OPENAI arm: it is a distinct wire, not chat-completions with a different key.\",\n    \"detail\": \"Five differences, any one of which 400s: the key rides x-api-key (not Authorization: Bearer) and anthropic-version is required; the system prompt is a top-level `system` param, not a role:\\\"system\\\" message; max_tokens is REQUIRED and has no server-side default; tools carry `input_schema` rather than `parameters`; and tool traffic is tool_use / tool_result CONTENT BLOCKS inside user/assistant turns, never a role:\\\"tool\\\" message. <ARM>_HEADERS can fix the header and nothing else. Hence the ANTHROPIC arm and the `anthropic` dialect - the same lesson the GEMINI arm already taught.\",\n    \"tags\": \"llm,anthropic,dialect,resolver\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878897877\n  }\n]\n","old":"[\n  {\n    \"claim\": \"Every LLM provider behind agent.llm's resolver accepts exactly ONE system message, and it must be at index 0.\",\n    \"detail\": \"vLLM's chat template answers a system message at any later index — including index 1, ahead of every user turn — with a hard 400: BadRequestError \\\"System message must be at the beginning.\\\" So client-side context injection (recall layer 4's mode pack) must be APPENDED to the existing system message, never inserted beside it. Appending rather than prepending is also the cache-friendly choice: the curriculum stays a stable prefix, so server-side prefix caching still hits. agent.agentloop's chatTurn does this in convo(), which builds a merged COPY — the venue's own messages array is never mutated.\",\n    \"tags\": \"llm,agent,platform-api,wire\",\n    \"source\": {\n      \"lib\": \"agent\",\n      \"ctl\": \"agentloop\",\n      \"facet\": \"js\",\n      \"hash\": \"6569fbeed2ace744\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1785964641067\n  }\n]\n","patch_id":"p4","time":1786878897878},{"author":"claude","cmd":"","facet":"memory","label":"memory: With thinking on, an Anthropic assistant turn that called a t...","new":"[\n  {\n    \"claim\": \"Every LLM provider behind agent.llm's resolver accepts exactly ONE system message, and it must be at index 0.\",\n    \"detail\": \"vLLM's chat template answers a system message at any later index — including index 1, ahead of every user turn — with a hard 400: BadRequestError \\\"System message must be at the beginning.\\\" So client-side context injection (recall layer 4's mode pack) must be APPENDED to the existing system message, never inserted beside it. Appending rather than prepending is also the cache-friendly choice: the curriculum stays a stable prefix, so server-side prefix caching still hits. agent.agentloop's chatTurn does this in convo(), which builds a merged COPY — the venue's own messages array is never mutated.\",\n    \"tags\": \"llm,agent,platform-api,wire\",\n    \"source\": {\n      \"lib\": \"agent\",\n      \"ctl\": \"agentloop\",\n      \"facet\": \"js\",\n      \"hash\": \"6569fbeed2ace744\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1785964641067\n  },\n  {\n    \"claim\": \"Anthropic's Messages API cannot be reached through the OPENAI arm: it is a distinct wire, not chat-completions with a different key.\",\n    \"detail\": \"Five differences, any one of which 400s: the key rides x-api-key (not Authorization: Bearer) and anthropic-version is required; the system prompt is a top-level `system` param, not a role:\\\"system\\\" message; max_tokens is REQUIRED and has no server-side default; tools carry `input_schema` rather than `parameters`; and tool traffic is tool_use / tool_result CONTENT BLOCKS inside user/assistant turns, never a role:\\\"tool\\\" message. <ARM>_HEADERS can fix the header and nothing else. Hence the ANTHROPIC arm and the `anthropic` dialect - the same lesson the GEMINI arm already taught.\",\n    \"tags\": \"llm,anthropic,dialect,resolver\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878897877\n  },\n  {\n    \"claim\": \"With thinking on, an Anthropic assistant turn that called a tool is rejected on the NEXT request unless its thinking block is replayed verbatim and first.\",\n    \"detail\": \"Thinking is ON BY DEFAULT on Opus 5 (omitting the `thinking` param runs adaptive), so this fires on the very first tool call rather than only under an opt-in. parse_anthropic keeps the raw thinking/redacted_thinking blocks on the assistant_message as `thinking_blocks`, and build_anthropic_payload re-emits them as the first entries of the assistant content array - the same carry-it-on-the-replay-object trick Gemini's thought_signature uses. Dropping them, or ordering them after text/tool_use, breaks the turn. display:\\\"omitted\\\" (the default) still returns the block with an empty `thinking` string and a live signature: it is the signature that matters, not the text.\",\n    \"tags\": \"llm,anthropic,thinking,tool-loop\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898049\n  }\n]\n","old":"[\n  {\n    \"claim\": \"Every LLM provider behind agent.llm's resolver accepts exactly ONE system message, and it must be at index 0.\",\n    \"detail\": \"vLLM's chat template answers a system message at any later index — including index 1, ahead of every user turn — with a hard 400: BadRequestError \\\"System message must be at the beginning.\\\" So client-side context injection (recall layer 4's mode pack) must be APPENDED to the existing system message, never inserted beside it. Appending rather than prepending is also the cache-friendly choice: the curriculum stays a stable prefix, so server-side prefix caching still hits. agent.agentloop's chatTurn does this in convo(), which builds a merged COPY — the venue's own messages array is never mutated.\",\n    \"tags\": \"llm,agent,platform-api,wire\",\n    \"source\": {\n      \"lib\": \"agent\",\n      \"ctl\": \"agentloop\",\n      \"facet\": \"js\",\n      \"hash\": \"6569fbeed2ace744\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1785964641067\n  },\n  {\n    \"claim\": \"Anthropic's Messages API cannot be reached through the OPENAI arm: it is a distinct wire, not chat-completions with a different key.\",\n    \"detail\": \"Five differences, any one of which 400s: the key rides x-api-key (not Authorization: Bearer) and anthropic-version is required; the system prompt is a top-level `system` param, not a role:\\\"system\\\" message; max_tokens is REQUIRED and has no server-side default; tools carry `input_schema` rather than `parameters`; and tool traffic is tool_use / tool_result CONTENT BLOCKS inside user/assistant turns, never a role:\\\"tool\\\" message. <ARM>_HEADERS can fix the header and nothing else. Hence the ANTHROPIC arm and the `anthropic` dialect - the same lesson the GEMINI arm already taught.\",\n    \"tags\": \"llm,anthropic,dialect,resolver\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878897877\n  }\n]\n","patch_id":"p5","time":1786878898050},{"author":"claude","cmd":"","facet":"memory","label":"memory: The anthropic dialect deliberately ignores LLM_TEMPERATURE: O...","new":"[\n  {\n    \"claim\": \"Every LLM provider behind agent.llm's resolver accepts exactly ONE system message, and it must be at index 0.\",\n    \"detail\": \"vLLM's chat template answers a system message at any later index — including index 1, ahead of every user turn — with a hard 400: BadRequestError \\\"System message must be at the beginning.\\\" So client-side context injection (recall layer 4's mode pack) must be APPENDED to the existing system message, never inserted beside it. Appending rather than prepending is also the cache-friendly choice: the curriculum stays a stable prefix, so server-side prefix caching still hits. agent.agentloop's chatTurn does this in convo(), which builds a merged COPY — the venue's own messages array is never mutated.\",\n    \"tags\": \"llm,agent,platform-api,wire\",\n    \"source\": {\n      \"lib\": \"agent\",\n      \"ctl\": \"agentloop\",\n      \"facet\": \"js\",\n      \"hash\": \"6569fbeed2ace744\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1785964641067\n  },\n  {\n    \"claim\": \"Anthropic's Messages API cannot be reached through the OPENAI arm: it is a distinct wire, not chat-completions with a different key.\",\n    \"detail\": \"Five differences, any one of which 400s: the key rides x-api-key (not Authorization: Bearer) and anthropic-version is required; the system prompt is a top-level `system` param, not a role:\\\"system\\\" message; max_tokens is REQUIRED and has no server-side default; tools carry `input_schema` rather than `parameters`; and tool traffic is tool_use / tool_result CONTENT BLOCKS inside user/assistant turns, never a role:\\\"tool\\\" message. <ARM>_HEADERS can fix the header and nothing else. Hence the ANTHROPIC arm and the `anthropic` dialect - the same lesson the GEMINI arm already taught.\",\n    \"tags\": \"llm,anthropic,dialect,resolver\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878897877\n  },\n  {\n    \"claim\": \"With thinking on, an Anthropic assistant turn that called a tool is rejected on the NEXT request unless its thinking block is replayed verbatim and first.\",\n    \"detail\": \"Thinking is ON BY DEFAULT on Opus 5 (omitting the `thinking` param runs adaptive), so this fires on the very first tool call rather than only under an opt-in. parse_anthropic keeps the raw thinking/redacted_thinking blocks on the assistant_message as `thinking_blocks`, and build_anthropic_payload re-emits them as the first entries of the assistant content array - the same carry-it-on-the-replay-object trick Gemini's thought_signature uses. Dropping them, or ordering them after text/tool_use, breaks the turn. display:\\\"omitted\\\" (the default) still returns the block with an empty `thinking` string and a live signature: it is the signature that matters, not the text.\",\n    \"tags\": \"llm,anthropic,thinking,tool-loop\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898049\n  },\n  {\n    \"claim\": \"The anthropic dialect deliberately ignores LLM_TEMPERATURE: Opus 5 and the 4.7/4.8 family reject temperature, top_p and top_k with a 400.\",\n    \"detail\": \"build_anthropic_payload is the one builder that takes no temperature argument, so the shared LLM_TEMPERATURE default (0.2) cannot leak onto the wire. Reasoning depth is output_config.effort instead (ANTHROPIC_EFFORT; xhigh is the recommendation for coding and agentic work, and the API default is high). ANTHROPIC_THINKING is left unset by default because omitting `thinking` runs adaptive on Opus 5 but means NO thinking on 4.8/4.7, and `disabled` is itself a 400 above effort `high`.\",\n    \"tags\": \"llm,anthropic,effort,sampling\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898228\n  }\n]\n","old":"[\n  {\n    \"claim\": \"Every LLM provider behind agent.llm's resolver accepts exactly ONE system message, and it must be at index 0.\",\n    \"detail\": \"vLLM's chat template answers a system message at any later index — including index 1, ahead of every user turn — with a hard 400: BadRequestError \\\"System message must be at the beginning.\\\" So client-side context injection (recall layer 4's mode pack) must be APPENDED to the existing system message, never inserted beside it. Appending rather than prepending is also the cache-friendly choice: the curriculum stays a stable prefix, so server-side prefix caching still hits. agent.agentloop's chatTurn does this in convo(), which builds a merged COPY — the venue's own messages array is never mutated.\",\n    \"tags\": \"llm,agent,platform-api,wire\",\n    \"source\": {\n      \"lib\": \"agent\",\n      \"ctl\": \"agentloop\",\n      \"facet\": \"js\",\n      \"hash\": \"6569fbeed2ace744\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1785964641067\n  },\n  {\n    \"claim\": \"Anthropic's Messages API cannot be reached through the OPENAI arm: it is a distinct wire, not chat-completions with a different key.\",\n    \"detail\": \"Five differences, any one of which 400s: the key rides x-api-key (not Authorization: Bearer) and anthropic-version is required; the system prompt is a top-level `system` param, not a role:\\\"system\\\" message; max_tokens is REQUIRED and has no server-side default; tools carry `input_schema` rather than `parameters`; and tool traffic is tool_use / tool_result CONTENT BLOCKS inside user/assistant turns, never a role:\\\"tool\\\" message. <ARM>_HEADERS can fix the header and nothing else. Hence the ANTHROPIC arm and the `anthropic` dialect - the same lesson the GEMINI arm already taught.\",\n    \"tags\": \"llm,anthropic,dialect,resolver\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878897877\n  },\n  {\n    \"claim\": \"With thinking on, an Anthropic assistant turn that called a tool is rejected on the NEXT request unless its thinking block is replayed verbatim and first.\",\n    \"detail\": \"Thinking is ON BY DEFAULT on Opus 5 (omitting the `thinking` param runs adaptive), so this fires on the very first tool call rather than only under an opt-in. parse_anthropic keeps the raw thinking/redacted_thinking blocks on the assistant_message as `thinking_blocks`, and build_anthropic_payload re-emits them as the first entries of the assistant content array - the same carry-it-on-the-replay-object trick Gemini's thought_signature uses. Dropping them, or ordering them after text/tool_use, breaks the turn. display:\\\"omitted\\\" (the default) still returns the block with an empty `thinking` string and a live signature: it is the signature that matters, not the text.\",\n    \"tags\": \"llm,anthropic,thinking,tool-loop\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898049\n  }\n]\n","patch_id":"p6","time":1786878898229},{"author":"claude","cmd":"","facet":"memory","label":"memory: Two Anthropic non-answers arrive as HTTP 200 with an empty co...","new":"[\n  {\n    \"claim\": \"Every LLM provider behind agent.llm's resolver accepts exactly ONE system message, and it must be at index 0.\",\n    \"detail\": \"vLLM's chat template answers a system message at any later index — including index 1, ahead of every user turn — with a hard 400: BadRequestError \\\"System message must be at the beginning.\\\" So client-side context injection (recall layer 4's mode pack) must be APPENDED to the existing system message, never inserted beside it. Appending rather than prepending is also the cache-friendly choice: the curriculum stays a stable prefix, so server-side prefix caching still hits. agent.agentloop's chatTurn does this in convo(), which builds a merged COPY — the venue's own messages array is never mutated.\",\n    \"tags\": \"llm,agent,platform-api,wire\",\n    \"source\": {\n      \"lib\": \"agent\",\n      \"ctl\": \"agentloop\",\n      \"facet\": \"js\",\n      \"hash\": \"6569fbeed2ace744\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1785964641067\n  },\n  {\n    \"claim\": \"Anthropic's Messages API cannot be reached through the OPENAI arm: it is a distinct wire, not chat-completions with a different key.\",\n    \"detail\": \"Five differences, any one of which 400s: the key rides x-api-key (not Authorization: Bearer) and anthropic-version is required; the system prompt is a top-level `system` param, not a role:\\\"system\\\" message; max_tokens is REQUIRED and has no server-side default; tools carry `input_schema` rather than `parameters`; and tool traffic is tool_use / tool_result CONTENT BLOCKS inside user/assistant turns, never a role:\\\"tool\\\" message. <ARM>_HEADERS can fix the header and nothing else. Hence the ANTHROPIC arm and the `anthropic` dialect - the same lesson the GEMINI arm already taught.\",\n    \"tags\": \"llm,anthropic,dialect,resolver\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878897877\n  },\n  {\n    \"claim\": \"With thinking on, an Anthropic assistant turn that called a tool is rejected on the NEXT request unless its thinking block is replayed verbatim and first.\",\n    \"detail\": \"Thinking is ON BY DEFAULT on Opus 5 (omitting the `thinking` param runs adaptive), so this fires on the very first tool call rather than only under an opt-in. parse_anthropic keeps the raw thinking/redacted_thinking blocks on the assistant_message as `thinking_blocks`, and build_anthropic_payload re-emits them as the first entries of the assistant content array - the same carry-it-on-the-replay-object trick Gemini's thought_signature uses. Dropping them, or ordering them after text/tool_use, breaks the turn. display:\\\"omitted\\\" (the default) still returns the block with an empty `thinking` string and a live signature: it is the signature that matters, not the text.\",\n    \"tags\": \"llm,anthropic,thinking,tool-loop\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898049\n  },\n  {\n    \"claim\": \"The anthropic dialect deliberately ignores LLM_TEMPERATURE: Opus 5 and the 4.7/4.8 family reject temperature, top_p and top_k with a 400.\",\n    \"detail\": \"build_anthropic_payload is the one builder that takes no temperature argument, so the shared LLM_TEMPERATURE default (0.2) cannot leak onto the wire. Reasoning depth is output_config.effort instead (ANTHROPIC_EFFORT; xhigh is the recommendation for coding and agentic work, and the API default is high). ANTHROPIC_THINKING is left unset by default because omitting `thinking` runs adaptive on Opus 5 but means NO thinking on 4.8/4.7, and `disabled` is itself a 400 above effort `high`.\",\n    \"tags\": \"llm,anthropic,effort,sampling\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898228\n  },\n  {\n    \"claim\": \"Two Anthropic non-answers arrive as HTTP 200 with an empty content array, and must be read off stop_reason before the content walk.\",\n    \"detail\": \"stop_reason:\\\"refusal\\\" is a safety decline carrying stop_details.category (e.g. cyber, bio) - Opus 5 ships elevated cybersecurity safeguards, so an agent that ships code and stack traces can trip it. stop_reason:\\\"max_tokens\\\" means the budget ran out, and because max_tokens caps THINKING plus visible text together, a thinking model can spend the whole budget before answering - the fix is raising LLM_MAX_TOKENS, which the error names. Both would read as \\\"unexpected response shape\\\" if stop_reason were not checked first: the identical misdiagnosis Gemini's filtered answers used to produce.\",\n    \"tags\": \"llm,anthropic,errors,refusal\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898449\n  }\n]\n","old":"[\n  {\n    \"claim\": \"Every LLM provider behind agent.llm's resolver accepts exactly ONE system message, and it must be at index 0.\",\n    \"detail\": \"vLLM's chat template answers a system message at any later index — including index 1, ahead of every user turn — with a hard 400: BadRequestError \\\"System message must be at the beginning.\\\" So client-side context injection (recall layer 4's mode pack) must be APPENDED to the existing system message, never inserted beside it. Appending rather than prepending is also the cache-friendly choice: the curriculum stays a stable prefix, so server-side prefix caching still hits. agent.agentloop's chatTurn does this in convo(), which builds a merged COPY — the venue's own messages array is never mutated.\",\n    \"tags\": \"llm,agent,platform-api,wire\",\n    \"source\": {\n      \"lib\": \"agent\",\n      \"ctl\": \"agentloop\",\n      \"facet\": \"js\",\n      \"hash\": \"6569fbeed2ace744\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1785964641067\n  },\n  {\n    \"claim\": \"Anthropic's Messages API cannot be reached through the OPENAI arm: it is a distinct wire, not chat-completions with a different key.\",\n    \"detail\": \"Five differences, any one of which 400s: the key rides x-api-key (not Authorization: Bearer) and anthropic-version is required; the system prompt is a top-level `system` param, not a role:\\\"system\\\" message; max_tokens is REQUIRED and has no server-side default; tools carry `input_schema` rather than `parameters`; and tool traffic is tool_use / tool_result CONTENT BLOCKS inside user/assistant turns, never a role:\\\"tool\\\" message. <ARM>_HEADERS can fix the header and nothing else. Hence the ANTHROPIC arm and the `anthropic` dialect - the same lesson the GEMINI arm already taught.\",\n    \"tags\": \"llm,anthropic,dialect,resolver\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878897877\n  },\n  {\n    \"claim\": \"With thinking on, an Anthropic assistant turn that called a tool is rejected on the NEXT request unless its thinking block is replayed verbatim and first.\",\n    \"detail\": \"Thinking is ON BY DEFAULT on Opus 5 (omitting the `thinking` param runs adaptive), so this fires on the very first tool call rather than only under an opt-in. parse_anthropic keeps the raw thinking/redacted_thinking blocks on the assistant_message as `thinking_blocks`, and build_anthropic_payload re-emits them as the first entries of the assistant content array - the same carry-it-on-the-replay-object trick Gemini's thought_signature uses. Dropping them, or ordering them after text/tool_use, breaks the turn. display:\\\"omitted\\\" (the default) still returns the block with an empty `thinking` string and a live signature: it is the signature that matters, not the text.\",\n    \"tags\": \"llm,anthropic,thinking,tool-loop\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898049\n  },\n  {\n    \"claim\": \"The anthropic dialect deliberately ignores LLM_TEMPERATURE: Opus 5 and the 4.7/4.8 family reject temperature, top_p and top_k with a 400.\",\n    \"detail\": \"build_anthropic_payload is the one builder that takes no temperature argument, so the shared LLM_TEMPERATURE default (0.2) cannot leak onto the wire. Reasoning depth is output_config.effort instead (ANTHROPIC_EFFORT; xhigh is the recommendation for coding and agentic work, and the API default is high). ANTHROPIC_THINKING is left unset by default because omitting `thinking` runs adaptive on Opus 5 but means NO thinking on 4.8/4.7, and `disabled` is itself a 400 above effort `high`.\",\n    \"tags\": \"llm,anthropic,effort,sampling\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898228\n  }\n]\n","patch_id":"p7","time":1786878898449},{"author":"claude","cmd":"","facet":"memory","label":"memory: agent.llm.claude_code is a delegating sub-agent, not a drop-i...","new":"[\n  {\n    \"claim\": \"Every LLM provider behind agent.llm's resolver accepts exactly ONE system message, and it must be at index 0.\",\n    \"detail\": \"vLLM's chat template answers a system message at any later index — including index 1, ahead of every user turn — with a hard 400: BadRequestError \\\"System message must be at the beginning.\\\" So client-side context injection (recall layer 4's mode pack) must be APPENDED to the existing system message, never inserted beside it. Appending rather than prepending is also the cache-friendly choice: the curriculum stays a stable prefix, so server-side prefix caching still hits. agent.agentloop's chatTurn does this in convo(), which builds a merged COPY — the venue's own messages array is never mutated.\",\n    \"tags\": \"llm,agent,platform-api,wire\",\n    \"source\": {\n      \"lib\": \"agent\",\n      \"ctl\": \"agentloop\",\n      \"facet\": \"js\",\n      \"hash\": \"6569fbeed2ace744\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1785964641067\n  },\n  {\n    \"claim\": \"Anthropic's Messages API cannot be reached through the OPENAI arm: it is a distinct wire, not chat-completions with a different key.\",\n    \"detail\": \"Five differences, any one of which 400s: the key rides x-api-key (not Authorization: Bearer) and anthropic-version is required; the system prompt is a top-level `system` param, not a role:\\\"system\\\" message; max_tokens is REQUIRED and has no server-side default; tools carry `input_schema` rather than `parameters`; and tool traffic is tool_use / tool_result CONTENT BLOCKS inside user/assistant turns, never a role:\\\"tool\\\" message. <ARM>_HEADERS can fix the header and nothing else. Hence the ANTHROPIC arm and the `anthropic` dialect - the same lesson the GEMINI arm already taught.\",\n    \"tags\": \"llm,anthropic,dialect,resolver\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878897877\n  },\n  {\n    \"claim\": \"With thinking on, an Anthropic assistant turn that called a tool is rejected on the NEXT request unless its thinking block is replayed verbatim and first.\",\n    \"detail\": \"Thinking is ON BY DEFAULT on Opus 5 (omitting the `thinking` param runs adaptive), so this fires on the very first tool call rather than only under an opt-in. parse_anthropic keeps the raw thinking/redacted_thinking blocks on the assistant_message as `thinking_blocks`, and build_anthropic_payload re-emits them as the first entries of the assistant content array - the same carry-it-on-the-replay-object trick Gemini's thought_signature uses. Dropping them, or ordering them after text/tool_use, breaks the turn. display:\\\"omitted\\\" (the default) still returns the block with an empty `thinking` string and a live signature: it is the signature that matters, not the text.\",\n    \"tags\": \"llm,anthropic,thinking,tool-loop\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898049\n  },\n  {\n    \"claim\": \"The anthropic dialect deliberately ignores LLM_TEMPERATURE: Opus 5 and the 4.7/4.8 family reject temperature, top_p and top_k with a 400.\",\n    \"detail\": \"build_anthropic_payload is the one builder that takes no temperature argument, so the shared LLM_TEMPERATURE default (0.2) cannot leak onto the wire. Reasoning depth is output_config.effort instead (ANTHROPIC_EFFORT; xhigh is the recommendation for coding and agentic work, and the API default is high). ANTHROPIC_THINKING is left unset by default because omitting `thinking` runs adaptive on Opus 5 but means NO thinking on 4.8/4.7, and `disabled` is itself a 400 above effort `high`.\",\n    \"tags\": \"llm,anthropic,effort,sampling\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898228\n  },\n  {\n    \"claim\": \"Two Anthropic non-answers arrive as HTTP 200 with an empty content array, and must be read off stop_reason before the content walk.\",\n    \"detail\": \"stop_reason:\\\"refusal\\\" is a safety decline carrying stop_details.category (e.g. cyber, bio) - Opus 5 ships elevated cybersecurity safeguards, so an agent that ships code and stack traces can trip it. stop_reason:\\\"max_tokens\\\" means the budget ran out, and because max_tokens caps THINKING plus visible text together, a thinking model can spend the whole budget before answering - the fix is raising LLM_MAX_TOKENS, which the error names. Both would read as \\\"unexpected response shape\\\" if stop_reason were not checked first: the identical misdiagnosis Gemini's filtered answers used to produce.\",\n    \"tags\": \"llm,anthropic,errors,refusal\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898449\n  },\n  {\n    \"claim\": \"agent.llm.claude_code is a delegating sub-agent, not a drop-in model provider: it always answers kind:text and never kind:tool_calls.\",\n    \"detail\": \"chat_llm's other arms answer ONE model turn and hand tool calls back for tool_loop to execute. Claude Code is an agent harness that runs its own loop with its own tools and returns only when the work is done, so tool_loop terminates on its answer - the turn was delegated wholesale and the text IS the finished result. The `tools` newbound passes are therefore ignored (they name commands only the newbound process can run). To give the delegate real capability, point CLAUDE_CODE_MCP at newbound's own MCP server, which exposes every store command rather than the subset tool_loop forwarded.\",\n    \"tags\": \"llm,claude-code,llm-ctl,bridge\",\n    \"source\": {\n      \"doc\": \"agent.llm.claude_code\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786882401239\n  }\n]\n","old":"[\n  {\n    \"claim\": \"Every LLM provider behind agent.llm's resolver accepts exactly ONE system message, and it must be at index 0.\",\n    \"detail\": \"vLLM's chat template answers a system message at any later index — including index 1, ahead of every user turn — with a hard 400: BadRequestError \\\"System message must be at the beginning.\\\" So client-side context injection (recall layer 4's mode pack) must be APPENDED to the existing system message, never inserted beside it. Appending rather than prepending is also the cache-friendly choice: the curriculum stays a stable prefix, so server-side prefix caching still hits. agent.agentloop's chatTurn does this in convo(), which builds a merged COPY — the venue's own messages array is never mutated.\",\n    \"tags\": \"llm,agent,platform-api,wire\",\n    \"source\": {\n      \"lib\": \"agent\",\n      \"ctl\": \"agentloop\",\n      \"facet\": \"js\",\n      \"hash\": \"6569fbeed2ace744\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1785964641067\n  },\n  {\n    \"claim\": \"Anthropic's Messages API cannot be reached through the OPENAI arm: it is a distinct wire, not chat-completions with a different key.\",\n    \"detail\": \"Five differences, any one of which 400s: the key rides x-api-key (not Authorization: Bearer) and anthropic-version is required; the system prompt is a top-level `system` param, not a role:\\\"system\\\" message; max_tokens is REQUIRED and has no server-side default; tools carry `input_schema` rather than `parameters`; and tool traffic is tool_use / tool_result CONTENT BLOCKS inside user/assistant turns, never a role:\\\"tool\\\" message. <ARM>_HEADERS can fix the header and nothing else. Hence the ANTHROPIC arm and the `anthropic` dialect - the same lesson the GEMINI arm already taught.\",\n    \"tags\": \"llm,anthropic,dialect,resolver\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878897877\n  },\n  {\n    \"claim\": \"With thinking on, an Anthropic assistant turn that called a tool is rejected on the NEXT request unless its thinking block is replayed verbatim and first.\",\n    \"detail\": \"Thinking is ON BY DEFAULT on Opus 5 (omitting the `thinking` param runs adaptive), so this fires on the very first tool call rather than only under an opt-in. parse_anthropic keeps the raw thinking/redacted_thinking blocks on the assistant_message as `thinking_blocks`, and build_anthropic_payload re-emits them as the first entries of the assistant content array - the same carry-it-on-the-replay-object trick Gemini's thought_signature uses. Dropping them, or ordering them after text/tool_use, breaks the turn. display:\\\"omitted\\\" (the default) still returns the block with an empty `thinking` string and a live signature: it is the signature that matters, not the text.\",\n    \"tags\": \"llm,anthropic,thinking,tool-loop\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898049\n  },\n  {\n    \"claim\": \"The anthropic dialect deliberately ignores LLM_TEMPERATURE: Opus 5 and the 4.7/4.8 family reject temperature, top_p and top_k with a 400.\",\n    \"detail\": \"build_anthropic_payload is the one builder that takes no temperature argument, so the shared LLM_TEMPERATURE default (0.2) cannot leak onto the wire. Reasoning depth is output_config.effort instead (ANTHROPIC_EFFORT; xhigh is the recommendation for coding and agentic work, and the API default is high). ANTHROPIC_THINKING is left unset by default because omitting `thinking` runs adaptive on Opus 5 but means NO thinking on 4.8/4.7, and `disabled` is itself a 400 above effort `high`.\",\n    \"tags\": \"llm,anthropic,effort,sampling\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898228\n  },\n  {\n    \"claim\": \"Two Anthropic non-answers arrive as HTTP 200 with an empty content array, and must be read off stop_reason before the content walk.\",\n    \"detail\": \"stop_reason:\\\"refusal\\\" is a safety decline carrying stop_details.category (e.g. cyber, bio) - Opus 5 ships elevated cybersecurity safeguards, so an agent that ships code and stack traces can trip it. stop_reason:\\\"max_tokens\\\" means the budget ran out, and because max_tokens caps THINKING plus visible text together, a thinking model can spend the whole budget before answering - the fix is raising LLM_MAX_TOKENS, which the error names. Both would read as \\\"unexpected response shape\\\" if stop_reason were not checked first: the identical misdiagnosis Gemini's filtered answers used to produce.\",\n    \"tags\": \"llm,anthropic,errors,refusal\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898449\n  }\n]\n","patch_id":"p8","time":1786882401241},{"author":"claude","cmd":"","facet":"memory","label":"memory: Replacing Claude Code's default system prompt and disabling i...","new":"[\n  {\n    \"claim\": \"Every LLM provider behind agent.llm's resolver accepts exactly ONE system message, and it must be at index 0.\",\n    \"detail\": \"vLLM's chat template answers a system message at any later index — including index 1, ahead of every user turn — with a hard 400: BadRequestError \\\"System message must be at the beginning.\\\" So client-side context injection (recall layer 4's mode pack) must be APPENDED to the existing system message, never inserted beside it. Appending rather than prepending is also the cache-friendly choice: the curriculum stays a stable prefix, so server-side prefix caching still hits. agent.agentloop's chatTurn does this in convo(), which builds a merged COPY — the venue's own messages array is never mutated.\",\n    \"tags\": \"llm,agent,platform-api,wire\",\n    \"source\": {\n      \"lib\": \"agent\",\n      \"ctl\": \"agentloop\",\n      \"facet\": \"js\",\n      \"hash\": \"6569fbeed2ace744\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1785964641067\n  },\n  {\n    \"claim\": \"Anthropic's Messages API cannot be reached through the OPENAI arm: it is a distinct wire, not chat-completions with a different key.\",\n    \"detail\": \"Five differences, any one of which 400s: the key rides x-api-key (not Authorization: Bearer) and anthropic-version is required; the system prompt is a top-level `system` param, not a role:\\\"system\\\" message; max_tokens is REQUIRED and has no server-side default; tools carry `input_schema` rather than `parameters`; and tool traffic is tool_use / tool_result CONTENT BLOCKS inside user/assistant turns, never a role:\\\"tool\\\" message. <ARM>_HEADERS can fix the header and nothing else. Hence the ANTHROPIC arm and the `anthropic` dialect - the same lesson the GEMINI arm already taught.\",\n    \"tags\": \"llm,anthropic,dialect,resolver\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878897877\n  },\n  {\n    \"claim\": \"With thinking on, an Anthropic assistant turn that called a tool is rejected on the NEXT request unless its thinking block is replayed verbatim and first.\",\n    \"detail\": \"Thinking is ON BY DEFAULT on Opus 5 (omitting the `thinking` param runs adaptive), so this fires on the very first tool call rather than only under an opt-in. parse_anthropic keeps the raw thinking/redacted_thinking blocks on the assistant_message as `thinking_blocks`, and build_anthropic_payload re-emits them as the first entries of the assistant content array - the same carry-it-on-the-replay-object trick Gemini's thought_signature uses. Dropping them, or ordering them after text/tool_use, breaks the turn. display:\\\"omitted\\\" (the default) still returns the block with an empty `thinking` string and a live signature: it is the signature that matters, not the text.\",\n    \"tags\": \"llm,anthropic,thinking,tool-loop\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898049\n  },\n  {\n    \"claim\": \"The anthropic dialect deliberately ignores LLM_TEMPERATURE: Opus 5 and the 4.7/4.8 family reject temperature, top_p and top_k with a 400.\",\n    \"detail\": \"build_anthropic_payload is the one builder that takes no temperature argument, so the shared LLM_TEMPERATURE default (0.2) cannot leak onto the wire. Reasoning depth is output_config.effort instead (ANTHROPIC_EFFORT; xhigh is the recommendation for coding and agentic work, and the API default is high). ANTHROPIC_THINKING is left unset by default because omitting `thinking` runs adaptive on Opus 5 but means NO thinking on 4.8/4.7, and `disabled` is itself a 400 above effort `high`.\",\n    \"tags\": \"llm,anthropic,effort,sampling\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898228\n  },\n  {\n    \"claim\": \"Two Anthropic non-answers arrive as HTTP 200 with an empty content array, and must be read off stop_reason before the content walk.\",\n    \"detail\": \"stop_reason:\\\"refusal\\\" is a safety decline carrying stop_details.category (e.g. cyber, bio) - Opus 5 ships elevated cybersecurity safeguards, so an agent that ships code and stack traces can trip it. stop_reason:\\\"max_tokens\\\" means the budget ran out, and because max_tokens caps THINKING plus visible text together, a thinking model can spend the whole budget before answering - the fix is raising LLM_MAX_TOKENS, which the error names. Both would read as \\\"unexpected response shape\\\" if stop_reason were not checked first: the identical misdiagnosis Gemini's filtered answers used to produce.\",\n    \"tags\": \"llm,anthropic,errors,refusal\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898449\n  },\n  {\n    \"claim\": \"agent.llm.claude_code is a delegating sub-agent, not a drop-in model provider: it always answers kind:text and never kind:tool_calls.\",\n    \"detail\": \"chat_llm's other arms answer ONE model turn and hand tool calls back for tool_loop to execute. Claude Code is an agent harness that runs its own loop with its own tools and returns only when the work is done, so tool_loop terminates on its answer - the turn was delegated wholesale and the text IS the finished result. The `tools` newbound passes are therefore ignored (they name commands only the newbound process can run). To give the delegate real capability, point CLAUDE_CODE_MCP at newbound's own MCP server, which exposes every store command rather than the subset tool_loop forwarded.\",\n    \"tags\": \"llm,claude-code,llm-ctl,bridge\",\n    \"source\": {\n      \"doc\": \"agent.llm.claude_code\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786882401239\n  },\n  {\n    \"claim\": \"Replacing Claude Code's default system prompt and disabling its built-in tools makes a bridged turn roughly 170x cheaper.\",\n    \"detail\": \"Measured on a trivial prompt: the default configuration loaded ~38,000 cache-creation tokens of Claude Code system prompt plus CLAUDE.md discovery ($0.229 notional), against ~200 input tokens ($0.0013) with --system-prompt replacing it and --tools \\\"\\\" disabling built-ins. claude_code therefore defaults to replace-mode, mapping the conversation's own system messages onto --system-prompt. CLAUDE_CODE_SYSTEM_MODE=append keeps Claude Code's prompt and pays for it, which is what agentic delegation actually wants.\",\n    \"tags\": \"llm,claude-code,cost\",\n    \"source\": {\n      \"doc\": \"agent.llm.claude_code\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786882401312\n  }\n]\n","old":"[\n  {\n    \"claim\": \"Every LLM provider behind agent.llm's resolver accepts exactly ONE system message, and it must be at index 0.\",\n    \"detail\": \"vLLM's chat template answers a system message at any later index — including index 1, ahead of every user turn — with a hard 400: BadRequestError \\\"System message must be at the beginning.\\\" So client-side context injection (recall layer 4's mode pack) must be APPENDED to the existing system message, never inserted beside it. Appending rather than prepending is also the cache-friendly choice: the curriculum stays a stable prefix, so server-side prefix caching still hits. agent.agentloop's chatTurn does this in convo(), which builds a merged COPY — the venue's own messages array is never mutated.\",\n    \"tags\": \"llm,agent,platform-api,wire\",\n    \"source\": {\n      \"lib\": \"agent\",\n      \"ctl\": \"agentloop\",\n      \"facet\": \"js\",\n      \"hash\": \"6569fbeed2ace744\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1785964641067\n  },\n  {\n    \"claim\": \"Anthropic's Messages API cannot be reached through the OPENAI arm: it is a distinct wire, not chat-completions with a different key.\",\n    \"detail\": \"Five differences, any one of which 400s: the key rides x-api-key (not Authorization: Bearer) and anthropic-version is required; the system prompt is a top-level `system` param, not a role:\\\"system\\\" message; max_tokens is REQUIRED and has no server-side default; tools carry `input_schema` rather than `parameters`; and tool traffic is tool_use / tool_result CONTENT BLOCKS inside user/assistant turns, never a role:\\\"tool\\\" message. <ARM>_HEADERS can fix the header and nothing else. Hence the ANTHROPIC arm and the `anthropic` dialect - the same lesson the GEMINI arm already taught.\",\n    \"tags\": \"llm,anthropic,dialect,resolver\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878897877\n  },\n  {\n    \"claim\": \"With thinking on, an Anthropic assistant turn that called a tool is rejected on the NEXT request unless its thinking block is replayed verbatim and first.\",\n    \"detail\": \"Thinking is ON BY DEFAULT on Opus 5 (omitting the `thinking` param runs adaptive), so this fires on the very first tool call rather than only under an opt-in. parse_anthropic keeps the raw thinking/redacted_thinking blocks on the assistant_message as `thinking_blocks`, and build_anthropic_payload re-emits them as the first entries of the assistant content array - the same carry-it-on-the-replay-object trick Gemini's thought_signature uses. Dropping them, or ordering them after text/tool_use, breaks the turn. display:\\\"omitted\\\" (the default) still returns the block with an empty `thinking` string and a live signature: it is the signature that matters, not the text.\",\n    \"tags\": \"llm,anthropic,thinking,tool-loop\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898049\n  },\n  {\n    \"claim\": \"The anthropic dialect deliberately ignores LLM_TEMPERATURE: Opus 5 and the 4.7/4.8 family reject temperature, top_p and top_k with a 400.\",\n    \"detail\": \"build_anthropic_payload is the one builder that takes no temperature argument, so the shared LLM_TEMPERATURE default (0.2) cannot leak onto the wire. Reasoning depth is output_config.effort instead (ANTHROPIC_EFFORT; xhigh is the recommendation for coding and agentic work, and the API default is high). ANTHROPIC_THINKING is left unset by default because omitting `thinking` runs adaptive on Opus 5 but means NO thinking on 4.8/4.7, and `disabled` is itself a 400 above effort `high`.\",\n    \"tags\": \"llm,anthropic,effort,sampling\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898228\n  },\n  {\n    \"claim\": \"Two Anthropic non-answers arrive as HTTP 200 with an empty content array, and must be read off stop_reason before the content walk.\",\n    \"detail\": \"stop_reason:\\\"refusal\\\" is a safety decline carrying stop_details.category (e.g. cyber, bio) - Opus 5 ships elevated cybersecurity safeguards, so an agent that ships code and stack traces can trip it. stop_reason:\\\"max_tokens\\\" means the budget ran out, and because max_tokens caps THINKING plus visible text together, a thinking model can spend the whole budget before answering - the fix is raising LLM_MAX_TOKENS, which the error names. Both would read as \\\"unexpected response shape\\\" if stop_reason were not checked first: the identical misdiagnosis Gemini's filtered answers used to produce.\",\n    \"tags\": \"llm,anthropic,errors,refusal\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898449\n  },\n  {\n    \"claim\": \"agent.llm.claude_code is a delegating sub-agent, not a drop-in model provider: it always answers kind:text and never kind:tool_calls.\",\n    \"detail\": \"chat_llm's other arms answer ONE model turn and hand tool calls back for tool_loop to execute. Claude Code is an agent harness that runs its own loop with its own tools and returns only when the work is done, so tool_loop terminates on its answer - the turn was delegated wholesale and the text IS the finished result. The `tools` newbound passes are therefore ignored (they name commands only the newbound process can run). To give the delegate real capability, point CLAUDE_CODE_MCP at newbound's own MCP server, which exposes every store command rather than the subset tool_loop forwarded.\",\n    \"tags\": \"llm,claude-code,llm-ctl,bridge\",\n    \"source\": {\n      \"doc\": \"agent.llm.claude_code\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786882401239\n  }\n]\n","patch_id":"p9","time":1786882401313},{"author":"claude","cmd":"","facet":"memory","label":"memory: The prompt must ride stdin, never argv, because Claude Code's...","new":"[\n  {\n    \"claim\": \"Every LLM provider behind agent.llm's resolver accepts exactly ONE system message, and it must be at index 0.\",\n    \"detail\": \"vLLM's chat template answers a system message at any later index — including index 1, ahead of every user turn — with a hard 400: BadRequestError \\\"System message must be at the beginning.\\\" So client-side context injection (recall layer 4's mode pack) must be APPENDED to the existing system message, never inserted beside it. Appending rather than prepending is also the cache-friendly choice: the curriculum stays a stable prefix, so server-side prefix caching still hits. agent.agentloop's chatTurn does this in convo(), which builds a merged COPY — the venue's own messages array is never mutated.\",\n    \"tags\": \"llm,agent,platform-api,wire\",\n    \"source\": {\n      \"lib\": \"agent\",\n      \"ctl\": \"agentloop\",\n      \"facet\": \"js\",\n      \"hash\": \"6569fbeed2ace744\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1785964641067\n  },\n  {\n    \"claim\": \"Anthropic's Messages API cannot be reached through the OPENAI arm: it is a distinct wire, not chat-completions with a different key.\",\n    \"detail\": \"Five differences, any one of which 400s: the key rides x-api-key (not Authorization: Bearer) and anthropic-version is required; the system prompt is a top-level `system` param, not a role:\\\"system\\\" message; max_tokens is REQUIRED and has no server-side default; tools carry `input_schema` rather than `parameters`; and tool traffic is tool_use / tool_result CONTENT BLOCKS inside user/assistant turns, never a role:\\\"tool\\\" message. <ARM>_HEADERS can fix the header and nothing else. Hence the ANTHROPIC arm and the `anthropic` dialect - the same lesson the GEMINI arm already taught.\",\n    \"tags\": \"llm,anthropic,dialect,resolver\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878897877\n  },\n  {\n    \"claim\": \"With thinking on, an Anthropic assistant turn that called a tool is rejected on the NEXT request unless its thinking block is replayed verbatim and first.\",\n    \"detail\": \"Thinking is ON BY DEFAULT on Opus 5 (omitting the `thinking` param runs adaptive), so this fires on the very first tool call rather than only under an opt-in. parse_anthropic keeps the raw thinking/redacted_thinking blocks on the assistant_message as `thinking_blocks`, and build_anthropic_payload re-emits them as the first entries of the assistant content array - the same carry-it-on-the-replay-object trick Gemini's thought_signature uses. Dropping them, or ordering them after text/tool_use, breaks the turn. display:\\\"omitted\\\" (the default) still returns the block with an empty `thinking` string and a live signature: it is the signature that matters, not the text.\",\n    \"tags\": \"llm,anthropic,thinking,tool-loop\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898049\n  },\n  {\n    \"claim\": \"The anthropic dialect deliberately ignores LLM_TEMPERATURE: Opus 5 and the 4.7/4.8 family reject temperature, top_p and top_k with a 400.\",\n    \"detail\": \"build_anthropic_payload is the one builder that takes no temperature argument, so the shared LLM_TEMPERATURE default (0.2) cannot leak onto the wire. Reasoning depth is output_config.effort instead (ANTHROPIC_EFFORT; xhigh is the recommendation for coding and agentic work, and the API default is high). ANTHROPIC_THINKING is left unset by default because omitting `thinking` runs adaptive on Opus 5 but means NO thinking on 4.8/4.7, and `disabled` is itself a 400 above effort `high`.\",\n    \"tags\": \"llm,anthropic,effort,sampling\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898228\n  },\n  {\n    \"claim\": \"Two Anthropic non-answers arrive as HTTP 200 with an empty content array, and must be read off stop_reason before the content walk.\",\n    \"detail\": \"stop_reason:\\\"refusal\\\" is a safety decline carrying stop_details.category (e.g. cyber, bio) - Opus 5 ships elevated cybersecurity safeguards, so an agent that ships code and stack traces can trip it. stop_reason:\\\"max_tokens\\\" means the budget ran out, and because max_tokens caps THINKING plus visible text together, a thinking model can spend the whole budget before answering - the fix is raising LLM_MAX_TOKENS, which the error names. Both would read as \\\"unexpected response shape\\\" if stop_reason were not checked first: the identical misdiagnosis Gemini's filtered answers used to produce.\",\n    \"tags\": \"llm,anthropic,errors,refusal\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898449\n  },\n  {\n    \"claim\": \"agent.llm.claude_code is a delegating sub-agent, not a drop-in model provider: it always answers kind:text and never kind:tool_calls.\",\n    \"detail\": \"chat_llm's other arms answer ONE model turn and hand tool calls back for tool_loop to execute. Claude Code is an agent harness that runs its own loop with its own tools and returns only when the work is done, so tool_loop terminates on its answer - the turn was delegated wholesale and the text IS the finished result. The `tools` newbound passes are therefore ignored (they name commands only the newbound process can run). To give the delegate real capability, point CLAUDE_CODE_MCP at newbound's own MCP server, which exposes every store command rather than the subset tool_loop forwarded.\",\n    \"tags\": \"llm,claude-code,llm-ctl,bridge\",\n    \"source\": {\n      \"doc\": \"agent.llm.claude_code\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786882401239\n  },\n  {\n    \"claim\": \"Replacing Claude Code's default system prompt and disabling its built-in tools makes a bridged turn roughly 170x cheaper.\",\n    \"detail\": \"Measured on a trivial prompt: the default configuration loaded ~38,000 cache-creation tokens of Claude Code system prompt plus CLAUDE.md discovery ($0.229 notional), against ~200 input tokens ($0.0013) with --system-prompt replacing it and --tools \\\"\\\" disabling built-ins. claude_code therefore defaults to replace-mode, mapping the conversation's own system messages onto --system-prompt. CLAUDE_CODE_SYSTEM_MODE=append keeps Claude Code's prompt and pays for it, which is what agentic delegation actually wants.\",\n    \"tags\": \"llm,claude-code,cost\",\n    \"source\": {\n      \"doc\": \"agent.llm.claude_code\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786882401312\n  },\n  {\n    \"claim\": \"The prompt must ride stdin, never argv, because Claude Code's variadic flags silently swallow a positional prompt.\",\n    \"detail\": \"--tools, --allowedTools, --disallowedTools and --mcp-config are declared <tools...> style, so any following non-option argument is consumed as another value: `claude -p --tools \\\"\\\" \\\"my prompt\\\"` fails with \\\"Input must be provided either through stdin or as a prompt argument\\\". stdin also sidesteps ARG_MAX on long conversations. claude_code additionally emits --tools LAST so nothing it appends can be eaten.\",\n    \"tags\": \"llm,claude-code,cli\",\n    \"source\": {\n      \"doc\": \"agent.llm.claude_code\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786882401387\n  }\n]\n","old":"[\n  {\n    \"claim\": \"Every LLM provider behind agent.llm's resolver accepts exactly ONE system message, and it must be at index 0.\",\n    \"detail\": \"vLLM's chat template answers a system message at any later index — including index 1, ahead of every user turn — with a hard 400: BadRequestError \\\"System message must be at the beginning.\\\" So client-side context injection (recall layer 4's mode pack) must be APPENDED to the existing system message, never inserted beside it. Appending rather than prepending is also the cache-friendly choice: the curriculum stays a stable prefix, so server-side prefix caching still hits. agent.agentloop's chatTurn does this in convo(), which builds a merged COPY — the venue's own messages array is never mutated.\",\n    \"tags\": \"llm,agent,platform-api,wire\",\n    \"source\": {\n      \"lib\": \"agent\",\n      \"ctl\": \"agentloop\",\n      \"facet\": \"js\",\n      \"hash\": \"6569fbeed2ace744\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1785964641067\n  },\n  {\n    \"claim\": \"Anthropic's Messages API cannot be reached through the OPENAI arm: it is a distinct wire, not chat-completions with a different key.\",\n    \"detail\": \"Five differences, any one of which 400s: the key rides x-api-key (not Authorization: Bearer) and anthropic-version is required; the system prompt is a top-level `system` param, not a role:\\\"system\\\" message; max_tokens is REQUIRED and has no server-side default; tools carry `input_schema` rather than `parameters`; and tool traffic is tool_use / tool_result CONTENT BLOCKS inside user/assistant turns, never a role:\\\"tool\\\" message. <ARM>_HEADERS can fix the header and nothing else. Hence the ANTHROPIC arm and the `anthropic` dialect - the same lesson the GEMINI arm already taught.\",\n    \"tags\": \"llm,anthropic,dialect,resolver\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878897877\n  },\n  {\n    \"claim\": \"With thinking on, an Anthropic assistant turn that called a tool is rejected on the NEXT request unless its thinking block is replayed verbatim and first.\",\n    \"detail\": \"Thinking is ON BY DEFAULT on Opus 5 (omitting the `thinking` param runs adaptive), so this fires on the very first tool call rather than only under an opt-in. parse_anthropic keeps the raw thinking/redacted_thinking blocks on the assistant_message as `thinking_blocks`, and build_anthropic_payload re-emits them as the first entries of the assistant content array - the same carry-it-on-the-replay-object trick Gemini's thought_signature uses. Dropping them, or ordering them after text/tool_use, breaks the turn. display:\\\"omitted\\\" (the default) still returns the block with an empty `thinking` string and a live signature: it is the signature that matters, not the text.\",\n    \"tags\": \"llm,anthropic,thinking,tool-loop\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898049\n  },\n  {\n    \"claim\": \"The anthropic dialect deliberately ignores LLM_TEMPERATURE: Opus 5 and the 4.7/4.8 family reject temperature, top_p and top_k with a 400.\",\n    \"detail\": \"build_anthropic_payload is the one builder that takes no temperature argument, so the shared LLM_TEMPERATURE default (0.2) cannot leak onto the wire. Reasoning depth is output_config.effort instead (ANTHROPIC_EFFORT; xhigh is the recommendation for coding and agentic work, and the API default is high). ANTHROPIC_THINKING is left unset by default because omitting `thinking` runs adaptive on Opus 5 but means NO thinking on 4.8/4.7, and `disabled` is itself a 400 above effort `high`.\",\n    \"tags\": \"llm,anthropic,effort,sampling\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898228\n  },\n  {\n    \"claim\": \"Two Anthropic non-answers arrive as HTTP 200 with an empty content array, and must be read off stop_reason before the content walk.\",\n    \"detail\": \"stop_reason:\\\"refusal\\\" is a safety decline carrying stop_details.category (e.g. cyber, bio) - Opus 5 ships elevated cybersecurity safeguards, so an agent that ships code and stack traces can trip it. stop_reason:\\\"max_tokens\\\" means the budget ran out, and because max_tokens caps THINKING plus visible text together, a thinking model can spend the whole budget before answering - the fix is raising LLM_MAX_TOKENS, which the error names. Both would read as \\\"unexpected response shape\\\" if stop_reason were not checked first: the identical misdiagnosis Gemini's filtered answers used to produce.\",\n    \"tags\": \"llm,anthropic,errors,refusal\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898449\n  },\n  {\n    \"claim\": \"agent.llm.claude_code is a delegating sub-agent, not a drop-in model provider: it always answers kind:text and never kind:tool_calls.\",\n    \"detail\": \"chat_llm's other arms answer ONE model turn and hand tool calls back for tool_loop to execute. Claude Code is an agent harness that runs its own loop with its own tools and returns only when the work is done, so tool_loop terminates on its answer - the turn was delegated wholesale and the text IS the finished result. The `tools` newbound passes are therefore ignored (they name commands only the newbound process can run). To give the delegate real capability, point CLAUDE_CODE_MCP at newbound's own MCP server, which exposes every store command rather than the subset tool_loop forwarded.\",\n    \"tags\": \"llm,claude-code,llm-ctl,bridge\",\n    \"source\": {\n      \"doc\": \"agent.llm.claude_code\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786882401239\n  },\n  {\n    \"claim\": \"Replacing Claude Code's default system prompt and disabling its built-in tools makes a bridged turn roughly 170x cheaper.\",\n    \"detail\": \"Measured on a trivial prompt: the default configuration loaded ~38,000 cache-creation tokens of Claude Code system prompt plus CLAUDE.md discovery ($0.229 notional), against ~200 input tokens ($0.0013) with --system-prompt replacing it and --tools \\\"\\\" disabling built-ins. claude_code therefore defaults to replace-mode, mapping the conversation's own system messages onto --system-prompt. CLAUDE_CODE_SYSTEM_MODE=append keeps Claude Code's prompt and pays for it, which is what agentic delegation actually wants.\",\n    \"tags\": \"llm,claude-code,cost\",\n    \"source\": {\n      \"doc\": \"agent.llm.claude_code\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786882401312\n  }\n]\n","patch_id":"p10","time":1786882401388},{"author":"claude","cmd":"","facet":"memory","label":"memory: Reaping a timed-out `claude` needs BOTH an explicit process g...","new":"[\n  {\n    \"claim\": \"Every LLM provider behind agent.llm's resolver accepts exactly ONE system message, and it must be at index 0.\",\n    \"detail\": \"vLLM's chat template answers a system message at any later index — including index 1, ahead of every user turn — with a hard 400: BadRequestError \\\"System message must be at the beginning.\\\" So client-side context injection (recall layer 4's mode pack) must be APPENDED to the existing system message, never inserted beside it. Appending rather than prepending is also the cache-friendly choice: the curriculum stays a stable prefix, so server-side prefix caching still hits. agent.agentloop's chatTurn does this in convo(), which builds a merged COPY — the venue's own messages array is never mutated.\",\n    \"tags\": \"llm,agent,platform-api,wire\",\n    \"source\": {\n      \"lib\": \"agent\",\n      \"ctl\": \"agentloop\",\n      \"facet\": \"js\",\n      \"hash\": \"6569fbeed2ace744\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1785964641067\n  },\n  {\n    \"claim\": \"Anthropic's Messages API cannot be reached through the OPENAI arm: it is a distinct wire, not chat-completions with a different key.\",\n    \"detail\": \"Five differences, any one of which 400s: the key rides x-api-key (not Authorization: Bearer) and anthropic-version is required; the system prompt is a top-level `system` param, not a role:\\\"system\\\" message; max_tokens is REQUIRED and has no server-side default; tools carry `input_schema` rather than `parameters`; and tool traffic is tool_use / tool_result CONTENT BLOCKS inside user/assistant turns, never a role:\\\"tool\\\" message. <ARM>_HEADERS can fix the header and nothing else. Hence the ANTHROPIC arm and the `anthropic` dialect - the same lesson the GEMINI arm already taught.\",\n    \"tags\": \"llm,anthropic,dialect,resolver\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878897877\n  },\n  {\n    \"claim\": \"With thinking on, an Anthropic assistant turn that called a tool is rejected on the NEXT request unless its thinking block is replayed verbatim and first.\",\n    \"detail\": \"Thinking is ON BY DEFAULT on Opus 5 (omitting the `thinking` param runs adaptive), so this fires on the very first tool call rather than only under an opt-in. parse_anthropic keeps the raw thinking/redacted_thinking blocks on the assistant_message as `thinking_blocks`, and build_anthropic_payload re-emits them as the first entries of the assistant content array - the same carry-it-on-the-replay-object trick Gemini's thought_signature uses. Dropping them, or ordering them after text/tool_use, breaks the turn. display:\\\"omitted\\\" (the default) still returns the block with an empty `thinking` string and a live signature: it is the signature that matters, not the text.\",\n    \"tags\": \"llm,anthropic,thinking,tool-loop\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898049\n  },\n  {\n    \"claim\": \"The anthropic dialect deliberately ignores LLM_TEMPERATURE: Opus 5 and the 4.7/4.8 family reject temperature, top_p and top_k with a 400.\",\n    \"detail\": \"build_anthropic_payload is the one builder that takes no temperature argument, so the shared LLM_TEMPERATURE default (0.2) cannot leak onto the wire. Reasoning depth is output_config.effort instead (ANTHROPIC_EFFORT; xhigh is the recommendation for coding and agentic work, and the API default is high). ANTHROPIC_THINKING is left unset by default because omitting `thinking` runs adaptive on Opus 5 but means NO thinking on 4.8/4.7, and `disabled` is itself a 400 above effort `high`.\",\n    \"tags\": \"llm,anthropic,effort,sampling\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898228\n  },\n  {\n    \"claim\": \"Two Anthropic non-answers arrive as HTTP 200 with an empty content array, and must be read off stop_reason before the content walk.\",\n    \"detail\": \"stop_reason:\\\"refusal\\\" is a safety decline carrying stop_details.category (e.g. cyber, bio) - Opus 5 ships elevated cybersecurity safeguards, so an agent that ships code and stack traces can trip it. stop_reason:\\\"max_tokens\\\" means the budget ran out, and because max_tokens caps THINKING plus visible text together, a thinking model can spend the whole budget before answering - the fix is raising LLM_MAX_TOKENS, which the error names. Both would read as \\\"unexpected response shape\\\" if stop_reason were not checked first: the identical misdiagnosis Gemini's filtered answers used to produce.\",\n    \"tags\": \"llm,anthropic,errors,refusal\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898449\n  },\n  {\n    \"claim\": \"agent.llm.claude_code is a delegating sub-agent, not a drop-in model provider: it always answers kind:text and never kind:tool_calls.\",\n    \"detail\": \"chat_llm's other arms answer ONE model turn and hand tool calls back for tool_loop to execute. Claude Code is an agent harness that runs its own loop with its own tools and returns only when the work is done, so tool_loop terminates on its answer - the turn was delegated wholesale and the text IS the finished result. The `tools` newbound passes are therefore ignored (they name commands only the newbound process can run). To give the delegate real capability, point CLAUDE_CODE_MCP at newbound's own MCP server, which exposes every store command rather than the subset tool_loop forwarded.\",\n    \"tags\": \"llm,claude-code,llm-ctl,bridge\",\n    \"source\": {\n      \"doc\": \"agent.llm.claude_code\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786882401239\n  },\n  {\n    \"claim\": \"Replacing Claude Code's default system prompt and disabling its built-in tools makes a bridged turn roughly 170x cheaper.\",\n    \"detail\": \"Measured on a trivial prompt: the default configuration loaded ~38,000 cache-creation tokens of Claude Code system prompt plus CLAUDE.md discovery ($0.229 notional), against ~200 input tokens ($0.0013) with --system-prompt replacing it and --tools \\\"\\\" disabling built-ins. claude_code therefore defaults to replace-mode, mapping the conversation's own system messages onto --system-prompt. CLAUDE_CODE_SYSTEM_MODE=append keeps Claude Code's prompt and pays for it, which is what agentic delegation actually wants.\",\n    \"tags\": \"llm,claude-code,cost\",\n    \"source\": {\n      \"doc\": \"agent.llm.claude_code\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786882401312\n  },\n  {\n    \"claim\": \"The prompt must ride stdin, never argv, because Claude Code's variadic flags silently swallow a positional prompt.\",\n    \"detail\": \"--tools, --allowedTools, --disallowedTools and --mcp-config are declared <tools...> style, so any following non-option argument is consumed as another value: `claude -p --tools \\\"\\\" \\\"my prompt\\\"` fails with \\\"Input must be provided either through stdin or as a prompt argument\\\". stdin also sidesteps ARG_MAX on long conversations. claude_code additionally emits --tools LAST so nothing it appends can be eaten.\",\n    \"tags\": \"llm,claude-code,cli\",\n    \"source\": {\n      \"doc\": \"agent.llm.claude_code\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786882401387\n  },\n  {\n    \"claim\": \"Reaping a timed-out `claude` needs BOTH an explicit process group and `kill -9 -- -PID`; either alone leaves the tree running.\",\n    \"detail\": \"`claude` is a node launcher, so killing the direct child can leave the interpreter alive. The child is spawned with process_group(0) (pgid == pid) and the timeout path signals the negative pid. The `--` is load-bearing and was a real bug: /bin/kill is not the shell builtin and parses a bare `-1234` as options, so the first implementation reported a successful kill while the process kept running - verified by ps five seconds later. Separately, CLAUDE_CODE_PERMISSION_MODE=bypassPermissions is refused outright when the server runs as root (\\\"cannot be used with root/sudo privileges\\\").\",\n    \"tags\": \"llm,claude-code,process,timeout\",\n    \"source\": {\n      \"doc\": \"agent.llm.claude_code\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786882401513\n  }\n]\n","old":"[\n  {\n    \"claim\": \"Every LLM provider behind agent.llm's resolver accepts exactly ONE system message, and it must be at index 0.\",\n    \"detail\": \"vLLM's chat template answers a system message at any later index — including index 1, ahead of every user turn — with a hard 400: BadRequestError \\\"System message must be at the beginning.\\\" So client-side context injection (recall layer 4's mode pack) must be APPENDED to the existing system message, never inserted beside it. Appending rather than prepending is also the cache-friendly choice: the curriculum stays a stable prefix, so server-side prefix caching still hits. agent.agentloop's chatTurn does this in convo(), which builds a merged COPY — the venue's own messages array is never mutated.\",\n    \"tags\": \"llm,agent,platform-api,wire\",\n    \"source\": {\n      \"lib\": \"agent\",\n      \"ctl\": \"agentloop\",\n      \"facet\": \"js\",\n      \"hash\": \"6569fbeed2ace744\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1785964641067\n  },\n  {\n    \"claim\": \"Anthropic's Messages API cannot be reached through the OPENAI arm: it is a distinct wire, not chat-completions with a different key.\",\n    \"detail\": \"Five differences, any one of which 400s: the key rides x-api-key (not Authorization: Bearer) and anthropic-version is required; the system prompt is a top-level `system` param, not a role:\\\"system\\\" message; max_tokens is REQUIRED and has no server-side default; tools carry `input_schema` rather than `parameters`; and tool traffic is tool_use / tool_result CONTENT BLOCKS inside user/assistant turns, never a role:\\\"tool\\\" message. <ARM>_HEADERS can fix the header and nothing else. Hence the ANTHROPIC arm and the `anthropic` dialect - the same lesson the GEMINI arm already taught.\",\n    \"tags\": \"llm,anthropic,dialect,resolver\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878897877\n  },\n  {\n    \"claim\": \"With thinking on, an Anthropic assistant turn that called a tool is rejected on the NEXT request unless its thinking block is replayed verbatim and first.\",\n    \"detail\": \"Thinking is ON BY DEFAULT on Opus 5 (omitting the `thinking` param runs adaptive), so this fires on the very first tool call rather than only under an opt-in. parse_anthropic keeps the raw thinking/redacted_thinking blocks on the assistant_message as `thinking_blocks`, and build_anthropic_payload re-emits them as the first entries of the assistant content array - the same carry-it-on-the-replay-object trick Gemini's thought_signature uses. Dropping them, or ordering them after text/tool_use, breaks the turn. display:\\\"omitted\\\" (the default) still returns the block with an empty `thinking` string and a live signature: it is the signature that matters, not the text.\",\n    \"tags\": \"llm,anthropic,thinking,tool-loop\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898049\n  },\n  {\n    \"claim\": \"The anthropic dialect deliberately ignores LLM_TEMPERATURE: Opus 5 and the 4.7/4.8 family reject temperature, top_p and top_k with a 400.\",\n    \"detail\": \"build_anthropic_payload is the one builder that takes no temperature argument, so the shared LLM_TEMPERATURE default (0.2) cannot leak onto the wire. Reasoning depth is output_config.effort instead (ANTHROPIC_EFFORT; xhigh is the recommendation for coding and agentic work, and the API default is high). ANTHROPIC_THINKING is left unset by default because omitting `thinking` runs adaptive on Opus 5 but means NO thinking on 4.8/4.7, and `disabled` is itself a 400 above effort `high`.\",\n    \"tags\": \"llm,anthropic,effort,sampling\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898228\n  },\n  {\n    \"claim\": \"Two Anthropic non-answers arrive as HTTP 200 with an empty content array, and must be read off stop_reason before the content walk.\",\n    \"detail\": \"stop_reason:\\\"refusal\\\" is a safety decline carrying stop_details.category (e.g. cyber, bio) - Opus 5 ships elevated cybersecurity safeguards, so an agent that ships code and stack traces can trip it. stop_reason:\\\"max_tokens\\\" means the budget ran out, and because max_tokens caps THINKING plus visible text together, a thinking model can spend the whole budget before answering - the fix is raising LLM_MAX_TOKENS, which the error names. Both would read as \\\"unexpected response shape\\\" if stop_reason were not checked first: the identical misdiagnosis Gemini's filtered answers used to produce.\",\n    \"tags\": \"llm,anthropic,errors,refusal\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898449\n  },\n  {\n    \"claim\": \"agent.llm.claude_code is a delegating sub-agent, not a drop-in model provider: it always answers kind:text and never kind:tool_calls.\",\n    \"detail\": \"chat_llm's other arms answer ONE model turn and hand tool calls back for tool_loop to execute. Claude Code is an agent harness that runs its own loop with its own tools and returns only when the work is done, so tool_loop terminates on its answer - the turn was delegated wholesale and the text IS the finished result. The `tools` newbound passes are therefore ignored (they name commands only the newbound process can run). To give the delegate real capability, point CLAUDE_CODE_MCP at newbound's own MCP server, which exposes every store command rather than the subset tool_loop forwarded.\",\n    \"tags\": \"llm,claude-code,llm-ctl,bridge\",\n    \"source\": {\n      \"doc\": \"agent.llm.claude_code\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786882401239\n  },\n  {\n    \"claim\": \"Replacing Claude Code's default system prompt and disabling its built-in tools makes a bridged turn roughly 170x cheaper.\",\n    \"detail\": \"Measured on a trivial prompt: the default configuration loaded ~38,000 cache-creation tokens of Claude Code system prompt plus CLAUDE.md discovery ($0.229 notional), against ~200 input tokens ($0.0013) with --system-prompt replacing it and --tools \\\"\\\" disabling built-ins. claude_code therefore defaults to replace-mode, mapping the conversation's own system messages onto --system-prompt. CLAUDE_CODE_SYSTEM_MODE=append keeps Claude Code's prompt and pays for it, which is what agentic delegation actually wants.\",\n    \"tags\": \"llm,claude-code,cost\",\n    \"source\": {\n      \"doc\": \"agent.llm.claude_code\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786882401312\n  },\n  {\n    \"claim\": \"The prompt must ride stdin, never argv, because Claude Code's variadic flags silently swallow a positional prompt.\",\n    \"detail\": \"--tools, --allowedTools, --disallowedTools and --mcp-config are declared <tools...> style, so any following non-option argument is consumed as another value: `claude -p --tools \\\"\\\" \\\"my prompt\\\"` fails with \\\"Input must be provided either through stdin or as a prompt argument\\\". stdin also sidesteps ARG_MAX on long conversations. claude_code additionally emits --tools LAST so nothing it appends can be eaten.\",\n    \"tags\": \"llm,claude-code,cli\",\n    \"source\": {\n      \"doc\": \"agent.llm.claude_code\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786882401387\n  }\n]\n","patch_id":"p11","time":1786882401514},{"author":"claude","cmd":"","facet":"memory","label":"memory: The CLAUDECODE arm (LLM=CLAUDECODE + LLM_CTL=agent:llm:claude...","new":"[\n  {\n    \"claim\": \"Every LLM provider behind agent.llm's resolver accepts exactly ONE system message, and it must be at index 0.\",\n    \"detail\": \"vLLM's chat template answers a system message at any later index — including index 1, ahead of every user turn — with a hard 400: BadRequestError \\\"System message must be at the beginning.\\\" So client-side context injection (recall layer 4's mode pack) must be APPENDED to the existing system message, never inserted beside it. Appending rather than prepending is also the cache-friendly choice: the curriculum stays a stable prefix, so server-side prefix caching still hits. agent.agentloop's chatTurn does this in convo(), which builds a merged COPY — the venue's own messages array is never mutated.\",\n    \"tags\": \"llm,agent,platform-api,wire\",\n    \"source\": {\n      \"lib\": \"agent\",\n      \"ctl\": \"agentloop\",\n      \"facet\": \"js\",\n      \"hash\": \"6569fbeed2ace744\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1785964641067\n  },\n  {\n    \"claim\": \"Anthropic's Messages API cannot be reached through the OPENAI arm: it is a distinct wire, not chat-completions with a different key.\",\n    \"detail\": \"Five differences, any one of which 400s: the key rides x-api-key (not Authorization: Bearer) and anthropic-version is required; the system prompt is a top-level `system` param, not a role:\\\"system\\\" message; max_tokens is REQUIRED and has no server-side default; tools carry `input_schema` rather than `parameters`; and tool traffic is tool_use / tool_result CONTENT BLOCKS inside user/assistant turns, never a role:\\\"tool\\\" message. <ARM>_HEADERS can fix the header and nothing else. Hence the ANTHROPIC arm and the `anthropic` dialect - the same lesson the GEMINI arm already taught.\",\n    \"tags\": \"llm,anthropic,dialect,resolver\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878897877\n  },\n  {\n    \"claim\": \"With thinking on, an Anthropic assistant turn that called a tool is rejected on the NEXT request unless its thinking block is replayed verbatim and first.\",\n    \"detail\": \"Thinking is ON BY DEFAULT on Opus 5 (omitting the `thinking` param runs adaptive), so this fires on the very first tool call rather than only under an opt-in. parse_anthropic keeps the raw thinking/redacted_thinking blocks on the assistant_message as `thinking_blocks`, and build_anthropic_payload re-emits them as the first entries of the assistant content array - the same carry-it-on-the-replay-object trick Gemini's thought_signature uses. Dropping them, or ordering them after text/tool_use, breaks the turn. display:\\\"omitted\\\" (the default) still returns the block with an empty `thinking` string and a live signature: it is the signature that matters, not the text.\",\n    \"tags\": \"llm,anthropic,thinking,tool-loop\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898049\n  },\n  {\n    \"claim\": \"The anthropic dialect deliberately ignores LLM_TEMPERATURE: Opus 5 and the 4.7/4.8 family reject temperature, top_p and top_k with a 400.\",\n    \"detail\": \"build_anthropic_payload is the one builder that takes no temperature argument, so the shared LLM_TEMPERATURE default (0.2) cannot leak onto the wire. Reasoning depth is output_config.effort instead (ANTHROPIC_EFFORT; xhigh is the recommendation for coding and agentic work, and the API default is high). ANTHROPIC_THINKING is left unset by default because omitting `thinking` runs adaptive on Opus 5 but means NO thinking on 4.8/4.7, and `disabled` is itself a 400 above effort `high`.\",\n    \"tags\": \"llm,anthropic,effort,sampling\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898228\n  },\n  {\n    \"claim\": \"Two Anthropic non-answers arrive as HTTP 200 with an empty content array, and must be read off stop_reason before the content walk.\",\n    \"detail\": \"stop_reason:\\\"refusal\\\" is a safety decline carrying stop_details.category (e.g. cyber, bio) - Opus 5 ships elevated cybersecurity safeguards, so an agent that ships code and stack traces can trip it. stop_reason:\\\"max_tokens\\\" means the budget ran out, and because max_tokens caps THINKING plus visible text together, a thinking model can spend the whole budget before answering - the fix is raising LLM_MAX_TOKENS, which the error names. Both would read as \\\"unexpected response shape\\\" if stop_reason were not checked first: the identical misdiagnosis Gemini's filtered answers used to produce.\",\n    \"tags\": \"llm,anthropic,errors,refusal\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898449\n  },\n  {\n    \"claim\": \"agent.llm.claude_code is a delegating sub-agent, not a drop-in model provider: it always answers kind:text and never kind:tool_calls.\",\n    \"detail\": \"chat_llm's other arms answer ONE model turn and hand tool calls back for tool_loop to execute. Claude Code is an agent harness that runs its own loop with its own tools and returns only when the work is done, so tool_loop terminates on its answer - the turn was delegated wholesale and the text IS the finished result. The `tools` newbound passes are therefore ignored (they name commands only the newbound process can run). To give the delegate real capability, point CLAUDE_CODE_MCP at newbound's own MCP server, which exposes every store command rather than the subset tool_loop forwarded.\",\n    \"tags\": \"llm,claude-code,llm-ctl,bridge\",\n    \"source\": {\n      \"doc\": \"agent.llm.claude_code\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786882401239\n  },\n  {\n    \"claim\": \"Replacing Claude Code's default system prompt and disabling its built-in tools makes a bridged turn roughly 170x cheaper.\",\n    \"detail\": \"Measured on a trivial prompt: the default configuration loaded ~38,000 cache-creation tokens of Claude Code system prompt plus CLAUDE.md discovery ($0.229 notional), against ~200 input tokens ($0.0013) with --system-prompt replacing it and --tools \\\"\\\" disabling built-ins. claude_code therefore defaults to replace-mode, mapping the conversation's own system messages onto --system-prompt. CLAUDE_CODE_SYSTEM_MODE=append keeps Claude Code's prompt and pays for it, which is what agentic delegation actually wants.\",\n    \"tags\": \"llm,claude-code,cost\",\n    \"source\": {\n      \"doc\": \"agent.llm.claude_code\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786882401312\n  },\n  {\n    \"claim\": \"The prompt must ride stdin, never argv, because Claude Code's variadic flags silently swallow a positional prompt.\",\n    \"detail\": \"--tools, --allowedTools, --disallowedTools and --mcp-config are declared <tools...> style, so any following non-option argument is consumed as another value: `claude -p --tools \\\"\\\" \\\"my prompt\\\"` fails with \\\"Input must be provided either through stdin or as a prompt argument\\\". stdin also sidesteps ARG_MAX on long conversations. claude_code additionally emits --tools LAST so nothing it appends can be eaten.\",\n    \"tags\": \"llm,claude-code,cli\",\n    \"source\": {\n      \"doc\": \"agent.llm.claude_code\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786882401387\n  },\n  {\n    \"claim\": \"Reaping a timed-out `claude` needs BOTH an explicit process group and `kill -9 -- -PID`; either alone leaves the tree running.\",\n    \"detail\": \"`claude` is a node launcher, so killing the direct child can leave the interpreter alive. The child is spawned with process_group(0) (pgid == pid) and the timeout path signals the negative pid. The `--` is load-bearing and was a real bug: /bin/kill is not the shell builtin and parses a bare `-1234` as options, so the first implementation reported a successful kill while the process kept running - verified by ps five seconds later. Separately, CLAUDE_CODE_PERMISSION_MODE=bypassPermissions is refused outright when the server runs as root (\\\"cannot be used with root/sudo privileges\\\").\",\n    \"tags\": \"llm,claude-code,process,timeout\",\n    \"source\": {\n      \"doc\": \"agent.llm.claude_code\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786882401513\n  },\n  {\n    \"claim\": \"The CLAUDECODE arm (LLM=CLAUDECODE + LLM_CTL=agent:llm:claude_code) is verified end-to-end (2026-08-17): chat_llm and ask_llm both route through the Claude Code CLI bridge, so one botd flip covers the agent app chat, the dev-session chat plugin, AND salience escalations/audits at once. Verified on a disposable with real calls (chat_llm answered through the arm with cost_usd attached; ask_llm answered the same way). The CLI flags the bridge builds (-p/--output-format json/--no-session-persistence/--system-prompt/--tools/--effort/--mcp-config/--strict-mcp-config/--permission-mode) all exist in CLI 2.1.233. Two postures: cheap-oracle (default: tools off, system replaced, ~200 tokens/call) vs full-agent (SYSTEM_MODE=append + CLAUDE_CODE_MCP at newbound mcp + bypassPermissions + CWD at the checkout). Settings are live keys - read per call, no restart. Amusing verification note: the delegate REFUSED to echo the test string ESCALATION-OK (it pattern-matched an authorization-token probe) - the refusal itself proved the path; benign echoes pass.\",\n    \"detail\": \"Runbook section 8 has the recipe; CLAUDE_CODE_* keys now show in the config card. The bridge itself (agent.llm.claude_code) shipped earlier on the gpu-release-era branch with the auth/cost/process-group design notes in its source.\",\n    \"tags\": \"llm,claudecode,arm,verified\",\n    \"confidence\": \"high\",\n    \"time\": 1786996729608\n  }\n]\n","old":"[\n  {\n    \"claim\": \"Every LLM provider behind agent.llm's resolver accepts exactly ONE system message, and it must be at index 0.\",\n    \"detail\": \"vLLM's chat template answers a system message at any later index — including index 1, ahead of every user turn — with a hard 400: BadRequestError \\\"System message must be at the beginning.\\\" So client-side context injection (recall layer 4's mode pack) must be APPENDED to the existing system message, never inserted beside it. Appending rather than prepending is also the cache-friendly choice: the curriculum stays a stable prefix, so server-side prefix caching still hits. agent.agentloop's chatTurn does this in convo(), which builds a merged COPY — the venue's own messages array is never mutated.\",\n    \"tags\": \"llm,agent,platform-api,wire\",\n    \"source\": {\n      \"lib\": \"agent\",\n      \"ctl\": \"agentloop\",\n      \"facet\": \"js\",\n      \"hash\": \"6569fbeed2ace744\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1785964641067\n  },\n  {\n    \"claim\": \"Anthropic's Messages API cannot be reached through the OPENAI arm: it is a distinct wire, not chat-completions with a different key.\",\n    \"detail\": \"Five differences, any one of which 400s: the key rides x-api-key (not Authorization: Bearer) and anthropic-version is required; the system prompt is a top-level `system` param, not a role:\\\"system\\\" message; max_tokens is REQUIRED and has no server-side default; tools carry `input_schema` rather than `parameters`; and tool traffic is tool_use / tool_result CONTENT BLOCKS inside user/assistant turns, never a role:\\\"tool\\\" message. <ARM>_HEADERS can fix the header and nothing else. Hence the ANTHROPIC arm and the `anthropic` dialect - the same lesson the GEMINI arm already taught.\",\n    \"tags\": \"llm,anthropic,dialect,resolver\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878897877\n  },\n  {\n    \"claim\": \"With thinking on, an Anthropic assistant turn that called a tool is rejected on the NEXT request unless its thinking block is replayed verbatim and first.\",\n    \"detail\": \"Thinking is ON BY DEFAULT on Opus 5 (omitting the `thinking` param runs adaptive), so this fires on the very first tool call rather than only under an opt-in. parse_anthropic keeps the raw thinking/redacted_thinking blocks on the assistant_message as `thinking_blocks`, and build_anthropic_payload re-emits them as the first entries of the assistant content array - the same carry-it-on-the-replay-object trick Gemini's thought_signature uses. Dropping them, or ordering them after text/tool_use, breaks the turn. display:\\\"omitted\\\" (the default) still returns the block with an empty `thinking` string and a live signature: it is the signature that matters, not the text.\",\n    \"tags\": \"llm,anthropic,thinking,tool-loop\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898049\n  },\n  {\n    \"claim\": \"The anthropic dialect deliberately ignores LLM_TEMPERATURE: Opus 5 and the 4.7/4.8 family reject temperature, top_p and top_k with a 400.\",\n    \"detail\": \"build_anthropic_payload is the one builder that takes no temperature argument, so the shared LLM_TEMPERATURE default (0.2) cannot leak onto the wire. Reasoning depth is output_config.effort instead (ANTHROPIC_EFFORT; xhigh is the recommendation for coding and agentic work, and the API default is high). ANTHROPIC_THINKING is left unset by default because omitting `thinking` runs adaptive on Opus 5 but means NO thinking on 4.8/4.7, and `disabled` is itself a 400 above effort `high`.\",\n    \"tags\": \"llm,anthropic,effort,sampling\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898228\n  },\n  {\n    \"claim\": \"Two Anthropic non-answers arrive as HTTP 200 with an empty content array, and must be read off stop_reason before the content walk.\",\n    \"detail\": \"stop_reason:\\\"refusal\\\" is a safety decline carrying stop_details.category (e.g. cyber, bio) - Opus 5 ships elevated cybersecurity safeguards, so an agent that ships code and stack traces can trip it. stop_reason:\\\"max_tokens\\\" means the budget ran out, and because max_tokens caps THINKING plus visible text together, a thinking model can spend the whole budget before answering - the fix is raising LLM_MAX_TOKENS, which the error names. Both would read as \\\"unexpected response shape\\\" if stop_reason were not checked first: the identical misdiagnosis Gemini's filtered answers used to produce.\",\n    \"tags\": \"llm,anthropic,errors,refusal\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898449\n  },\n  {\n    \"claim\": \"agent.llm.claude_code is a delegating sub-agent, not a drop-in model provider: it always answers kind:text and never kind:tool_calls.\",\n    \"detail\": \"chat_llm's other arms answer ONE model turn and hand tool calls back for tool_loop to execute. Claude Code is an agent harness that runs its own loop with its own tools and returns only when the work is done, so tool_loop terminates on its answer - the turn was delegated wholesale and the text IS the finished result. The `tools` newbound passes are therefore ignored (they name commands only the newbound process can run). To give the delegate real capability, point CLAUDE_CODE_MCP at newbound's own MCP server, which exposes every store command rather than the subset tool_loop forwarded.\",\n    \"tags\": \"llm,claude-code,llm-ctl,bridge\",\n    \"source\": {\n      \"doc\": \"agent.llm.claude_code\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786882401239\n  },\n  {\n    \"claim\": \"Replacing Claude Code's default system prompt and disabling its built-in tools makes a bridged turn roughly 170x cheaper.\",\n    \"detail\": \"Measured on a trivial prompt: the default configuration loaded ~38,000 cache-creation tokens of Claude Code system prompt plus CLAUDE.md discovery ($0.229 notional), against ~200 input tokens ($0.0013) with --system-prompt replacing it and --tools \\\"\\\" disabling built-ins. claude_code therefore defaults to replace-mode, mapping the conversation's own system messages onto --system-prompt. CLAUDE_CODE_SYSTEM_MODE=append keeps Claude Code's prompt and pays for it, which is what agentic delegation actually wants.\",\n    \"tags\": \"llm,claude-code,cost\",\n    \"source\": {\n      \"doc\": \"agent.llm.claude_code\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786882401312\n  },\n  {\n    \"claim\": \"The prompt must ride stdin, never argv, because Claude Code's variadic flags silently swallow a positional prompt.\",\n    \"detail\": \"--tools, --allowedTools, --disallowedTools and --mcp-config are declared <tools...> style, so any following non-option argument is consumed as another value: `claude -p --tools \\\"\\\" \\\"my prompt\\\"` fails with \\\"Input must be provided either through stdin or as a prompt argument\\\". stdin also sidesteps ARG_MAX on long conversations. claude_code additionally emits --tools LAST so nothing it appends can be eaten.\",\n    \"tags\": \"llm,claude-code,cli\",\n    \"source\": {\n      \"doc\": \"agent.llm.claude_code\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786882401387\n  },\n  {\n    \"claim\": \"Reaping a timed-out `claude` needs BOTH an explicit process group and `kill -9 -- -PID`; either alone leaves the tree running.\",\n    \"detail\": \"`claude` is a node launcher, so killing the direct child can leave the interpreter alive. The child is spawned with process_group(0) (pgid == pid) and the timeout path signals the negative pid. The `--` is load-bearing and was a real bug: /bin/kill is not the shell builtin and parses a bare `-1234` as options, so the first implementation reported a successful kill while the process kept running - verified by ps five seconds later. Separately, CLAUDE_CODE_PERMISSION_MODE=bypassPermissions is refused outright when the server runs as root (\\\"cannot be used with root/sudo privileges\\\").\",\n    \"tags\": \"llm,claude-code,process,timeout\",\n    \"source\": {\n      \"doc\": \"agent.llm.claude_code\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786882401513\n  }\n]\n","patch_id":"p12","time":1786996729610},{"author":"claude","cmd":"","facet":"memory","label":"memory: The inside delegate's CLAUDE.md-equivalent is FOUR store-resi...","new":"[\n  {\n    \"claim\": \"Every LLM provider behind agent.llm's resolver accepts exactly ONE system message, and it must be at index 0.\",\n    \"detail\": \"vLLM's chat template answers a system message at any later index — including index 1, ahead of every user turn — with a hard 400: BadRequestError \\\"System message must be at the beginning.\\\" So client-side context injection (recall layer 4's mode pack) must be APPENDED to the existing system message, never inserted beside it. Appending rather than prepending is also the cache-friendly choice: the curriculum stays a stable prefix, so server-side prefix caching still hits. agent.agentloop's chatTurn does this in convo(), which builds a merged COPY — the venue's own messages array is never mutated.\",\n    \"tags\": \"llm,agent,platform-api,wire\",\n    \"source\": {\n      \"lib\": \"agent\",\n      \"ctl\": \"agentloop\",\n      \"facet\": \"js\",\n      \"hash\": \"6569fbeed2ace744\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1785964641067\n  },\n  {\n    \"claim\": \"Anthropic's Messages API cannot be reached through the OPENAI arm: it is a distinct wire, not chat-completions with a different key.\",\n    \"detail\": \"Five differences, any one of which 400s: the key rides x-api-key (not Authorization: Bearer) and anthropic-version is required; the system prompt is a top-level `system` param, not a role:\\\"system\\\" message; max_tokens is REQUIRED and has no server-side default; tools carry `input_schema` rather than `parameters`; and tool traffic is tool_use / tool_result CONTENT BLOCKS inside user/assistant turns, never a role:\\\"tool\\\" message. <ARM>_HEADERS can fix the header and nothing else. Hence the ANTHROPIC arm and the `anthropic` dialect - the same lesson the GEMINI arm already taught.\",\n    \"tags\": \"llm,anthropic,dialect,resolver\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878897877\n  },\n  {\n    \"claim\": \"With thinking on, an Anthropic assistant turn that called a tool is rejected on the NEXT request unless its thinking block is replayed verbatim and first.\",\n    \"detail\": \"Thinking is ON BY DEFAULT on Opus 5 (omitting the `thinking` param runs adaptive), so this fires on the very first tool call rather than only under an opt-in. parse_anthropic keeps the raw thinking/redacted_thinking blocks on the assistant_message as `thinking_blocks`, and build_anthropic_payload re-emits them as the first entries of the assistant content array - the same carry-it-on-the-replay-object trick Gemini's thought_signature uses. Dropping them, or ordering them after text/tool_use, breaks the turn. display:\\\"omitted\\\" (the default) still returns the block with an empty `thinking` string and a live signature: it is the signature that matters, not the text.\",\n    \"tags\": \"llm,anthropic,thinking,tool-loop\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898049\n  },\n  {\n    \"claim\": \"The anthropic dialect deliberately ignores LLM_TEMPERATURE: Opus 5 and the 4.7/4.8 family reject temperature, top_p and top_k with a 400.\",\n    \"detail\": \"build_anthropic_payload is the one builder that takes no temperature argument, so the shared LLM_TEMPERATURE default (0.2) cannot leak onto the wire. Reasoning depth is output_config.effort instead (ANTHROPIC_EFFORT; xhigh is the recommendation for coding and agentic work, and the API default is high). ANTHROPIC_THINKING is left unset by default because omitting `thinking` runs adaptive on Opus 5 but means NO thinking on 4.8/4.7, and `disabled` is itself a 400 above effort `high`.\",\n    \"tags\": \"llm,anthropic,effort,sampling\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898228\n  },\n  {\n    \"claim\": \"Two Anthropic non-answers arrive as HTTP 200 with an empty content array, and must be read off stop_reason before the content walk.\",\n    \"detail\": \"stop_reason:\\\"refusal\\\" is a safety decline carrying stop_details.category (e.g. cyber, bio) - Opus 5 ships elevated cybersecurity safeguards, so an agent that ships code and stack traces can trip it. stop_reason:\\\"max_tokens\\\" means the budget ran out, and because max_tokens caps THINKING plus visible text together, a thinking model can spend the whole budget before answering - the fix is raising LLM_MAX_TOKENS, which the error names. Both would read as \\\"unexpected response shape\\\" if stop_reason were not checked first: the identical misdiagnosis Gemini's filtered answers used to produce.\",\n    \"tags\": \"llm,anthropic,errors,refusal\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898449\n  },\n  {\n    \"claim\": \"agent.llm.claude_code is a delegating sub-agent, not a drop-in model provider: it always answers kind:text and never kind:tool_calls.\",\n    \"detail\": \"chat_llm's other arms answer ONE model turn and hand tool calls back for tool_loop to execute. Claude Code is an agent harness that runs its own loop with its own tools and returns only when the work is done, so tool_loop terminates on its answer - the turn was delegated wholesale and the text IS the finished result. The `tools` newbound passes are therefore ignored (they name commands only the newbound process can run). To give the delegate real capability, point CLAUDE_CODE_MCP at newbound's own MCP server, which exposes every store command rather than the subset tool_loop forwarded.\",\n    \"tags\": \"llm,claude-code,llm-ctl,bridge\",\n    \"source\": {\n      \"doc\": \"agent.llm.claude_code\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786882401239\n  },\n  {\n    \"claim\": \"Replacing Claude Code's default system prompt and disabling its built-in tools makes a bridged turn roughly 170x cheaper.\",\n    \"detail\": \"Measured on a trivial prompt: the default configuration loaded ~38,000 cache-creation tokens of Claude Code system prompt plus CLAUDE.md discovery ($0.229 notional), against ~200 input tokens ($0.0013) with --system-prompt replacing it and --tools \\\"\\\" disabling built-ins. claude_code therefore defaults to replace-mode, mapping the conversation's own system messages onto --system-prompt. CLAUDE_CODE_SYSTEM_MODE=append keeps Claude Code's prompt and pays for it, which is what agentic delegation actually wants.\",\n    \"tags\": \"llm,claude-code,cost\",\n    \"source\": {\n      \"doc\": \"agent.llm.claude_code\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786882401312\n  },\n  {\n    \"claim\": \"The prompt must ride stdin, never argv, because Claude Code's variadic flags silently swallow a positional prompt.\",\n    \"detail\": \"--tools, --allowedTools, --disallowedTools and --mcp-config are declared <tools...> style, so any following non-option argument is consumed as another value: `claude -p --tools \\\"\\\" \\\"my prompt\\\"` fails with \\\"Input must be provided either through stdin or as a prompt argument\\\". stdin also sidesteps ARG_MAX on long conversations. claude_code additionally emits --tools LAST so nothing it appends can be eaten.\",\n    \"tags\": \"llm,claude-code,cli\",\n    \"source\": {\n      \"doc\": \"agent.llm.claude_code\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786882401387\n  },\n  {\n    \"claim\": \"Reaping a timed-out `claude` needs BOTH an explicit process group and `kill -9 -- -PID`; either alone leaves the tree running.\",\n    \"detail\": \"`claude` is a node launcher, so killing the direct child can leave the interpreter alive. The child is spawned with process_group(0) (pgid == pid) and the timeout path signals the negative pid. The `--` is load-bearing and was a real bug: /bin/kill is not the shell builtin and parses a bare `-1234` as options, so the first implementation reported a successful kill while the process kept running - verified by ps five seconds later. Separately, CLAUDE_CODE_PERMISSION_MODE=bypassPermissions is refused outright when the server runs as root (\\\"cannot be used with root/sudo privileges\\\").\",\n    \"tags\": \"llm,claude-code,process,timeout\",\n    \"source\": {\n      \"doc\": \"agent.llm.claude_code\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786882401513\n  },\n  {\n    \"claim\": \"The CLAUDECODE arm (LLM=CLAUDECODE + LLM_CTL=agent:llm:claude_code) is verified end-to-end (2026-08-17): chat_llm and ask_llm both route through the Claude Code CLI bridge, so one botd flip covers the agent app chat, the dev-session chat plugin, AND salience escalations/audits at once. Verified on a disposable with real calls (chat_llm answered through the arm with cost_usd attached; ask_llm answered the same way). The CLI flags the bridge builds (-p/--output-format json/--no-session-persistence/--system-prompt/--tools/--effort/--mcp-config/--strict-mcp-config/--permission-mode) all exist in CLI 2.1.233. Two postures: cheap-oracle (default: tools off, system replaced, ~200 tokens/call) vs full-agent (SYSTEM_MODE=append + CLAUDE_CODE_MCP at newbound mcp + bypassPermissions + CWD at the checkout). Settings are live keys - read per call, no restart. Amusing verification note: the delegate REFUSED to echo the test string ESCALATION-OK (it pattern-matched an authorization-token probe) - the refusal itself proved the path; benign echoes pass.\",\n    \"detail\": \"Runbook section 8 has the recipe; CLAUDE_CODE_* keys now show in the config card. The bridge itself (agent.llm.claude_code) shipped earlier on the gpu-release-era branch with the auth/cost/process-group design notes in its source.\",\n    \"tags\": \"llm,claudecode,arm,verified\",\n    \"confidence\": \"high\",\n    \"time\": 1786996729608\n  },\n  {\n    \"claim\": \"The inside delegate's CLAUDE.md-equivalent is FOUR store-resident prompt layers, all riding every chat turn as system messages (both surfaces assemble them; the bridge folds all system messages into --system-prompt): (1) the platform curriculum (agent.prompts prompt facet), (2) the venue shell + auto-injected memory index, (3) the owner addendum (agentprompt control js facet), (4) the RESIDENT CONTEXT the claude_code bridge itself injects whenever CLAUDE_CODE_MCP is set (CLAUDE_CODE_CONTEXT=off suppresses): where it is (LIVE instance), its hands (store commands as MCP tools, every param required), write rules (journaled commands only), the memory ritual (recall to orient; remember only for user asks or durable lessons), doc locations - and an explicit override of the native find_tools/call_command tool instructions, which describe a harness the delegate does not have. Verified live in full-agent posture: the delegate MCP-called the store, quoted a real command desc, and recited the write rules. Gotcha: bypassPermissions is refused under root - run the instance as a normal user, or CLAUDE_CODE_ARGS=--allowedTools mcp__newbound with empty permission mode.\",\n    \"detail\": \"Environment knowledge attaches at the layer that knows the environment: the bridge knows whether the delegate has hands, so the bridge injects the rules that govern hands. Full-agent verification turn cost ~$0.35 (MCP schemas + 2 turns) vs ~$0.008-0.016 for oracle calls.\",\n    \"tags\": \"llm,claudecode,resident-context,prompts\",\n    \"confidence\": \"high\",\n    \"time\": 1786997803520\n  }\n]\n","old":"[\n  {\n    \"claim\": \"Every LLM provider behind agent.llm's resolver accepts exactly ONE system message, and it must be at index 0.\",\n    \"detail\": \"vLLM's chat template answers a system message at any later index — including index 1, ahead of every user turn — with a hard 400: BadRequestError \\\"System message must be at the beginning.\\\" So client-side context injection (recall layer 4's mode pack) must be APPENDED to the existing system message, never inserted beside it. Appending rather than prepending is also the cache-friendly choice: the curriculum stays a stable prefix, so server-side prefix caching still hits. agent.agentloop's chatTurn does this in convo(), which builds a merged COPY — the venue's own messages array is never mutated.\",\n    \"tags\": \"llm,agent,platform-api,wire\",\n    \"source\": {\n      \"lib\": \"agent\",\n      \"ctl\": \"agentloop\",\n      \"facet\": \"js\",\n      \"hash\": \"6569fbeed2ace744\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1785964641067\n  },\n  {\n    \"claim\": \"Anthropic's Messages API cannot be reached through the OPENAI arm: it is a distinct wire, not chat-completions with a different key.\",\n    \"detail\": \"Five differences, any one of which 400s: the key rides x-api-key (not Authorization: Bearer) and anthropic-version is required; the system prompt is a top-level `system` param, not a role:\\\"system\\\" message; max_tokens is REQUIRED and has no server-side default; tools carry `input_schema` rather than `parameters`; and tool traffic is tool_use / tool_result CONTENT BLOCKS inside user/assistant turns, never a role:\\\"tool\\\" message. <ARM>_HEADERS can fix the header and nothing else. Hence the ANTHROPIC arm and the `anthropic` dialect - the same lesson the GEMINI arm already taught.\",\n    \"tags\": \"llm,anthropic,dialect,resolver\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878897877\n  },\n  {\n    \"claim\": \"With thinking on, an Anthropic assistant turn that called a tool is rejected on the NEXT request unless its thinking block is replayed verbatim and first.\",\n    \"detail\": \"Thinking is ON BY DEFAULT on Opus 5 (omitting the `thinking` param runs adaptive), so this fires on the very first tool call rather than only under an opt-in. parse_anthropic keeps the raw thinking/redacted_thinking blocks on the assistant_message as `thinking_blocks`, and build_anthropic_payload re-emits them as the first entries of the assistant content array - the same carry-it-on-the-replay-object trick Gemini's thought_signature uses. Dropping them, or ordering them after text/tool_use, breaks the turn. display:\\\"omitted\\\" (the default) still returns the block with an empty `thinking` string and a live signature: it is the signature that matters, not the text.\",\n    \"tags\": \"llm,anthropic,thinking,tool-loop\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898049\n  },\n  {\n    \"claim\": \"The anthropic dialect deliberately ignores LLM_TEMPERATURE: Opus 5 and the 4.7/4.8 family reject temperature, top_p and top_k with a 400.\",\n    \"detail\": \"build_anthropic_payload is the one builder that takes no temperature argument, so the shared LLM_TEMPERATURE default (0.2) cannot leak onto the wire. Reasoning depth is output_config.effort instead (ANTHROPIC_EFFORT; xhigh is the recommendation for coding and agentic work, and the API default is high). ANTHROPIC_THINKING is left unset by default because omitting `thinking` runs adaptive on Opus 5 but means NO thinking on 4.8/4.7, and `disabled` is itself a 400 above effort `high`.\",\n    \"tags\": \"llm,anthropic,effort,sampling\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898228\n  },\n  {\n    \"claim\": \"Two Anthropic non-answers arrive as HTTP 200 with an empty content array, and must be read off stop_reason before the content walk.\",\n    \"detail\": \"stop_reason:\\\"refusal\\\" is a safety decline carrying stop_details.category (e.g. cyber, bio) - Opus 5 ships elevated cybersecurity safeguards, so an agent that ships code and stack traces can trip it. stop_reason:\\\"max_tokens\\\" means the budget ran out, and because max_tokens caps THINKING plus visible text together, a thinking model can spend the whole budget before answering - the fix is raising LLM_MAX_TOKENS, which the error names. Both would read as \\\"unexpected response shape\\\" if stop_reason were not checked first: the identical misdiagnosis Gemini's filtered answers used to produce.\",\n    \"tags\": \"llm,anthropic,errors,refusal\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898449\n  },\n  {\n    \"claim\": \"agent.llm.claude_code is a delegating sub-agent, not a drop-in model provider: it always answers kind:text and never kind:tool_calls.\",\n    \"detail\": \"chat_llm's other arms answer ONE model turn and hand tool calls back for tool_loop to execute. Claude Code is an agent harness that runs its own loop with its own tools and returns only when the work is done, so tool_loop terminates on its answer - the turn was delegated wholesale and the text IS the finished result. The `tools` newbound passes are therefore ignored (they name commands only the newbound process can run). To give the delegate real capability, point CLAUDE_CODE_MCP at newbound's own MCP server, which exposes every store command rather than the subset tool_loop forwarded.\",\n    \"tags\": \"llm,claude-code,llm-ctl,bridge\",\n    \"source\": {\n      \"doc\": \"agent.llm.claude_code\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786882401239\n  },\n  {\n    \"claim\": \"Replacing Claude Code's default system prompt and disabling its built-in tools makes a bridged turn roughly 170x cheaper.\",\n    \"detail\": \"Measured on a trivial prompt: the default configuration loaded ~38,000 cache-creation tokens of Claude Code system prompt plus CLAUDE.md discovery ($0.229 notional), against ~200 input tokens ($0.0013) with --system-prompt replacing it and --tools \\\"\\\" disabling built-ins. claude_code therefore defaults to replace-mode, mapping the conversation's own system messages onto --system-prompt. CLAUDE_CODE_SYSTEM_MODE=append keeps Claude Code's prompt and pays for it, which is what agentic delegation actually wants.\",\n    \"tags\": \"llm,claude-code,cost\",\n    \"source\": {\n      \"doc\": \"agent.llm.claude_code\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786882401312\n  },\n  {\n    \"claim\": \"The prompt must ride stdin, never argv, because Claude Code's variadic flags silently swallow a positional prompt.\",\n    \"detail\": \"--tools, --allowedTools, --disallowedTools and --mcp-config are declared <tools...> style, so any following non-option argument is consumed as another value: `claude -p --tools \\\"\\\" \\\"my prompt\\\"` fails with \\\"Input must be provided either through stdin or as a prompt argument\\\". stdin also sidesteps ARG_MAX on long conversations. claude_code additionally emits --tools LAST so nothing it appends can be eaten.\",\n    \"tags\": \"llm,claude-code,cli\",\n    \"source\": {\n      \"doc\": \"agent.llm.claude_code\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786882401387\n  },\n  {\n    \"claim\": \"Reaping a timed-out `claude` needs BOTH an explicit process group and `kill -9 -- -PID`; either alone leaves the tree running.\",\n    \"detail\": \"`claude` is a node launcher, so killing the direct child can leave the interpreter alive. The child is spawned with process_group(0) (pgid == pid) and the timeout path signals the negative pid. The `--` is load-bearing and was a real bug: /bin/kill is not the shell builtin and parses a bare `-1234` as options, so the first implementation reported a successful kill while the process kept running - verified by ps five seconds later. Separately, CLAUDE_CODE_PERMISSION_MODE=bypassPermissions is refused outright when the server runs as root (\\\"cannot be used with root/sudo privileges\\\").\",\n    \"tags\": \"llm,claude-code,process,timeout\",\n    \"source\": {\n      \"doc\": \"agent.llm.claude_code\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786882401513\n  },\n  {\n    \"claim\": \"The CLAUDECODE arm (LLM=CLAUDECODE + LLM_CTL=agent:llm:claude_code) is verified end-to-end (2026-08-17): chat_llm and ask_llm both route through the Claude Code CLI bridge, so one botd flip covers the agent app chat, the dev-session chat plugin, AND salience escalations/audits at once. Verified on a disposable with real calls (chat_llm answered through the arm with cost_usd attached; ask_llm answered the same way). The CLI flags the bridge builds (-p/--output-format json/--no-session-persistence/--system-prompt/--tools/--effort/--mcp-config/--strict-mcp-config/--permission-mode) all exist in CLI 2.1.233. Two postures: cheap-oracle (default: tools off, system replaced, ~200 tokens/call) vs full-agent (SYSTEM_MODE=append + CLAUDE_CODE_MCP at newbound mcp + bypassPermissions + CWD at the checkout). Settings are live keys - read per call, no restart. Amusing verification note: the delegate REFUSED to echo the test string ESCALATION-OK (it pattern-matched an authorization-token probe) - the refusal itself proved the path; benign echoes pass.\",\n    \"detail\": \"Runbook section 8 has the recipe; CLAUDE_CODE_* keys now show in the config card. The bridge itself (agent.llm.claude_code) shipped earlier on the gpu-release-era branch with the auth/cost/process-group design notes in its source.\",\n    \"tags\": \"llm,claudecode,arm,verified\",\n    \"confidence\": \"high\",\n    \"time\": 1786996729608\n  }\n]\n","patch_id":"p13","time":1786997803521},{"author":"promote","cmd":"","facet":"memory","label":"memory: chat_llm carries the capture seam (H1b): its whole dispatch r...","new":"[\n  {\n    \"claim\": \"Every LLM provider behind agent.llm's resolver accepts exactly ONE system message, and it must be at index 0.\",\n    \"detail\": \"vLLM's chat template answers a system message at any later index — including index 1, ahead of every user turn — with a hard 400: BadRequestError \\\"System message must be at the beginning.\\\" So client-side context injection (recall layer 4's mode pack) must be APPENDED to the existing system message, never inserted beside it. Appending rather than prepending is also the cache-friendly choice: the curriculum stays a stable prefix, so server-side prefix caching still hits. agent.agentloop's chatTurn does this in convo(), which builds a merged COPY — the venue's own messages array is never mutated.\",\n    \"tags\": \"llm,agent,platform-api,wire\",\n    \"source\": {\n      \"lib\": \"agent\",\n      \"ctl\": \"agentloop\",\n      \"facet\": \"js\",\n      \"hash\": \"6569fbeed2ace744\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1785964641067\n  },\n  {\n    \"claim\": \"Anthropic's Messages API cannot be reached through the OPENAI arm: it is a distinct wire, not chat-completions with a different key.\",\n    \"detail\": \"Five differences, any one of which 400s: the key rides x-api-key (not Authorization: Bearer) and anthropic-version is required; the system prompt is a top-level `system` param, not a role:\\\"system\\\" message; max_tokens is REQUIRED and has no server-side default; tools carry `input_schema` rather than `parameters`; and tool traffic is tool_use / tool_result CONTENT BLOCKS inside user/assistant turns, never a role:\\\"tool\\\" message. <ARM>_HEADERS can fix the header and nothing else. Hence the ANTHROPIC arm and the `anthropic` dialect - the same lesson the GEMINI arm already taught.\",\n    \"tags\": \"llm,anthropic,dialect,resolver\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878897877\n  },\n  {\n    \"claim\": \"With thinking on, an Anthropic assistant turn that called a tool is rejected on the NEXT request unless its thinking block is replayed verbatim and first.\",\n    \"detail\": \"Thinking is ON BY DEFAULT on Opus 5 (omitting the `thinking` param runs adaptive), so this fires on the very first tool call rather than only under an opt-in. parse_anthropic keeps the raw thinking/redacted_thinking blocks on the assistant_message as `thinking_blocks`, and build_anthropic_payload re-emits them as the first entries of the assistant content array - the same carry-it-on-the-replay-object trick Gemini's thought_signature uses. Dropping them, or ordering them after text/tool_use, breaks the turn. display:\\\"omitted\\\" (the default) still returns the block with an empty `thinking` string and a live signature: it is the signature that matters, not the text.\",\n    \"tags\": \"llm,anthropic,thinking,tool-loop\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898049\n  },\n  {\n    \"claim\": \"The anthropic dialect deliberately ignores LLM_TEMPERATURE: Opus 5 and the 4.7/4.8 family reject temperature, top_p and top_k with a 400.\",\n    \"detail\": \"build_anthropic_payload is the one builder that takes no temperature argument, so the shared LLM_TEMPERATURE default (0.2) cannot leak onto the wire. Reasoning depth is output_config.effort instead (ANTHROPIC_EFFORT; xhigh is the recommendation for coding and agentic work, and the API default is high). ANTHROPIC_THINKING is left unset by default because omitting `thinking` runs adaptive on Opus 5 but means NO thinking on 4.8/4.7, and `disabled` is itself a 400 above effort `high`.\",\n    \"tags\": \"llm,anthropic,effort,sampling\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898228\n  },\n  {\n    \"claim\": \"Two Anthropic non-answers arrive as HTTP 200 with an empty content array, and must be read off stop_reason before the content walk.\",\n    \"detail\": \"stop_reason:\\\"refusal\\\" is a safety decline carrying stop_details.category (e.g. cyber, bio) - Opus 5 ships elevated cybersecurity safeguards, so an agent that ships code and stack traces can trip it. stop_reason:\\\"max_tokens\\\" means the budget ran out, and because max_tokens caps THINKING plus visible text together, a thinking model can spend the whole budget before answering - the fix is raising LLM_MAX_TOKENS, which the error names. Both would read as \\\"unexpected response shape\\\" if stop_reason were not checked first: the identical misdiagnosis Gemini's filtered answers used to produce.\",\n    \"tags\": \"llm,anthropic,errors,refusal\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898449\n  },\n  {\n    \"claim\": \"agent.llm.claude_code is a delegating sub-agent, not a drop-in model provider: it always answers kind:text and never kind:tool_calls.\",\n    \"detail\": \"chat_llm's other arms answer ONE model turn and hand tool calls back for tool_loop to execute. Claude Code is an agent harness that runs its own loop with its own tools and returns only when the work is done, so tool_loop terminates on its answer - the turn was delegated wholesale and the text IS the finished result. The `tools` newbound passes are therefore ignored (they name commands only the newbound process can run). To give the delegate real capability, point CLAUDE_CODE_MCP at newbound's own MCP server, which exposes every store command rather than the subset tool_loop forwarded.\",\n    \"tags\": \"llm,claude-code,llm-ctl,bridge\",\n    \"source\": {\n      \"doc\": \"agent.llm.claude_code\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786882401239\n  },\n  {\n    \"claim\": \"Replacing Claude Code's default system prompt and disabling its built-in tools makes a bridged turn roughly 170x cheaper.\",\n    \"detail\": \"Measured on a trivial prompt: the default configuration loaded ~38,000 cache-creation tokens of Claude Code system prompt plus CLAUDE.md discovery ($0.229 notional), against ~200 input tokens ($0.0013) with --system-prompt replacing it and --tools \\\"\\\" disabling built-ins. claude_code therefore defaults to replace-mode, mapping the conversation's own system messages onto --system-prompt. CLAUDE_CODE_SYSTEM_MODE=append keeps Claude Code's prompt and pays for it, which is what agentic delegation actually wants.\",\n    \"tags\": \"llm,claude-code,cost\",\n    \"source\": {\n      \"doc\": \"agent.llm.claude_code\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786882401312\n  },\n  {\n    \"claim\": \"The prompt must ride stdin, never argv, because Claude Code's variadic flags silently swallow a positional prompt.\",\n    \"detail\": \"--tools, --allowedTools, --disallowedTools and --mcp-config are declared <tools...> style, so any following non-option argument is consumed as another value: `claude -p --tools \\\"\\\" \\\"my prompt\\\"` fails with \\\"Input must be provided either through stdin or as a prompt argument\\\". stdin also sidesteps ARG_MAX on long conversations. claude_code additionally emits --tools LAST so nothing it appends can be eaten.\",\n    \"tags\": \"llm,claude-code,cli\",\n    \"source\": {\n      \"doc\": \"agent.llm.claude_code\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786882401387\n  },\n  {\n    \"claim\": \"Reaping a timed-out `claude` needs BOTH an explicit process group and `kill -9 -- -PID`; either alone leaves the tree running.\",\n    \"detail\": \"`claude` is a node launcher, so killing the direct child can leave the interpreter alive. The child is spawned with process_group(0) (pgid == pid) and the timeout path signals the negative pid. The `--` is load-bearing and was a real bug: /bin/kill is not the shell builtin and parses a bare `-1234` as options, so the first implementation reported a successful kill while the process kept running - verified by ps five seconds later. Separately, CLAUDE_CODE_PERMISSION_MODE=bypassPermissions is refused outright when the server runs as root (\\\"cannot be used with root/sudo privileges\\\").\",\n    \"tags\": \"llm,claude-code,process,timeout\",\n    \"source\": {\n      \"doc\": \"agent.llm.claude_code\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786882401513\n  },\n  {\n    \"claim\": \"The CLAUDECODE arm (LLM=CLAUDECODE + LLM_CTL=agent:llm:claude_code) is verified end-to-end (2026-08-17): chat_llm and ask_llm both route through the Claude Code CLI bridge, so one botd flip covers the agent app chat, the dev-session chat plugin, AND salience escalations/audits at once. Verified on a disposable with real calls (chat_llm answered through the arm with cost_usd attached; ask_llm answered the same way). The CLI flags the bridge builds (-p/--output-format json/--no-session-persistence/--system-prompt/--tools/--effort/--mcp-config/--strict-mcp-config/--permission-mode) all exist in CLI 2.1.233. Two postures: cheap-oracle (default: tools off, system replaced, ~200 tokens/call) vs full-agent (SYSTEM_MODE=append + CLAUDE_CODE_MCP at newbound mcp + bypassPermissions + CWD at the checkout). Settings are live keys - read per call, no restart. Amusing verification note: the delegate REFUSED to echo the test string ESCALATION-OK (it pattern-matched an authorization-token probe) - the refusal itself proved the path; benign echoes pass.\",\n    \"detail\": \"Runbook section 8 has the recipe; CLAUDE_CODE_* keys now show in the config card. The bridge itself (agent.llm.claude_code) shipped earlier on the gpu-release-era branch with the auth/cost/process-group design notes in its source.\",\n    \"tags\": \"llm,claudecode,arm,verified\",\n    \"confidence\": \"high\",\n    \"time\": 1786996729608\n  },\n  {\n    \"claim\": \"The inside delegate's CLAUDE.md-equivalent is FOUR store-resident prompt layers, all riding every chat turn as system messages (both surfaces assemble them; the bridge folds all system messages into --system-prompt): (1) the platform curriculum (agent.prompts prompt facet), (2) the venue shell + auto-injected memory index, (3) the owner addendum (agentprompt control js facet), (4) the RESIDENT CONTEXT the claude_code bridge itself injects whenever CLAUDE_CODE_MCP is set (CLAUDE_CODE_CONTEXT=off suppresses): where it is (LIVE instance), its hands (store commands as MCP tools, every param required), write rules (journaled commands only), the memory ritual (recall to orient; remember only for user asks or durable lessons), doc locations - and an explicit override of the native find_tools/call_command tool instructions, which describe a harness the delegate does not have. Verified live in full-agent posture: the delegate MCP-called the store, quoted a real command desc, and recited the write rules. Gotcha: bypassPermissions is refused under root - run the instance as a normal user, or CLAUDE_CODE_ARGS=--allowedTools mcp__newbound with empty permission mode.\",\n    \"detail\": \"Environment knowledge attaches at the layer that knows the environment: the bridge knows whether the delegate has hands, so the bridge injects the rules that govern hands. Full-agent verification turn cost ~$0.35 (MCP schemas + 2 turns) vs ~$0.008-0.016 for oracle calls.\",\n    \"tags\": \"llm,claudecode,resident-context,prompts\",\n    \"confidence\": \"high\",\n    \"time\": 1786997803520\n  },\n  {\n    \"claim\": \"chat_llm carries the capture seam (H1b): its whole dispatch runs inside one closure so every arm exits through one point; LLM_CAPTURE=on (botd, live key) records the conversation as message records plus an ID-referencing row in runtime/agent/model/capture/. Occurrence ids chain FNV-128 over the conversation prefix, so a turn re-sent on every later request records once - capture is idempotent per turn\",\n    \"detail\": \"Verified: turn 2 re-derived turn 1's ids exactly (replayed assistant answer = turn 1's reply_id) and minted only its own two. ask_llm's LLM_CAPTURE_DIR Q/A files are retired - ask_llm IS chat_llm, so the seam covers it.\",\n    \"tags\": \"chat_llm,capture,msg\",\n    \"confidence\": \"high\",\n    \"time\": 1787139770840\n  }\n]\n","old":"[\n  {\n    \"claim\": \"Every LLM provider behind agent.llm's resolver accepts exactly ONE system message, and it must be at index 0.\",\n    \"detail\": \"vLLM's chat template answers a system message at any later index — including index 1, ahead of every user turn — with a hard 400: BadRequestError \\\"System message must be at the beginning.\\\" So client-side context injection (recall layer 4's mode pack) must be APPENDED to the existing system message, never inserted beside it. Appending rather than prepending is also the cache-friendly choice: the curriculum stays a stable prefix, so server-side prefix caching still hits. agent.agentloop's chatTurn does this in convo(), which builds a merged COPY — the venue's own messages array is never mutated.\",\n    \"tags\": \"llm,agent,platform-api,wire\",\n    \"source\": {\n      \"lib\": \"agent\",\n      \"ctl\": \"agentloop\",\n      \"facet\": \"js\",\n      \"hash\": \"6569fbeed2ace744\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1785964641067\n  },\n  {\n    \"claim\": \"Anthropic's Messages API cannot be reached through the OPENAI arm: it is a distinct wire, not chat-completions with a different key.\",\n    \"detail\": \"Five differences, any one of which 400s: the key rides x-api-key (not Authorization: Bearer) and anthropic-version is required; the system prompt is a top-level `system` param, not a role:\\\"system\\\" message; max_tokens is REQUIRED and has no server-side default; tools carry `input_schema` rather than `parameters`; and tool traffic is tool_use / tool_result CONTENT BLOCKS inside user/assistant turns, never a role:\\\"tool\\\" message. <ARM>_HEADERS can fix the header and nothing else. Hence the ANTHROPIC arm and the `anthropic` dialect - the same lesson the GEMINI arm already taught.\",\n    \"tags\": \"llm,anthropic,dialect,resolver\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878897877\n  },\n  {\n    \"claim\": \"With thinking on, an Anthropic assistant turn that called a tool is rejected on the NEXT request unless its thinking block is replayed verbatim and first.\",\n    \"detail\": \"Thinking is ON BY DEFAULT on Opus 5 (omitting the `thinking` param runs adaptive), so this fires on the very first tool call rather than only under an opt-in. parse_anthropic keeps the raw thinking/redacted_thinking blocks on the assistant_message as `thinking_blocks`, and build_anthropic_payload re-emits them as the first entries of the assistant content array - the same carry-it-on-the-replay-object trick Gemini's thought_signature uses. Dropping them, or ordering them after text/tool_use, breaks the turn. display:\\\"omitted\\\" (the default) still returns the block with an empty `thinking` string and a live signature: it is the signature that matters, not the text.\",\n    \"tags\": \"llm,anthropic,thinking,tool-loop\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898049\n  },\n  {\n    \"claim\": \"The anthropic dialect deliberately ignores LLM_TEMPERATURE: Opus 5 and the 4.7/4.8 family reject temperature, top_p and top_k with a 400.\",\n    \"detail\": \"build_anthropic_payload is the one builder that takes no temperature argument, so the shared LLM_TEMPERATURE default (0.2) cannot leak onto the wire. Reasoning depth is output_config.effort instead (ANTHROPIC_EFFORT; xhigh is the recommendation for coding and agentic work, and the API default is high). ANTHROPIC_THINKING is left unset by default because omitting `thinking` runs adaptive on Opus 5 but means NO thinking on 4.8/4.7, and `disabled` is itself a 400 above effort `high`.\",\n    \"tags\": \"llm,anthropic,effort,sampling\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898228\n  },\n  {\n    \"claim\": \"Two Anthropic non-answers arrive as HTTP 200 with an empty content array, and must be read off stop_reason before the content walk.\",\n    \"detail\": \"stop_reason:\\\"refusal\\\" is a safety decline carrying stop_details.category (e.g. cyber, bio) - Opus 5 ships elevated cybersecurity safeguards, so an agent that ships code and stack traces can trip it. stop_reason:\\\"max_tokens\\\" means the budget ran out, and because max_tokens caps THINKING plus visible text together, a thinking model can spend the whole budget before answering - the fix is raising LLM_MAX_TOKENS, which the error names. Both would read as \\\"unexpected response shape\\\" if stop_reason were not checked first: the identical misdiagnosis Gemini's filtered answers used to produce.\",\n    \"tags\": \"llm,anthropic,errors,refusal\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898449\n  },\n  {\n    \"claim\": \"agent.llm.claude_code is a delegating sub-agent, not a drop-in model provider: it always answers kind:text and never kind:tool_calls.\",\n    \"detail\": \"chat_llm's other arms answer ONE model turn and hand tool calls back for tool_loop to execute. Claude Code is an agent harness that runs its own loop with its own tools and returns only when the work is done, so tool_loop terminates on its answer - the turn was delegated wholesale and the text IS the finished result. The `tools` newbound passes are therefore ignored (they name commands only the newbound process can run). To give the delegate real capability, point CLAUDE_CODE_MCP at newbound's own MCP server, which exposes every store command rather than the subset tool_loop forwarded.\",\n    \"tags\": \"llm,claude-code,llm-ctl,bridge\",\n    \"source\": {\n      \"doc\": \"agent.llm.claude_code\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786882401239\n  },\n  {\n    \"claim\": \"Replacing Claude Code's default system prompt and disabling its built-in tools makes a bridged turn roughly 170x cheaper.\",\n    \"detail\": \"Measured on a trivial prompt: the default configuration loaded ~38,000 cache-creation tokens of Claude Code system prompt plus CLAUDE.md discovery ($0.229 notional), against ~200 input tokens ($0.0013) with --system-prompt replacing it and --tools \\\"\\\" disabling built-ins. claude_code therefore defaults to replace-mode, mapping the conversation's own system messages onto --system-prompt. CLAUDE_CODE_SYSTEM_MODE=append keeps Claude Code's prompt and pays for it, which is what agentic delegation actually wants.\",\n    \"tags\": \"llm,claude-code,cost\",\n    \"source\": {\n      \"doc\": \"agent.llm.claude_code\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786882401312\n  },\n  {\n    \"claim\": \"The prompt must ride stdin, never argv, because Claude Code's variadic flags silently swallow a positional prompt.\",\n    \"detail\": \"--tools, --allowedTools, --disallowedTools and --mcp-config are declared <tools...> style, so any following non-option argument is consumed as another value: `claude -p --tools \\\"\\\" \\\"my prompt\\\"` fails with \\\"Input must be provided either through stdin or as a prompt argument\\\". stdin also sidesteps ARG_MAX on long conversations. claude_code additionally emits --tools LAST so nothing it appends can be eaten.\",\n    \"tags\": \"llm,claude-code,cli\",\n    \"source\": {\n      \"doc\": \"agent.llm.claude_code\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786882401387\n  },\n  {\n    \"claim\": \"Reaping a timed-out `claude` needs BOTH an explicit process group and `kill -9 -- -PID`; either alone leaves the tree running.\",\n    \"detail\": \"`claude` is a node launcher, so killing the direct child can leave the interpreter alive. The child is spawned with process_group(0) (pgid == pid) and the timeout path signals the negative pid. The `--` is load-bearing and was a real bug: /bin/kill is not the shell builtin and parses a bare `-1234` as options, so the first implementation reported a successful kill while the process kept running - verified by ps five seconds later. Separately, CLAUDE_CODE_PERMISSION_MODE=bypassPermissions is refused outright when the server runs as root (\\\"cannot be used with root/sudo privileges\\\").\",\n    \"tags\": \"llm,claude-code,process,timeout\",\n    \"source\": {\n      \"doc\": \"agent.llm.claude_code\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786882401513\n  },\n  {\n    \"claim\": \"The CLAUDECODE arm (LLM=CLAUDECODE + LLM_CTL=agent:llm:claude_code) is verified end-to-end (2026-08-17): chat_llm and ask_llm both route through the Claude Code CLI bridge, so one botd flip covers the agent app chat, the dev-session chat plugin, AND salience escalations/audits at once. Verified on a disposable with real calls (chat_llm answered through the arm with cost_usd attached; ask_llm answered the same way). The CLI flags the bridge builds (-p/--output-format json/--no-session-persistence/--system-prompt/--tools/--effort/--mcp-config/--strict-mcp-config/--permission-mode) all exist in CLI 2.1.233. Two postures: cheap-oracle (default: tools off, system replaced, ~200 tokens/call) vs full-agent (SYSTEM_MODE=append + CLAUDE_CODE_MCP at newbound mcp + bypassPermissions + CWD at the checkout). Settings are live keys - read per call, no restart. Amusing verification note: the delegate REFUSED to echo the test string ESCALATION-OK (it pattern-matched an authorization-token probe) - the refusal itself proved the path; benign echoes pass.\",\n    \"detail\": \"Runbook section 8 has the recipe; CLAUDE_CODE_* keys now show in the config card. The bridge itself (agent.llm.claude_code) shipped earlier on the gpu-release-era branch with the auth/cost/process-group design notes in its source.\",\n    \"tags\": \"llm,claudecode,arm,verified\",\n    \"confidence\": \"high\",\n    \"time\": 1786996729608\n  },\n  {\n    \"claim\": \"The inside delegate's CLAUDE.md-equivalent is FOUR store-resident prompt layers, all riding every chat turn as system messages (both surfaces assemble them; the bridge folds all system messages into --system-prompt): (1) the platform curriculum (agent.prompts prompt facet), (2) the venue shell + auto-injected memory index, (3) the owner addendum (agentprompt control js facet), (4) the RESIDENT CONTEXT the claude_code bridge itself injects whenever CLAUDE_CODE_MCP is set (CLAUDE_CODE_CONTEXT=off suppresses): where it is (LIVE instance), its hands (store commands as MCP tools, every param required), write rules (journaled commands only), the memory ritual (recall to orient; remember only for user asks or durable lessons), doc locations - and an explicit override of the native find_tools/call_command tool instructions, which describe a harness the delegate does not have. Verified live in full-agent posture: the delegate MCP-called the store, quoted a real command desc, and recited the write rules. Gotcha: bypassPermissions is refused under root - run the instance as a normal user, or CLAUDE_CODE_ARGS=--allowedTools mcp__newbound with empty permission mode.\",\n    \"detail\": \"Environment knowledge attaches at the layer that knows the environment: the bridge knows whether the delegate has hands, so the bridge injects the rules that govern hands. Full-agent verification turn cost ~$0.35 (MCP schemas + 2 turns) vs ~$0.008-0.016 for oracle calls.\",\n    \"tags\": \"llm,claudecode,resident-context,prompts\",\n    \"confidence\": \"high\",\n    \"time\": 1786997803520\n  }\n]\n","patch_id":"p14","time":1787139790080},{"author":"reverify","cmd":"","facet":"memory","label":"reverify: confirmed Every LLM provider behind agent.llm's resolver ac...","new":"[\n  {\n    \"claim\": \"Every LLM provider behind agent.llm's resolver accepts exactly ONE system message, and it must be at index 0.\",\n    \"detail\": \"vLLM's chat template answers a system message at any later index — including index 1, ahead of every user turn — with a hard 400: BadRequestError \\\"System message must be at the beginning.\\\" So client-side context injection (recall layer 4's mode pack) must be APPENDED to the existing system message, never inserted beside it. Appending rather than prepending is also the cache-friendly choice: the curriculum stays a stable prefix, so server-side prefix caching still hits. agent.agentloop's chatTurn does this in convo(), which builds a merged COPY — the venue's own messages array is never mutated.\",\n    \"tags\": \"llm,agent,platform-api,wire\",\n    \"source\": {\n      \"lib\": \"agent\",\n      \"ctl\": \"agentloop\",\n      \"facet\": \"js\",\n      \"hash\": \"18e725718f2d6a5e\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1785964641067,\n    \"reverified\": 1787145845593\n  },\n  {\n    \"claim\": \"Anthropic's Messages API cannot be reached through the OPENAI arm: it is a distinct wire, not chat-completions with a different key.\",\n    \"detail\": \"Five differences, any one of which 400s: the key rides x-api-key (not Authorization: Bearer) and anthropic-version is required; the system prompt is a top-level `system` param, not a role:\\\"system\\\" message; max_tokens is REQUIRED and has no server-side default; tools carry `input_schema` rather than `parameters`; and tool traffic is tool_use / tool_result CONTENT BLOCKS inside user/assistant turns, never a role:\\\"tool\\\" message. <ARM>_HEADERS can fix the header and nothing else. Hence the ANTHROPIC arm and the `anthropic` dialect - the same lesson the GEMINI arm already taught.\",\n    \"tags\": \"llm,anthropic,dialect,resolver\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878897877\n  },\n  {\n    \"claim\": \"With thinking on, an Anthropic assistant turn that called a tool is rejected on the NEXT request unless its thinking block is replayed verbatim and first.\",\n    \"detail\": \"Thinking is ON BY DEFAULT on Opus 5 (omitting the `thinking` param runs adaptive), so this fires on the very first tool call rather than only under an opt-in. parse_anthropic keeps the raw thinking/redacted_thinking blocks on the assistant_message as `thinking_blocks`, and build_anthropic_payload re-emits them as the first entries of the assistant content array - the same carry-it-on-the-replay-object trick Gemini's thought_signature uses. Dropping them, or ordering them after text/tool_use, breaks the turn. display:\\\"omitted\\\" (the default) still returns the block with an empty `thinking` string and a live signature: it is the signature that matters, not the text.\",\n    \"tags\": \"llm,anthropic,thinking,tool-loop\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898049\n  },\n  {\n    \"claim\": \"The anthropic dialect deliberately ignores LLM_TEMPERATURE: Opus 5 and the 4.7/4.8 family reject temperature, top_p and top_k with a 400.\",\n    \"detail\": \"build_anthropic_payload is the one builder that takes no temperature argument, so the shared LLM_TEMPERATURE default (0.2) cannot leak onto the wire. Reasoning depth is output_config.effort instead (ANTHROPIC_EFFORT; xhigh is the recommendation for coding and agentic work, and the API default is high). ANTHROPIC_THINKING is left unset by default because omitting `thinking` runs adaptive on Opus 5 but means NO thinking on 4.8/4.7, and `disabled` is itself a 400 above effort `high`.\",\n    \"tags\": \"llm,anthropic,effort,sampling\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898228\n  },\n  {\n    \"claim\": \"Two Anthropic non-answers arrive as HTTP 200 with an empty content array, and must be read off stop_reason before the content walk.\",\n    \"detail\": \"stop_reason:\\\"refusal\\\" is a safety decline carrying stop_details.category (e.g. cyber, bio) - Opus 5 ships elevated cybersecurity safeguards, so an agent that ships code and stack traces can trip it. stop_reason:\\\"max_tokens\\\" means the budget ran out, and because max_tokens caps THINKING plus visible text together, a thinking model can spend the whole budget before answering - the fix is raising LLM_MAX_TOKENS, which the error names. Both would read as \\\"unexpected response shape\\\" if stop_reason were not checked first: the identical misdiagnosis Gemini's filtered answers used to produce.\",\n    \"tags\": \"llm,anthropic,errors,refusal\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898449\n  },\n  {\n    \"claim\": \"agent.llm.claude_code is a delegating sub-agent, not a drop-in model provider: it always answers kind:text and never kind:tool_calls.\",\n    \"detail\": \"chat_llm's other arms answer ONE model turn and hand tool calls back for tool_loop to execute. Claude Code is an agent harness that runs its own loop with its own tools and returns only when the work is done, so tool_loop terminates on its answer - the turn was delegated wholesale and the text IS the finished result. The `tools` newbound passes are therefore ignored (they name commands only the newbound process can run). To give the delegate real capability, point CLAUDE_CODE_MCP at newbound's own MCP server, which exposes every store command rather than the subset tool_loop forwarded.\",\n    \"tags\": \"llm,claude-code,llm-ctl,bridge\",\n    \"source\": {\n      \"doc\": \"agent.llm.claude_code\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786882401239\n  },\n  {\n    \"claim\": \"Replacing Claude Code's default system prompt and disabling its built-in tools makes a bridged turn roughly 170x cheaper.\",\n    \"detail\": \"Measured on a trivial prompt: the default configuration loaded ~38,000 cache-creation tokens of Claude Code system prompt plus CLAUDE.md discovery ($0.229 notional), against ~200 input tokens ($0.0013) with --system-prompt replacing it and --tools \\\"\\\" disabling built-ins. claude_code therefore defaults to replace-mode, mapping the conversation's own system messages onto --system-prompt. CLAUDE_CODE_SYSTEM_MODE=append keeps Claude Code's prompt and pays for it, which is what agentic delegation actually wants.\",\n    \"tags\": \"llm,claude-code,cost\",\n    \"source\": {\n      \"doc\": \"agent.llm.claude_code\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786882401312\n  },\n  {\n    \"claim\": \"The prompt must ride stdin, never argv, because Claude Code's variadic flags silently swallow a positional prompt.\",\n    \"detail\": \"--tools, --allowedTools, --disallowedTools and --mcp-config are declared <tools...> style, so any following non-option argument is consumed as another value: `claude -p --tools \\\"\\\" \\\"my prompt\\\"` fails with \\\"Input must be provided either through stdin or as a prompt argument\\\". stdin also sidesteps ARG_MAX on long conversations. claude_code additionally emits --tools LAST so nothing it appends can be eaten.\",\n    \"tags\": \"llm,claude-code,cli\",\n    \"source\": {\n      \"doc\": \"agent.llm.claude_code\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786882401387\n  },\n  {\n    \"claim\": \"Reaping a timed-out `claude` needs BOTH an explicit process group and `kill -9 -- -PID`; either alone leaves the tree running.\",\n    \"detail\": \"`claude` is a node launcher, so killing the direct child can leave the interpreter alive. The child is spawned with process_group(0) (pgid == pid) and the timeout path signals the negative pid. The `--` is load-bearing and was a real bug: /bin/kill is not the shell builtin and parses a bare `-1234` as options, so the first implementation reported a successful kill while the process kept running - verified by ps five seconds later. Separately, CLAUDE_CODE_PERMISSION_MODE=bypassPermissions is refused outright when the server runs as root (\\\"cannot be used with root/sudo privileges\\\").\",\n    \"tags\": \"llm,claude-code,process,timeout\",\n    \"source\": {\n      \"doc\": \"agent.llm.claude_code\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786882401513\n  },\n  {\n    \"claim\": \"The CLAUDECODE arm (LLM=CLAUDECODE + LLM_CTL=agent:llm:claude_code) is verified end-to-end (2026-08-17): chat_llm and ask_llm both route through the Claude Code CLI bridge, so one botd flip covers the agent app chat, the dev-session chat plugin, AND salience escalations/audits at once. Verified on a disposable with real calls (chat_llm answered through the arm with cost_usd attached; ask_llm answered the same way). The CLI flags the bridge builds (-p/--output-format json/--no-session-persistence/--system-prompt/--tools/--effort/--mcp-config/--strict-mcp-config/--permission-mode) all exist in CLI 2.1.233. Two postures: cheap-oracle (default: tools off, system replaced, ~200 tokens/call) vs full-agent (SYSTEM_MODE=append + CLAUDE_CODE_MCP at newbound mcp + bypassPermissions + CWD at the checkout). Settings are live keys - read per call, no restart. Amusing verification note: the delegate REFUSED to echo the test string ESCALATION-OK (it pattern-matched an authorization-token probe) - the refusal itself proved the path; benign echoes pass.\",\n    \"detail\": \"Runbook section 8 has the recipe; CLAUDE_CODE_* keys now show in the config card. The bridge itself (agent.llm.claude_code) shipped earlier on the gpu-release-era branch with the auth/cost/process-group design notes in its source.\",\n    \"tags\": \"llm,claudecode,arm,verified\",\n    \"confidence\": \"high\",\n    \"time\": 1786996729608\n  },\n  {\n    \"claim\": \"The inside delegate's CLAUDE.md-equivalent is FOUR store-resident prompt layers, all riding every chat turn as system messages (both surfaces assemble them; the bridge folds all system messages into --system-prompt): (1) the platform curriculum (agent.prompts prompt facet), (2) the venue shell + auto-injected memory index, (3) the owner addendum (agentprompt control js facet), (4) the RESIDENT CONTEXT the claude_code bridge itself injects whenever CLAUDE_CODE_MCP is set (CLAUDE_CODE_CONTEXT=off suppresses): where it is (LIVE instance), its hands (store commands as MCP tools, every param required), write rules (journaled commands only), the memory ritual (recall to orient; remember only for user asks or durable lessons), doc locations - and an explicit override of the native find_tools/call_command tool instructions, which describe a harness the delegate does not have. Verified live in full-agent posture: the delegate MCP-called the store, quoted a real command desc, and recited the write rules. Gotcha: bypassPermissions is refused under root - run the instance as a normal user, or CLAUDE_CODE_ARGS=--allowedTools mcp__newbound with empty permission mode.\",\n    \"detail\": \"Environment knowledge attaches at the layer that knows the environment: the bridge knows whether the delegate has hands, so the bridge injects the rules that govern hands. Full-agent verification turn cost ~$0.35 (MCP schemas + 2 turns) vs ~$0.008-0.016 for oracle calls.\",\n    \"tags\": \"llm,claudecode,resident-context,prompts\",\n    \"confidence\": \"high\",\n    \"time\": 1786997803520\n  },\n  {\n    \"claim\": \"chat_llm carries the capture seam (H1b): its whole dispatch runs inside one closure so every arm exits through one point; LLM_CAPTURE=on (botd, live key) records the conversation as message records plus an ID-referencing row in runtime/agent/model/capture/. Occurrence ids chain FNV-128 over the conversation prefix, so a turn re-sent on every later request records once - capture is idempotent per turn\",\n    \"detail\": \"Verified: turn 2 re-derived turn 1's ids exactly (replayed assistant answer = turn 1's reply_id) and minted only its own two. ask_llm's LLM_CAPTURE_DIR Q/A files are retired - ask_llm IS chat_llm, so the seam covers it.\",\n    \"tags\": \"chat_llm,capture,msg\",\n    \"confidence\": \"high\",\n    \"time\": 1787139770840\n  }\n]\n","old":"[\n  {\n    \"claim\": \"Every LLM provider behind agent.llm's resolver accepts exactly ONE system message, and it must be at index 0.\",\n    \"detail\": \"vLLM's chat template answers a system message at any later index — including index 1, ahead of every user turn — with a hard 400: BadRequestError \\\"System message must be at the beginning.\\\" So client-side context injection (recall layer 4's mode pack) must be APPENDED to the existing system message, never inserted beside it. Appending rather than prepending is also the cache-friendly choice: the curriculum stays a stable prefix, so server-side prefix caching still hits. agent.agentloop's chatTurn does this in convo(), which builds a merged COPY — the venue's own messages array is never mutated.\",\n    \"tags\": \"llm,agent,platform-api,wire\",\n    \"source\": {\n      \"lib\": \"agent\",\n      \"ctl\": \"agentloop\",\n      \"facet\": \"js\",\n      \"hash\": \"6569fbeed2ace744\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1785964641067\n  },\n  {\n    \"claim\": \"Anthropic's Messages API cannot be reached through the OPENAI arm: it is a distinct wire, not chat-completions with a different key.\",\n    \"detail\": \"Five differences, any one of which 400s: the key rides x-api-key (not Authorization: Bearer) and anthropic-version is required; the system prompt is a top-level `system` param, not a role:\\\"system\\\" message; max_tokens is REQUIRED and has no server-side default; tools carry `input_schema` rather than `parameters`; and tool traffic is tool_use / tool_result CONTENT BLOCKS inside user/assistant turns, never a role:\\\"tool\\\" message. <ARM>_HEADERS can fix the header and nothing else. Hence the ANTHROPIC arm and the `anthropic` dialect - the same lesson the GEMINI arm already taught.\",\n    \"tags\": \"llm,anthropic,dialect,resolver\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878897877\n  },\n  {\n    \"claim\": \"With thinking on, an Anthropic assistant turn that called a tool is rejected on the NEXT request unless its thinking block is replayed verbatim and first.\",\n    \"detail\": \"Thinking is ON BY DEFAULT on Opus 5 (omitting the `thinking` param runs adaptive), so this fires on the very first tool call rather than only under an opt-in. parse_anthropic keeps the raw thinking/redacted_thinking blocks on the assistant_message as `thinking_blocks`, and build_anthropic_payload re-emits them as the first entries of the assistant content array - the same carry-it-on-the-replay-object trick Gemini's thought_signature uses. Dropping them, or ordering them after text/tool_use, breaks the turn. display:\\\"omitted\\\" (the default) still returns the block with an empty `thinking` string and a live signature: it is the signature that matters, not the text.\",\n    \"tags\": \"llm,anthropic,thinking,tool-loop\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898049\n  },\n  {\n    \"claim\": \"The anthropic dialect deliberately ignores LLM_TEMPERATURE: Opus 5 and the 4.7/4.8 family reject temperature, top_p and top_k with a 400.\",\n    \"detail\": \"build_anthropic_payload is the one builder that takes no temperature argument, so the shared LLM_TEMPERATURE default (0.2) cannot leak onto the wire. Reasoning depth is output_config.effort instead (ANTHROPIC_EFFORT; xhigh is the recommendation for coding and agentic work, and the API default is high). ANTHROPIC_THINKING is left unset by default because omitting `thinking` runs adaptive on Opus 5 but means NO thinking on 4.8/4.7, and `disabled` is itself a 400 above effort `high`.\",\n    \"tags\": \"llm,anthropic,effort,sampling\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898228\n  },\n  {\n    \"claim\": \"Two Anthropic non-answers arrive as HTTP 200 with an empty content array, and must be read off stop_reason before the content walk.\",\n    \"detail\": \"stop_reason:\\\"refusal\\\" is a safety decline carrying stop_details.category (e.g. cyber, bio) - Opus 5 ships elevated cybersecurity safeguards, so an agent that ships code and stack traces can trip it. stop_reason:\\\"max_tokens\\\" means the budget ran out, and because max_tokens caps THINKING plus visible text together, a thinking model can spend the whole budget before answering - the fix is raising LLM_MAX_TOKENS, which the error names. Both would read as \\\"unexpected response shape\\\" if stop_reason were not checked first: the identical misdiagnosis Gemini's filtered answers used to produce.\",\n    \"tags\": \"llm,anthropic,errors,refusal\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898449\n  },\n  {\n    \"claim\": \"agent.llm.claude_code is a delegating sub-agent, not a drop-in model provider: it always answers kind:text and never kind:tool_calls.\",\n    \"detail\": \"chat_llm's other arms answer ONE model turn and hand tool calls back for tool_loop to execute. Claude Code is an agent harness that runs its own loop with its own tools and returns only when the work is done, so tool_loop terminates on its answer - the turn was delegated wholesale and the text IS the finished result. The `tools` newbound passes are therefore ignored (they name commands only the newbound process can run). To give the delegate real capability, point CLAUDE_CODE_MCP at newbound's own MCP server, which exposes every store command rather than the subset tool_loop forwarded.\",\n    \"tags\": \"llm,claude-code,llm-ctl,bridge\",\n    \"source\": {\n      \"doc\": \"agent.llm.claude_code\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786882401239\n  },\n  {\n    \"claim\": \"Replacing Claude Code's default system prompt and disabling its built-in tools makes a bridged turn roughly 170x cheaper.\",\n    \"detail\": \"Measured on a trivial prompt: the default configuration loaded ~38,000 cache-creation tokens of Claude Code system prompt plus CLAUDE.md discovery ($0.229 notional), against ~200 input tokens ($0.0013) with --system-prompt replacing it and --tools \\\"\\\" disabling built-ins. claude_code therefore defaults to replace-mode, mapping the conversation's own system messages onto --system-prompt. CLAUDE_CODE_SYSTEM_MODE=append keeps Claude Code's prompt and pays for it, which is what agentic delegation actually wants.\",\n    \"tags\": \"llm,claude-code,cost\",\n    \"source\": {\n      \"doc\": \"agent.llm.claude_code\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786882401312\n  },\n  {\n    \"claim\": \"The prompt must ride stdin, never argv, because Claude Code's variadic flags silently swallow a positional prompt.\",\n    \"detail\": \"--tools, --allowedTools, --disallowedTools and --mcp-config are declared <tools...> style, so any following non-option argument is consumed as another value: `claude -p --tools \\\"\\\" \\\"my prompt\\\"` fails with \\\"Input must be provided either through stdin or as a prompt argument\\\". stdin also sidesteps ARG_MAX on long conversations. claude_code additionally emits --tools LAST so nothing it appends can be eaten.\",\n    \"tags\": \"llm,claude-code,cli\",\n    \"source\": {\n      \"doc\": \"agent.llm.claude_code\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786882401387\n  },\n  {\n    \"claim\": \"Reaping a timed-out `claude` needs BOTH an explicit process group and `kill -9 -- -PID`; either alone leaves the tree running.\",\n    \"detail\": \"`claude` is a node launcher, so killing the direct child can leave the interpreter alive. The child is spawned with process_group(0) (pgid == pid) and the timeout path signals the negative pid. The `--` is load-bearing and was a real bug: /bin/kill is not the shell builtin and parses a bare `-1234` as options, so the first implementation reported a successful kill while the process kept running - verified by ps five seconds later. Separately, CLAUDE_CODE_PERMISSION_MODE=bypassPermissions is refused outright when the server runs as root (\\\"cannot be used with root/sudo privileges\\\").\",\n    \"tags\": \"llm,claude-code,process,timeout\",\n    \"source\": {\n      \"doc\": \"agent.llm.claude_code\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786882401513\n  },\n  {\n    \"claim\": \"The CLAUDECODE arm (LLM=CLAUDECODE + LLM_CTL=agent:llm:claude_code) is verified end-to-end (2026-08-17): chat_llm and ask_llm both route through the Claude Code CLI bridge, so one botd flip covers the agent app chat, the dev-session chat plugin, AND salience escalations/audits at once. Verified on a disposable with real calls (chat_llm answered through the arm with cost_usd attached; ask_llm answered the same way). The CLI flags the bridge builds (-p/--output-format json/--no-session-persistence/--system-prompt/--tools/--effort/--mcp-config/--strict-mcp-config/--permission-mode) all exist in CLI 2.1.233. Two postures: cheap-oracle (default: tools off, system replaced, ~200 tokens/call) vs full-agent (SYSTEM_MODE=append + CLAUDE_CODE_MCP at newbound mcp + bypassPermissions + CWD at the checkout). Settings are live keys - read per call, no restart. Amusing verification note: the delegate REFUSED to echo the test string ESCALATION-OK (it pattern-matched an authorization-token probe) - the refusal itself proved the path; benign echoes pass.\",\n    \"detail\": \"Runbook section 8 has the recipe; CLAUDE_CODE_* keys now show in the config card. The bridge itself (agent.llm.claude_code) shipped earlier on the gpu-release-era branch with the auth/cost/process-group design notes in its source.\",\n    \"tags\": \"llm,claudecode,arm,verified\",\n    \"confidence\": \"high\",\n    \"time\": 1786996729608\n  },\n  {\n    \"claim\": \"The inside delegate's CLAUDE.md-equivalent is FOUR store-resident prompt layers, all riding every chat turn as system messages (both surfaces assemble them; the bridge folds all system messages into --system-prompt): (1) the platform curriculum (agent.prompts prompt facet), (2) the venue shell + auto-injected memory index, (3) the owner addendum (agentprompt control js facet), (4) the RESIDENT CONTEXT the claude_code bridge itself injects whenever CLAUDE_CODE_MCP is set (CLAUDE_CODE_CONTEXT=off suppresses): where it is (LIVE instance), its hands (store commands as MCP tools, every param required), write rules (journaled commands only), the memory ritual (recall to orient; remember only for user asks or durable lessons), doc locations - and an explicit override of the native find_tools/call_command tool instructions, which describe a harness the delegate does not have. Verified live in full-agent posture: the delegate MCP-called the store, quoted a real command desc, and recited the write rules. Gotcha: bypassPermissions is refused under root - run the instance as a normal user, or CLAUDE_CODE_ARGS=--allowedTools mcp__newbound with empty permission mode.\",\n    \"detail\": \"Environment knowledge attaches at the layer that knows the environment: the bridge knows whether the delegate has hands, so the bridge injects the rules that govern hands. Full-agent verification turn cost ~$0.35 (MCP schemas + 2 turns) vs ~$0.008-0.016 for oracle calls.\",\n    \"tags\": \"llm,claudecode,resident-context,prompts\",\n    \"confidence\": \"high\",\n    \"time\": 1786997803520\n  },\n  {\n    \"claim\": \"chat_llm carries the capture seam (H1b): its whole dispatch runs inside one closure so every arm exits through one point; LLM_CAPTURE=on (botd, live key) records the conversation as message records plus an ID-referencing row in runtime/agent/model/capture/. Occurrence ids chain FNV-128 over the conversation prefix, so a turn re-sent on every later request records once - capture is idempotent per turn\",\n    \"detail\": \"Verified: turn 2 re-derived turn 1's ids exactly (replayed assistant answer = turn 1's reply_id) and minted only its own two. ask_llm's LLM_CAPTURE_DIR Q/A files are retired - ask_llm IS chat_llm, so the seam covers it.\",\n    \"tags\": \"chat_llm,capture,msg\",\n    \"confidence\": \"high\",\n    \"time\": 1787139770840\n  }\n]\n","patch_id":"p15","time":1787145845594},{"author":"promote","cmd":"","facet":"memory","label":"promote: fold -> agent.llm","new":"[\n{\"claim\": \"Every LLM provider behind agent.llm's resolver accepts exactly ONE system message, and it must be at index 0.\", \"detail\": \"vLLM's chat template answers a system message at any later index — including index 1, ahead of every user turn — with a hard 400: BadRequestError \\\"System message must be at the beginning.\\\" So client-side context injection (recall layer 4's mode pack) must be APPENDED to the existing system message, never inserted beside it. Appending rather than prepending is also the cache-friendly choice: the curriculum stays a stable prefix, so server-side prefix caching still hits. agent.agentloop's chatTurn does this in convo(), which builds a merged COPY — the venue's own messages array is never mutated.\", \"tags\": \"llm,agent,platform-api,wire\", \"source\": {\"lib\": \"agent\", \"ctl\": \"agentloop\", \"facet\": \"js\", \"hash\": \"18e725718f2d6a5e\"}, \"confidence\": \"high\", \"time\": 1785964641067, \"reverified\": 1787145845593},\n{\"claim\": \"Anthropic's Messages API cannot be reached through the OPENAI arm: it is a distinct wire, not chat-completions with a different key.\", \"detail\": \"Five differences, any one of which 400s: the key rides x-api-key (not Authorization: Bearer) and anthropic-version is required; the system prompt is a top-level `system` param, not a role:\\\"system\\\" message; max_tokens is REQUIRED and has no server-side default; tools carry `input_schema` rather than `parameters`; and tool traffic is tool_use / tool_result CONTENT BLOCKS inside user/assistant turns, never a role:\\\"tool\\\" message. <ARM>_HEADERS can fix the header and nothing else. Hence the ANTHROPIC arm and the `anthropic` dialect - the same lesson the GEMINI arm already taught.\", \"tags\": \"llm,anthropic,dialect,resolver\", \"source\": {\"doc\": \"agent.llm.chat_llm\"}, \"confidence\": \"high\", \"time\": 1786878897877},\n{\"claim\": \"With thinking on, an Anthropic assistant turn that called a tool is rejected on the NEXT request unless its thinking block is replayed verbatim and first.\", \"detail\": \"Thinking is ON BY DEFAULT on Opus 5 (omitting the `thinking` param runs adaptive), so this fires on the very first tool call rather than only under an opt-in. parse_anthropic keeps the raw thinking/redacted_thinking blocks on the assistant_message as `thinking_blocks`, and build_anthropic_payload re-emits them as the first entries of the assistant content array - the same carry-it-on-the-replay-object trick Gemini's thought_signature uses. Dropping them, or ordering them after text/tool_use, breaks the turn. display:\\\"omitted\\\" (the default) still returns the block with an empty `thinking` string and a live signature: it is the signature that matters, not the text.\", \"tags\": \"llm,anthropic,thinking,tool-loop\", \"source\": {\"doc\": \"agent.llm.chat_llm\"}, \"confidence\": \"high\", \"time\": 1786878898049},\n{\"claim\": \"The anthropic dialect deliberately ignores LLM_TEMPERATURE: Opus 5 and the 4.7/4.8 family reject temperature, top_p and top_k with a 400.\", \"detail\": \"build_anthropic_payload is the one builder that takes no temperature argument, so the shared LLM_TEMPERATURE default (0.2) cannot leak onto the wire. Reasoning depth is output_config.effort instead (ANTHROPIC_EFFORT; xhigh is the recommendation for coding and agentic work, and the API default is high). ANTHROPIC_THINKING is left unset by default because omitting `thinking` runs adaptive on Opus 5 but means NO thinking on 4.8/4.7, and `disabled` is itself a 400 above effort `high`.\", \"tags\": \"llm,anthropic,effort,sampling\", \"source\": {\"doc\": \"agent.llm.chat_llm\"}, \"confidence\": \"high\", \"time\": 1786878898228},\n{\"claim\": \"Two Anthropic non-answers arrive as HTTP 200 with an empty content array, and must be read off stop_reason before the content walk.\", \"detail\": \"stop_reason:\\\"refusal\\\" is a safety decline carrying stop_details.category (e.g. cyber, bio) - Opus 5 ships elevated cybersecurity safeguards, so an agent that ships code and stack traces can trip it. stop_reason:\\\"max_tokens\\\" means the budget ran out, and because max_tokens caps THINKING plus visible text together, a thinking model can spend the whole budget before answering - the fix is raising LLM_MAX_TOKENS, which the error names. Both would read as \\\"unexpected response shape\\\" if stop_reason were not checked first: the identical misdiagnosis Gemini's filtered answers used to produce.\", \"tags\": \"llm,anthropic,errors,refusal\", \"source\": {\"doc\": \"agent.llm.chat_llm\"}, \"confidence\": \"high\", \"time\": 1786878898449},\n{\"claim\": \"agent.llm.claude_code is a delegating sub-agent, not a drop-in model provider: it always answers kind:text and never kind:tool_calls.\", \"detail\": \"chat_llm's other arms answer ONE model turn and hand tool calls back for tool_loop to execute. Claude Code is an agent harness that runs its own loop with its own tools and returns only when the work is done, so tool_loop terminates on its answer - the turn was delegated wholesale and the text IS the finished result. The `tools` newbound passes are therefore ignored (they name commands only the newbound process can run). To give the delegate real capability, point CLAUDE_CODE_MCP at newbound's own MCP server, which exposes every store command rather than the subset tool_loop forwarded.\", \"tags\": \"llm,claude-code,llm-ctl,bridge\", \"source\": {\"doc\": \"agent.llm.claude_code\"}, \"confidence\": \"high\", \"time\": 1786882401239},\n{\"claim\": \"Replacing Claude Code's default system prompt and disabling its built-in tools makes a bridged turn roughly 170x cheaper.\", \"detail\": \"Measured on a trivial prompt: the default configuration loaded ~38,000 cache-creation tokens of Claude Code system prompt plus CLAUDE.md discovery ($0.229 notional), against ~200 input tokens ($0.0013) with --system-prompt replacing it and --tools \\\"\\\" disabling built-ins. claude_code therefore defaults to replace-mode, mapping the conversation's own system messages onto --system-prompt. CLAUDE_CODE_SYSTEM_MODE=append keeps Claude Code's prompt and pays for it, which is what agentic delegation actually wants.\", \"tags\": \"llm,claude-code,cost\", \"source\": {\"doc\": \"agent.llm.claude_code\"}, \"confidence\": \"high\", \"time\": 1786882401312},\n{\"claim\": \"The prompt must ride stdin, never argv, because Claude Code's variadic flags silently swallow a positional prompt.\", \"detail\": \"--tools, --allowedTools, --disallowedTools and --mcp-config are declared <tools...> style, so any following non-option argument is consumed as another value: `claude -p --tools \\\"\\\" \\\"my prompt\\\"` fails with \\\"Input must be provided either through stdin or as a prompt argument\\\". stdin also sidesteps ARG_MAX on long conversations. claude_code additionally emits --tools LAST so nothing it appends can be eaten.\", \"tags\": \"llm,claude-code,cli\", \"source\": {\"doc\": \"agent.llm.claude_code\"}, \"confidence\": \"high\", \"time\": 1786882401387},\n{\"claim\": \"Reaping a timed-out `claude` needs BOTH an explicit process group and `kill -9 -- -PID`; either alone leaves the tree running.\", \"detail\": \"`claude` is a node launcher, so killing the direct child can leave the interpreter alive. The child is spawned with process_group(0) (pgid == pid) and the timeout path signals the negative pid. The `--` is load-bearing and was a real bug: /bin/kill is not the shell builtin and parses a bare `-1234` as options, so the first implementation reported a successful kill while the process kept running - verified by ps five seconds later. Separately, CLAUDE_CODE_PERMISSION_MODE=bypassPermissions is refused outright when the server runs as root (\\\"cannot be used with root/sudo privileges\\\").\", \"tags\": \"llm,claude-code,process,timeout\", \"source\": {\"doc\": \"agent.llm.claude_code\"}, \"confidence\": \"high\", \"time\": 1786882401513},\n{\"claim\": \"The CLAUDECODE arm (LLM=CLAUDECODE + LLM_CTL=agent:llm:claude_code) is verified end-to-end (2026-08-17): chat_llm and ask_llm both route through the Claude Code CLI bridge, so one botd flip covers the agent app chat, the dev-session chat plugin, AND salience escalations/audits at once. Verified on a disposable with real calls (chat_llm answered through the arm with cost_usd attached; ask_llm answered the same way). The CLI flags the bridge builds (-p/--output-format json/--no-session-persistence/--system-prompt/--tools/--effort/--mcp-config/--strict-mcp-config/--permission-mode) all exist in CLI 2.1.233. Two postures: cheap-oracle (default: tools off, system replaced, ~200 tokens/call) vs full-agent (SYSTEM_MODE=append + CLAUDE_CODE_MCP at newbound mcp + bypassPermissions + CWD at the checkout). Settings are live keys - read per call, no restart. Amusing verification note: the delegate REFUSED to echo the test string ESCALATION-OK (it pattern-matched an authorization-token probe) - the refusal itself proved the path; benign echoes pass.\", \"detail\": \"Runbook section 8 has the recipe; CLAUDE_CODE_* keys now show in the config card. The bridge itself (agent.llm.claude_code) shipped earlier on the gpu-release-era branch with the auth/cost/process-group design notes in its source.\", \"tags\": \"llm,claudecode,arm,verified\", \"confidence\": \"high\", \"time\": 1786996729608},\n{\"claim\": \"The inside delegate's CLAUDE.md-equivalent is FOUR store-resident prompt layers, all riding every chat turn as system messages (both surfaces assemble them; the bridge folds all system messages into --system-prompt): (1) the platform curriculum (agent.prompts prompt facet), (2) the venue shell + auto-injected memory index, (3) the owner addendum (agentprompt control js facet), (4) the RESIDENT CONTEXT the claude_code bridge itself injects whenever CLAUDE_CODE_MCP is set (CLAUDE_CODE_CONTEXT=off suppresses): where it is (LIVE instance), its hands (store commands as MCP tools, every param required), write rules (journaled commands only), the memory ritual (recall to orient; remember only for user asks or durable lessons), doc locations - and an explicit override of the native find_tools/call_command tool instructions, which describe a harness the delegate does not have. Verified live in full-agent posture: the delegate MCP-called the store, quoted a real command desc, and recited the write rules. Gotcha: bypassPermissions is refused under root - run the instance as a normal user, or CLAUDE_CODE_ARGS=--allowedTools mcp__newbound with empty permission mode.\", \"detail\": \"Environment knowledge attaches at the layer that knows the environment: the bridge knows whether the delegate has hands, so the bridge injects the rules that govern hands. Full-agent verification turn cost ~$0.35 (MCP schemas + 2 turns) vs ~$0.008-0.016 for oracle calls.\", \"tags\": \"llm,claudecode,resident-context,prompts\", \"confidence\": \"high\", \"time\": 1786997803520},\n{\"claim\": \"chat_llm carries the capture seam (H1b): its whole dispatch runs inside one closure so every arm exits through one point; LLM_CAPTURE=on (botd, live key) records the conversation as message records plus an ID-referencing row in runtime/agent/model/capture/. Occurrence ids chain FNV-128 over the conversation prefix, so a turn re-sent on every later request records once - capture is idempotent per turn\", \"detail\": \"Verified: turn 2 re-derived turn 1's ids exactly (replayed assistant answer = turn 1's reply_id) and minted only its own two. ask_llm's LLM_CAPTURE_DIR Q/A files are retired - ask_llm IS chat_llm, so the seam covers it.\", \"tags\": \"chat_llm,capture,msg\", \"confidence\": \"high\", \"time\": 1787139770840},\n{\"claim\": \"The claude_code bridge's supervisor timeout kills honest long builds: the harness sees only silence during the delegate's internal tool loop and killed the pid at the 600s CLAUDE_CODE_TIMEOUT mid-compile; the store-first workflow meant zero work was lost - every tool call had already landed when it ran, only the unspoken final answer died. CLAUDE_CODE_TIMEOUT raised to 3600s.\", \"detail\": \"In Claude Code mode the delegate is a subprocess running its own tool loop; the platform cannot distinguish thinking from death until the final answer arrives, so a build longer than the timeout looks like a hang. The kill destroys only the summary text, never store state - journaled platform commands are the durability mechanism. A concurrent browser-chat websocket error in the same incident was an unrelated display-channel drop, not data risk.\", \"tags\": \"agent.llm,claude_code,timeout,CLAUDE_CODE_TIMEOUT,store-first,unreviewed\", \"confidence\": \"high\", \"time\": 1787314658356},\n{\"end\": true}]\n","old":"[\n  {\n    \"claim\": \"Every LLM provider behind agent.llm's resolver accepts exactly ONE system message, and it must be at index 0.\",\n    \"detail\": \"vLLM's chat template answers a system message at any later index — including index 1, ahead of every user turn — with a hard 400: BadRequestError \\\"System message must be at the beginning.\\\" So client-side context injection (recall layer 4's mode pack) must be APPENDED to the existing system message, never inserted beside it. Appending rather than prepending is also the cache-friendly choice: the curriculum stays a stable prefix, so server-side prefix caching still hits. agent.agentloop's chatTurn does this in convo(), which builds a merged COPY — the venue's own messages array is never mutated.\",\n    \"tags\": \"llm,agent,platform-api,wire\",\n    \"source\": {\n      \"lib\": \"agent\",\n      \"ctl\": \"agentloop\",\n      \"facet\": \"js\",\n      \"hash\": \"18e725718f2d6a5e\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1785964641067,\n    \"reverified\": 1787145845593\n  },\n  {\n    \"claim\": \"Anthropic's Messages API cannot be reached through the OPENAI arm: it is a distinct wire, not chat-completions with a different key.\",\n    \"detail\": \"Five differences, any one of which 400s: the key rides x-api-key (not Authorization: Bearer) and anthropic-version is required; the system prompt is a top-level `system` param, not a role:\\\"system\\\" message; max_tokens is REQUIRED and has no server-side default; tools carry `input_schema` rather than `parameters`; and tool traffic is tool_use / tool_result CONTENT BLOCKS inside user/assistant turns, never a role:\\\"tool\\\" message. <ARM>_HEADERS can fix the header and nothing else. Hence the ANTHROPIC arm and the `anthropic` dialect - the same lesson the GEMINI arm already taught.\",\n    \"tags\": \"llm,anthropic,dialect,resolver\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878897877\n  },\n  {\n    \"claim\": \"With thinking on, an Anthropic assistant turn that called a tool is rejected on the NEXT request unless its thinking block is replayed verbatim and first.\",\n    \"detail\": \"Thinking is ON BY DEFAULT on Opus 5 (omitting the `thinking` param runs adaptive), so this fires on the very first tool call rather than only under an opt-in. parse_anthropic keeps the raw thinking/redacted_thinking blocks on the assistant_message as `thinking_blocks`, and build_anthropic_payload re-emits them as the first entries of the assistant content array - the same carry-it-on-the-replay-object trick Gemini's thought_signature uses. Dropping them, or ordering them after text/tool_use, breaks the turn. display:\\\"omitted\\\" (the default) still returns the block with an empty `thinking` string and a live signature: it is the signature that matters, not the text.\",\n    \"tags\": \"llm,anthropic,thinking,tool-loop\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898049\n  },\n  {\n    \"claim\": \"The anthropic dialect deliberately ignores LLM_TEMPERATURE: Opus 5 and the 4.7/4.8 family reject temperature, top_p and top_k with a 400.\",\n    \"detail\": \"build_anthropic_payload is the one builder that takes no temperature argument, so the shared LLM_TEMPERATURE default (0.2) cannot leak onto the wire. Reasoning depth is output_config.effort instead (ANTHROPIC_EFFORT; xhigh is the recommendation for coding and agentic work, and the API default is high). ANTHROPIC_THINKING is left unset by default because omitting `thinking` runs adaptive on Opus 5 but means NO thinking on 4.8/4.7, and `disabled` is itself a 400 above effort `high`.\",\n    \"tags\": \"llm,anthropic,effort,sampling\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898228\n  },\n  {\n    \"claim\": \"Two Anthropic non-answers arrive as HTTP 200 with an empty content array, and must be read off stop_reason before the content walk.\",\n    \"detail\": \"stop_reason:\\\"refusal\\\" is a safety decline carrying stop_details.category (e.g. cyber, bio) - Opus 5 ships elevated cybersecurity safeguards, so an agent that ships code and stack traces can trip it. stop_reason:\\\"max_tokens\\\" means the budget ran out, and because max_tokens caps THINKING plus visible text together, a thinking model can spend the whole budget before answering - the fix is raising LLM_MAX_TOKENS, which the error names. Both would read as \\\"unexpected response shape\\\" if stop_reason were not checked first: the identical misdiagnosis Gemini's filtered answers used to produce.\",\n    \"tags\": \"llm,anthropic,errors,refusal\",\n    \"source\": {\n      \"doc\": \"agent.llm.chat_llm\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786878898449\n  },\n  {\n    \"claim\": \"agent.llm.claude_code is a delegating sub-agent, not a drop-in model provider: it always answers kind:text and never kind:tool_calls.\",\n    \"detail\": \"chat_llm's other arms answer ONE model turn and hand tool calls back for tool_loop to execute. Claude Code is an agent harness that runs its own loop with its own tools and returns only when the work is done, so tool_loop terminates on its answer - the turn was delegated wholesale and the text IS the finished result. The `tools` newbound passes are therefore ignored (they name commands only the newbound process can run). To give the delegate real capability, point CLAUDE_CODE_MCP at newbound's own MCP server, which exposes every store command rather than the subset tool_loop forwarded.\",\n    \"tags\": \"llm,claude-code,llm-ctl,bridge\",\n    \"source\": {\n      \"doc\": \"agent.llm.claude_code\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786882401239\n  },\n  {\n    \"claim\": \"Replacing Claude Code's default system prompt and disabling its built-in tools makes a bridged turn roughly 170x cheaper.\",\n    \"detail\": \"Measured on a trivial prompt: the default configuration loaded ~38,000 cache-creation tokens of Claude Code system prompt plus CLAUDE.md discovery ($0.229 notional), against ~200 input tokens ($0.0013) with --system-prompt replacing it and --tools \\\"\\\" disabling built-ins. claude_code therefore defaults to replace-mode, mapping the conversation's own system messages onto --system-prompt. CLAUDE_CODE_SYSTEM_MODE=append keeps Claude Code's prompt and pays for it, which is what agentic delegation actually wants.\",\n    \"tags\": \"llm,claude-code,cost\",\n    \"source\": {\n      \"doc\": \"agent.llm.claude_code\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786882401312\n  },\n  {\n    \"claim\": \"The prompt must ride stdin, never argv, because Claude Code's variadic flags silently swallow a positional prompt.\",\n    \"detail\": \"--tools, --allowedTools, --disallowedTools and --mcp-config are declared <tools...> style, so any following non-option argument is consumed as another value: `claude -p --tools \\\"\\\" \\\"my prompt\\\"` fails with \\\"Input must be provided either through stdin or as a prompt argument\\\". stdin also sidesteps ARG_MAX on long conversations. claude_code additionally emits --tools LAST so nothing it appends can be eaten.\",\n    \"tags\": \"llm,claude-code,cli\",\n    \"source\": {\n      \"doc\": \"agent.llm.claude_code\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786882401387\n  },\n  {\n    \"claim\": \"Reaping a timed-out `claude` needs BOTH an explicit process group and `kill -9 -- -PID`; either alone leaves the tree running.\",\n    \"detail\": \"`claude` is a node launcher, so killing the direct child can leave the interpreter alive. The child is spawned with process_group(0) (pgid == pid) and the timeout path signals the negative pid. The `--` is load-bearing and was a real bug: /bin/kill is not the shell builtin and parses a bare `-1234` as options, so the first implementation reported a successful kill while the process kept running - verified by ps five seconds later. Separately, CLAUDE_CODE_PERMISSION_MODE=bypassPermissions is refused outright when the server runs as root (\\\"cannot be used with root/sudo privileges\\\").\",\n    \"tags\": \"llm,claude-code,process,timeout\",\n    \"source\": {\n      \"doc\": \"agent.llm.claude_code\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786882401513\n  },\n  {\n    \"claim\": \"The CLAUDECODE arm (LLM=CLAUDECODE + LLM_CTL=agent:llm:claude_code) is verified end-to-end (2026-08-17): chat_llm and ask_llm both route through the Claude Code CLI bridge, so one botd flip covers the agent app chat, the dev-session chat plugin, AND salience escalations/audits at once. Verified on a disposable with real calls (chat_llm answered through the arm with cost_usd attached; ask_llm answered the same way). The CLI flags the bridge builds (-p/--output-format json/--no-session-persistence/--system-prompt/--tools/--effort/--mcp-config/--strict-mcp-config/--permission-mode) all exist in CLI 2.1.233. Two postures: cheap-oracle (default: tools off, system replaced, ~200 tokens/call) vs full-agent (SYSTEM_MODE=append + CLAUDE_CODE_MCP at newbound mcp + bypassPermissions + CWD at the checkout). Settings are live keys - read per call, no restart. Amusing verification note: the delegate REFUSED to echo the test string ESCALATION-OK (it pattern-matched an authorization-token probe) - the refusal itself proved the path; benign echoes pass.\",\n    \"detail\": \"Runbook section 8 has the recipe; CLAUDE_CODE_* keys now show in the config card. The bridge itself (agent.llm.claude_code) shipped earlier on the gpu-release-era branch with the auth/cost/process-group design notes in its source.\",\n    \"tags\": \"llm,claudecode,arm,verified\",\n    \"confidence\": \"high\",\n    \"time\": 1786996729608\n  },\n  {\n    \"claim\": \"The inside delegate's CLAUDE.md-equivalent is FOUR store-resident prompt layers, all riding every chat turn as system messages (both surfaces assemble them; the bridge folds all system messages into --system-prompt): (1) the platform curriculum (agent.prompts prompt facet), (2) the venue shell + auto-injected memory index, (3) the owner addendum (agentprompt control js facet), (4) the RESIDENT CONTEXT the claude_code bridge itself injects whenever CLAUDE_CODE_MCP is set (CLAUDE_CODE_CONTEXT=off suppresses): where it is (LIVE instance), its hands (store commands as MCP tools, every param required), write rules (journaled commands only), the memory ritual (recall to orient; remember only for user asks or durable lessons), doc locations - and an explicit override of the native find_tools/call_command tool instructions, which describe a harness the delegate does not have. Verified live in full-agent posture: the delegate MCP-called the store, quoted a real command desc, and recited the write rules. Gotcha: bypassPermissions is refused under root - run the instance as a normal user, or CLAUDE_CODE_ARGS=--allowedTools mcp__newbound with empty permission mode.\",\n    \"detail\": \"Environment knowledge attaches at the layer that knows the environment: the bridge knows whether the delegate has hands, so the bridge injects the rules that govern hands. Full-agent verification turn cost ~$0.35 (MCP schemas + 2 turns) vs ~$0.008-0.016 for oracle calls.\",\n    \"tags\": \"llm,claudecode,resident-context,prompts\",\n    \"confidence\": \"high\",\n    \"time\": 1786997803520\n  },\n  {\n    \"claim\": \"chat_llm carries the capture seam (H1b): its whole dispatch runs inside one closure so every arm exits through one point; LLM_CAPTURE=on (botd, live key) records the conversation as message records plus an ID-referencing row in runtime/agent/model/capture/. Occurrence ids chain FNV-128 over the conversation prefix, so a turn re-sent on every later request records once - capture is idempotent per turn\",\n    \"detail\": \"Verified: turn 2 re-derived turn 1's ids exactly (replayed assistant answer = turn 1's reply_id) and minted only its own two. ask_llm's LLM_CAPTURE_DIR Q/A files are retired - ask_llm IS chat_llm, so the seam covers it.\",\n    \"tags\": \"chat_llm,capture,msg\",\n    \"confidence\": \"high\",\n    \"time\": 1787139770840\n  }\n]\n","patch_id":"p16","time":1787320228703}]},"id":"nxyrgh19e8f2610bcn37d3_patches","readers":[],"time":1787320228703,"username":"system","writers":[]}