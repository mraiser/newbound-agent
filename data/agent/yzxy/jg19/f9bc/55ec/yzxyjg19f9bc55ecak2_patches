{"data":{"list":[{"author":"installer","facet":"js","label":"install sync","new":"// agentloop.js — the client side of \"chat with the code\", the way the\n// owner called it (2026-07-25): skip control_query AND tool_loop, call\n// agent.llm.chat_llm directly. This module owns what control_query used to\n// do server-side — the system prompt, the context fences, the history —\n// plus the tool loop the notebook can actually SHOW: chat_llm returns\n// normalized tool_calls; the session renders each as a cell and gates\n// mutating ones behind its typed confirm before the loop continues.\n//\n// chat_llm contract (agent.llm, JSONObject envelope — payload under data):\n//   in:  messages (system/user/assistant/tool), tools (OpenAI function defs)\n//   out: {kind:\"text\", content, assistant_message}\n//      | {kind:\"tool_calls\", tool_calls:[{id,name,arguments(JSON string)}],\n//         assistant_message}   — assistant_message goes back on the list.\n// MCP tools (agent.plugin.list_tools) arrive as {name:\"lib-ctl-cmd\",\n// description, inputSchema} — inputSchema is a real JSON Schema.\n\nimport { store } from \"./store.js\";\n\nexport const MAX_ROUNDS = 6;\n\n// The system prompt lives client-side now — the cost of skipping\n// control_query, and the point: the bench knows what the model is looking\n// at and how its replies get used. Model-agnostic (Qwen or Claude).\nexport const SYSTEM_PROMPT = `You are the coding agent inside Bench, the Newbound platform's IDE. You help the user read, understand, and change the code they are looking at.\n\nTHE PLATFORM\n- Newbound organizes everything as libraries. A library contains controls; a control has UI facets (html, css, js — plus data and three) and commands (its backend verbs, written in rust, python, or flow — flow commands are dataflow graphs). Controls may also bind timers and event handlers to commands.\n- Code and data live as records in the platform's DataStore. Edits made through platform commands are journaled per control and revertible; that journal is the change history.\n- Rust command bodies use the ndata crate: DataObject/DataArray with typed getters/setters (get_string, get_int, put_object, ...). Parameters arrive as typed function arguments; FLAT-returning commands build and return a DataObject.\n\nWHAT YOU SEE\n- Current code arrives in fenced blocks labeled lib:ctl.facet (facets) or lib:ctl:command.lang (command bodies; the first line may be a synthesized signature comment). A flow body arrives as JSON labeled lib:ctl:command.flow.\n- \"Recent notebook activity\" lists commands run in this session — by the user or by you — with truncated results.\n\nHOW TO ANSWER\n- Be concise and concrete. If you are unsure, say so plainly rather than inventing platform behavior.\n- To propose a facet change, reply with ONE complete fenced block per changed facet, tagged html, css, or js. The user can apply such a block as a journaled whole-facet patch in one click — a partial snippet cannot be applied, so always return the full facet.\n- Command-body suggestions: a fenced block tagged rust or python containing the full body.\n\nTOOLS\n- Tools are platform commands, named lib-ctl-cmd. Read-only tools run immediately; anything mutating requires the user's typed confirmation first — a denial is the user's decision: respect it and continue without that action.\n- Read before you write. For edits prefer the agent API tools (agent-code-*): patch_control_facet, write_flow_body, set_timer, set_event_handler, the meta setters — they are journaled and revertible. Never suggest editing DataStore files directly.\n- Keep tool use purposeful: the user watches every call land in their notebook.`;\n\n// Newbound meta types -> Rust types, for synthesized signatures (ported\n// from control_query's lookup_rust_api_data_type).\nconst RUST_TYPE = {\n  FLAT: \"DataObject\", JSONObject: \"DataObject\", JSONArray: \"DataArray\",\n  InputStream: \"DataBytes\", float: \"f64\", Integer: \"i64\", Boolean: \"bool\",\n  Any: \"Data\", NULL: \"DNull\",\n};\nconst rustType = (t) => RUST_TYPE[t] ?? \"String\";\n\n/** The context prompt, ported from control_query: an editing preamble plus\n    one fence per included piece, tagged the way the backend prompt always\n    tagged them. `included` = chatctx snapshot entries the user checked. */\nexport function contextBlock(included) {\n  const merged = {};\n  for (const p of included) Object.assign(merged, p.fields);\n  const { lib, ctl } = merged;\n  if (!lib && !ctl) return \"\";\n  let out = `We are editing the ${ctl} control in the ${lib} library.`;\n  for (const facet of [\"html\", \"css\", \"js\"]) {\n    const v = (merged[facet] ?? \"\").trim?.() ?? \"\";\n    if (v) out += `\\n\\n\\`\\`\\`${lib}:${ctl}.${facet}\\n${v}\\n\\`\\`\\``;\n  }\n  if (merged.code && merged.cmdname) {\n    const lang = merged.lang === \"rust\" ? \"rs\" : merged.lang === \"python\" ? \"py\" : merged.lang;\n    let signature = \"\";\n    if (merged.cmdparams) {\n      const ps = merged.cmdparams.map((p) =>\n        lang === \"rs\" ? `${p.name}: ${rustType(p.type)}` : p.name).join(\", \");\n      signature = lang === \"rs\"\n        ? `// fn ${merged.cmdname}(${ps}) -> ${rustType(merged.cmdreturn ?? \"String\")}\\n`\n        : `# def ${merged.cmdname}(${ps}):\\n`;\n    }\n    out += `\\n\\n\\`\\`\\`${lib}:${ctl}:${merged.cmdname}.${lang}\\n${signature}${merged.code.trim()}\\n\\`\\`\\``;\n  }\n  if (merged.flow && merged.cmdname) {\n    out += `\\n\\n\\`\\`\\`${lib}:${ctl}:${merged.cmdname}.flow\\n${merged.flow}\\n\\`\\`\\``;\n  }\n  return out;\n}\n\nexport const clamp = (s, n = 2000) =>\n  s.length <= n ? s : s.slice(0, n) + `\\n…[${s.length - n} chars clipped]`;\n\n/** MCP tool descriptors -> OpenAI function defs, enabled names only. */\nexport function toolDefs(mcpTools, enabledNames) {\n  const on = new Set(enabledNames);\n  return (mcpTools ?? [])\n    .filter((t) => on.has(t.name))\n    .map((t) => ({\n      type: \"function\",\n      function: {\n        name: t.name,\n        description: t.description ?? \"\",\n        parameters: t.inputSchema ?? { type: \"object\", properties: {} },\n      },\n    }));\n}\n\n/** \"lib-ctl-cmd\" -> {lib, ctl, cmd} (ctl may itself contain dashes). */\nexport function parseToolName(name) {\n  const parts = name.split(\"-\");\n  if (parts.length < 3) return null;\n  return { lib: parts[0], ctl: parts.slice(1, -1).join(\"-\"), cmd: parts.at(-1) };\n}\n\n/**\n * One conversational turn, driving chat_llm until it answers in text.\n * execTool(call) is the session's: it renders the cell, gates mutating\n * commands behind the typed confirm, runs it, and resolves the tool-result\n * CONTENT string (or a denial note). Returns the final text.\n */\nexport async function chatTurn({ messages, tools, execTool, onRound }) {\n  for (let round = 0; round < MAX_ROUNDS; round++) {\n    const r = await store.chatLlm(messages, tools);\n    if (r.status !== \"ok\") throw new Error(r.msg ?? \"chat_llm failed\");\n    const d = r.data ?? {};\n    if (d.kind !== \"tool_calls\") {\n      return d.content ?? \"\";\n    }\n    messages.push(d.assistant_message);\n    onRound?.(d.tool_calls);\n    for (const call of d.tool_calls) {\n      const content = await execTool(call);\n      messages.push({ role: \"tool\", tool_call_id: call.id, content });\n    }\n  }\n  return \"(stopped: the agent used its whole tool budget without a final answer)\";\n}\n","old":"","patch_id":"p1","time":1785024831376},{"author":"installer","facet":"js","label":"install sync","new":"// agentloop.js — the client side of \"chat with the code\", the way the\n// owner called it (2026-07-25): skip control_query AND tool_loop, call\n// agent.llm.chat_llm directly. This module owns what control_query used to\n// do server-side — the system prompt, the context fences, the history —\n// plus the tool loop the notebook can actually SHOW: chat_llm returns\n// normalized tool_calls; the session renders each as a cell and gates\n// mutating ones behind its typed confirm before the loop continues.\n//\n// chat_llm contract (agent.llm, JSONObject envelope — payload under data):\n//   in:  messages (system/user/assistant/tool), tools (OpenAI function defs)\n//   out: {kind:\"text\", content, assistant_message}\n//      | {kind:\"tool_calls\", tool_calls:[{id,name,arguments(JSON string)}],\n//         assistant_message}   — assistant_message goes back on the list.\n// MCP tools (agent.plugin.list_tools) arrive as {name:\"lib-ctl-cmd\",\n// description, inputSchema} — inputSchema is a real JSON Schema.\n\nimport { store } from \"./store.js\";\n\nexport const MAX_ROUNDS = 6;\n\n// The system prompt lives client-side now — the cost of skipping\n// control_query, and the point: the bench knows what the model is looking\n// at and how its replies get used. Model-agnostic (Qwen or Claude).\nexport const SYSTEM_PROMPT = `You are the coding agent inside Bench, the Newbound platform's IDE. You help the user read, understand, and change the code they are looking at.\n\nTHE PLATFORM\n- Newbound organizes everything as libraries. A library contains controls; a control has UI facets (html, css, js — plus data and three) and commands (its backend verbs, written in rust, python, or flow — flow commands are dataflow graphs). Controls may also bind timers and event handlers to commands.\n- Code and data live as records in the platform's DataStore. Edits made through platform commands are journaled per control and revertible; that journal is the change history.\n- Rust command bodies use the ndata crate: DataObject/DataArray with typed getters/setters (get_string, get_int, put_object, ...). Parameters arrive as typed function arguments; FLAT-returning commands build and return a DataObject.\n\nWHAT YOU SEE\n- Current code arrives in fenced blocks labeled lib:ctl.facet (facets) or lib:ctl:command.lang (command bodies; the first line may be a synthesized signature comment). A flow body arrives as JSON labeled lib:ctl:command.flow.\n- \"Recent notebook activity\" lists commands run in this session — by the user or by you — with truncated results.\n\nHOW TO ANSWER\n- Be concise and concrete. If you are unsure, say so plainly rather than inventing platform behavior.\n- To propose a facet change, reply with ONE complete fenced block per changed facet, tagged html, css, or js. The user can apply such a block as a journaled whole-facet patch in one click — a partial snippet cannot be applied, so always return the full facet.\n- Command-body suggestions: a fenced block tagged rust or python containing the full body.`;\n\n// Appended to the system prompt ONLY when tools are actually attached —\n// describing tools the model doesn't have produces confident confabulated\n// refusals (observed with Qwen on the first live run).\nexport const TOOLS_PROMPT = `\n\nTOOLS\n- Tools are platform commands, named lib-ctl-cmd. Read-only tools run immediately; anything mutating requires the user's typed confirmation first — a denial is the user's decision: respect it and continue without that action.\n- Read before you write. For edits prefer the agent API tools (agent-code-*): patch_control_facet, write_flow_body, set_timer, set_event_handler, the meta setters — they are journaled and revertible. Never suggest editing DataStore files directly.\n- Keep tool use purposeful: the user watches every call land in their notebook.`;\n\nexport const NO_TOOLS_PROMPT = `\n\nTOOLS\n- No tools are enabled for this conversation: you can only read the provided context and answer. If the user asks you to run or change something, tell them to enable the needed commands in the notebook's \"tools ▸\" picker — do not describe the lack of tools as a platform restriction or security policy.`;\n\n// Newbound meta types -> Rust types, for synthesized signatures (ported\n// from control_query's lookup_rust_api_data_type).\nconst RUST_TYPE = {\n  FLAT: \"DataObject\", JSONObject: \"DataObject\", JSONArray: \"DataArray\",\n  InputStream: \"DataBytes\", float: \"f64\", Integer: \"i64\", Boolean: \"bool\",\n  Any: \"Data\", NULL: \"DNull\",\n};\nconst rustType = (t) => RUST_TYPE[t] ?? \"String\";\n\n/** The context prompt, ported from control_query: an editing preamble plus\n    one fence per included piece, tagged the way the backend prompt always\n    tagged them. `included` = chatctx snapshot entries the user checked. */\nexport function contextBlock(included) {\n  const merged = {};\n  for (const p of included) Object.assign(merged, p.fields);\n  const { lib, ctl } = merged;\n  if (!lib && !ctl) return \"\";\n  let out = `We are editing the ${ctl} control in the ${lib} library.`;\n  for (const facet of [\"html\", \"css\", \"js\"]) {\n    const v = (merged[facet] ?? \"\").trim?.() ?? \"\";\n    if (v) out += `\\n\\n\\`\\`\\`${lib}:${ctl}.${facet}\\n${v}\\n\\`\\`\\``;\n  }\n  if (merged.code && merged.cmdname) {\n    const lang = merged.lang === \"rust\" ? \"rs\" : merged.lang === \"python\" ? \"py\" : merged.lang;\n    let signature = \"\";\n    if (merged.cmdparams) {\n      const ps = merged.cmdparams.map((p) =>\n        lang === \"rs\" ? `${p.name}: ${rustType(p.type)}` : p.name).join(\", \");\n      signature = lang === \"rs\"\n        ? `// fn ${merged.cmdname}(${ps}) -> ${rustType(merged.cmdreturn ?? \"String\")}\\n`\n        : `# def ${merged.cmdname}(${ps}):\\n`;\n    }\n    out += `\\n\\n\\`\\`\\`${lib}:${ctl}:${merged.cmdname}.${lang}\\n${signature}${merged.code.trim()}\\n\\`\\`\\``;\n  }\n  if (merged.flow && merged.cmdname) {\n    out += `\\n\\n\\`\\`\\`${lib}:${ctl}:${merged.cmdname}.flow\\n${merged.flow}\\n\\`\\`\\``;\n  }\n  return out;\n}\n\nexport const clamp = (s, n = 2000) =>\n  s.length <= n ? s : s.slice(0, n) + `\\n…[${s.length - n} chars clipped]`;\n\n/** MCP tool descriptors -> OpenAI function defs, enabled names only. */\nexport function toolDefs(mcpTools, enabledNames) {\n  const on = new Set(enabledNames);\n  return (mcpTools ?? [])\n    .filter((t) => on.has(t.name))\n    .map((t) => ({\n      type: \"function\",\n      function: {\n        name: t.name,\n        description: t.description ?? \"\",\n        parameters: t.inputSchema ?? { type: \"object\", properties: {} },\n      },\n    }));\n}\n\n/** \"lib-ctl-cmd\" -> {lib, ctl, cmd} (ctl may itself contain dashes). */\nexport function parseToolName(name) {\n  const parts = name.split(\"-\");\n  if (parts.length < 3) return null;\n  return { lib: parts[0], ctl: parts.slice(1, -1).join(\"-\"), cmd: parts.at(-1) };\n}\n\n/**\n * One conversational turn, driving chat_llm until it answers in text.\n * execTool(call) is the session's: it renders the cell, gates mutating\n * commands behind the typed confirm, runs it, and resolves the tool-result\n * CONTENT string (or a denial note). Returns the final text.\n */\nexport async function chatTurn({ messages, tools, execTool, onRound }) {\n  for (let round = 0; round < MAX_ROUNDS; round++) {\n    const r = await store.chatLlm(messages, tools);\n    if (r.status !== \"ok\") throw new Error(r.msg ?? \"chat_llm failed\");\n    const d = r.data ?? {};\n    if (d.kind !== \"tool_calls\") {\n      return d.content ?? \"\";\n    }\n    messages.push(d.assistant_message);\n    onRound?.(d.tool_calls);\n    for (const call of d.tool_calls) {\n      const content = await execTool(call);\n      messages.push({ role: \"tool\", tool_call_id: call.id, content });\n    }\n  }\n  return \"(stopped: the agent used its whole tool budget without a final answer)\";\n}\n","old":"// agentloop.js — the client side of \"chat with the code\", the way the\n// owner called it (2026-07-25): skip control_query AND tool_loop, call\n// agent.llm.chat_llm directly. This module owns what control_query used to\n// do server-side — the system prompt, the context fences, the history —\n// plus the tool loop the notebook can actually SHOW: chat_llm returns\n// normalized tool_calls; the session renders each as a cell and gates\n// mutating ones behind its typed confirm before the loop continues.\n//\n// chat_llm contract (agent.llm, JSONObject envelope — payload under data):\n//   in:  messages (system/user/assistant/tool), tools (OpenAI function defs)\n//   out: {kind:\"text\", content, assistant_message}\n//      | {kind:\"tool_calls\", tool_calls:[{id,name,arguments(JSON string)}],\n//         assistant_message}   — assistant_message goes back on the list.\n// MCP tools (agent.plugin.list_tools) arrive as {name:\"lib-ctl-cmd\",\n// description, inputSchema} — inputSchema is a real JSON Schema.\n\nimport { store } from \"./store.js\";\n\nexport const MAX_ROUNDS = 6;\n\n// The system prompt lives client-side now — the cost of skipping\n// control_query, and the point: the bench knows what the model is looking\n// at and how its replies get used. Model-agnostic (Qwen or Claude).\nexport const SYSTEM_PROMPT = `You are the coding agent inside Bench, the Newbound platform's IDE. You help the user read, understand, and change the code they are looking at.\n\nTHE PLATFORM\n- Newbound organizes everything as libraries. A library contains controls; a control has UI facets (html, css, js — plus data and three) and commands (its backend verbs, written in rust, python, or flow — flow commands are dataflow graphs). Controls may also bind timers and event handlers to commands.\n- Code and data live as records in the platform's DataStore. Edits made through platform commands are journaled per control and revertible; that journal is the change history.\n- Rust command bodies use the ndata crate: DataObject/DataArray with typed getters/setters (get_string, get_int, put_object, ...). Parameters arrive as typed function arguments; FLAT-returning commands build and return a DataObject.\n\nWHAT YOU SEE\n- Current code arrives in fenced blocks labeled lib:ctl.facet (facets) or lib:ctl:command.lang (command bodies; the first line may be a synthesized signature comment). A flow body arrives as JSON labeled lib:ctl:command.flow.\n- \"Recent notebook activity\" lists commands run in this session — by the user or by you — with truncated results.\n\nHOW TO ANSWER\n- Be concise and concrete. If you are unsure, say so plainly rather than inventing platform behavior.\n- To propose a facet change, reply with ONE complete fenced block per changed facet, tagged html, css, or js. The user can apply such a block as a journaled whole-facet patch in one click — a partial snippet cannot be applied, so always return the full facet.\n- Command-body suggestions: a fenced block tagged rust or python containing the full body.\n\nTOOLS\n- Tools are platform commands, named lib-ctl-cmd. Read-only tools run immediately; anything mutating requires the user's typed confirmation first — a denial is the user's decision: respect it and continue without that action.\n- Read before you write. For edits prefer the agent API tools (agent-code-*): patch_control_facet, write_flow_body, set_timer, set_event_handler, the meta setters — they are journaled and revertible. Never suggest editing DataStore files directly.\n- Keep tool use purposeful: the user watches every call land in their notebook.`;\n\n// Newbound meta types -> Rust types, for synthesized signatures (ported\n// from control_query's lookup_rust_api_data_type).\nconst RUST_TYPE = {\n  FLAT: \"DataObject\", JSONObject: \"DataObject\", JSONArray: \"DataArray\",\n  InputStream: \"DataBytes\", float: \"f64\", Integer: \"i64\", Boolean: \"bool\",\n  Any: \"Data\", NULL: \"DNull\",\n};\nconst rustType = (t) => RUST_TYPE[t] ?? \"String\";\n\n/** The context prompt, ported from control_query: an editing preamble plus\n    one fence per included piece, tagged the way the backend prompt always\n    tagged them. `included` = chatctx snapshot entries the user checked. */\nexport function contextBlock(included) {\n  const merged = {};\n  for (const p of included) Object.assign(merged, p.fields);\n  const { lib, ctl } = merged;\n  if (!lib && !ctl) return \"\";\n  let out = `We are editing the ${ctl} control in the ${lib} library.`;\n  for (const facet of [\"html\", \"css\", \"js\"]) {\n    const v = (merged[facet] ?? \"\").trim?.() ?? \"\";\n    if (v) out += `\\n\\n\\`\\`\\`${lib}:${ctl}.${facet}\\n${v}\\n\\`\\`\\``;\n  }\n  if (merged.code && merged.cmdname) {\n    const lang = merged.lang === \"rust\" ? \"rs\" : merged.lang === \"python\" ? \"py\" : merged.lang;\n    let signature = \"\";\n    if (merged.cmdparams) {\n      const ps = merged.cmdparams.map((p) =>\n        lang === \"rs\" ? `${p.name}: ${rustType(p.type)}` : p.name).join(\", \");\n      signature = lang === \"rs\"\n        ? `// fn ${merged.cmdname}(${ps}) -> ${rustType(merged.cmdreturn ?? \"String\")}\\n`\n        : `# def ${merged.cmdname}(${ps}):\\n`;\n    }\n    out += `\\n\\n\\`\\`\\`${lib}:${ctl}:${merged.cmdname}.${lang}\\n${signature}${merged.code.trim()}\\n\\`\\`\\``;\n  }\n  if (merged.flow && merged.cmdname) {\n    out += `\\n\\n\\`\\`\\`${lib}:${ctl}:${merged.cmdname}.flow\\n${merged.flow}\\n\\`\\`\\``;\n  }\n  return out;\n}\n\nexport const clamp = (s, n = 2000) =>\n  s.length <= n ? s : s.slice(0, n) + `\\n…[${s.length - n} chars clipped]`;\n\n/** MCP tool descriptors -> OpenAI function defs, enabled names only. */\nexport function toolDefs(mcpTools, enabledNames) {\n  const on = new Set(enabledNames);\n  return (mcpTools ?? [])\n    .filter((t) => on.has(t.name))\n    .map((t) => ({\n      type: \"function\",\n      function: {\n        name: t.name,\n        description: t.description ?? \"\",\n        parameters: t.inputSchema ?? { type: \"object\", properties: {} },\n      },\n    }));\n}\n\n/** \"lib-ctl-cmd\" -> {lib, ctl, cmd} (ctl may itself contain dashes). */\nexport function parseToolName(name) {\n  const parts = name.split(\"-\");\n  if (parts.length < 3) return null;\n  return { lib: parts[0], ctl: parts.slice(1, -1).join(\"-\"), cmd: parts.at(-1) };\n}\n\n/**\n * One conversational turn, driving chat_llm until it answers in text.\n * execTool(call) is the session's: it renders the cell, gates mutating\n * commands behind the typed confirm, runs it, and resolves the tool-result\n * CONTENT string (or a denial note). Returns the final text.\n */\nexport async function chatTurn({ messages, tools, execTool, onRound }) {\n  for (let round = 0; round < MAX_ROUNDS; round++) {\n    const r = await store.chatLlm(messages, tools);\n    if (r.status !== \"ok\") throw new Error(r.msg ?? \"chat_llm failed\");\n    const d = r.data ?? {};\n    if (d.kind !== \"tool_calls\") {\n      return d.content ?? \"\";\n    }\n    messages.push(d.assistant_message);\n    onRound?.(d.tool_calls);\n    for (const call of d.tool_calls) {\n      const content = await execTool(call);\n      messages.push({ role: \"tool\", tool_call_id: call.id, content });\n    }\n  }\n  return \"(stopped: the agent used its whole tool budget without a final answer)\";\n}\n","patch_id":"p2","time":1785028202515},{"author":"installer","facet":"js","label":"install sync","new":"// agentloop.js — the client side of \"chat with the code\", the way the\n// owner called it (2026-07-25): skip control_query AND tool_loop, call\n// agent.llm.chat_llm directly. This module owns what control_query used to\n// do server-side — the system prompt, the context fences, the history —\n// plus the tool loop the notebook can actually SHOW: chat_llm returns\n// normalized tool_calls; the session renders each as a cell and gates\n// mutating ones behind its typed confirm before the loop continues.\n//\n// chat_llm contract (agent.llm, JSONObject envelope — payload under data):\n//   in:  messages (system/user/assistant/tool), tools (OpenAI function defs)\n//   out: {kind:\"text\", content, assistant_message}\n//      | {kind:\"tool_calls\", tool_calls:[{id,name,arguments(JSON string)}],\n//         assistant_message}   — assistant_message goes back on the list.\n// MCP tools (agent.plugin.list_tools) arrive as {name:\"lib-ctl-cmd\",\n// description, inputSchema} — inputSchema is a real JSON Schema.\n\nimport { store } from \"./store.js\";\n\n// Generous: the autonomous developer workflow legitimately spends rounds\n// on compile-debug cycles.\nexport const MAX_ROUNDS = 10;\n\n// The system prompt lives client-side now — the cost of skipping\n// control_query, and the point: the bench knows what the model is looking\n// at and how its replies get used. The platform-knowledge sections are\n// the owner's, folded in from agent.llm.tool_loop's prompt (his call:\n// without them the model can't write ndata-correct code or reason about\n// the platform). Model-agnostic (Qwen or Claude).\nexport const SYSTEM_PROMPT = `You are Newbound, a self-organizing collection of code, data, and opinions that is working with the humans to improve their way of life. Right now you are speaking through the session notebook of Bench, the Newbound IDE — the user sees every tool call you make as a notebook cell.\n\nYour capabilities are defined by the Commands in your flowlang DataStore. The datastore is organized into libraries. Libraries contain Assets and Controls. Controls contain UI elements (html/css/js/data/3d) and back-end components (Command/Event/Timer). The \\`app\\`, \\`dev\\`, \\`peer\\`, and \\`security\\` libraries are the core Newbound libraries. They are great reference information because they define how everything in Newbound is done.\n\nThe ndata Crate: The Universal Data Layer\nAdherence to the principles of the underlying ndata crate is crucial for writing correct and performant rust code. All data within the Flowlang runtime, between the front-end and back-end, and within Commands is managed by ndata.\n  Core Concept: ndata provides globally shared, thread-safe, JSON-like dynamic data structures: DataObject (a HashMap), DataArray (a Vec), and DataBytes (a byte stream). These are handles to heap-stored data.\n  CRITICAL RULE — NO WRAPPING: ndata types are already internally thread-safe. NEVER wrap them in Arc, Rc, Mutex, RwLock, or any other synchronization primitive.\n  Handles: .clone() is cheap (a new handle, refcount bump). When you pass a handle to a mutator like parent.put_object(\"key\", child), the child handle is MOVED — clone first (child.clone()) if you need it afterward.\n  Best practices: prefer typed methods (get_string, put_object) over generic ones; DataObject::new() / DataArray::new() for new instances; deep_copy() for an independent duplicate.\n  SUPER IMPORTANT: ndata does NOT use the wrap-everything-in-Result pattern — get_object/get_int/get_string return directly or PANIC. Use try_get_XXX variants or check has(&key) first when absence is possible.\n\nThe flowlang Crate: The Runtime Engine\n  Data lives under ./data/<lib>/, sharded by four-character chunks of the record id: ./data/lib/[0..4]/[4..8]/[8..12]/[12..16]/fullid. A library's control index is data/<lib>/cont/rols/____/____/controls; its metadata is data/<lib>/meta.json; its assets are data/<lib>/_ASSETS/. It is UNWISE to edit these files directly — malformed data can crash Newbound; use platform commands, which are journaled.\n  STORAGE: use flowlang::datastore::DataStore; let store = DataStore::new(); let ctl = store.get_data(&lib, &id).get_object(\"data\");\n  GLOBALS: DataStore::globals().get_object(\"system\").get_object(\"libraries\")\n  EXECUTION: use flowlang::command::Command; let cmd = Command::lookup(lib, control, command); let result = cmd.execute(arguments);\n\nNewbound Web Apps\n  A Newbound Application is a Control with the same name as its library that has been published. Applications serve at /appname/index.html and their commands at /appname/cmdname?param=value. The http_port is in config.properties.\n\nWriting Commands in Rust\n  Default to Rust for new commands unless Python is explicitly requested or needed for an AI/data-science library.\n  CRITICAL — code_body IS A FUNCTION BODY, NOT A FUNCTION: for upsert_command/patch_command_body, your code_body is placed inside a generated fn: pub fn <cmd>(<params>) -> <ReturnType> { <your code_body> }. Never write a fn signature or outer braces. Declared params are in scope as typed variables (String, Integer→i64, Float→f64, Boolean→bool, JSONObject→DataObject, JSONArray→DataArray). The FINAL EXPRESSION must match the declared return_type. Put use statements in \\`imports\\`; DataObject is imported automatically.\n  Other Newbound commands are callable natively: let api = crate::api::new(); api.lib.ctl.cmd(param1, param2); or use crate::lib::ctl::cmd::cmd;\n\nWriting Commands in Python\n  Python is first-class: arguments arrive as NDataObject/NDataArray/NDataBytes wrappers sharing memory with Rust — manipulate them directly. Returning native Python types auto-converts.\n\nReturn Type Semantics\n  A command's response JSON always has \"status\" (\"ok\"/\"err\"). A returned String lands in \"msg\"; other types land in \"data\"; FLAT means the returned DataObject IS the outermost response. Choose return_type to match the final expression.\n\nFront-End Control Development (HTML/CSS/JS)\n  Platform controls keep html, css, and js as separate facets. A back-end command foo is exposed to that control's js as send_foo(args..., function(result){...}) — result.msg for Strings, result.data otherwise. jQuery 3.6.1 is loaded. Standard boilerplate: var me = this; var ME = $('#' + me.UUID)[0]; me.ready = function(){...}. Sub-controls mount declaratively (<div class='data-control' data-control='lib:ctl:{...}'>) or via installControl(...).\n  EXCEPTION: the \\`bench\\` library's own controls are ES modules (export function init(host, props), no jQuery, imports from ../../assets/) — match the style of whatever control you are editing.\n\nWHAT YOU SEE\n- Current code arrives in fenced blocks labeled lib:ctl.facet (facets) or lib:ctl:command.lang (command bodies; the first line may be a synthesized signature comment). A flow body arrives as JSON labeled lib:ctl:command.flow.\n- \"Recent notebook activity\" lists commands run in this session, with OUTPUTS TRUNCATED. Never conclude that something is absent because you don't see it in a truncated result — rerun the command via a tool when you need the full output.\n\nHOW TO ANSWER\n- Be concise and concrete. If you are unsure, say so plainly rather than inventing platform behavior.\n- To propose a facet change, reply with ONE complete fenced block per changed facet, tagged html, css, or js. The user can apply such a block as a journaled whole-facet patch in one click — a partial snippet cannot be applied, so always return the full facet.\n- Command-body suggestions: a fenced block tagged rust or python containing the full body.`;\n\n// Appended to the system prompt ONLY when tools are actually attached —\n// describing tools the model doesn't have produces confident confabulated\n// refusals (observed with Qwen on the first live run).\nexport const TOOLS_PROMPT = `\n\nTOOLS\nThe Newbound commands you have been granted are available as NATIVE TOOLS — call them through the tool-calling interface, never by describing a call in text. Tool names are library-control-command (e.g. agent-code-read_command). Every call you make appears as a cell in the user's notebook. Read-only tools run immediately; mutating tools stop for the user's typed confirmation — a denial is the user's decision: respect it and continue without that action. Only the tools attached to this conversation exist; the user can grant more in the notebook's \"tools ▸\" picker.\n\nThe Autonomous Developer Workflow\nWhen asked to build a feature, process data, or perform a task requiring code, act as an autonomous developer:\n1. READ: understand existing code first (agent-code-read_command, agent-code-read_control_facet).\n2. WRITE: agent-code-upsert_command or agent-code-patch_command_body — the system generates the source, compiles, and hot-swaps on success.\n3. DEBUG: if compilation fails, do not apologize — read the compiler error from the tool result, patch, and compile again until it succeeds.\n4. EXECUTE: agent-code-invoke_command to run and verify.\n5. RESPOND: give your final answer only after execution succeeds, summarizing what you built and the results.\n6. OBSTACLES: if a step stays blocked after 2-3 DIFFERENT approaches, stop and report what worked, what didn't, and the exact blocking error. A clear partial result beats a burned budget.\nFor edits to UI facets prefer agent-code-patch_control_facet (journaled and revertible); for flow commands, agent-code-read_flow_body / write_flow_body.\n\nScratchpad Execution\nFor complex logical, mathematical, or data-parsing questions, write and run a temporary Rust script with agent-code-evaluate_rust instead of guessing. Your \\`code\\` is compiled inside a function returning DataObject — the FINAL EXPRESSION must be a DataObject:\n    let mut out = DataObject::new();\n    out.put_int(\"result\", my_value as i64);\n    out\nPut use statements in \\`imports\\` (DataObject is automatic).\n\nAdvanced Native Orchestration\nYou can invoke any Newbound command from WITHIN an evaluate_rust script via let api = crate::api::new(); — chain tools, process large data, or run multi-step pipelines in one script instead of many chat rounds. Pass large intermediate values through DataStore::globals().get_object(\"AGENT_MEMORY\") or the runtime library rather than through this conversation.`;\n\nexport const NO_TOOLS_PROMPT = `\n\nTOOLS\n- No tools are enabled for this conversation: you can only read the provided context and answer. If the user asks you to run or change something, tell them to enable the needed commands in the notebook's \"tools ▸\" picker — do not describe the lack of tools as a platform restriction or security policy.`;\n\n// Newbound meta types -> Rust types, for synthesized signatures (ported\n// from control_query's lookup_rust_api_data_type).\nconst RUST_TYPE = {\n  FLAT: \"DataObject\", JSONObject: \"DataObject\", JSONArray: \"DataArray\",\n  InputStream: \"DataBytes\", float: \"f64\", Integer: \"i64\", Boolean: \"bool\",\n  Any: \"Data\", NULL: \"DNull\",\n};\nconst rustType = (t) => RUST_TYPE[t] ?? \"String\";\n\n/** The context prompt, ported from control_query: an editing preamble plus\n    one fence per included piece, tagged the way the backend prompt always\n    tagged them. `included` = chatctx snapshot entries the user checked. */\nexport function contextBlock(included) {\n  const merged = {};\n  for (const p of included) Object.assign(merged, p.fields);\n  const { lib, ctl } = merged;\n  if (!lib && !ctl) return \"\";\n  let out = `We are editing the ${ctl} control in the ${lib} library.`;\n  for (const facet of [\"html\", \"css\", \"js\"]) {\n    const v = (merged[facet] ?? \"\").trim?.() ?? \"\";\n    if (v) out += `\\n\\n\\`\\`\\`${lib}:${ctl}.${facet}\\n${v}\\n\\`\\`\\``;\n  }\n  if (merged.code && merged.cmdname) {\n    const lang = merged.lang === \"rust\" ? \"rs\" : merged.lang === \"python\" ? \"py\" : merged.lang;\n    let signature = \"\";\n    if (merged.cmdparams) {\n      const ps = merged.cmdparams.map((p) =>\n        lang === \"rs\" ? `${p.name}: ${rustType(p.type)}` : p.name).join(\", \");\n      signature = lang === \"rs\"\n        ? `// fn ${merged.cmdname}(${ps}) -> ${rustType(merged.cmdreturn ?? \"String\")}\\n`\n        : `# def ${merged.cmdname}(${ps}):\\n`;\n    }\n    out += `\\n\\n\\`\\`\\`${lib}:${ctl}:${merged.cmdname}.${lang}\\n${signature}${merged.code.trim()}\\n\\`\\`\\``;\n  }\n  if (merged.flow && merged.cmdname) {\n    out += `\\n\\n\\`\\`\\`${lib}:${ctl}:${merged.cmdname}.flow\\n${merged.flow}\\n\\`\\`\\``;\n  }\n  return out;\n}\n\nexport const clamp = (s, n = 2000) =>\n  s.length <= n ? s : s.slice(0, n) + `\\n…[${s.length - n} chars clipped]`;\n\n/** MCP tool descriptors -> OpenAI function defs, enabled names only. */\nexport function toolDefs(mcpTools, enabledNames) {\n  const on = new Set(enabledNames);\n  return (mcpTools ?? [])\n    .filter((t) => on.has(t.name))\n    .map((t) => ({\n      type: \"function\",\n      function: {\n        name: t.name,\n        description: t.description ?? \"\",\n        parameters: t.inputSchema ?? { type: \"object\", properties: {} },\n      },\n    }));\n}\n\n/** \"lib-ctl-cmd\" -> {lib, ctl, cmd} (ctl may itself contain dashes). */\nexport function parseToolName(name) {\n  const parts = name.split(\"-\");\n  if (parts.length < 3) return null;\n  return { lib: parts[0], ctl: parts.slice(1, -1).join(\"-\"), cmd: parts.at(-1) };\n}\n\n/**\n * One conversational turn, driving chat_llm until it answers in text.\n * execTool(call) is the session's: it renders the cell, gates mutating\n * commands behind the typed confirm, runs it, and resolves the tool-result\n * CONTENT string (or a denial note). Returns the final text.\n */\nexport async function chatTurn({ messages, tools, execTool, onRound }) {\n  for (let round = 0; round < MAX_ROUNDS; round++) {\n    const r = await store.chatLlm(messages, tools);\n    if (r.status !== \"ok\") throw new Error(r.msg ?? \"chat_llm failed\");\n    const d = r.data ?? {};\n    if (d.kind !== \"tool_calls\") {\n      return d.content ?? \"\";\n    }\n    messages.push(d.assistant_message);\n    onRound?.(d.tool_calls);\n    for (const call of d.tool_calls) {\n      const content = await execTool(call);\n      messages.push({ role: \"tool\", tool_call_id: call.id, content });\n    }\n  }\n  return \"(stopped: the agent used its whole tool budget without a final answer)\";\n}\n","old":"// agentloop.js — the client side of \"chat with the code\", the way the\n// owner called it (2026-07-25): skip control_query AND tool_loop, call\n// agent.llm.chat_llm directly. This module owns what control_query used to\n// do server-side — the system prompt, the context fences, the history —\n// plus the tool loop the notebook can actually SHOW: chat_llm returns\n// normalized tool_calls; the session renders each as a cell and gates\n// mutating ones behind its typed confirm before the loop continues.\n//\n// chat_llm contract (agent.llm, JSONObject envelope — payload under data):\n//   in:  messages (system/user/assistant/tool), tools (OpenAI function defs)\n//   out: {kind:\"text\", content, assistant_message}\n//      | {kind:\"tool_calls\", tool_calls:[{id,name,arguments(JSON string)}],\n//         assistant_message}   — assistant_message goes back on the list.\n// MCP tools (agent.plugin.list_tools) arrive as {name:\"lib-ctl-cmd\",\n// description, inputSchema} — inputSchema is a real JSON Schema.\n\nimport { store } from \"./store.js\";\n\nexport const MAX_ROUNDS = 6;\n\n// The system prompt lives client-side now — the cost of skipping\n// control_query, and the point: the bench knows what the model is looking\n// at and how its replies get used. Model-agnostic (Qwen or Claude).\nexport const SYSTEM_PROMPT = `You are the coding agent inside Bench, the Newbound platform's IDE. You help the user read, understand, and change the code they are looking at.\n\nTHE PLATFORM\n- Newbound organizes everything as libraries. A library contains controls; a control has UI facets (html, css, js — plus data and three) and commands (its backend verbs, written in rust, python, or flow — flow commands are dataflow graphs). Controls may also bind timers and event handlers to commands.\n- Code and data live as records in the platform's DataStore. Edits made through platform commands are journaled per control and revertible; that journal is the change history.\n- Rust command bodies use the ndata crate: DataObject/DataArray with typed getters/setters (get_string, get_int, put_object, ...). Parameters arrive as typed function arguments; FLAT-returning commands build and return a DataObject.\n\nWHAT YOU SEE\n- Current code arrives in fenced blocks labeled lib:ctl.facet (facets) or lib:ctl:command.lang (command bodies; the first line may be a synthesized signature comment). A flow body arrives as JSON labeled lib:ctl:command.flow.\n- \"Recent notebook activity\" lists commands run in this session — by the user or by you — with truncated results.\n\nHOW TO ANSWER\n- Be concise and concrete. If you are unsure, say so plainly rather than inventing platform behavior.\n- To propose a facet change, reply with ONE complete fenced block per changed facet, tagged html, css, or js. The user can apply such a block as a journaled whole-facet patch in one click — a partial snippet cannot be applied, so always return the full facet.\n- Command-body suggestions: a fenced block tagged rust or python containing the full body.`;\n\n// Appended to the system prompt ONLY when tools are actually attached —\n// describing tools the model doesn't have produces confident confabulated\n// refusals (observed with Qwen on the first live run).\nexport const TOOLS_PROMPT = `\n\nTOOLS\n- Tools are platform commands, named lib-ctl-cmd. Read-only tools run immediately; anything mutating requires the user's typed confirmation first — a denial is the user's decision: respect it and continue without that action.\n- Read before you write. For edits prefer the agent API tools (agent-code-*): patch_control_facet, write_flow_body, set_timer, set_event_handler, the meta setters — they are journaled and revertible. Never suggest editing DataStore files directly.\n- Keep tool use purposeful: the user watches every call land in their notebook.`;\n\nexport const NO_TOOLS_PROMPT = `\n\nTOOLS\n- No tools are enabled for this conversation: you can only read the provided context and answer. If the user asks you to run or change something, tell them to enable the needed commands in the notebook's \"tools ▸\" picker — do not describe the lack of tools as a platform restriction or security policy.`;\n\n// Newbound meta types -> Rust types, for synthesized signatures (ported\n// from control_query's lookup_rust_api_data_type).\nconst RUST_TYPE = {\n  FLAT: \"DataObject\", JSONObject: \"DataObject\", JSONArray: \"DataArray\",\n  InputStream: \"DataBytes\", float: \"f64\", Integer: \"i64\", Boolean: \"bool\",\n  Any: \"Data\", NULL: \"DNull\",\n};\nconst rustType = (t) => RUST_TYPE[t] ?? \"String\";\n\n/** The context prompt, ported from control_query: an editing preamble plus\n    one fence per included piece, tagged the way the backend prompt always\n    tagged them. `included` = chatctx snapshot entries the user checked. */\nexport function contextBlock(included) {\n  const merged = {};\n  for (const p of included) Object.assign(merged, p.fields);\n  const { lib, ctl } = merged;\n  if (!lib && !ctl) return \"\";\n  let out = `We are editing the ${ctl} control in the ${lib} library.`;\n  for (const facet of [\"html\", \"css\", \"js\"]) {\n    const v = (merged[facet] ?? \"\").trim?.() ?? \"\";\n    if (v) out += `\\n\\n\\`\\`\\`${lib}:${ctl}.${facet}\\n${v}\\n\\`\\`\\``;\n  }\n  if (merged.code && merged.cmdname) {\n    const lang = merged.lang === \"rust\" ? \"rs\" : merged.lang === \"python\" ? \"py\" : merged.lang;\n    let signature = \"\";\n    if (merged.cmdparams) {\n      const ps = merged.cmdparams.map((p) =>\n        lang === \"rs\" ? `${p.name}: ${rustType(p.type)}` : p.name).join(\", \");\n      signature = lang === \"rs\"\n        ? `// fn ${merged.cmdname}(${ps}) -> ${rustType(merged.cmdreturn ?? \"String\")}\\n`\n        : `# def ${merged.cmdname}(${ps}):\\n`;\n    }\n    out += `\\n\\n\\`\\`\\`${lib}:${ctl}:${merged.cmdname}.${lang}\\n${signature}${merged.code.trim()}\\n\\`\\`\\``;\n  }\n  if (merged.flow && merged.cmdname) {\n    out += `\\n\\n\\`\\`\\`${lib}:${ctl}:${merged.cmdname}.flow\\n${merged.flow}\\n\\`\\`\\``;\n  }\n  return out;\n}\n\nexport const clamp = (s, n = 2000) =>\n  s.length <= n ? s : s.slice(0, n) + `\\n…[${s.length - n} chars clipped]`;\n\n/** MCP tool descriptors -> OpenAI function defs, enabled names only. */\nexport function toolDefs(mcpTools, enabledNames) {\n  const on = new Set(enabledNames);\n  return (mcpTools ?? [])\n    .filter((t) => on.has(t.name))\n    .map((t) => ({\n      type: \"function\",\n      function: {\n        name: t.name,\n        description: t.description ?? \"\",\n        parameters: t.inputSchema ?? { type: \"object\", properties: {} },\n      },\n    }));\n}\n\n/** \"lib-ctl-cmd\" -> {lib, ctl, cmd} (ctl may itself contain dashes). */\nexport function parseToolName(name) {\n  const parts = name.split(\"-\");\n  if (parts.length < 3) return null;\n  return { lib: parts[0], ctl: parts.slice(1, -1).join(\"-\"), cmd: parts.at(-1) };\n}\n\n/**\n * One conversational turn, driving chat_llm until it answers in text.\n * execTool(call) is the session's: it renders the cell, gates mutating\n * commands behind the typed confirm, runs it, and resolves the tool-result\n * CONTENT string (or a denial note). Returns the final text.\n */\nexport async function chatTurn({ messages, tools, execTool, onRound }) {\n  for (let round = 0; round < MAX_ROUNDS; round++) {\n    const r = await store.chatLlm(messages, tools);\n    if (r.status !== \"ok\") throw new Error(r.msg ?? \"chat_llm failed\");\n    const d = r.data ?? {};\n    if (d.kind !== \"tool_calls\") {\n      return d.content ?? \"\";\n    }\n    messages.push(d.assistant_message);\n    onRound?.(d.tool_calls);\n    for (const call of d.tool_calls) {\n      const content = await execTool(call);\n      messages.push({ role: \"tool\", tool_call_id: call.id, content });\n    }\n  }\n  return \"(stopped: the agent used its whole tool budget without a final answer)\";\n}\n","patch_id":"p3","time":1785029017508},{"author":"installer","facet":"js","label":"install sync","new":"// agentloop.js — the client side of \"chat with the code\", the way the\n// owner called it (2026-07-25): skip control_query AND tool_loop, call\n// agent.llm.chat_llm directly. This module owns what control_query used to\n// do server-side — the system prompt, the context fences, the history —\n// plus the tool loop the notebook can actually SHOW: chat_llm returns\n// normalized tool_calls; the session renders each as a cell and gates\n// mutating ones behind its typed confirm before the loop continues.\n//\n// chat_llm contract (agent.llm, JSONObject envelope — payload under data):\n//   in:  messages (system/user/assistant/tool), tools (OpenAI function defs)\n//   out: {kind:\"text\", content, assistant_message}\n//      | {kind:\"tool_calls\", tool_calls:[{id,name,arguments(JSON string)}],\n//         assistant_message}   — assistant_message goes back on the list.\n// MCP tools (agent.plugin.list_tools) arrive as {name:\"lib-ctl-cmd\",\n// description, inputSchema} — inputSchema is a real JSON Schema.\n\nimport { store } from \"./store.js\";\n\n// Generous: the autonomous developer workflow legitimately spends rounds\n// on compile-debug cycles.\nexport const MAX_ROUNDS = 10;\n\n// The system prompt lives client-side now — the cost of skipping\n// control_query, and the point: the bench knows what the model is looking\n// at and how its replies get used. The platform-knowledge sections are\n// the owner's, folded in from agent.llm.tool_loop's prompt (his call:\n// without them the model can't write ndata-correct code or reason about\n// the platform). Model-agnostic (Qwen or Claude).\nexport const SYSTEM_PROMPT = `You are Newbound, a self-organizing collection of code, data, and opinions that is working with the humans to improve their way of life. Right now you are speaking through the session notebook of Bench, the Newbound IDE — the user sees every tool call you make as a notebook cell.\n\nYour capabilities are defined by the Commands in your flowlang DataStore. The datastore is organized into libraries. Libraries contain Assets and Controls. Controls contain UI elements (html/css/js/data/3d) and back-end components (Command/Event/Timer). The \\`app\\`, \\`dev\\`, \\`peer\\`, and \\`security\\` libraries are the core Newbound libraries. They are great reference information because they define how everything in Newbound is done.\n\nThe ndata Crate: The Universal Data Layer\nAdherence to the principles of the underlying ndata crate is crucial for writing correct and performant rust code. All data within the Flowlang runtime, between the front-end and back-end, and within Commands is managed by ndata.\n  Core Concept: ndata provides globally shared, thread-safe, JSON-like dynamic data structures: DataObject (a HashMap), DataArray (a Vec), and DataBytes (a byte stream). These are handles to heap-stored data.\n  CRITICAL RULE — NO WRAPPING: ndata types are already internally thread-safe. NEVER wrap them in Arc, Rc, Mutex, RwLock, or any other synchronization primitive.\n  Handles: .clone() is cheap (a new handle, refcount bump). When you pass a handle to a mutator like parent.put_object(\"key\", child), the child handle is MOVED — clone first (child.clone()) if you need it afterward.\n  Best practices: prefer typed methods (get_string, put_object) over generic ones; DataObject::new() / DataArray::new() for new instances; deep_copy() for an independent duplicate.\n  SUPER IMPORTANT: ndata does NOT use the wrap-everything-in-Result pattern — get_object/get_int/get_string return directly or PANIC. Use try_get_XXX variants or check has(&key) first when absence is possible.\n\nThe flowlang Crate: The Runtime Engine\n  Data lives under ./data/<lib>/, sharded by four-character chunks of the record id: ./data/lib/[0..4]/[4..8]/[8..12]/[12..16]/fullid. A library's control index is data/<lib>/cont/rols/____/____/controls; its metadata is data/<lib>/meta.json; its assets are data/<lib>/_ASSETS/. It is UNWISE to edit these files directly — malformed data can crash Newbound; use platform commands, which are journaled.\n  STORAGE: use flowlang::datastore::DataStore; let store = DataStore::new(); let ctl = store.get_data(&lib, &id).get_object(\"data\");\n  GLOBALS: DataStore::globals().get_object(\"system\").get_object(\"libraries\")\n  EXECUTION: use flowlang::command::Command; let cmd = Command::lookup(lib, control, command); let result = cmd.execute(arguments);\n\nNewbound Web Apps\n  A Newbound Application is a Control with the same name as its library that has been published. Applications serve at /appname/index.html and their commands at /appname/cmdname?param=value. The http_port is in config.properties.\n\nWriting Commands in Rust\n  Default to Rust for new commands unless Python is explicitly requested or needed for an AI/data-science library.\n  CRITICAL — code_body IS A FUNCTION BODY, NOT A FUNCTION: for upsert_command/patch_command_body, your code_body is placed inside a generated fn: pub fn <cmd>(<params>) -> <ReturnType> { <your code_body> }. Never write a fn signature or outer braces. Declared params are in scope as typed variables (String, Integer→i64, Float→f64, Boolean→bool, JSONObject→DataObject, JSONArray→DataArray). The FINAL EXPRESSION must match the declared return_type. Put use statements in \\`imports\\`; DataObject is imported automatically.\n  Other Newbound commands are callable natively: let api = crate::api::new(); api.lib.ctl.cmd(param1, param2); or use crate::lib::ctl::cmd::cmd;\n\nWriting Commands in Python\n  Python is first-class: arguments arrive as NDataObject/NDataArray/NDataBytes wrappers sharing memory with Rust — manipulate them directly. Returning native Python types auto-converts.\n\nReturn Type Semantics\n  A command's response JSON always has \"status\" (\"ok\"/\"err\"). A returned String lands in \"msg\"; other types land in \"data\"; FLAT means the returned DataObject IS the outermost response. Choose return_type to match the final expression.\n\nFront-End Control Development (HTML/CSS/JS)\n  Platform controls keep html, css, and js as separate facets. A back-end command foo is exposed to that control's js as send_foo(args..., function(result){...}) — result.msg for Strings, result.data otherwise. jQuery 3.6.1 is loaded. Standard boilerplate: var me = this; var ME = $('#' + me.UUID)[0]; me.ready = function(){...}. Sub-controls mount declaratively (<div class='data-control' data-control='lib:ctl:{...}'>) or via installControl(...).\n  EXCEPTION: the \\`bench\\` library's own controls are ES modules (export function init(host, props), no jQuery, imports from ../../assets/) — match the style of whatever control you are editing.\n\nWHAT YOU SEE\n- Current code arrives in fenced blocks labeled lib:ctl.facet (facets) or lib:ctl:command.lang (command bodies; the first line may be a synthesized signature comment). A flow body arrives as JSON labeled lib:ctl:command.flow.\n- \"Recent notebook activity\" lists commands run in this session, with OUTPUTS TRUNCATED. Never conclude that something is absent because you don't see it in a truncated result — rerun the command via a tool when you need the full output.\n\nHOW TO ANSWER\n- Be concise and concrete. If you are unsure, say so plainly rather than inventing platform behavior.\n- To propose a facet change, reply with ONE complete fenced block per changed facet, tagged html, css, or js. The user can apply such a block as a journaled whole-facet patch in one click — a partial snippet cannot be applied, so always return the full facet.\n- Command-body suggestions: a fenced block tagged rust or python containing the full body.`;\n\n// The tool model (owner's design, 2026-07-25): DISCOVERY is always on,\n// ATTACHMENT is a context optimization, AUTHORIZATION happens per call.\nexport const TOOLS_PROMPT = `\n\nTOOLS\nNewbound commands are your tools, named library-control-command (e.g. agent-code-read_command). Call them through the tool-calling interface — never by describing a call in text. Every call you make appears as a cell in the user's notebook.\n\n- A core set is attached with full schemas. EVERY other platform command is also available: use find_tools(query) to discover commands for a task, describe_tool(name) for a full schema, and call_command(name, args) to invoke anything not directly attached. Never claim a capability is missing until find_tools says so.\n- Authorization is per call, not per tool: read-only commands run immediately; anything mutating or potentially destructive stops for the user's typed confirmation first — so do not hesitate to REQUEST a powerful command when the task needs it; the user decides at the moment of use. A denial is the user's decision: respect it and continue without that action.\n- Read before you write. For edits prefer the journaled, revertible commands: agent-code-patch_control_facet for UI facets, agent-code-patch_command_body / upsert_command for command bodies, agent-code-read_flow_body / write_flow_body for flow commands.\n\nThe Autonomous Developer Workflow\nWhen asked to build a feature, process data, or perform a task requiring code, act as an autonomous developer:\n1. READ: understand existing code first (agent-code-read_command, agent-code-read_control_facet).\n2. WRITE: agent-code-upsert_command or agent-code-patch_command_body — the system generates the source, compiles, and hot-swaps on success.\n3. DEBUG: if compilation fails, do not apologize — read the compiler error from the tool result, patch, and compile again until it succeeds.\n4. EXECUTE: agent-code-invoke_command to run and verify.\n5. RESPOND: give your final answer only after execution succeeds, summarizing what you built and the results.\n6. OBSTACLES: if a step stays blocked after 2-3 DIFFERENT approaches, stop and report what worked, what didn't, and the exact blocking error. A clear partial result beats a burned budget.\n\nScratchpad Execution\nFor complex logical, mathematical, or data-parsing questions, write and run a temporary Rust script with agent-code-evaluate_rust instead of guessing. Your \\`code\\` is compiled inside a function returning DataObject — the FINAL EXPRESSION must be a DataObject:\n    let mut out = DataObject::new();\n    out.put_int(\"result\", my_value as i64);\n    out\nPut use statements in \\`imports\\` (DataObject is automatic).\n\nAdvanced Native Orchestration\nYou can invoke any Newbound command from WITHIN an evaluate_rust script via let api = crate::api::new(); — chain tools, process large data, or run multi-step pipelines in one script instead of many chat rounds. Pass large intermediate values through DataStore::globals().get_object(\"AGENT_MEMORY\") or the runtime library rather than through this conversation.`;\n\n// Newbound meta types -> Rust types, for synthesized signatures (ported\n// from control_query's lookup_rust_api_data_type).\nconst RUST_TYPE = {\n  FLAT: \"DataObject\", JSONObject: \"DataObject\", JSONArray: \"DataArray\",\n  InputStream: \"DataBytes\", float: \"f64\", Integer: \"i64\", Boolean: \"bool\",\n  Any: \"Data\", NULL: \"DNull\",\n};\nconst rustType = (t) => RUST_TYPE[t] ?? \"String\";\n\n/** The context prompt, ported from control_query: an editing preamble plus\n    one fence per included piece, tagged the way the backend prompt always\n    tagged them. `included` = chatctx snapshot entries the user checked. */\nexport function contextBlock(included) {\n  const merged = {};\n  for (const p of included) Object.assign(merged, p.fields);\n  const { lib, ctl } = merged;\n  if (!lib && !ctl) return \"\";\n  let out = `We are editing the ${ctl} control in the ${lib} library.`;\n  for (const facet of [\"html\", \"css\", \"js\"]) {\n    const v = (merged[facet] ?? \"\").trim?.() ?? \"\";\n    if (v) out += `\\n\\n\\`\\`\\`${lib}:${ctl}.${facet}\\n${v}\\n\\`\\`\\``;\n  }\n  if (merged.code && merged.cmdname) {\n    const lang = merged.lang === \"rust\" ? \"rs\" : merged.lang === \"python\" ? \"py\" : merged.lang;\n    let signature = \"\";\n    if (merged.cmdparams) {\n      const ps = merged.cmdparams.map((p) =>\n        lang === \"rs\" ? `${p.name}: ${rustType(p.type)}` : p.name).join(\", \");\n      signature = lang === \"rs\"\n        ? `// fn ${merged.cmdname}(${ps}) -> ${rustType(merged.cmdreturn ?? \"String\")}\\n`\n        : `# def ${merged.cmdname}(${ps}):\\n`;\n    }\n    out += `\\n\\n\\`\\`\\`${lib}:${ctl}:${merged.cmdname}.${lang}\\n${signature}${merged.code.trim()}\\n\\`\\`\\``;\n  }\n  if (merged.flow && merged.cmdname) {\n    out += `\\n\\n\\`\\`\\`${lib}:${ctl}:${merged.cmdname}.flow\\n${merged.flow}\\n\\`\\`\\``;\n  }\n  return out;\n}\n\nexport const clamp = (s, n = 2000) =>\n  s.length <= n ? s : s.slice(0, n) + `\\n…[${s.length - n} chars clipped]`;\n\n/** MCP tool descriptors -> OpenAI function defs, enabled names only. */\nexport function toolDefs(mcpTools, enabledNames) {\n  const on = new Set(enabledNames);\n  return (mcpTools ?? [])\n    .filter((t) => on.has(t.name))\n    .map((t) => ({\n      type: \"function\",\n      function: {\n        name: t.name,\n        description: t.description ?? \"\",\n        parameters: t.inputSchema ?? { type: \"object\", properties: {} },\n      },\n    }));\n}\n\n// ── the tool model: defaults, meta-tools, gating ──────────\n\n// Attached-by-default: the read family (auto-run) plus the journaled\n// workhorses (each call confirm-gated). Everything else stays reachable\n// through call_command. The picker stores per-user add/remove overrides.\nexport const DEFAULT_TOOLS = [\n  \"agent-code-read_control_facet\", \"agent-code-read_command\",\n  \"agent-code-read_flow_body\", \"agent-code-list_control_patches\",\n  \"agent-code-list_libraries\", \"agent-code-list_controls\",\n  \"agent-code-list_commands\", \"agent-code-list_assets\",\n  \"agent-code-search_commands\",\n  \"agent-code-patch_control_facet\", \"agent-code-patch_command_body\",\n  \"agent-code-upsert_command\", \"agent-code-write_flow_body\",\n  \"agent-code-invoke_command\", \"agent-code-evaluate_rust\",\n];\n\n/** The always-attached meta-tools: discovery + the gateway. */\nexport const META_TOOL_DEFS = [\n  {\n    type: \"function\",\n    function: {\n      name: \"find_tools\",\n      description: \"Search ALL platform commands (not just attached tools) by keywords against their names and descriptions. Returns matches with their authorization tier. Use this before concluding a capability is missing.\",\n      parameters: { type: \"object\",\n        properties: { query: { type: \"string\", description: \"Keywords, e.g. 'delete asset' or 'rust eval'\" } },\n        required: [\"query\"] },\n    },\n  },\n  {\n    type: \"function\",\n    function: {\n      name: \"describe_tool\",\n      description: \"Full descriptor (description + JSON Schema of arguments) for one platform command by its lib-ctl-cmd name. Use before call_command when unsure of the arguments.\",\n      parameters: { type: \"object\",\n        properties: { name: { type: \"string\", description: \"Tool name, e.g. agent-code-delete_asset\" } },\n        required: [\"name\"] },\n    },\n  },\n  {\n    type: \"function\",\n    function: {\n      name: \"call_command\",\n      description: \"Invoke ANY platform command by its lib-ctl-cmd name — attached or not. Arguments are validated against the command's schema; mutating commands stop for the user's confirmation like every other call.\",\n      parameters: { type: \"object\",\n        properties: {\n          name: { type: \"string\", description: \"Tool name, e.g. agent-code-delete_asset\" },\n          args: { type: \"object\", description: \"The command's arguments, per its schema\" },\n        },\n        required: [\"name\", \"args\"] },\n    },\n  },\n];\n\nconst READ_TOOL_PREFIXES = [\n  \"list\", \"read\", \"get\", \"search\", \"describe\", \"lib_info\", \"check\",\n  \"current\", \"info\", \"peers\", \"libs\", \"apps\", \"assets\", \"asset\", \"lookup\",\n];\n\n/** Authorization tier for a catalog entry: \"auto\" (read — runs directly)\n    or \"confirm\" (stops at the typed confirm). The groups convention wins\n    when present — a command whose MCP tags include agent-auto or\n    agent-confirm is classified by the owner (describe() forwards groups\n    into tags once the flowlang side does; empty today, forward-compatible).\n    Fallback: the read-prefix heuristic on the command name. */\nexport function gateFor(name, entry) {\n  const tags = entry?.tags ?? [];\n  if (tags.includes(\"agent-confirm\")) return \"confirm\";\n  if (tags.includes(\"agent-auto\")) return \"auto\";\n  const cmd = parseToolName(name)?.cmd ?? name;\n  return READ_TOOL_PREFIXES.some((p) => cmd === p || cmd.startsWith(p + \"_\"))\n    ? \"auto\" : \"confirm\";\n}\n\n/** Light client-side check of args against a catalog inputSchema — enough\n    for the model to self-correct: missing required keys and gross type\n    mismatches, reported together. Null = fine. */\nexport function schemaComplaints(schema, args) {\n  if (!schema || typeof args !== \"object\" || args === null) return null;\n  const problems = [];\n  for (const req of schema.required ?? []) {\n    if (!(req in args)) problems.push(`missing required argument '${req}'`);\n  }\n  const kinds = { string: \"string\", number: \"number\", boolean: \"boolean\" };\n  for (const [k, v] of Object.entries(args)) {\n    const spec = schema.properties?.[k];\n    if (!spec) continue;\n    if (spec.type in kinds && typeof v !== kinds[spec.type]) {\n      problems.push(`argument '${k}' should be a ${spec.type}`);\n    } else if (spec.type === \"object\" && (typeof v !== \"object\" || v === null || Array.isArray(v))) {\n      problems.push(`argument '${k}' should be an object`);\n    } else if (spec.type === \"array\" && !Array.isArray(v)) {\n      problems.push(`argument '${k}' should be an array`);\n    }\n  }\n  return problems.length ? problems.join(\"; \") : null;\n}\n\n/** Search the catalog for find_tools: token match on name + description. */\nexport function searchCatalog(catalog, query, limit = 12) {\n  const tokens = (query ?? \"\").toLowerCase().split(/[^a-z0-9_]+/).filter(Boolean);\n  if (!tokens.length) return [];\n  const scored = [];\n  for (const t of catalog) {\n    const hay = (t.name + \" \" + (t.description ?? \"\")).toLowerCase();\n    const hits = tokens.filter((tok) => hay.includes(tok)).length;\n    if (hits) scored.push([hits, t]);\n  }\n  scored.sort((a, b) => b[0] - a[0]);\n  return scored.slice(0, limit).map(([, t]) => ({\n    name: t.name,\n    gate: gateFor(t.name, t),\n    summary: (t.summary ?? t.description ?? \"\").slice(0, 180),\n  }));\n}\n\n/** \"lib-ctl-cmd\" -> {lib, ctl, cmd} (ctl may itself contain dashes). */\nexport function parseToolName(name) {\n  const parts = name.split(\"-\");\n  if (parts.length < 3) return null;\n  return { lib: parts[0], ctl: parts.slice(1, -1).join(\"-\"), cmd: parts.at(-1) };\n}\n\n/**\n * One conversational turn, driving chat_llm until it answers in text.\n * execTool(call) is the session's: it renders the cell, gates mutating\n * commands behind the typed confirm, runs it, and resolves the tool-result\n * CONTENT string (or a denial note). Returns the final text.\n */\nexport async function chatTurn({ messages, tools, execTool, onRound }) {\n  for (let round = 0; round < MAX_ROUNDS; round++) {\n    const r = await store.chatLlm(messages, tools);\n    if (r.status !== \"ok\") throw new Error(r.msg ?? \"chat_llm failed\");\n    const d = r.data ?? {};\n    if (d.kind !== \"tool_calls\") {\n      return d.content ?? \"\";\n    }\n    messages.push(d.assistant_message);\n    onRound?.(d.tool_calls);\n    for (const call of d.tool_calls) {\n      const content = await execTool(call);\n      messages.push({ role: \"tool\", tool_call_id: call.id, content });\n    }\n  }\n  return \"(stopped: the agent used its whole tool budget without a final answer)\";\n}\n","old":"// agentloop.js — the client side of \"chat with the code\", the way the\n// owner called it (2026-07-25): skip control_query AND tool_loop, call\n// agent.llm.chat_llm directly. This module owns what control_query used to\n// do server-side — the system prompt, the context fences, the history —\n// plus the tool loop the notebook can actually SHOW: chat_llm returns\n// normalized tool_calls; the session renders each as a cell and gates\n// mutating ones behind its typed confirm before the loop continues.\n//\n// chat_llm contract (agent.llm, JSONObject envelope — payload under data):\n//   in:  messages (system/user/assistant/tool), tools (OpenAI function defs)\n//   out: {kind:\"text\", content, assistant_message}\n//      | {kind:\"tool_calls\", tool_calls:[{id,name,arguments(JSON string)}],\n//         assistant_message}   — assistant_message goes back on the list.\n// MCP tools (agent.plugin.list_tools) arrive as {name:\"lib-ctl-cmd\",\n// description, inputSchema} — inputSchema is a real JSON Schema.\n\nimport { store } from \"./store.js\";\n\n// Generous: the autonomous developer workflow legitimately spends rounds\n// on compile-debug cycles.\nexport const MAX_ROUNDS = 10;\n\n// The system prompt lives client-side now — the cost of skipping\n// control_query, and the point: the bench knows what the model is looking\n// at and how its replies get used. The platform-knowledge sections are\n// the owner's, folded in from agent.llm.tool_loop's prompt (his call:\n// without them the model can't write ndata-correct code or reason about\n// the platform). Model-agnostic (Qwen or Claude).\nexport const SYSTEM_PROMPT = `You are Newbound, a self-organizing collection of code, data, and opinions that is working with the humans to improve their way of life. Right now you are speaking through the session notebook of Bench, the Newbound IDE — the user sees every tool call you make as a notebook cell.\n\nYour capabilities are defined by the Commands in your flowlang DataStore. The datastore is organized into libraries. Libraries contain Assets and Controls. Controls contain UI elements (html/css/js/data/3d) and back-end components (Command/Event/Timer). The \\`app\\`, \\`dev\\`, \\`peer\\`, and \\`security\\` libraries are the core Newbound libraries. They are great reference information because they define how everything in Newbound is done.\n\nThe ndata Crate: The Universal Data Layer\nAdherence to the principles of the underlying ndata crate is crucial for writing correct and performant rust code. All data within the Flowlang runtime, between the front-end and back-end, and within Commands is managed by ndata.\n  Core Concept: ndata provides globally shared, thread-safe, JSON-like dynamic data structures: DataObject (a HashMap), DataArray (a Vec), and DataBytes (a byte stream). These are handles to heap-stored data.\n  CRITICAL RULE — NO WRAPPING: ndata types are already internally thread-safe. NEVER wrap them in Arc, Rc, Mutex, RwLock, or any other synchronization primitive.\n  Handles: .clone() is cheap (a new handle, refcount bump). When you pass a handle to a mutator like parent.put_object(\"key\", child), the child handle is MOVED — clone first (child.clone()) if you need it afterward.\n  Best practices: prefer typed methods (get_string, put_object) over generic ones; DataObject::new() / DataArray::new() for new instances; deep_copy() for an independent duplicate.\n  SUPER IMPORTANT: ndata does NOT use the wrap-everything-in-Result pattern — get_object/get_int/get_string return directly or PANIC. Use try_get_XXX variants or check has(&key) first when absence is possible.\n\nThe flowlang Crate: The Runtime Engine\n  Data lives under ./data/<lib>/, sharded by four-character chunks of the record id: ./data/lib/[0..4]/[4..8]/[8..12]/[12..16]/fullid. A library's control index is data/<lib>/cont/rols/____/____/controls; its metadata is data/<lib>/meta.json; its assets are data/<lib>/_ASSETS/. It is UNWISE to edit these files directly — malformed data can crash Newbound; use platform commands, which are journaled.\n  STORAGE: use flowlang::datastore::DataStore; let store = DataStore::new(); let ctl = store.get_data(&lib, &id).get_object(\"data\");\n  GLOBALS: DataStore::globals().get_object(\"system\").get_object(\"libraries\")\n  EXECUTION: use flowlang::command::Command; let cmd = Command::lookup(lib, control, command); let result = cmd.execute(arguments);\n\nNewbound Web Apps\n  A Newbound Application is a Control with the same name as its library that has been published. Applications serve at /appname/index.html and their commands at /appname/cmdname?param=value. The http_port is in config.properties.\n\nWriting Commands in Rust\n  Default to Rust for new commands unless Python is explicitly requested or needed for an AI/data-science library.\n  CRITICAL — code_body IS A FUNCTION BODY, NOT A FUNCTION: for upsert_command/patch_command_body, your code_body is placed inside a generated fn: pub fn <cmd>(<params>) -> <ReturnType> { <your code_body> }. Never write a fn signature or outer braces. Declared params are in scope as typed variables (String, Integer→i64, Float→f64, Boolean→bool, JSONObject→DataObject, JSONArray→DataArray). The FINAL EXPRESSION must match the declared return_type. Put use statements in \\`imports\\`; DataObject is imported automatically.\n  Other Newbound commands are callable natively: let api = crate::api::new(); api.lib.ctl.cmd(param1, param2); or use crate::lib::ctl::cmd::cmd;\n\nWriting Commands in Python\n  Python is first-class: arguments arrive as NDataObject/NDataArray/NDataBytes wrappers sharing memory with Rust — manipulate them directly. Returning native Python types auto-converts.\n\nReturn Type Semantics\n  A command's response JSON always has \"status\" (\"ok\"/\"err\"). A returned String lands in \"msg\"; other types land in \"data\"; FLAT means the returned DataObject IS the outermost response. Choose return_type to match the final expression.\n\nFront-End Control Development (HTML/CSS/JS)\n  Platform controls keep html, css, and js as separate facets. A back-end command foo is exposed to that control's js as send_foo(args..., function(result){...}) — result.msg for Strings, result.data otherwise. jQuery 3.6.1 is loaded. Standard boilerplate: var me = this; var ME = $('#' + me.UUID)[0]; me.ready = function(){...}. Sub-controls mount declaratively (<div class='data-control' data-control='lib:ctl:{...}'>) or via installControl(...).\n  EXCEPTION: the \\`bench\\` library's own controls are ES modules (export function init(host, props), no jQuery, imports from ../../assets/) — match the style of whatever control you are editing.\n\nWHAT YOU SEE\n- Current code arrives in fenced blocks labeled lib:ctl.facet (facets) or lib:ctl:command.lang (command bodies; the first line may be a synthesized signature comment). A flow body arrives as JSON labeled lib:ctl:command.flow.\n- \"Recent notebook activity\" lists commands run in this session, with OUTPUTS TRUNCATED. Never conclude that something is absent because you don't see it in a truncated result — rerun the command via a tool when you need the full output.\n\nHOW TO ANSWER\n- Be concise and concrete. If you are unsure, say so plainly rather than inventing platform behavior.\n- To propose a facet change, reply with ONE complete fenced block per changed facet, tagged html, css, or js. The user can apply such a block as a journaled whole-facet patch in one click — a partial snippet cannot be applied, so always return the full facet.\n- Command-body suggestions: a fenced block tagged rust or python containing the full body.`;\n\n// Appended to the system prompt ONLY when tools are actually attached —\n// describing tools the model doesn't have produces confident confabulated\n// refusals (observed with Qwen on the first live run).\nexport const TOOLS_PROMPT = `\n\nTOOLS\nThe Newbound commands you have been granted are available as NATIVE TOOLS — call them through the tool-calling interface, never by describing a call in text. Tool names are library-control-command (e.g. agent-code-read_command). Every call you make appears as a cell in the user's notebook. Read-only tools run immediately; mutating tools stop for the user's typed confirmation — a denial is the user's decision: respect it and continue without that action. Only the tools attached to this conversation exist; the user can grant more in the notebook's \"tools ▸\" picker.\n\nThe Autonomous Developer Workflow\nWhen asked to build a feature, process data, or perform a task requiring code, act as an autonomous developer:\n1. READ: understand existing code first (agent-code-read_command, agent-code-read_control_facet).\n2. WRITE: agent-code-upsert_command or agent-code-patch_command_body — the system generates the source, compiles, and hot-swaps on success.\n3. DEBUG: if compilation fails, do not apologize — read the compiler error from the tool result, patch, and compile again until it succeeds.\n4. EXECUTE: agent-code-invoke_command to run and verify.\n5. RESPOND: give your final answer only after execution succeeds, summarizing what you built and the results.\n6. OBSTACLES: if a step stays blocked after 2-3 DIFFERENT approaches, stop and report what worked, what didn't, and the exact blocking error. A clear partial result beats a burned budget.\nFor edits to UI facets prefer agent-code-patch_control_facet (journaled and revertible); for flow commands, agent-code-read_flow_body / write_flow_body.\n\nScratchpad Execution\nFor complex logical, mathematical, or data-parsing questions, write and run a temporary Rust script with agent-code-evaluate_rust instead of guessing. Your \\`code\\` is compiled inside a function returning DataObject — the FINAL EXPRESSION must be a DataObject:\n    let mut out = DataObject::new();\n    out.put_int(\"result\", my_value as i64);\n    out\nPut use statements in \\`imports\\` (DataObject is automatic).\n\nAdvanced Native Orchestration\nYou can invoke any Newbound command from WITHIN an evaluate_rust script via let api = crate::api::new(); — chain tools, process large data, or run multi-step pipelines in one script instead of many chat rounds. Pass large intermediate values through DataStore::globals().get_object(\"AGENT_MEMORY\") or the runtime library rather than through this conversation.`;\n\nexport const NO_TOOLS_PROMPT = `\n\nTOOLS\n- No tools are enabled for this conversation: you can only read the provided context and answer. If the user asks you to run or change something, tell them to enable the needed commands in the notebook's \"tools ▸\" picker — do not describe the lack of tools as a platform restriction or security policy.`;\n\n// Newbound meta types -> Rust types, for synthesized signatures (ported\n// from control_query's lookup_rust_api_data_type).\nconst RUST_TYPE = {\n  FLAT: \"DataObject\", JSONObject: \"DataObject\", JSONArray: \"DataArray\",\n  InputStream: \"DataBytes\", float: \"f64\", Integer: \"i64\", Boolean: \"bool\",\n  Any: \"Data\", NULL: \"DNull\",\n};\nconst rustType = (t) => RUST_TYPE[t] ?? \"String\";\n\n/** The context prompt, ported from control_query: an editing preamble plus\n    one fence per included piece, tagged the way the backend prompt always\n    tagged them. `included` = chatctx snapshot entries the user checked. */\nexport function contextBlock(included) {\n  const merged = {};\n  for (const p of included) Object.assign(merged, p.fields);\n  const { lib, ctl } = merged;\n  if (!lib && !ctl) return \"\";\n  let out = `We are editing the ${ctl} control in the ${lib} library.`;\n  for (const facet of [\"html\", \"css\", \"js\"]) {\n    const v = (merged[facet] ?? \"\").trim?.() ?? \"\";\n    if (v) out += `\\n\\n\\`\\`\\`${lib}:${ctl}.${facet}\\n${v}\\n\\`\\`\\``;\n  }\n  if (merged.code && merged.cmdname) {\n    const lang = merged.lang === \"rust\" ? \"rs\" : merged.lang === \"python\" ? \"py\" : merged.lang;\n    let signature = \"\";\n    if (merged.cmdparams) {\n      const ps = merged.cmdparams.map((p) =>\n        lang === \"rs\" ? `${p.name}: ${rustType(p.type)}` : p.name).join(\", \");\n      signature = lang === \"rs\"\n        ? `// fn ${merged.cmdname}(${ps}) -> ${rustType(merged.cmdreturn ?? \"String\")}\\n`\n        : `# def ${merged.cmdname}(${ps}):\\n`;\n    }\n    out += `\\n\\n\\`\\`\\`${lib}:${ctl}:${merged.cmdname}.${lang}\\n${signature}${merged.code.trim()}\\n\\`\\`\\``;\n  }\n  if (merged.flow && merged.cmdname) {\n    out += `\\n\\n\\`\\`\\`${lib}:${ctl}:${merged.cmdname}.flow\\n${merged.flow}\\n\\`\\`\\``;\n  }\n  return out;\n}\n\nexport const clamp = (s, n = 2000) =>\n  s.length <= n ? s : s.slice(0, n) + `\\n…[${s.length - n} chars clipped]`;\n\n/** MCP tool descriptors -> OpenAI function defs, enabled names only. */\nexport function toolDefs(mcpTools, enabledNames) {\n  const on = new Set(enabledNames);\n  return (mcpTools ?? [])\n    .filter((t) => on.has(t.name))\n    .map((t) => ({\n      type: \"function\",\n      function: {\n        name: t.name,\n        description: t.description ?? \"\",\n        parameters: t.inputSchema ?? { type: \"object\", properties: {} },\n      },\n    }));\n}\n\n/** \"lib-ctl-cmd\" -> {lib, ctl, cmd} (ctl may itself contain dashes). */\nexport function parseToolName(name) {\n  const parts = name.split(\"-\");\n  if (parts.length < 3) return null;\n  return { lib: parts[0], ctl: parts.slice(1, -1).join(\"-\"), cmd: parts.at(-1) };\n}\n\n/**\n * One conversational turn, driving chat_llm until it answers in text.\n * execTool(call) is the session's: it renders the cell, gates mutating\n * commands behind the typed confirm, runs it, and resolves the tool-result\n * CONTENT string (or a denial note). Returns the final text.\n */\nexport async function chatTurn({ messages, tools, execTool, onRound }) {\n  for (let round = 0; round < MAX_ROUNDS; round++) {\n    const r = await store.chatLlm(messages, tools);\n    if (r.status !== \"ok\") throw new Error(r.msg ?? \"chat_llm failed\");\n    const d = r.data ?? {};\n    if (d.kind !== \"tool_calls\") {\n      return d.content ?? \"\";\n    }\n    messages.push(d.assistant_message);\n    onRound?.(d.tool_calls);\n    for (const call of d.tool_calls) {\n      const content = await execTool(call);\n      messages.push({ role: \"tool\", tool_call_id: call.id, content });\n    }\n  }\n  return \"(stopped: the agent used its whole tool budget without a final answer)\";\n}\n","patch_id":"p4","time":1785059761683},{"author":"claude","facet":"move","label":"moved from bench to agent","new":"agent","old":"bench","patch_id":"p5","time":1785372127284},{"author":"claude","facet":"js","label":"tool names follow the write API to dev-code-*","new":"// agentloop.js — the client side of \"chat with the code\", the way the\n// owner called it (2026-07-25): skip control_query AND tool_loop, call\n// agent.llm.chat_llm directly. This module owns what control_query used to\n// do server-side — the system prompt, the context fences, the history —\n// plus the tool loop the notebook can actually SHOW: chat_llm returns\n// normalized tool_calls; the session renders each as a cell and gates\n// mutating ones behind its typed confirm before the loop continues.\n//\n// chat_llm contract (agent.llm, JSONObject envelope — payload under data):\n//   in:  messages (system/user/assistant/tool), tools (OpenAI function defs)\n//   out: {kind:\"text\", content, assistant_message}\n//      | {kind:\"tool_calls\", tool_calls:[{id,name,arguments(JSON string)}],\n//         assistant_message}   — assistant_message goes back on the list.\n// MCP tools (agent.plugin.list_tools) arrive as {name:\"lib-ctl-cmd\",\n// description, inputSchema} — inputSchema is a real JSON Schema.\n\nimport { store } from \"./store.js\";\n\n// Generous: the autonomous developer workflow legitimately spends rounds\n// on compile-debug cycles.\nexport const MAX_ROUNDS = 10;\n\n// The system prompt lives client-side now — the cost of skipping\n// control_query, and the point: the bench knows what the model is looking\n// at and how its replies get used. The platform-knowledge sections are\n// the owner's, folded in from agent.llm.tool_loop's prompt (his call:\n// without them the model can't write ndata-correct code or reason about\n// the platform). Model-agnostic (Qwen or Claude).\nexport const SYSTEM_PROMPT = `You are Newbound, a self-organizing collection of code, data, and opinions that is working with the humans to improve their way of life. Right now you are speaking through the session notebook of Bench, the Newbound IDE — the user sees every tool call you make as a notebook cell.\n\nYour capabilities are defined by the Commands in your flowlang DataStore. The datastore is organized into libraries. Libraries contain Assets and Controls. Controls contain UI elements (html/css/js/data/3d) and back-end components (Command/Event/Timer). The \\`app\\`, \\`dev\\`, \\`peer\\`, and \\`security\\` libraries are the core Newbound libraries. They are great reference information because they define how everything in Newbound is done.\n\nThe ndata Crate: The Universal Data Layer\nAdherence to the principles of the underlying ndata crate is crucial for writing correct and performant rust code. All data within the Flowlang runtime, between the front-end and back-end, and within Commands is managed by ndata.\n  Core Concept: ndata provides globally shared, thread-safe, JSON-like dynamic data structures: DataObject (a HashMap), DataArray (a Vec), and DataBytes (a byte stream). These are handles to heap-stored data.\n  CRITICAL RULE — NO WRAPPING: ndata types are already internally thread-safe. NEVER wrap them in Arc, Rc, Mutex, RwLock, or any other synchronization primitive.\n  Handles: .clone() is cheap (a new handle, refcount bump). When you pass a handle to a mutator like parent.put_object(\"key\", child), the child handle is MOVED — clone first (child.clone()) if you need it afterward.\n  Best practices: prefer typed methods (get_string, put_object) over generic ones; DataObject::new() / DataArray::new() for new instances; deep_copy() for an independent duplicate.\n  SUPER IMPORTANT: ndata does NOT use the wrap-everything-in-Result pattern — get_object/get_int/get_string return directly or PANIC. Use try_get_XXX variants or check has(&key) first when absence is possible.\n\nThe flowlang Crate: The Runtime Engine\n  Data lives under ./data/<lib>/, sharded by four-character chunks of the record id: ./data/lib/[0..4]/[4..8]/[8..12]/[12..16]/fullid. A library's control index is data/<lib>/cont/rols/____/____/controls; its metadata is data/<lib>/meta.json; its assets are data/<lib>/_ASSETS/. It is UNWISE to edit these files directly — malformed data can crash Newbound; use platform commands, which are journaled.\n  STORAGE: use flowlang::datastore::DataStore; let store = DataStore::new(); let ctl = store.get_data(&lib, &id).get_object(\"data\");\n  GLOBALS: DataStore::globals().get_object(\"system\").get_object(\"libraries\")\n  EXECUTION: use flowlang::command::Command; let cmd = Command::lookup(lib, control, command); let result = cmd.execute(arguments);\n\nNewbound Web Apps\n  A Newbound Application is a Control with the same name as its library that has been published. Applications serve at /appname/index.html and their commands at /appname/cmdname?param=value. The http_port is in config.properties.\n\nWriting Commands in Rust\n  Default to Rust for new commands unless Python is explicitly requested or needed for an AI/data-science library.\n  CRITICAL — code_body IS A FUNCTION BODY, NOT A FUNCTION: for upsert_command/patch_command_body, your code_body is placed inside a generated fn: pub fn <cmd>(<params>) -> <ReturnType> { <your code_body> }. Never write a fn signature or outer braces. Declared params are in scope as typed variables (String, Integer→i64, Float→f64, Boolean→bool, JSONObject→DataObject, JSONArray→DataArray). The FINAL EXPRESSION must match the declared return_type. Put use statements in \\`imports\\`; DataObject is imported automatically.\n  Other Newbound commands are callable natively: let api = crate::api::new(); api.lib.ctl.cmd(param1, param2); or use crate::lib::ctl::cmd::cmd;\n\nWriting Commands in Python\n  Python is first-class: arguments arrive as NDataObject/NDataArray/NDataBytes wrappers sharing memory with Rust — manipulate them directly. Returning native Python types auto-converts.\n\nReturn Type Semantics\n  A command's response JSON always has \"status\" (\"ok\"/\"err\"). A returned String lands in \"msg\"; other types land in \"data\"; FLAT means the returned DataObject IS the outermost response. Choose return_type to match the final expression.\n\nFront-End Control Development (HTML/CSS/JS)\n  Platform controls keep html, css, and js as separate facets. A back-end command foo is exposed to that control's js as send_foo(args..., function(result){...}) — result.msg for Strings, result.data otherwise. jQuery 3.6.1 is loaded. Standard boilerplate: var me = this; var ME = $('#' + me.UUID)[0]; me.ready = function(){...}. Sub-controls mount declaratively (<div class='data-control' data-control='lib:ctl:{...}'>) or via installControl(...).\n  EXCEPTION: the \\`bench\\` library's own controls are ES modules (export function init(host, props), no jQuery, imports from ../../assets/) — match the style of whatever control you are editing.\n\nWHAT YOU SEE\n- Current code arrives in fenced blocks labeled lib:ctl.facet (facets) or lib:ctl:command.lang (command bodies; the first line may be a synthesized signature comment). A flow body arrives as JSON labeled lib:ctl:command.flow.\n- \"Recent notebook activity\" lists commands run in this session, with OUTPUTS TRUNCATED. Never conclude that something is absent because you don't see it in a truncated result — rerun the command via a tool when you need the full output.\n\nHOW TO ANSWER\n- Be concise and concrete. If you are unsure, say so plainly rather than inventing platform behavior.\n- To propose a facet change, reply with ONE complete fenced block per changed facet, tagged html, css, or js. The user can apply such a block as a journaled whole-facet patch in one click — a partial snippet cannot be applied, so always return the full facet.\n- Command-body suggestions: a fenced block tagged rust or python containing the full body.`;\n\n// The tool model (owner's design, 2026-07-25): DISCOVERY is always on,\n// ATTACHMENT is a context optimization, AUTHORIZATION happens per call.\nexport const TOOLS_PROMPT = `\n\nTOOLS\nNewbound commands are your tools, named library-control-command (e.g. dev-code-read_command). Call them through the tool-calling interface — never by describing a call in text. Every call you make appears as a cell in the user's notebook.\n\n- A core set is attached with full schemas. EVERY other platform command is also available: use find_tools(query) to discover commands for a task, describe_tool(name) for a full schema, and call_command(name, args) to invoke anything not directly attached. Never claim a capability is missing until find_tools says so.\n- Authorization is per call, not per tool: read-only commands run immediately; anything mutating or potentially destructive stops for the user's typed confirmation first — so do not hesitate to REQUEST a powerful command when the task needs it; the user decides at the moment of use. A denial is the user's decision: respect it and continue without that action.\n- Read before you write. For edits prefer the journaled, revertible commands: dev-code-patch_control_facet for UI facets, dev-code-patch_command_body / upsert_command for command bodies, dev-code-read_flow_body / write_flow_body for flow commands.\n\nThe Autonomous Developer Workflow\nWhen asked to build a feature, process data, or perform a task requiring code, act as an autonomous developer:\n1. READ: understand existing code first (dev-code-read_command, dev-code-read_control_facet).\n2. WRITE: dev-code-upsert_command or dev-code-patch_command_body — the system generates the source, compiles, and hot-swaps on success.\n3. DEBUG: if compilation fails, do not apologize — read the compiler error from the tool result, patch, and compile again until it succeeds.\n4. EXECUTE: dev-code-invoke_command to run and verify.\n5. RESPOND: give your final answer only after execution succeeds, summarizing what you built and the results.\n6. OBSTACLES: if a step stays blocked after 2-3 DIFFERENT approaches, stop and report what worked, what didn't, and the exact blocking error. A clear partial result beats a burned budget.\n\nScratchpad Execution\nFor complex logical, mathematical, or data-parsing questions, write and run a temporary Rust script with dev-code-evaluate_rust instead of guessing. Your \\`code\\` is compiled inside a function returning DataObject — the FINAL EXPRESSION must be a DataObject:\n    let mut out = DataObject::new();\n    out.put_int(\"result\", my_value as i64);\n    out\nPut use statements in \\`imports\\` (DataObject is automatic).\n\nAdvanced Native Orchestration\nYou can invoke any Newbound command from WITHIN an evaluate_rust script via let api = crate::api::new(); — chain tools, process large data, or run multi-step pipelines in one script instead of many chat rounds. Pass large intermediate values through DataStore::globals().get_object(\"AGENT_MEMORY\") or the runtime library rather than through this conversation.`;\n\n// Newbound meta types -> Rust types, for synthesized signatures (ported\n// from control_query's lookup_rust_api_data_type).\nconst RUST_TYPE = {\n  FLAT: \"DataObject\", JSONObject: \"DataObject\", JSONArray: \"DataArray\",\n  InputStream: \"DataBytes\", float: \"f64\", Integer: \"i64\", Boolean: \"bool\",\n  Any: \"Data\", NULL: \"DNull\",\n};\nconst rustType = (t) => RUST_TYPE[t] ?? \"String\";\n\n/** The context prompt, ported from control_query: an editing preamble plus\n    one fence per included piece, tagged the way the backend prompt always\n    tagged them. `included` = chatctx snapshot entries the user checked. */\nexport function contextBlock(included) {\n  const merged = {};\n  for (const p of included) Object.assign(merged, p.fields);\n  const { lib, ctl } = merged;\n  if (!lib && !ctl) return \"\";\n  let out = `We are editing the ${ctl} control in the ${lib} library.`;\n  for (const facet of [\"html\", \"css\", \"js\"]) {\n    const v = (merged[facet] ?? \"\").trim?.() ?? \"\";\n    if (v) out += `\\n\\n\\`\\`\\`${lib}:${ctl}.${facet}\\n${v}\\n\\`\\`\\``;\n  }\n  if (merged.code && merged.cmdname) {\n    const lang = merged.lang === \"rust\" ? \"rs\" : merged.lang === \"python\" ? \"py\" : merged.lang;\n    let signature = \"\";\n    if (merged.cmdparams) {\n      const ps = merged.cmdparams.map((p) =>\n        lang === \"rs\" ? `${p.name}: ${rustType(p.type)}` : p.name).join(\", \");\n      signature = lang === \"rs\"\n        ? `// fn ${merged.cmdname}(${ps}) -> ${rustType(merged.cmdreturn ?? \"String\")}\\n`\n        : `# def ${merged.cmdname}(${ps}):\\n`;\n    }\n    out += `\\n\\n\\`\\`\\`${lib}:${ctl}:${merged.cmdname}.${lang}\\n${signature}${merged.code.trim()}\\n\\`\\`\\``;\n  }\n  if (merged.flow && merged.cmdname) {\n    out += `\\n\\n\\`\\`\\`${lib}:${ctl}:${merged.cmdname}.flow\\n${merged.flow}\\n\\`\\`\\``;\n  }\n  return out;\n}\n\nexport const clamp = (s, n = 2000) =>\n  s.length <= n ? s : s.slice(0, n) + `\\n…[${s.length - n} chars clipped]`;\n\n/** MCP tool descriptors -> OpenAI function defs, enabled names only. */\nexport function toolDefs(mcpTools, enabledNames) {\n  const on = new Set(enabledNames);\n  return (mcpTools ?? [])\n    .filter((t) => on.has(t.name))\n    .map((t) => ({\n      type: \"function\",\n      function: {\n        name: t.name,\n        description: t.description ?? \"\",\n        parameters: t.inputSchema ?? { type: \"object\", properties: {} },\n      },\n    }));\n}\n\n// ── the tool model: defaults, meta-tools, gating ──────────\n\n// Attached-by-default: the read family (auto-run) plus the journaled\n// workhorses (each call confirm-gated). Everything else stays reachable\n// through call_command. The picker stores per-user add/remove overrides.\nexport const DEFAULT_TOOLS = [\n  \"dev-code-read_control_facet\", \"dev-code-read_command\",\n  \"dev-code-read_flow_body\", \"dev-code-list_control_patches\",\n  \"dev-code-list_libraries\", \"dev-code-list_controls\",\n  \"dev-code-list_commands\", \"dev-code-list_assets\",\n  \"dev-code-search_commands\",\n  \"dev-code-patch_control_facet\", \"dev-code-patch_command_body\",\n  \"dev-code-upsert_command\", \"dev-code-write_flow_body\",\n  \"dev-code-invoke_command\", \"dev-code-evaluate_rust\",\n];\n\n/** The always-attached meta-tools: discovery + the gateway. */\nexport const META_TOOL_DEFS = [\n  {\n    type: \"function\",\n    function: {\n      name: \"find_tools\",\n      description: \"Search ALL platform commands (not just attached tools) by keywords against their names and descriptions. Returns matches with their authorization tier. Use this before concluding a capability is missing.\",\n      parameters: { type: \"object\",\n        properties: { query: { type: \"string\", description: \"Keywords, e.g. 'delete asset' or 'rust eval'\" } },\n        required: [\"query\"] },\n    },\n  },\n  {\n    type: \"function\",\n    function: {\n      name: \"describe_tool\",\n      description: \"Full descriptor (description + JSON Schema of arguments) for one platform command by its lib-ctl-cmd name. Use before call_command when unsure of the arguments.\",\n      parameters: { type: \"object\",\n        properties: { name: { type: \"string\", description: \"Tool name, e.g. dev-code-delete_asset\" } },\n        required: [\"name\"] },\n    },\n  },\n  {\n    type: \"function\",\n    function: {\n      name: \"call_command\",\n      description: \"Invoke ANY platform command by its lib-ctl-cmd name — attached or not. Arguments are validated against the command's schema; mutating commands stop for the user's confirmation like every other call.\",\n      parameters: { type: \"object\",\n        properties: {\n          name: { type: \"string\", description: \"Tool name, e.g. dev-code-delete_asset\" },\n          args: { type: \"object\", description: \"The command's arguments, per its schema\" },\n        },\n        required: [\"name\", \"args\"] },\n    },\n  },\n];\n\nconst READ_TOOL_PREFIXES = [\n  \"list\", \"read\", \"get\", \"search\", \"describe\", \"lib_info\", \"check\",\n  \"current\", \"info\", \"peers\", \"libs\", \"apps\", \"assets\", \"asset\", \"lookup\",\n];\n\n/** Authorization tier for a catalog entry: \"auto\" (read — runs directly)\n    or \"confirm\" (stops at the typed confirm). The groups convention wins\n    when present — a command whose MCP tags include agent-auto or\n    agent-confirm is classified by the owner (describe() forwards groups\n    into tags once the flowlang side does; empty today, forward-compatible).\n    Fallback: the read-prefix heuristic on the command name. */\nexport function gateFor(name, entry) {\n  const tags = entry?.tags ?? [];\n  if (tags.includes(\"agent-confirm\")) return \"confirm\";\n  if (tags.includes(\"agent-auto\")) return \"auto\";\n  const cmd = parseToolName(name)?.cmd ?? name;\n  return READ_TOOL_PREFIXES.some((p) => cmd === p || cmd.startsWith(p + \"_\"))\n    ? \"auto\" : \"confirm\";\n}\n\n/** Light client-side check of args against a catalog inputSchema — enough\n    for the model to self-correct: missing required keys and gross type\n    mismatches, reported together. Null = fine. */\nexport function schemaComplaints(schema, args) {\n  if (!schema || typeof args !== \"object\" || args === null) return null;\n  const problems = [];\n  for (const req of schema.required ?? []) {\n    if (!(req in args)) problems.push(`missing required argument '${req}'`);\n  }\n  const kinds = { string: \"string\", number: \"number\", boolean: \"boolean\" };\n  for (const [k, v] of Object.entries(args)) {\n    const spec = schema.properties?.[k];\n    if (!spec) continue;\n    if (spec.type in kinds && typeof v !== kinds[spec.type]) {\n      problems.push(`argument '${k}' should be a ${spec.type}`);\n    } else if (spec.type === \"object\" && (typeof v !== \"object\" || v === null || Array.isArray(v))) {\n      problems.push(`argument '${k}' should be an object`);\n    } else if (spec.type === \"array\" && !Array.isArray(v)) {\n      problems.push(`argument '${k}' should be an array`);\n    }\n  }\n  return problems.length ? problems.join(\"; \") : null;\n}\n\n/** Search the catalog for find_tools: token match on name + description. */\nexport function searchCatalog(catalog, query, limit = 12) {\n  const tokens = (query ?? \"\").toLowerCase().split(/[^a-z0-9_]+/).filter(Boolean);\n  if (!tokens.length) return [];\n  const scored = [];\n  for (const t of catalog) {\n    const hay = (t.name + \" \" + (t.description ?? \"\")).toLowerCase();\n    const hits = tokens.filter((tok) => hay.includes(tok)).length;\n    if (hits) scored.push([hits, t]);\n  }\n  scored.sort((a, b) => b[0] - a[0]);\n  return scored.slice(0, limit).map(([, t]) => ({\n    name: t.name,\n    gate: gateFor(t.name, t),\n    summary: (t.summary ?? t.description ?? \"\").slice(0, 180),\n  }));\n}\n\n/** \"lib-ctl-cmd\" -> {lib, ctl, cmd} (ctl may itself contain dashes). */\nexport function parseToolName(name) {\n  const parts = name.split(\"-\");\n  if (parts.length < 3) return null;\n  return { lib: parts[0], ctl: parts.slice(1, -1).join(\"-\"), cmd: parts.at(-1) };\n}\n\n/**\n * One conversational turn, driving chat_llm until it answers in text.\n * execTool(call) is the session's: it renders the cell, gates mutating\n * commands behind the typed confirm, runs it, and resolves the tool-result\n * CONTENT string (or a denial note). Returns the final text.\n */\nexport async function chatTurn({ messages, tools, execTool, onRound }) {\n  for (let round = 0; round < MAX_ROUNDS; round++) {\n    const r = await store.chatLlm(messages, tools);\n    if (r.status !== \"ok\") throw new Error(r.msg ?? \"chat_llm failed\");\n    const d = r.data ?? {};\n    if (d.kind !== \"tool_calls\") {\n      return d.content ?? \"\";\n    }\n    messages.push(d.assistant_message);\n    onRound?.(d.tool_calls);\n    for (const call of d.tool_calls) {\n      const content = await execTool(call);\n      messages.push({ role: \"tool\", tool_call_id: call.id, content });\n    }\n  }\n  return \"(stopped: the agent used its whole tool budget without a final answer)\";\n}\n","old":"// agentloop.js — the client side of \"chat with the code\", the way the\n// owner called it (2026-07-25): skip control_query AND tool_loop, call\n// agent.llm.chat_llm directly. This module owns what control_query used to\n// do server-side — the system prompt, the context fences, the history —\n// plus the tool loop the notebook can actually SHOW: chat_llm returns\n// normalized tool_calls; the session renders each as a cell and gates\n// mutating ones behind its typed confirm before the loop continues.\n//\n// chat_llm contract (agent.llm, JSONObject envelope — payload under data):\n//   in:  messages (system/user/assistant/tool), tools (OpenAI function defs)\n//   out: {kind:\"text\", content, assistant_message}\n//      | {kind:\"tool_calls\", tool_calls:[{id,name,arguments(JSON string)}],\n//         assistant_message}   — assistant_message goes back on the list.\n// MCP tools (agent.plugin.list_tools) arrive as {name:\"lib-ctl-cmd\",\n// description, inputSchema} — inputSchema is a real JSON Schema.\n\nimport { store } from \"./store.js\";\n\n// Generous: the autonomous developer workflow legitimately spends rounds\n// on compile-debug cycles.\nexport const MAX_ROUNDS = 10;\n\n// The system prompt lives client-side now — the cost of skipping\n// control_query, and the point: the bench knows what the model is looking\n// at and how its replies get used. The platform-knowledge sections are\n// the owner's, folded in from agent.llm.tool_loop's prompt (his call:\n// without them the model can't write ndata-correct code or reason about\n// the platform). Model-agnostic (Qwen or Claude).\nexport const SYSTEM_PROMPT = `You are Newbound, a self-organizing collection of code, data, and opinions that is working with the humans to improve their way of life. Right now you are speaking through the session notebook of Bench, the Newbound IDE — the user sees every tool call you make as a notebook cell.\n\nYour capabilities are defined by the Commands in your flowlang DataStore. The datastore is organized into libraries. Libraries contain Assets and Controls. Controls contain UI elements (html/css/js/data/3d) and back-end components (Command/Event/Timer). The \\`app\\`, \\`dev\\`, \\`peer\\`, and \\`security\\` libraries are the core Newbound libraries. They are great reference information because they define how everything in Newbound is done.\n\nThe ndata Crate: The Universal Data Layer\nAdherence to the principles of the underlying ndata crate is crucial for writing correct and performant rust code. All data within the Flowlang runtime, between the front-end and back-end, and within Commands is managed by ndata.\n  Core Concept: ndata provides globally shared, thread-safe, JSON-like dynamic data structures: DataObject (a HashMap), DataArray (a Vec), and DataBytes (a byte stream). These are handles to heap-stored data.\n  CRITICAL RULE — NO WRAPPING: ndata types are already internally thread-safe. NEVER wrap them in Arc, Rc, Mutex, RwLock, or any other synchronization primitive.\n  Handles: .clone() is cheap (a new handle, refcount bump). When you pass a handle to a mutator like parent.put_object(\"key\", child), the child handle is MOVED — clone first (child.clone()) if you need it afterward.\n  Best practices: prefer typed methods (get_string, put_object) over generic ones; DataObject::new() / DataArray::new() for new instances; deep_copy() for an independent duplicate.\n  SUPER IMPORTANT: ndata does NOT use the wrap-everything-in-Result pattern — get_object/get_int/get_string return directly or PANIC. Use try_get_XXX variants or check has(&key) first when absence is possible.\n\nThe flowlang Crate: The Runtime Engine\n  Data lives under ./data/<lib>/, sharded by four-character chunks of the record id: ./data/lib/[0..4]/[4..8]/[8..12]/[12..16]/fullid. A library's control index is data/<lib>/cont/rols/____/____/controls; its metadata is data/<lib>/meta.json; its assets are data/<lib>/_ASSETS/. It is UNWISE to edit these files directly — malformed data can crash Newbound; use platform commands, which are journaled.\n  STORAGE: use flowlang::datastore::DataStore; let store = DataStore::new(); let ctl = store.get_data(&lib, &id).get_object(\"data\");\n  GLOBALS: DataStore::globals().get_object(\"system\").get_object(\"libraries\")\n  EXECUTION: use flowlang::command::Command; let cmd = Command::lookup(lib, control, command); let result = cmd.execute(arguments);\n\nNewbound Web Apps\n  A Newbound Application is a Control with the same name as its library that has been published. Applications serve at /appname/index.html and their commands at /appname/cmdname?param=value. The http_port is in config.properties.\n\nWriting Commands in Rust\n  Default to Rust for new commands unless Python is explicitly requested or needed for an AI/data-science library.\n  CRITICAL — code_body IS A FUNCTION BODY, NOT A FUNCTION: for upsert_command/patch_command_body, your code_body is placed inside a generated fn: pub fn <cmd>(<params>) -> <ReturnType> { <your code_body> }. Never write a fn signature or outer braces. Declared params are in scope as typed variables (String, Integer→i64, Float→f64, Boolean→bool, JSONObject→DataObject, JSONArray→DataArray). The FINAL EXPRESSION must match the declared return_type. Put use statements in \\`imports\\`; DataObject is imported automatically.\n  Other Newbound commands are callable natively: let api = crate::api::new(); api.lib.ctl.cmd(param1, param2); or use crate::lib::ctl::cmd::cmd;\n\nWriting Commands in Python\n  Python is first-class: arguments arrive as NDataObject/NDataArray/NDataBytes wrappers sharing memory with Rust — manipulate them directly. Returning native Python types auto-converts.\n\nReturn Type Semantics\n  A command's response JSON always has \"status\" (\"ok\"/\"err\"). A returned String lands in \"msg\"; other types land in \"data\"; FLAT means the returned DataObject IS the outermost response. Choose return_type to match the final expression.\n\nFront-End Control Development (HTML/CSS/JS)\n  Platform controls keep html, css, and js as separate facets. A back-end command foo is exposed to that control's js as send_foo(args..., function(result){...}) — result.msg for Strings, result.data otherwise. jQuery 3.6.1 is loaded. Standard boilerplate: var me = this; var ME = $('#' + me.UUID)[0]; me.ready = function(){...}. Sub-controls mount declaratively (<div class='data-control' data-control='lib:ctl:{...}'>) or via installControl(...).\n  EXCEPTION: the \\`bench\\` library's own controls are ES modules (export function init(host, props), no jQuery, imports from ../../assets/) — match the style of whatever control you are editing.\n\nWHAT YOU SEE\n- Current code arrives in fenced blocks labeled lib:ctl.facet (facets) or lib:ctl:command.lang (command bodies; the first line may be a synthesized signature comment). A flow body arrives as JSON labeled lib:ctl:command.flow.\n- \"Recent notebook activity\" lists commands run in this session, with OUTPUTS TRUNCATED. Never conclude that something is absent because you don't see it in a truncated result — rerun the command via a tool when you need the full output.\n\nHOW TO ANSWER\n- Be concise and concrete. If you are unsure, say so plainly rather than inventing platform behavior.\n- To propose a facet change, reply with ONE complete fenced block per changed facet, tagged html, css, or js. The user can apply such a block as a journaled whole-facet patch in one click — a partial snippet cannot be applied, so always return the full facet.\n- Command-body suggestions: a fenced block tagged rust or python containing the full body.`;\n\n// The tool model (owner's design, 2026-07-25): DISCOVERY is always on,\n// ATTACHMENT is a context optimization, AUTHORIZATION happens per call.\nexport const TOOLS_PROMPT = `\n\nTOOLS\nNewbound commands are your tools, named library-control-command (e.g. agent-code-read_command). Call them through the tool-calling interface — never by describing a call in text. Every call you make appears as a cell in the user's notebook.\n\n- A core set is attached with full schemas. EVERY other platform command is also available: use find_tools(query) to discover commands for a task, describe_tool(name) for a full schema, and call_command(name, args) to invoke anything not directly attached. Never claim a capability is missing until find_tools says so.\n- Authorization is per call, not per tool: read-only commands run immediately; anything mutating or potentially destructive stops for the user's typed confirmation first — so do not hesitate to REQUEST a powerful command when the task needs it; the user decides at the moment of use. A denial is the user's decision: respect it and continue without that action.\n- Read before you write. For edits prefer the journaled, revertible commands: agent-code-patch_control_facet for UI facets, agent-code-patch_command_body / upsert_command for command bodies, agent-code-read_flow_body / write_flow_body for flow commands.\n\nThe Autonomous Developer Workflow\nWhen asked to build a feature, process data, or perform a task requiring code, act as an autonomous developer:\n1. READ: understand existing code first (agent-code-read_command, agent-code-read_control_facet).\n2. WRITE: agent-code-upsert_command or agent-code-patch_command_body — the system generates the source, compiles, and hot-swaps on success.\n3. DEBUG: if compilation fails, do not apologize — read the compiler error from the tool result, patch, and compile again until it succeeds.\n4. EXECUTE: agent-code-invoke_command to run and verify.\n5. RESPOND: give your final answer only after execution succeeds, summarizing what you built and the results.\n6. OBSTACLES: if a step stays blocked after 2-3 DIFFERENT approaches, stop and report what worked, what didn't, and the exact blocking error. A clear partial result beats a burned budget.\n\nScratchpad Execution\nFor complex logical, mathematical, or data-parsing questions, write and run a temporary Rust script with agent-code-evaluate_rust instead of guessing. Your \\`code\\` is compiled inside a function returning DataObject — the FINAL EXPRESSION must be a DataObject:\n    let mut out = DataObject::new();\n    out.put_int(\"result\", my_value as i64);\n    out\nPut use statements in \\`imports\\` (DataObject is automatic).\n\nAdvanced Native Orchestration\nYou can invoke any Newbound command from WITHIN an evaluate_rust script via let api = crate::api::new(); — chain tools, process large data, or run multi-step pipelines in one script instead of many chat rounds. Pass large intermediate values through DataStore::globals().get_object(\"AGENT_MEMORY\") or the runtime library rather than through this conversation.`;\n\n// Newbound meta types -> Rust types, for synthesized signatures (ported\n// from control_query's lookup_rust_api_data_type).\nconst RUST_TYPE = {\n  FLAT: \"DataObject\", JSONObject: \"DataObject\", JSONArray: \"DataArray\",\n  InputStream: \"DataBytes\", float: \"f64\", Integer: \"i64\", Boolean: \"bool\",\n  Any: \"Data\", NULL: \"DNull\",\n};\nconst rustType = (t) => RUST_TYPE[t] ?? \"String\";\n\n/** The context prompt, ported from control_query: an editing preamble plus\n    one fence per included piece, tagged the way the backend prompt always\n    tagged them. `included` = chatctx snapshot entries the user checked. */\nexport function contextBlock(included) {\n  const merged = {};\n  for (const p of included) Object.assign(merged, p.fields);\n  const { lib, ctl } = merged;\n  if (!lib && !ctl) return \"\";\n  let out = `We are editing the ${ctl} control in the ${lib} library.`;\n  for (const facet of [\"html\", \"css\", \"js\"]) {\n    const v = (merged[facet] ?? \"\").trim?.() ?? \"\";\n    if (v) out += `\\n\\n\\`\\`\\`${lib}:${ctl}.${facet}\\n${v}\\n\\`\\`\\``;\n  }\n  if (merged.code && merged.cmdname) {\n    const lang = merged.lang === \"rust\" ? \"rs\" : merged.lang === \"python\" ? \"py\" : merged.lang;\n    let signature = \"\";\n    if (merged.cmdparams) {\n      const ps = merged.cmdparams.map((p) =>\n        lang === \"rs\" ? `${p.name}: ${rustType(p.type)}` : p.name).join(\", \");\n      signature = lang === \"rs\"\n        ? `// fn ${merged.cmdname}(${ps}) -> ${rustType(merged.cmdreturn ?? \"String\")}\\n`\n        : `# def ${merged.cmdname}(${ps}):\\n`;\n    }\n    out += `\\n\\n\\`\\`\\`${lib}:${ctl}:${merged.cmdname}.${lang}\\n${signature}${merged.code.trim()}\\n\\`\\`\\``;\n  }\n  if (merged.flow && merged.cmdname) {\n    out += `\\n\\n\\`\\`\\`${lib}:${ctl}:${merged.cmdname}.flow\\n${merged.flow}\\n\\`\\`\\``;\n  }\n  return out;\n}\n\nexport const clamp = (s, n = 2000) =>\n  s.length <= n ? s : s.slice(0, n) + `\\n…[${s.length - n} chars clipped]`;\n\n/** MCP tool descriptors -> OpenAI function defs, enabled names only. */\nexport function toolDefs(mcpTools, enabledNames) {\n  const on = new Set(enabledNames);\n  return (mcpTools ?? [])\n    .filter((t) => on.has(t.name))\n    .map((t) => ({\n      type: \"function\",\n      function: {\n        name: t.name,\n        description: t.description ?? \"\",\n        parameters: t.inputSchema ?? { type: \"object\", properties: {} },\n      },\n    }));\n}\n\n// ── the tool model: defaults, meta-tools, gating ──────────\n\n// Attached-by-default: the read family (auto-run) plus the journaled\n// workhorses (each call confirm-gated). Everything else stays reachable\n// through call_command. The picker stores per-user add/remove overrides.\nexport const DEFAULT_TOOLS = [\n  \"agent-code-read_control_facet\", \"agent-code-read_command\",\n  \"agent-code-read_flow_body\", \"agent-code-list_control_patches\",\n  \"agent-code-list_libraries\", \"agent-code-list_controls\",\n  \"agent-code-list_commands\", \"agent-code-list_assets\",\n  \"agent-code-search_commands\",\n  \"agent-code-patch_control_facet\", \"agent-code-patch_command_body\",\n  \"agent-code-upsert_command\", \"agent-code-write_flow_body\",\n  \"agent-code-invoke_command\", \"agent-code-evaluate_rust\",\n];\n\n/** The always-attached meta-tools: discovery + the gateway. */\nexport const META_TOOL_DEFS = [\n  {\n    type: \"function\",\n    function: {\n      name: \"find_tools\",\n      description: \"Search ALL platform commands (not just attached tools) by keywords against their names and descriptions. Returns matches with their authorization tier. Use this before concluding a capability is missing.\",\n      parameters: { type: \"object\",\n        properties: { query: { type: \"string\", description: \"Keywords, e.g. 'delete asset' or 'rust eval'\" } },\n        required: [\"query\"] },\n    },\n  },\n  {\n    type: \"function\",\n    function: {\n      name: \"describe_tool\",\n      description: \"Full descriptor (description + JSON Schema of arguments) for one platform command by its lib-ctl-cmd name. Use before call_command when unsure of the arguments.\",\n      parameters: { type: \"object\",\n        properties: { name: { type: \"string\", description: \"Tool name, e.g. agent-code-delete_asset\" } },\n        required: [\"name\"] },\n    },\n  },\n  {\n    type: \"function\",\n    function: {\n      name: \"call_command\",\n      description: \"Invoke ANY platform command by its lib-ctl-cmd name — attached or not. Arguments are validated against the command's schema; mutating commands stop for the user's confirmation like every other call.\",\n      parameters: { type: \"object\",\n        properties: {\n          name: { type: \"string\", description: \"Tool name, e.g. agent-code-delete_asset\" },\n          args: { type: \"object\", description: \"The command's arguments, per its schema\" },\n        },\n        required: [\"name\", \"args\"] },\n    },\n  },\n];\n\nconst READ_TOOL_PREFIXES = [\n  \"list\", \"read\", \"get\", \"search\", \"describe\", \"lib_info\", \"check\",\n  \"current\", \"info\", \"peers\", \"libs\", \"apps\", \"assets\", \"asset\", \"lookup\",\n];\n\n/** Authorization tier for a catalog entry: \"auto\" (read — runs directly)\n    or \"confirm\" (stops at the typed confirm). The groups convention wins\n    when present — a command whose MCP tags include agent-auto or\n    agent-confirm is classified by the owner (describe() forwards groups\n    into tags once the flowlang side does; empty today, forward-compatible).\n    Fallback: the read-prefix heuristic on the command name. */\nexport function gateFor(name, entry) {\n  const tags = entry?.tags ?? [];\n  if (tags.includes(\"agent-confirm\")) return \"confirm\";\n  if (tags.includes(\"agent-auto\")) return \"auto\";\n  const cmd = parseToolName(name)?.cmd ?? name;\n  return READ_TOOL_PREFIXES.some((p) => cmd === p || cmd.startsWith(p + \"_\"))\n    ? \"auto\" : \"confirm\";\n}\n\n/** Light client-side check of args against a catalog inputSchema — enough\n    for the model to self-correct: missing required keys and gross type\n    mismatches, reported together. Null = fine. */\nexport function schemaComplaints(schema, args) {\n  if (!schema || typeof args !== \"object\" || args === null) return null;\n  const problems = [];\n  for (const req of schema.required ?? []) {\n    if (!(req in args)) problems.push(`missing required argument '${req}'`);\n  }\n  const kinds = { string: \"string\", number: \"number\", boolean: \"boolean\" };\n  for (const [k, v] of Object.entries(args)) {\n    const spec = schema.properties?.[k];\n    if (!spec) continue;\n    if (spec.type in kinds && typeof v !== kinds[spec.type]) {\n      problems.push(`argument '${k}' should be a ${spec.type}`);\n    } else if (spec.type === \"object\" && (typeof v !== \"object\" || v === null || Array.isArray(v))) {\n      problems.push(`argument '${k}' should be an object`);\n    } else if (spec.type === \"array\" && !Array.isArray(v)) {\n      problems.push(`argument '${k}' should be an array`);\n    }\n  }\n  return problems.length ? problems.join(\"; \") : null;\n}\n\n/** Search the catalog for find_tools: token match on name + description. */\nexport function searchCatalog(catalog, query, limit = 12) {\n  const tokens = (query ?? \"\").toLowerCase().split(/[^a-z0-9_]+/).filter(Boolean);\n  if (!tokens.length) return [];\n  const scored = [];\n  for (const t of catalog) {\n    const hay = (t.name + \" \" + (t.description ?? \"\")).toLowerCase();\n    const hits = tokens.filter((tok) => hay.includes(tok)).length;\n    if (hits) scored.push([hits, t]);\n  }\n  scored.sort((a, b) => b[0] - a[0]);\n  return scored.slice(0, limit).map(([, t]) => ({\n    name: t.name,\n    gate: gateFor(t.name, t),\n    summary: (t.summary ?? t.description ?? \"\").slice(0, 180),\n  }));\n}\n\n/** \"lib-ctl-cmd\" -> {lib, ctl, cmd} (ctl may itself contain dashes). */\nexport function parseToolName(name) {\n  const parts = name.split(\"-\");\n  if (parts.length < 3) return null;\n  return { lib: parts[0], ctl: parts.slice(1, -1).join(\"-\"), cmd: parts.at(-1) };\n}\n\n/**\n * One conversational turn, driving chat_llm until it answers in text.\n * execTool(call) is the session's: it renders the cell, gates mutating\n * commands behind the typed confirm, runs it, and resolves the tool-result\n * CONTENT string (or a denial note). Returns the final text.\n */\nexport async function chatTurn({ messages, tools, execTool, onRound }) {\n  for (let round = 0; round < MAX_ROUNDS; round++) {\n    const r = await store.chatLlm(messages, tools);\n    if (r.status !== \"ok\") throw new Error(r.msg ?? \"chat_llm failed\");\n    const d = r.data ?? {};\n    if (d.kind !== \"tool_calls\") {\n      return d.content ?? \"\";\n    }\n    messages.push(d.assistant_message);\n    onRound?.(d.tool_calls);\n    for (const call of d.tool_calls) {\n      const content = await execTool(call);\n      messages.push({ role: \"tool\", tool_call_id: call.id, content });\n    }\n  }\n  return \"(stopped: the agent used its whole tool budget without a final answer)\";\n}\n","patch_id":"p6","time":1785416364957},{"author":"claude","facet":"js","label":"tags not groups: gate doc names the tags field","new":"/** Authorization tier for a catalog entry: \"auto\" (read — runs directly)\n    or \"confirm\" (stops at the typed confirm). The TAGS convention wins\n    when present — a command whose MCP tags include agent-auto or\n    agent-confirm is classified by the owner (describe() forwards the\n    record's `tags` field once the flowlang side does; empty today,\n    forward-compatible). `groups` is SECURITY groups and is never read\n    for gating. Fallback: the read-prefix heuristic on the command name. */","old":"/** Authorization tier for a catalog entry: \"auto\" (read — runs directly)\n    or \"confirm\" (stops at the typed confirm). The groups convention wins\n    when present — a command whose MCP tags include agent-auto or\n    agent-confirm is classified by the owner (describe() forwards groups\n    into tags once the flowlang side does; empty today, forward-compatible).\n    Fallback: the read-prefix heuristic on the command name. */","patch_id":"p7","time":1785503548993},{"author":"claude","facet":"js","label":"memory v0: memory prompt section","new":"LONG-TERM MEMORY (the kb library — docs/memory.md)\n- The kb library is your curated long-term memory. Each control is a knowledge DOMAIN; its \\`memory\\` facet is a JSON array of entries {claim, detail?, tags, source?, confidence, time}. When memory exists, your context includes a memory:index fence listing the domains.\n- RECALL: when a task touches a domain, read its entries — dev-code-read_control_facet {lib:\"kb\", ctl:\"<domain>\", facet:\"memory\"} (a read; runs without confirmation). Entries carry POINTERS ({lib, ctl, facet, hash} or {doc}) rather than copies: read the referent for current truth, and if the referent's hash no longer matches source.hash, the entry may be STALE — say so.\n- REMEMBER: when the user asks you to remember something, or you learn a durable non-obvious fact worth keeping, propose dev-code-patch_control_facet {lib:\"kb\", ctl:\"<domain>\", facet:\"memory\", ...} appending one entry, label \"memory: <short claim>\". Every memory write stops at the user's confirm. Create a new domain (dev-code-add_control on kb) only when no existing domain fits.\n- Keep entries ATOMIC (one claim each), tag them with the libs/controls they concern, and never copy what canon already says — record the why/gotcha/technique, with a pointer.\n\nHOW TO ANSWER\n- Be concise and concrete. If you are unsure, say so plainly rather than inventing platform behavior.","old":"HOW TO ANSWER\n- Be concise and concrete. If you are unsure, say so plainly rather than inventing platform behavior.","patch_id":"p8","time":1785504785280},{"author":"claude","facet":"js","label":"memory v0: memory context fence","new":"export function contextBlock(included) {\n  const merged = {};\n  for (const p of included) Object.assign(merged, p.fields);\n  const { lib, ctl } = merged;\n  let out = \"\";\n  if (lib || ctl) {\n    out = `We are editing the ${ctl} control in the ${lib} library.`;\n    for (const facet of [\"html\", \"css\", \"js\"]) {\n      const v = (merged[facet] ?? \"\").trim?.() ?? \"\";\n      if (v) out += `\\n\\n\\`\\`\\`${lib}:${ctl}.${facet}\\n${v}\\n\\`\\`\\``;\n    }\n    if (merged.code && merged.cmdname) {\n      const lang = merged.lang === \"rust\" ? \"rs\" : merged.lang === \"python\" ? \"py\" : merged.lang;\n      let signature = \"\";\n      if (merged.cmdparams) {\n        const ps = merged.cmdparams.map((p) =>\n          lang === \"rs\" ? `${p.name}: ${rustType(p.type)}` : p.name).join(\", \");\n        signature = lang === \"rs\"\n          ? `// fn ${merged.cmdname}(${ps}) -> ${rustType(merged.cmdreturn ?? \"String\")}\\n`\n          : `# def ${merged.cmdname}(${ps}):\\n`;\n      }\n      out += `\\n\\n\\`\\`\\`${lib}:${ctl}:${merged.cmdname}.${lang}\\n${signature}${merged.code.trim()}\\n\\`\\`\\``;\n    }\n    if (merged.flow && merged.cmdname) {\n      out += `\\n\\n\\`\\`\\`${lib}:${ctl}:${merged.cmdname}.flow\\n${merged.flow}\\n\\`\\`\\``;\n    }\n  }\n  if (merged.memory) {\n    out += (out ? \"\\n\\n\" : \"\") + `\\`\\`\\`memory:index\\n${merged.memory.trim()}\\n\\`\\`\\``;\n  }\n  return out;\n}","old":"export function contextBlock(included) {\n  const merged = {};\n  for (const p of included) Object.assign(merged, p.fields);\n  const { lib, ctl } = merged;\n  if (!lib && !ctl) return \"\";\n  let out = `We are editing the ${ctl} control in the ${lib} library.`;\n  for (const facet of [\"html\", \"css\", \"js\"]) {\n    const v = (merged[facet] ?? \"\").trim?.() ?? \"\";\n    if (v) out += `\\n\\n\\`\\`\\`${lib}:${ctl}.${facet}\\n${v}\\n\\`\\`\\``;\n  }\n  if (merged.code && merged.cmdname) {\n    const lang = merged.lang === \"rust\" ? \"rs\" : merged.lang === \"python\" ? \"py\" : merged.lang;\n    let signature = \"\";\n    if (merged.cmdparams) {\n      const ps = merged.cmdparams.map((p) =>\n        lang === \"rs\" ? `${p.name}: ${rustType(p.type)}` : p.name).join(\", \");\n      signature = lang === \"rs\"\n        ? `// fn ${merged.cmdname}(${ps}) -> ${rustType(merged.cmdreturn ?? \"String\")}\\n`\n        : `# def ${merged.cmdname}(${ps}):\\n`;\n    }\n    out += `\\n\\n\\`\\`\\`${lib}:${ctl}:${merged.cmdname}.${lang}\\n${signature}${merged.code.trim()}\\n\\`\\`\\``;\n  }\n  if (merged.flow && merged.cmdname) {\n    out += `\\n\\n\\`\\`\\`${lib}:${ctl}:${merged.cmdname}.flow\\n${merged.flow}\\n\\`\\`\\``;\n  }\n  return out;\n}","patch_id":"p9","time":1785504785283},{"author":"claude","facet":"js","label":"memory: prompt routes by memory type","new":"LONG-TERM MEMORY (the kb + episodic libraries — docs/memory.md)","old":"LONG-TERM MEMORY (the kb library — docs/memory.md)","patch_id":"p10","time":1785507725785},{"author":"claude","facet":"js","label":"memory: prompt routes by memory type","new":"- Memory is split by TYPE: kb is curated knowledge (durable facts and techniques, by knowledge DOMAIN); episodic is dated history (lessons and decisions; controls are month buckets like m2026-07). Each memory control's","old":"- The kb library is your curated long-term memory. Each control is a knowledge DOMAIN; its","patch_id":"p11","time":1785507725797},{"author":"claude","facet":"js","label":"memory: prompt routes by memory type","new":"dev-code-read_control_facet {lib:\"kb\"|\"episodic\", ctl:\"<domain>\", facet:\"memory\"} (a read; runs without confirmation)","old":"dev-code-read_control_facet {lib:\"kb\", ctl:\"<domain>\", facet:\"memory\"} (a read; runs without confirmation)","patch_id":"p12","time":1785507725801},{"author":"claude","facet":"js","label":"memory: prompt routes by memory type","new":"route by type — durable knowledge to the fitting kb domain, session events and lessons to episodic's current month — and propose dev-code-patch_control_facet {lib, ctl:\"<domain>\", facet:\"memory\", ...} appending one entry, label \"memory: <short claim>\". Every memory write stops at the user's confirm. Create a new domain (dev-code-add_control) only when none fits; promoting an episodic lesson into kb is a deliberate copy — say so when you do it.","old":"propose dev-code-patch_control_facet {lib:\"kb\", ctl:\"<domain>\", facet:\"memory\", ...} appending one entry, label \"memory: <short claim>\". Every memory write stops at the user's confirm. Create a new domain (dev-code-add_control on kb) only when no existing domain fits.","patch_id":"p13","time":1785507725805},{"author":"claude","facet":"js","label":"memory: prompt back to one library","new":"LONG-TERM MEMORY (the kb library — docs/memory.md)","old":"LONG-TERM MEMORY (the kb + episodic libraries — docs/memory.md)","patch_id":"p14","time":1785508834199},{"author":"claude","facet":"js","label":"memory: prompt back to one library","new":"- The kb library is your long-term memory. Controls are knowledge DOMAINS — reference domains (platform-api, workflow, …) plus dated MONTH BUCKETS (m2026-07) for episodic history; the type rides naming and tags, not structure. Each memory control's","old":"- Memory is split by TYPE: kb is curated knowledge (durable facts and techniques, by knowledge DOMAIN); episodic is dated history (lessons and decisions; controls are month buckets like m2026-07). Each memory control's","patch_id":"p15","time":1785508834203},{"author":"claude","facet":"js","label":"memory: prompt back to one library","new":"dev-code-read_control_facet {lib:\"kb\", ctl:\"<domain>\", facet:\"memory\"} (a read; runs without confirmation)","old":"dev-code-read_control_facet {lib:\"kb\"|\"episodic\", ctl:\"<domain>\", facet:\"memory\"} (a read; runs without confirmation)","patch_id":"p16","time":1785508834207},{"author":"claude","facet":"js","label":"memory: prompt back to one library","new":"route by type — durable knowledge to the fitting reference domain, session events and lessons to the current month bucket — and propose dev-code-patch_control_facet {lib:\"kb\", ctl:\"<domain>\", facet:\"memory\", ...} appending one entry, label \"memory: <short claim>\". Every memory write stops at the user's confirm. Create a new domain (dev-code-add_control) only when none fits; promoting a month-bucket lesson into a reference domain is a deliberate copy — say so when you do it.","old":"route by type — durable knowledge to the fitting kb domain, session events and lessons to episodic's current month — and propose dev-code-patch_control_facet {lib, ctl:\"<domain>\", facet:\"memory\", ...} appending one entry, label \"memory: <short claim>\". Every memory write stops at the user's confirm. Create a new domain (dev-code-add_control) only when none fits; promoting an episodic lesson into kb is a deliberate copy — say so when you do it.","patch_id":"p17","time":1785508834211},{"author":"claude","facet":"js","label":"memory: attach remember","new":"  \"dev-code-invoke_command\", \"dev-code-evaluate_rust\",\n  \"dev-code-remember\",\n];","old":"  \"dev-code-invoke_command\", \"dev-code-evaluate_rust\",\n];","patch_id":"p18","time":1785510437657},{"author":"claude","facet":"js","label":"memory: stale marks in RECALL","new":"read the referent for current truth, and if the referent's hash no longer matches source.hash, the entry may be STALE — say so. The index fence marks domains carrying such entries (\"N stale?\").","old":"read the referent for current truth, and if the referent's hash no longer matches source.hash, the entry may be STALE — say so.","patch_id":"p19","time":1785510437676},{"author":"claude","facet":"js","label":"memory: REMEMBER via the command","new":"route by type — durable knowledge to the fitting reference domain, session events and lessons to the current month bucket — and call dev-code-remember {lib:\"kb\", domain:\"<domain>\", entry:{claim, detail?, tags, source?, confidence?}}. ONE atomic claim per call. The command validates the entry, stamps \\`time\\` for you, auto-stamps a facet-pointer source's hash ({lib, ctl, facet} — omit the hash), refuses to touch a malformed facet, and journals the write. Every memory write stops at the user's confirm. Create a new domain (dev-code-add_control) only when none fits; promoting a month-bucket lesson into a reference domain is a deliberate copy — say so when you do it.","old":"route by type — durable knowledge to the fitting reference domain, session events and lessons to the current month bucket — and propose dev-code-patch_control_facet {lib:\"kb\", ctl:\"<domain>\", facet:\"memory\", ...} appending one entry, label \"memory: <short claim>\". Every memory write stops at the user's confirm. Create a new domain (dev-code-add_control) only when none fits; promoting a month-bucket lesson into a reference domain is a deliberate copy — say so when you do it.","patch_id":"p20","time":1785510437702},{"author":"claude","facet":"js","label":"prompting: shell over the store-resident core (corePrompt)","new":"// agentloop.js — the client side of \"chat with the code\", the way the\n// owner called it (2026-07-25): skip control_query AND tool_loop, call\n// agent.llm.chat_llm directly. This module owns what control_query used to\n// do server-side — the system prompt, the context fences, the history —\n// plus the tool loop the notebook can actually SHOW: chat_llm returns\n// normalized tool_calls; the session renders each as a cell and gates\n// mutating ones behind its typed confirm before the loop continues.\n//\n// chat_llm contract (agent.llm, JSONObject envelope — payload under data):\n//   in:  messages (system/user/assistant/tool), tools (OpenAI function defs)\n//   out: {kind:\"text\", content, assistant_message}\n//      | {kind:\"tool_calls\", tool_calls:[{id,name,arguments(JSON string)}],\n//         assistant_message}   — assistant_message goes back on the list.\n// MCP tools (agent.plugin.list_tools) arrive as {name:\"lib-ctl-cmd\",\n// description, inputSchema} — inputSchema is a real JSON Schema.\n\nimport { store } from \"./store.js\";\n\n// Generous: the autonomous developer workflow legitimately spends rounds\n// on compile-debug cycles.\nexport const MAX_ROUNDS = 10;\n\n// The system prompt lives client-side now — the cost of skipping\n// control_query, and the point: the bench knows what the model is looking\n// at and how its replies get used. The platform-knowledge sections are\n// the owner's, folded in from agent.llm.tool_loop's prompt (his call:\n// without them the model can't write ndata-correct code or reason about\n// the platform). Model-agnostic (Qwen or Claude).\n// The platform-knowledge CORE lives in the store (dev.prompts `prompt`\n// facet — docs/prompting.md): ONE journaled curriculum shared with the\n// agent app's tool_loop. SYSTEM_PROMPT here is the notebook's SHELL —\n// venue, fences, memory, answer style; session assembles core + shell +\n// TOOLS_PROMPT + the owner addendum at ask time.\nexport const SYSTEM_PROMPT = `Right now you are speaking through the session notebook of Bench, the Newbound IDE — the user sees every tool call you make as a notebook cell.\n\nWHAT YOU SEE\n- Current code arrives in fenced blocks labeled lib:ctl.facet (facets) or lib:ctl:command.lang (command bodies; the first line may be a synthesized signature comment). A flow body arrives as JSON labeled lib:ctl:command.flow.\n- \"Recent notebook activity\" lists commands run in this session, with OUTPUTS TRUNCATED. Never conclude that something is absent because you don't see it in a truncated result — rerun the command via a tool when you need the full output.\n\nLONG-TERM MEMORY (the kb library — docs/memory.md)\n- The kb library is your long-term memory. Controls are knowledge DOMAINS — reference domains (platform-api, workflow, …) plus dated MONTH BUCKETS (m2026-07) for episodic history; the type rides naming and tags, not structure. Each memory control's \\`memory\\` facet is a JSON array of entries {claim, detail?, tags, source?, confidence, time}. When memory exists, your context includes a memory:index fence listing the domains.\n- RECALL: when a task touches a domain, read its entries — dev-code-read_control_facet {lib:\"kb\", ctl:\"<domain>\", facet:\"memory\"} (a read; runs without confirmation). Entries carry POINTERS ({lib, ctl, facet, hash} or {doc}) rather than copies: read the referent for current truth, and if the referent's hash no longer matches source.hash, the entry may be STALE — say so. The index fence marks domains carrying such entries (\"N stale?\").\n- REMEMBER: when the user asks you to remember something, or you learn a durable non-obvious fact worth keeping, route by type — durable knowledge to the fitting reference domain, session events and lessons to the current month bucket — and call dev-code-remember {lib:\"kb\", domain:\"<domain>\", entry:{claim, detail?, tags, source?, confidence?}}. ONE atomic claim per call. The command validates the entry, stamps \\`time\\` for you, auto-stamps a facet-pointer source's hash ({lib, ctl, facet} — omit the hash), refuses to touch a malformed facet, and journals the write. Every memory write stops at the user's confirm. Create a new domain (dev-code-add_control) only when none fits; promoting a month-bucket lesson into a reference domain is a deliberate copy — say so when you do it.\n- Keep entries ATOMIC (one claim each), tag them with the libs/controls they concern, and never copy what canon already says — record the why/gotcha/technique, with a pointer.\n\nHOW TO ANSWER\n- Be concise and concrete. If you are unsure, say so plainly rather than inventing platform behavior.\n- To propose a facet change, reply with ONE complete fenced block per changed facet, tagged html, css, or js. The user can apply such a block as a journaled whole-facet patch in one click — a partial snippet cannot be applied, so always return the full facet.\n- Command-body suggestions: a fenced block tagged rust or python containing the full body.`;\n\n// Fetched lazily at ask time (the connection exists by then; module\n// install happens before it does). Cached per page; a failed fetch\n// clears the cache so the next ask retries, and the error is LOUD —\n// a silently thin prompt regresses the confabulation lesson.\nlet corePromise = null;\nexport function corePrompt() {\n  if (!corePromise) {\n    corePromise = (async () => {\n      const r = await store.readFacet(\"dev\", \"prompts\", \"prompt\");\n      if (r?.status === \"ok\" && r.exists && (r.source ?? \"\").trim()) {\n        return r.source.trim();\n      }\n      corePromise = null;\n      throw new Error(\"dev.prompts `prompt` facet unavailable — the platform curriculum is store-resident (docs/prompting.md)\");\n    })();\n  }\n  return corePromise;\n}\n\n// The tool model (owner's design, 2026-07-25): DISCOVERY is always on,\n// ATTACHMENT is a context optimization, AUTHORIZATION happens per call.\nexport const TOOLS_PROMPT = `\n\nTOOLS\nNewbound commands are your tools, named library-control-command (e.g. dev-code-read_command). Call them through the tool-calling interface — never by describing a call in text. Every call you make appears as a cell in the user's notebook.\n\n- A core set is attached with full schemas. EVERY other platform command is also available: use find_tools(query) to discover commands for a task, describe_tool(name) for a full schema, and call_command(name, args) to invoke anything not directly attached. Never claim a capability is missing until find_tools says so.\n- Authorization is per call, not per tool: read-only commands run immediately; anything mutating or potentially destructive stops for the user's typed confirmation first — so do not hesitate to REQUEST a powerful command when the task needs it; the user decides at the moment of use. A denial is the user's decision: respect it and continue without that action.\n- Read before you write. For edits prefer the journaled, revertible commands: dev-code-patch_control_facet for UI facets, dev-code-patch_command_body / upsert_command for command bodies, dev-code-read_flow_body / write_flow_body for flow commands.`;\n\n// Newbound meta types -> Rust types, for synthesized signatures (ported\n// from control_query's lookup_rust_api_data_type).\nconst RUST_TYPE = {\n  FLAT: \"DataObject\", JSONObject: \"DataObject\", JSONArray: \"DataArray\",\n  InputStream: \"DataBytes\", float: \"f64\", Integer: \"i64\", Boolean: \"bool\",\n  Any: \"Data\", NULL: \"DNull\",\n};\nconst rustType = (t) => RUST_TYPE[t] ?? \"String\";\n\n/** The context prompt, ported from control_query: an editing preamble plus\n    one fence per included piece, tagged the way the backend prompt always\n    tagged them. `included` = chatctx snapshot entries the user checked. */\nexport function contextBlock(included) {\n  const merged = {};\n  for (const p of included) Object.assign(merged, p.fields);\n  const { lib, ctl } = merged;\n  let out = \"\";\n  if (lib || ctl) {\n    out = `We are editing the ${ctl} control in the ${lib} library.`;\n    for (const facet of [\"html\", \"css\", \"js\"]) {\n      const v = (merged[facet] ?? \"\").trim?.() ?? \"\";\n      if (v) out += `\\n\\n\\`\\`\\`${lib}:${ctl}.${facet}\\n${v}\\n\\`\\`\\``;\n    }\n    if (merged.code && merged.cmdname) {\n      const lang = merged.lang === \"rust\" ? \"rs\" : merged.lang === \"python\" ? \"py\" : merged.lang;\n      let signature = \"\";\n      if (merged.cmdparams) {\n        const ps = merged.cmdparams.map((p) =>\n          lang === \"rs\" ? `${p.name}: ${rustType(p.type)}` : p.name).join(\", \");\n        signature = lang === \"rs\"\n          ? `// fn ${merged.cmdname}(${ps}) -> ${rustType(merged.cmdreturn ?? \"String\")}\\n`\n          : `# def ${merged.cmdname}(${ps}):\\n`;\n      }\n      out += `\\n\\n\\`\\`\\`${lib}:${ctl}:${merged.cmdname}.${lang}\\n${signature}${merged.code.trim()}\\n\\`\\`\\``;\n    }\n    if (merged.flow && merged.cmdname) {\n      out += `\\n\\n\\`\\`\\`${lib}:${ctl}:${merged.cmdname}.flow\\n${merged.flow}\\n\\`\\`\\``;\n    }\n  }\n  if (merged.memory) {\n    out += (out ? \"\\n\\n\" : \"\") + `\\`\\`\\`memory:index\\n${merged.memory.trim()}\\n\\`\\`\\``;\n  }\n  return out;\n}\n\nexport const clamp = (s, n = 2000) =>\n  s.length <= n ? s : s.slice(0, n) + `\\n…[${s.length - n} chars clipped]`;\n\n/** MCP tool descriptors -> OpenAI function defs, enabled names only. */\nexport function toolDefs(mcpTools, enabledNames) {\n  const on = new Set(enabledNames);\n  return (mcpTools ?? [])\n    .filter((t) => on.has(t.name))\n    .map((t) => ({\n      type: \"function\",\n      function: {\n        name: t.name,\n        description: t.description ?? \"\",\n        parameters: t.inputSchema ?? { type: \"object\", properties: {} },\n      },\n    }));\n}\n\n// ── the tool model: defaults, meta-tools, gating ──────────\n\n// Attached-by-default: the read family (auto-run) plus the journaled\n// workhorses (each call confirm-gated). Everything else stays reachable\n// through call_command. The picker stores per-user add/remove overrides.\nexport const DEFAULT_TOOLS = [\n  \"dev-code-read_control_facet\", \"dev-code-read_command\",\n  \"dev-code-read_flow_body\", \"dev-code-list_control_patches\",\n  \"dev-code-list_libraries\", \"dev-code-list_controls\",\n  \"dev-code-list_commands\", \"dev-code-list_assets\",\n  \"dev-code-search_commands\",\n  \"dev-code-patch_control_facet\", \"dev-code-patch_command_body\",\n  \"dev-code-upsert_command\", \"dev-code-write_flow_body\",\n  \"dev-code-invoke_command\", \"dev-code-evaluate_rust\",\n  \"dev-code-remember\",\n];\n\n/** The always-attached meta-tools: discovery + the gateway. */\nexport const META_TOOL_DEFS = [\n  {\n    type: \"function\",\n    function: {\n      name: \"find_tools\",\n      description: \"Search ALL platform commands (not just attached tools) by keywords against their names and descriptions. Returns matches with their authorization tier. Use this before concluding a capability is missing.\",\n      parameters: { type: \"object\",\n        properties: { query: { type: \"string\", description: \"Keywords, e.g. 'delete asset' or 'rust eval'\" } },\n        required: [\"query\"] },\n    },\n  },\n  {\n    type: \"function\",\n    function: {\n      name: \"describe_tool\",\n      description: \"Full descriptor (description + JSON Schema of arguments) for one platform command by its lib-ctl-cmd name. Use before call_command when unsure of the arguments.\",\n      parameters: { type: \"object\",\n        properties: { name: { type: \"string\", description: \"Tool name, e.g. dev-code-delete_asset\" } },\n        required: [\"name\"] },\n    },\n  },\n  {\n    type: \"function\",\n    function: {\n      name: \"call_command\",\n      description: \"Invoke ANY platform command by its lib-ctl-cmd name — attached or not. Arguments are validated against the command's schema; mutating commands stop for the user's confirmation like every other call.\",\n      parameters: { type: \"object\",\n        properties: {\n          name: { type: \"string\", description: \"Tool name, e.g. dev-code-delete_asset\" },\n          args: { type: \"object\", description: \"The command's arguments, per its schema\" },\n        },\n        required: [\"name\", \"args\"] },\n    },\n  },\n];\n\nconst READ_TOOL_PREFIXES = [\n  \"list\", \"read\", \"get\", \"search\", \"describe\", \"lib_info\", \"check\",\n  \"current\", \"info\", \"peers\", \"libs\", \"apps\", \"assets\", \"asset\", \"lookup\",\n];\n\n/** Authorization tier for a catalog entry: \"auto\" (read — runs directly)\n    or \"confirm\" (stops at the typed confirm). The TAGS convention wins\n    when present — a command whose MCP tags include agent-auto or\n    agent-confirm is classified by the owner (describe() forwards the\n    record's `tags` field once the flowlang side does; empty today,\n    forward-compatible). `groups` is SECURITY groups and is never read\n    for gating. Fallback: the read-prefix heuristic on the command name. */\nexport function gateFor(name, entry) {\n  const tags = entry?.tags ?? [];\n  if (tags.includes(\"agent-confirm\")) return \"confirm\";\n  if (tags.includes(\"agent-auto\")) return \"auto\";\n  const cmd = parseToolName(name)?.cmd ?? name;\n  return READ_TOOL_PREFIXES.some((p) => cmd === p || cmd.startsWith(p + \"_\"))\n    ? \"auto\" : \"confirm\";\n}\n\n/** Light client-side check of args against a catalog inputSchema — enough\n    for the model to self-correct: missing required keys and gross type\n    mismatches, reported together. Null = fine. */\nexport function schemaComplaints(schema, args) {\n  if (!schema || typeof args !== \"object\" || args === null) return null;\n  const problems = [];\n  for (const req of schema.required ?? []) {\n    if (!(req in args)) problems.push(`missing required argument '${req}'`);\n  }\n  const kinds = { string: \"string\", number: \"number\", boolean: \"boolean\" };\n  for (const [k, v] of Object.entries(args)) {\n    const spec = schema.properties?.[k];\n    if (!spec) continue;\n    if (spec.type in kinds && typeof v !== kinds[spec.type]) {\n      problems.push(`argument '${k}' should be a ${spec.type}`);\n    } else if (spec.type === \"object\" && (typeof v !== \"object\" || v === null || Array.isArray(v))) {\n      problems.push(`argument '${k}' should be an object`);\n    } else if (spec.type === \"array\" && !Array.isArray(v)) {\n      problems.push(`argument '${k}' should be an array`);\n    }\n  }\n  return problems.length ? problems.join(\"; \") : null;\n}\n\n/** Search the catalog for find_tools: token match on name + description. */\nexport function searchCatalog(catalog, query, limit = 12) {\n  const tokens = (query ?? \"\").toLowerCase().split(/[^a-z0-9_]+/).filter(Boolean);\n  if (!tokens.length) return [];\n  const scored = [];\n  for (const t of catalog) {\n    const hay = (t.name + \" \" + (t.description ?? \"\")).toLowerCase();\n    const hits = tokens.filter((tok) => hay.includes(tok)).length;\n    if (hits) scored.push([hits, t]);\n  }\n  scored.sort((a, b) => b[0] - a[0]);\n  return scored.slice(0, limit).map(([, t]) => ({\n    name: t.name,\n    gate: gateFor(t.name, t),\n    summary: (t.summary ?? t.description ?? \"\").slice(0, 180),\n  }));\n}\n\n/** \"lib-ctl-cmd\" -> {lib, ctl, cmd} (ctl may itself contain dashes). */\nexport function parseToolName(name) {\n  const parts = name.split(\"-\");\n  if (parts.length < 3) return null;\n  return { lib: parts[0], ctl: parts.slice(1, -1).join(\"-\"), cmd: parts.at(-1) };\n}\n\n/**\n * One conversational turn, driving chat_llm until it answers in text.\n * execTool(call) is the session's: it renders the cell, gates mutating\n * commands behind the typed confirm, runs it, and resolves the tool-result\n * CONTENT string (or a denial note). Returns the final text.\n */\nexport async function chatTurn({ messages, tools, execTool, onRound }) {\n  for (let round = 0; round < MAX_ROUNDS; round++) {\n    const r = await store.chatLlm(messages, tools);\n    if (r.status !== \"ok\") throw new Error(r.msg ?? \"chat_llm failed\");\n    const d = r.data ?? {};\n    if (d.kind !== \"tool_calls\") {\n      return d.content ?? \"\";\n    }\n    messages.push(d.assistant_message);\n    onRound?.(d.tool_calls);\n    for (const call of d.tool_calls) {\n      const content = await execTool(call);\n      messages.push({ role: \"tool\", tool_call_id: call.id, content });\n    }\n  }\n  return \"(stopped: the agent used its whole tool budget without a final answer)\";\n}\n","old":"// agentloop.js — the client side of \"chat with the code\", the way the\n// owner called it (2026-07-25): skip control_query AND tool_loop, call\n// agent.llm.chat_llm directly. This module owns what control_query used to\n// do server-side — the system prompt, the context fences, the history —\n// plus the tool loop the notebook can actually SHOW: chat_llm returns\n// normalized tool_calls; the session renders each as a cell and gates\n// mutating ones behind its typed confirm before the loop continues.\n//\n// chat_llm contract (agent.llm, JSONObject envelope — payload under data):\n//   in:  messages (system/user/assistant/tool), tools (OpenAI function defs)\n//   out: {kind:\"text\", content, assistant_message}\n//      | {kind:\"tool_calls\", tool_calls:[{id,name,arguments(JSON string)}],\n//         assistant_message}   — assistant_message goes back on the list.\n// MCP tools (agent.plugin.list_tools) arrive as {name:\"lib-ctl-cmd\",\n// description, inputSchema} — inputSchema is a real JSON Schema.\n\nimport { store } from \"./store.js\";\n\n// Generous: the autonomous developer workflow legitimately spends rounds\n// on compile-debug cycles.\nexport const MAX_ROUNDS = 10;\n\n// The system prompt lives client-side now — the cost of skipping\n// control_query, and the point: the bench knows what the model is looking\n// at and how its replies get used. The platform-knowledge sections are\n// the owner's, folded in from agent.llm.tool_loop's prompt (his call:\n// without them the model can't write ndata-correct code or reason about\n// the platform). Model-agnostic (Qwen or Claude).\nexport const SYSTEM_PROMPT = `You are Newbound, a self-organizing collection of code, data, and opinions that is working with the humans to improve their way of life. Right now you are speaking through the session notebook of Bench, the Newbound IDE — the user sees every tool call you make as a notebook cell.\n\nYour capabilities are defined by the Commands in your flowlang DataStore. The datastore is organized into libraries. Libraries contain Assets and Controls. Controls contain UI elements (html/css/js/data/3d) and back-end components (Command/Event/Timer). The \\`app\\`, \\`dev\\`, \\`peer\\`, and \\`security\\` libraries are the core Newbound libraries. They are great reference information because they define how everything in Newbound is done.\n\nThe ndata Crate: The Universal Data Layer\nAdherence to the principles of the underlying ndata crate is crucial for writing correct and performant rust code. All data within the Flowlang runtime, between the front-end and back-end, and within Commands is managed by ndata.\n  Core Concept: ndata provides globally shared, thread-safe, JSON-like dynamic data structures: DataObject (a HashMap), DataArray (a Vec), and DataBytes (a byte stream). These are handles to heap-stored data.\n  CRITICAL RULE — NO WRAPPING: ndata types are already internally thread-safe. NEVER wrap them in Arc, Rc, Mutex, RwLock, or any other synchronization primitive.\n  Handles: .clone() is cheap (a new handle, refcount bump). When you pass a handle to a mutator like parent.put_object(\"key\", child), the child handle is MOVED — clone first (child.clone()) if you need it afterward.\n  Best practices: prefer typed methods (get_string, put_object) over generic ones; DataObject::new() / DataArray::new() for new instances; deep_copy() for an independent duplicate.\n  SUPER IMPORTANT: ndata does NOT use the wrap-everything-in-Result pattern — get_object/get_int/get_string return directly or PANIC. Use try_get_XXX variants or check has(&key) first when absence is possible.\n\nThe flowlang Crate: The Runtime Engine\n  Data lives under ./data/<lib>/, sharded by four-character chunks of the record id: ./data/lib/[0..4]/[4..8]/[8..12]/[12..16]/fullid. A library's control index is data/<lib>/cont/rols/____/____/controls; its metadata is data/<lib>/meta.json; its assets are data/<lib>/_ASSETS/. It is UNWISE to edit these files directly — malformed data can crash Newbound; use platform commands, which are journaled.\n  STORAGE: use flowlang::datastore::DataStore; let store = DataStore::new(); let ctl = store.get_data(&lib, &id).get_object(\"data\");\n  GLOBALS: DataStore::globals().get_object(\"system\").get_object(\"libraries\")\n  EXECUTION: use flowlang::command::Command; let cmd = Command::lookup(lib, control, command); let result = cmd.execute(arguments);\n\nNewbound Web Apps\n  A Newbound Application is a Control with the same name as its library that has been published. Applications serve at /appname/index.html and their commands at /appname/cmdname?param=value. The http_port is in config.properties.\n\nWriting Commands in Rust\n  Default to Rust for new commands unless Python is explicitly requested or needed for an AI/data-science library.\n  CRITICAL — code_body IS A FUNCTION BODY, NOT A FUNCTION: for upsert_command/patch_command_body, your code_body is placed inside a generated fn: pub fn <cmd>(<params>) -> <ReturnType> { <your code_body> }. Never write a fn signature or outer braces. Declared params are in scope as typed variables (String, Integer→i64, Float→f64, Boolean→bool, JSONObject→DataObject, JSONArray→DataArray). The FINAL EXPRESSION must match the declared return_type. Put use statements in \\`imports\\`; DataObject is imported automatically.\n  Other Newbound commands are callable natively: let api = crate::api::new(); api.lib.ctl.cmd(param1, param2); or use crate::lib::ctl::cmd::cmd;\n\nWriting Commands in Python\n  Python is first-class: arguments arrive as NDataObject/NDataArray/NDataBytes wrappers sharing memory with Rust — manipulate them directly. Returning native Python types auto-converts.\n\nReturn Type Semantics\n  A command's response JSON always has \"status\" (\"ok\"/\"err\"). A returned String lands in \"msg\"; other types land in \"data\"; FLAT means the returned DataObject IS the outermost response. Choose return_type to match the final expression.\n\nFront-End Control Development (HTML/CSS/JS)\n  Platform controls keep html, css, and js as separate facets. A back-end command foo is exposed to that control's js as send_foo(args..., function(result){...}) — result.msg for Strings, result.data otherwise. jQuery 3.6.1 is loaded. Standard boilerplate: var me = this; var ME = $('#' + me.UUID)[0]; me.ready = function(){...}. Sub-controls mount declaratively (<div class='data-control' data-control='lib:ctl:{...}'>) or via installControl(...).\n  EXCEPTION: the \\`bench\\` library's own controls are ES modules (export function init(host, props), no jQuery, imports from ../../assets/) — match the style of whatever control you are editing.\n\nWHAT YOU SEE\n- Current code arrives in fenced blocks labeled lib:ctl.facet (facets) or lib:ctl:command.lang (command bodies; the first line may be a synthesized signature comment). A flow body arrives as JSON labeled lib:ctl:command.flow.\n- \"Recent notebook activity\" lists commands run in this session, with OUTPUTS TRUNCATED. Never conclude that something is absent because you don't see it in a truncated result — rerun the command via a tool when you need the full output.\n\nLONG-TERM MEMORY (the kb library — docs/memory.md)\n- The kb library is your long-term memory. Controls are knowledge DOMAINS — reference domains (platform-api, workflow, …) plus dated MONTH BUCKETS (m2026-07) for episodic history; the type rides naming and tags, not structure. Each memory control's \\`memory\\` facet is a JSON array of entries {claim, detail?, tags, source?, confidence, time}. When memory exists, your context includes a memory:index fence listing the domains.\n- RECALL: when a task touches a domain, read its entries — dev-code-read_control_facet {lib:\"kb\", ctl:\"<domain>\", facet:\"memory\"} (a read; runs without confirmation). Entries carry POINTERS ({lib, ctl, facet, hash} or {doc}) rather than copies: read the referent for current truth, and if the referent's hash no longer matches source.hash, the entry may be STALE — say so. The index fence marks domains carrying such entries (\"N stale?\").\n- REMEMBER: when the user asks you to remember something, or you learn a durable non-obvious fact worth keeping, route by type — durable knowledge to the fitting reference domain, session events and lessons to the current month bucket — and call dev-code-remember {lib:\"kb\", domain:\"<domain>\", entry:{claim, detail?, tags, source?, confidence?}}. ONE atomic claim per call. The command validates the entry, stamps \\`time\\` for you, auto-stamps a facet-pointer source's hash ({lib, ctl, facet} — omit the hash), refuses to touch a malformed facet, and journals the write. Every memory write stops at the user's confirm. Create a new domain (dev-code-add_control) only when none fits; promoting a month-bucket lesson into a reference domain is a deliberate copy — say so when you do it.\n- Keep entries ATOMIC (one claim each), tag them with the libs/controls they concern, and never copy what canon already says — record the why/gotcha/technique, with a pointer.\n\nHOW TO ANSWER\n- Be concise and concrete. If you are unsure, say so plainly rather than inventing platform behavior.\n- To propose a facet change, reply with ONE complete fenced block per changed facet, tagged html, css, or js. The user can apply such a block as a journaled whole-facet patch in one click — a partial snippet cannot be applied, so always return the full facet.\n- Command-body suggestions: a fenced block tagged rust or python containing the full body.`;\n\n// The tool model (owner's design, 2026-07-25): DISCOVERY is always on,\n// ATTACHMENT is a context optimization, AUTHORIZATION happens per call.\nexport const TOOLS_PROMPT = `\n\nTOOLS\nNewbound commands are your tools, named library-control-command (e.g. dev-code-read_command). Call them through the tool-calling interface — never by describing a call in text. Every call you make appears as a cell in the user's notebook.\n\n- A core set is attached with full schemas. EVERY other platform command is also available: use find_tools(query) to discover commands for a task, describe_tool(name) for a full schema, and call_command(name, args) to invoke anything not directly attached. Never claim a capability is missing until find_tools says so.\n- Authorization is per call, not per tool: read-only commands run immediately; anything mutating or potentially destructive stops for the user's typed confirmation first — so do not hesitate to REQUEST a powerful command when the task needs it; the user decides at the moment of use. A denial is the user's decision: respect it and continue without that action.\n- Read before you write. For edits prefer the journaled, revertible commands: dev-code-patch_control_facet for UI facets, dev-code-patch_command_body / upsert_command for command bodies, dev-code-read_flow_body / write_flow_body for flow commands.\n\nThe Autonomous Developer Workflow\nWhen asked to build a feature, process data, or perform a task requiring code, act as an autonomous developer:\n1. READ: understand existing code first (dev-code-read_command, dev-code-read_control_facet).\n2. WRITE: dev-code-upsert_command or dev-code-patch_command_body — the system generates the source, compiles, and hot-swaps on success.\n3. DEBUG: if compilation fails, do not apologize — read the compiler error from the tool result, patch, and compile again until it succeeds.\n4. EXECUTE: dev-code-invoke_command to run and verify.\n5. RESPOND: give your final answer only after execution succeeds, summarizing what you built and the results.\n6. OBSTACLES: if a step stays blocked after 2-3 DIFFERENT approaches, stop and report what worked, what didn't, and the exact blocking error. A clear partial result beats a burned budget.\n\nScratchpad Execution\nFor complex logical, mathematical, or data-parsing questions, write and run a temporary Rust script with dev-code-evaluate_rust instead of guessing. Your \\`code\\` is compiled inside a function returning DataObject — the FINAL EXPRESSION must be a DataObject:\n    let mut out = DataObject::new();\n    out.put_int(\"result\", my_value as i64);\n    out\nPut use statements in \\`imports\\` (DataObject is automatic).\n\nAdvanced Native Orchestration\nYou can invoke any Newbound command from WITHIN an evaluate_rust script via let api = crate::api::new(); — chain tools, process large data, or run multi-step pipelines in one script instead of many chat rounds. Pass large intermediate values through DataStore::globals().get_object(\"AGENT_MEMORY\") or the runtime library rather than through this conversation.`;\n\n// Newbound meta types -> Rust types, for synthesized signatures (ported\n// from control_query's lookup_rust_api_data_type).\nconst RUST_TYPE = {\n  FLAT: \"DataObject\", JSONObject: \"DataObject\", JSONArray: \"DataArray\",\n  InputStream: \"DataBytes\", float: \"f64\", Integer: \"i64\", Boolean: \"bool\",\n  Any: \"Data\", NULL: \"DNull\",\n};\nconst rustType = (t) => RUST_TYPE[t] ?? \"String\";\n\n/** The context prompt, ported from control_query: an editing preamble plus\n    one fence per included piece, tagged the way the backend prompt always\n    tagged them. `included` = chatctx snapshot entries the user checked. */\nexport function contextBlock(included) {\n  const merged = {};\n  for (const p of included) Object.assign(merged, p.fields);\n  const { lib, ctl } = merged;\n  let out = \"\";\n  if (lib || ctl) {\n    out = `We are editing the ${ctl} control in the ${lib} library.`;\n    for (const facet of [\"html\", \"css\", \"js\"]) {\n      const v = (merged[facet] ?? \"\").trim?.() ?? \"\";\n      if (v) out += `\\n\\n\\`\\`\\`${lib}:${ctl}.${facet}\\n${v}\\n\\`\\`\\``;\n    }\n    if (merged.code && merged.cmdname) {\n      const lang = merged.lang === \"rust\" ? \"rs\" : merged.lang === \"python\" ? \"py\" : merged.lang;\n      let signature = \"\";\n      if (merged.cmdparams) {\n        const ps = merged.cmdparams.map((p) =>\n          lang === \"rs\" ? `${p.name}: ${rustType(p.type)}` : p.name).join(\", \");\n        signature = lang === \"rs\"\n          ? `// fn ${merged.cmdname}(${ps}) -> ${rustType(merged.cmdreturn ?? \"String\")}\\n`\n          : `# def ${merged.cmdname}(${ps}):\\n`;\n      }\n      out += `\\n\\n\\`\\`\\`${lib}:${ctl}:${merged.cmdname}.${lang}\\n${signature}${merged.code.trim()}\\n\\`\\`\\``;\n    }\n    if (merged.flow && merged.cmdname) {\n      out += `\\n\\n\\`\\`\\`${lib}:${ctl}:${merged.cmdname}.flow\\n${merged.flow}\\n\\`\\`\\``;\n    }\n  }\n  if (merged.memory) {\n    out += (out ? \"\\n\\n\" : \"\") + `\\`\\`\\`memory:index\\n${merged.memory.trim()}\\n\\`\\`\\``;\n  }\n  return out;\n}\n\nexport const clamp = (s, n = 2000) =>\n  s.length <= n ? s : s.slice(0, n) + `\\n…[${s.length - n} chars clipped]`;\n\n/** MCP tool descriptors -> OpenAI function defs, enabled names only. */\nexport function toolDefs(mcpTools, enabledNames) {\n  const on = new Set(enabledNames);\n  return (mcpTools ?? [])\n    .filter((t) => on.has(t.name))\n    .map((t) => ({\n      type: \"function\",\n      function: {\n        name: t.name,\n        description: t.description ?? \"\",\n        parameters: t.inputSchema ?? { type: \"object\", properties: {} },\n      },\n    }));\n}\n\n// ── the tool model: defaults, meta-tools, gating ──────────\n\n// Attached-by-default: the read family (auto-run) plus the journaled\n// workhorses (each call confirm-gated). Everything else stays reachable\n// through call_command. The picker stores per-user add/remove overrides.\nexport const DEFAULT_TOOLS = [\n  \"dev-code-read_control_facet\", \"dev-code-read_command\",\n  \"dev-code-read_flow_body\", \"dev-code-list_control_patches\",\n  \"dev-code-list_libraries\", \"dev-code-list_controls\",\n  \"dev-code-list_commands\", \"dev-code-list_assets\",\n  \"dev-code-search_commands\",\n  \"dev-code-patch_control_facet\", \"dev-code-patch_command_body\",\n  \"dev-code-upsert_command\", \"dev-code-write_flow_body\",\n  \"dev-code-invoke_command\", \"dev-code-evaluate_rust\",\n  \"dev-code-remember\",\n];\n\n/** The always-attached meta-tools: discovery + the gateway. */\nexport const META_TOOL_DEFS = [\n  {\n    type: \"function\",\n    function: {\n      name: \"find_tools\",\n      description: \"Search ALL platform commands (not just attached tools) by keywords against their names and descriptions. Returns matches with their authorization tier. Use this before concluding a capability is missing.\",\n      parameters: { type: \"object\",\n        properties: { query: { type: \"string\", description: \"Keywords, e.g. 'delete asset' or 'rust eval'\" } },\n        required: [\"query\"] },\n    },\n  },\n  {\n    type: \"function\",\n    function: {\n      name: \"describe_tool\",\n      description: \"Full descriptor (description + JSON Schema of arguments) for one platform command by its lib-ctl-cmd name. Use before call_command when unsure of the arguments.\",\n      parameters: { type: \"object\",\n        properties: { name: { type: \"string\", description: \"Tool name, e.g. dev-code-delete_asset\" } },\n        required: [\"name\"] },\n    },\n  },\n  {\n    type: \"function\",\n    function: {\n      name: \"call_command\",\n      description: \"Invoke ANY platform command by its lib-ctl-cmd name — attached or not. Arguments are validated against the command's schema; mutating commands stop for the user's confirmation like every other call.\",\n      parameters: { type: \"object\",\n        properties: {\n          name: { type: \"string\", description: \"Tool name, e.g. dev-code-delete_asset\" },\n          args: { type: \"object\", description: \"The command's arguments, per its schema\" },\n        },\n        required: [\"name\", \"args\"] },\n    },\n  },\n];\n\nconst READ_TOOL_PREFIXES = [\n  \"list\", \"read\", \"get\", \"search\", \"describe\", \"lib_info\", \"check\",\n  \"current\", \"info\", \"peers\", \"libs\", \"apps\", \"assets\", \"asset\", \"lookup\",\n];\n\n/** Authorization tier for a catalog entry: \"auto\" (read — runs directly)\n    or \"confirm\" (stops at the typed confirm). The TAGS convention wins\n    when present — a command whose MCP tags include agent-auto or\n    agent-confirm is classified by the owner (describe() forwards the\n    record's `tags` field once the flowlang side does; empty today,\n    forward-compatible). `groups` is SECURITY groups and is never read\n    for gating. Fallback: the read-prefix heuristic on the command name. */\nexport function gateFor(name, entry) {\n  const tags = entry?.tags ?? [];\n  if (tags.includes(\"agent-confirm\")) return \"confirm\";\n  if (tags.includes(\"agent-auto\")) return \"auto\";\n  const cmd = parseToolName(name)?.cmd ?? name;\n  return READ_TOOL_PREFIXES.some((p) => cmd === p || cmd.startsWith(p + \"_\"))\n    ? \"auto\" : \"confirm\";\n}\n\n/** Light client-side check of args against a catalog inputSchema — enough\n    for the model to self-correct: missing required keys and gross type\n    mismatches, reported together. Null = fine. */\nexport function schemaComplaints(schema, args) {\n  if (!schema || typeof args !== \"object\" || args === null) return null;\n  const problems = [];\n  for (const req of schema.required ?? []) {\n    if (!(req in args)) problems.push(`missing required argument '${req}'`);\n  }\n  const kinds = { string: \"string\", number: \"number\", boolean: \"boolean\" };\n  for (const [k, v] of Object.entries(args)) {\n    const spec = schema.properties?.[k];\n    if (!spec) continue;\n    if (spec.type in kinds && typeof v !== kinds[spec.type]) {\n      problems.push(`argument '${k}' should be a ${spec.type}`);\n    } else if (spec.type === \"object\" && (typeof v !== \"object\" || v === null || Array.isArray(v))) {\n      problems.push(`argument '${k}' should be an object`);\n    } else if (spec.type === \"array\" && !Array.isArray(v)) {\n      problems.push(`argument '${k}' should be an array`);\n    }\n  }\n  return problems.length ? problems.join(\"; \") : null;\n}\n\n/** Search the catalog for find_tools: token match on name + description. */\nexport function searchCatalog(catalog, query, limit = 12) {\n  const tokens = (query ?? \"\").toLowerCase().split(/[^a-z0-9_]+/).filter(Boolean);\n  if (!tokens.length) return [];\n  const scored = [];\n  for (const t of catalog) {\n    const hay = (t.name + \" \" + (t.description ?? \"\")).toLowerCase();\n    const hits = tokens.filter((tok) => hay.includes(tok)).length;\n    if (hits) scored.push([hits, t]);\n  }\n  scored.sort((a, b) => b[0] - a[0]);\n  return scored.slice(0, limit).map(([, t]) => ({\n    name: t.name,\n    gate: gateFor(t.name, t),\n    summary: (t.summary ?? t.description ?? \"\").slice(0, 180),\n  }));\n}\n\n/** \"lib-ctl-cmd\" -> {lib, ctl, cmd} (ctl may itself contain dashes). */\nexport function parseToolName(name) {\n  const parts = name.split(\"-\");\n  if (parts.length < 3) return null;\n  return { lib: parts[0], ctl: parts.slice(1, -1).join(\"-\"), cmd: parts.at(-1) };\n}\n\n/**\n * One conversational turn, driving chat_llm until it answers in text.\n * execTool(call) is the session's: it renders the cell, gates mutating\n * commands behind the typed confirm, runs it, and resolves the tool-result\n * CONTENT string (or a denial note). Returns the final text.\n */\nexport async function chatTurn({ messages, tools, execTool, onRound }) {\n  for (let round = 0; round < MAX_ROUNDS; round++) {\n    const r = await store.chatLlm(messages, tools);\n    if (r.status !== \"ok\") throw new Error(r.msg ?? \"chat_llm failed\");\n    const d = r.data ?? {};\n    if (d.kind !== \"tool_calls\") {\n      return d.content ?? \"\";\n    }\n    messages.push(d.assistant_message);\n    onRound?.(d.tool_calls);\n    for (const call of d.tool_calls) {\n      const content = await execTool(call);\n      messages.push({ role: \"tool\", tool_call_id: call.id, content });\n    }\n  }\n  return \"(stopped: the agent used its whole tool budget without a final answer)\";\n}\n","patch_id":"p21","time":1785513239111},{"author":"claude","facet":"js","label":"archivist: venue memory block; remember auto on explicit ask","new":"// agentloop.js — the client side of \"chat with the code\", the way the\n// owner called it (2026-07-25): skip control_query AND tool_loop, call\n// agent.llm.chat_llm directly. This module owns what control_query used to\n// do server-side — the system prompt, the context fences, the history —\n// plus the tool loop the notebook can actually SHOW: chat_llm returns\n// normalized tool_calls; the session renders each as a cell and gates\n// mutating ones behind its typed confirm before the loop continues.\n//\n// chat_llm contract (agent.llm, JSONObject envelope — payload under data):\n//   in:  messages (system/user/assistant/tool), tools (OpenAI function defs)\n//   out: {kind:\"text\", content, assistant_message}\n//      | {kind:\"tool_calls\", tool_calls:[{id,name,arguments(JSON string)}],\n//         assistant_message}   — assistant_message goes back on the list.\n// MCP tools (agent.plugin.list_tools) arrive as {name:\"lib-ctl-cmd\",\n// description, inputSchema} — inputSchema is a real JSON Schema.\n\nimport { store } from \"./store.js\";\n\n// Generous: the autonomous developer workflow legitimately spends rounds\n// on compile-debug cycles.\nexport const MAX_ROUNDS = 10;\n\n// The system prompt lives client-side now — the cost of skipping\n// control_query, and the point: the bench knows what the model is looking\n// at and how its replies get used. The platform-knowledge sections are\n// the owner's, folded in from agent.llm.tool_loop's prompt (his call:\n// without them the model can't write ndata-correct code or reason about\n// the platform). Model-agnostic (Qwen or Claude).\n// The platform-knowledge CORE lives in the store (dev.prompts `prompt`\n// facet — docs/prompting.md): ONE journaled curriculum shared with the\n// agent app's tool_loop. SYSTEM_PROMPT here is the notebook's SHELL —\n// venue, fences, memory, answer style; session assembles core + shell +\n// TOOLS_PROMPT + the owner addendum at ask time.\nexport const SYSTEM_PROMPT = `Right now you are speaking through the session notebook of Bench, the Newbound IDE — the user sees every tool call you make as a notebook cell.\n\nWHAT YOU SEE\n- Current code arrives in fenced blocks labeled lib:ctl.facet (facets) or lib:ctl:command.lang (command bodies; the first line may be a synthesized signature comment). A flow body arrives as JSON labeled lib:ctl:command.flow.\n- \"Recent notebook activity\" lists commands run in this session, with OUTPUTS TRUNCATED. Never conclude that something is absent because you don't see it in a truncated result — rerun the command via a tool when you need the full output.\n\nMEMORY IN THIS NOTEBOOK\n- Your context includes a memory:index fence: every kb domain with entry counts, tags, staleness marks (\"N stale?\" — re-read those referents before trusting the marked entries), and entries pushed for the surface open on the bench (\"Relevant now\").\n- Memory FORMATION is automatic here: the archivist reviews completed sessions in the background and files what is durable (journaled, tagged unreviewed). Do not file memories on your own initiative. When the user explicitly asks you to remember something, call dev-code-remember — it runs without confirmation, because the request itself is the authorization.\n\nHOW TO ANSWER\n- Be concise and concrete. If you are unsure, say so plainly rather than inventing platform behavior.\n- To propose a facet change, reply with ONE complete fenced block per changed facet, tagged html, css, or js. The user can apply such a block as a journaled whole-facet patch in one click — a partial snippet cannot be applied, so always return the full facet.\n- Command-body suggestions: a fenced block tagged rust or python containing the full body.`;\n\n// Fetched lazily at ask time (the connection exists by then; module\n// install happens before it does). Cached per page; a failed fetch\n// clears the cache so the next ask retries, and the error is LOUD —\n// a silently thin prompt regresses the confabulation lesson.\nlet corePromise = null;\nexport function corePrompt() {\n  if (!corePromise) {\n    corePromise = (async () => {\n      const r = await store.readFacet(\"dev\", \"prompts\", \"prompt\");\n      if (r?.status === \"ok\" && r.exists && (r.source ?? \"\").trim()) {\n        return r.source.trim();\n      }\n      corePromise = null;\n      throw new Error(\"dev.prompts `prompt` facet unavailable — the platform curriculum is store-resident (docs/prompting.md)\");\n    })();\n  }\n  return corePromise;\n}\n\n// The tool model (owner's design, 2026-07-25): DISCOVERY is always on,\n// ATTACHMENT is a context optimization, AUTHORIZATION happens per call.\nexport const TOOLS_PROMPT = `\n\nTOOLS\nNewbound commands are your tools, named library-control-command (e.g. dev-code-read_command). Call them through the tool-calling interface — never by describing a call in text. Every call you make appears as a cell in the user's notebook.\n\n- A core set is attached with full schemas. EVERY other platform command is also available: use find_tools(query) to discover commands for a task, describe_tool(name) for a full schema, and call_command(name, args) to invoke anything not directly attached. Never claim a capability is missing until find_tools says so.\n- Authorization is per call, not per tool: read-only commands run immediately; anything mutating or potentially destructive stops for the user's typed confirmation first — so do not hesitate to REQUEST a powerful command when the task needs it; the user decides at the moment of use. A denial is the user's decision: respect it and continue without that action.\n- Read before you write. For edits prefer the journaled, revertible commands: dev-code-patch_control_facet for UI facets, dev-code-patch_command_body / upsert_command for command bodies, dev-code-read_flow_body / write_flow_body for flow commands.`;\n\n// Newbound meta types -> Rust types, for synthesized signatures (ported\n// from control_query's lookup_rust_api_data_type).\nconst RUST_TYPE = {\n  FLAT: \"DataObject\", JSONObject: \"DataObject\", JSONArray: \"DataArray\",\n  InputStream: \"DataBytes\", float: \"f64\", Integer: \"i64\", Boolean: \"bool\",\n  Any: \"Data\", NULL: \"DNull\",\n};\nconst rustType = (t) => RUST_TYPE[t] ?? \"String\";\n\n/** The context prompt, ported from control_query: an editing preamble plus\n    one fence per included piece, tagged the way the backend prompt always\n    tagged them. `included` = chatctx snapshot entries the user checked. */\nexport function contextBlock(included) {\n  const merged = {};\n  for (const p of included) Object.assign(merged, p.fields);\n  const { lib, ctl } = merged;\n  let out = \"\";\n  if (lib || ctl) {\n    out = `We are editing the ${ctl} control in the ${lib} library.`;\n    for (const facet of [\"html\", \"css\", \"js\"]) {\n      const v = (merged[facet] ?? \"\").trim?.() ?? \"\";\n      if (v) out += `\\n\\n\\`\\`\\`${lib}:${ctl}.${facet}\\n${v}\\n\\`\\`\\``;\n    }\n    if (merged.code && merged.cmdname) {\n      const lang = merged.lang === \"rust\" ? \"rs\" : merged.lang === \"python\" ? \"py\" : merged.lang;\n      let signature = \"\";\n      if (merged.cmdparams) {\n        const ps = merged.cmdparams.map((p) =>\n          lang === \"rs\" ? `${p.name}: ${rustType(p.type)}` : p.name).join(\", \");\n        signature = lang === \"rs\"\n          ? `// fn ${merged.cmdname}(${ps}) -> ${rustType(merged.cmdreturn ?? \"String\")}\\n`\n          : `# def ${merged.cmdname}(${ps}):\\n`;\n      }\n      out += `\\n\\n\\`\\`\\`${lib}:${ctl}:${merged.cmdname}.${lang}\\n${signature}${merged.code.trim()}\\n\\`\\`\\``;\n    }\n    if (merged.flow && merged.cmdname) {\n      out += `\\n\\n\\`\\`\\`${lib}:${ctl}:${merged.cmdname}.flow\\n${merged.flow}\\n\\`\\`\\``;\n    }\n  }\n  if (merged.memory) {\n    out += (out ? \"\\n\\n\" : \"\") + `\\`\\`\\`memory:index\\n${merged.memory.trim()}\\n\\`\\`\\``;\n  }\n  return out;\n}\n\nexport const clamp = (s, n = 2000) =>\n  s.length <= n ? s : s.slice(0, n) + `\\n…[${s.length - n} chars clipped]`;\n\n/** MCP tool descriptors -> OpenAI function defs, enabled names only. */\nexport function toolDefs(mcpTools, enabledNames) {\n  const on = new Set(enabledNames);\n  return (mcpTools ?? [])\n    .filter((t) => on.has(t.name))\n    .map((t) => ({\n      type: \"function\",\n      function: {\n        name: t.name,\n        description: t.description ?? \"\",\n        parameters: t.inputSchema ?? { type: \"object\", properties: {} },\n      },\n    }));\n}\n\n// ── the tool model: defaults, meta-tools, gating ──────────\n\n// Attached-by-default: the read family (auto-run) plus the journaled\n// workhorses (each call confirm-gated). Everything else stays reachable\n// through call_command. The picker stores per-user add/remove overrides.\nexport const DEFAULT_TOOLS = [\n  \"dev-code-read_control_facet\", \"dev-code-read_command\",\n  \"dev-code-read_flow_body\", \"dev-code-list_control_patches\",\n  \"dev-code-list_libraries\", \"dev-code-list_controls\",\n  \"dev-code-list_commands\", \"dev-code-list_assets\",\n  \"dev-code-search_commands\",\n  \"dev-code-patch_control_facet\", \"dev-code-patch_command_body\",\n  \"dev-code-upsert_command\", \"dev-code-write_flow_body\",\n  \"dev-code-invoke_command\", \"dev-code-evaluate_rust\",\n  \"dev-code-remember\",\n];\n\n/** The always-attached meta-tools: discovery + the gateway. */\nexport const META_TOOL_DEFS = [\n  {\n    type: \"function\",\n    function: {\n      name: \"find_tools\",\n      description: \"Search ALL platform commands (not just attached tools) by keywords against their names and descriptions. Returns matches with their authorization tier. Use this before concluding a capability is missing.\",\n      parameters: { type: \"object\",\n        properties: { query: { type: \"string\", description: \"Keywords, e.g. 'delete asset' or 'rust eval'\" } },\n        required: [\"query\"] },\n    },\n  },\n  {\n    type: \"function\",\n    function: {\n      name: \"describe_tool\",\n      description: \"Full descriptor (description + JSON Schema of arguments) for one platform command by its lib-ctl-cmd name. Use before call_command when unsure of the arguments.\",\n      parameters: { type: \"object\",\n        properties: { name: { type: \"string\", description: \"Tool name, e.g. dev-code-delete_asset\" } },\n        required: [\"name\"] },\n    },\n  },\n  {\n    type: \"function\",\n    function: {\n      name: \"call_command\",\n      description: \"Invoke ANY platform command by its lib-ctl-cmd name — attached or not. Arguments are validated against the command's schema; mutating commands stop for the user's confirmation like every other call.\",\n      parameters: { type: \"object\",\n        properties: {\n          name: { type: \"string\", description: \"Tool name, e.g. dev-code-delete_asset\" },\n          args: { type: \"object\", description: \"The command's arguments, per its schema\" },\n        },\n        required: [\"name\", \"args\"] },\n    },\n  },\n];\n\nconst READ_TOOL_PREFIXES = [\n  \"list\", \"read\", \"get\", \"search\", \"describe\", \"lib_info\", \"check\",\n  \"current\", \"info\", \"peers\", \"libs\", \"apps\", \"assets\", \"asset\", \"lookup\",\n];\n\n/** Authorization tier for a catalog entry: \"auto\" (read — runs directly)\n    or \"confirm\" (stops at the typed confirm). The TAGS convention wins\n    when present — a command whose MCP tags include agent-auto or\n    agent-confirm is classified by the owner (describe() forwards the\n    record's `tags` field once the flowlang side does; empty today,\n    forward-compatible). `groups` is SECURITY groups and is never read\n    for gating. Fallback: the read-prefix heuristic on the command name. */\n// remember runs WITHOUT confirmation on explicit user request — the ask\n// itself is the authorization (docs/memory.md); autonomous memory\n// formation belongs to the archivist, and the prompt says so.\nconst AUTO_OVERRIDES = new Set([\"dev-code-remember\"]);\n\nexport function gateFor(name, entry) {\n  if (AUTO_OVERRIDES.has(name)) return \"auto\";\n  const tags = entry?.tags ?? [];\n  if (tags.includes(\"agent-confirm\")) return \"confirm\";\n  if (tags.includes(\"agent-auto\")) return \"auto\";\n  const cmd = parseToolName(name)?.cmd ?? name;\n  return READ_TOOL_PREFIXES.some((p) => cmd === p || cmd.startsWith(p + \"_\"))\n    ? \"auto\" : \"confirm\";\n}\n\n/** Light client-side check of args against a catalog inputSchema — enough\n    for the model to self-correct: missing required keys and gross type\n    mismatches, reported together. Null = fine. */\nexport function schemaComplaints(schema, args) {\n  if (!schema || typeof args !== \"object\" || args === null) return null;\n  const problems = [];\n  for (const req of schema.required ?? []) {\n    if (!(req in args)) problems.push(`missing required argument '${req}'`);\n  }\n  const kinds = { string: \"string\", number: \"number\", boolean: \"boolean\" };\n  for (const [k, v] of Object.entries(args)) {\n    const spec = schema.properties?.[k];\n    if (!spec) continue;\n    if (spec.type in kinds && typeof v !== kinds[spec.type]) {\n      problems.push(`argument '${k}' should be a ${spec.type}`);\n    } else if (spec.type === \"object\" && (typeof v !== \"object\" || v === null || Array.isArray(v))) {\n      problems.push(`argument '${k}' should be an object`);\n    } else if (spec.type === \"array\" && !Array.isArray(v)) {\n      problems.push(`argument '${k}' should be an array`);\n    }\n  }\n  return problems.length ? problems.join(\"; \") : null;\n}\n\n/** Search the catalog for find_tools: token match on name + description. */\nexport function searchCatalog(catalog, query, limit = 12) {\n  const tokens = (query ?? \"\").toLowerCase().split(/[^a-z0-9_]+/).filter(Boolean);\n  if (!tokens.length) return [];\n  const scored = [];\n  for (const t of catalog) {\n    const hay = (t.name + \" \" + (t.description ?? \"\")).toLowerCase();\n    const hits = tokens.filter((tok) => hay.includes(tok)).length;\n    if (hits) scored.push([hits, t]);\n  }\n  scored.sort((a, b) => b[0] - a[0]);\n  return scored.slice(0, limit).map(([, t]) => ({\n    name: t.name,\n    gate: gateFor(t.name, t),\n    summary: (t.summary ?? t.description ?? \"\").slice(0, 180),\n  }));\n}\n\n/** \"lib-ctl-cmd\" -> {lib, ctl, cmd} (ctl may itself contain dashes). */\nexport function parseToolName(name) {\n  const parts = name.split(\"-\");\n  if (parts.length < 3) return null;\n  return { lib: parts[0], ctl: parts.slice(1, -1).join(\"-\"), cmd: parts.at(-1) };\n}\n\n/**\n * One conversational turn, driving chat_llm until it answers in text.\n * execTool(call) is the session's: it renders the cell, gates mutating\n * commands behind the typed confirm, runs it, and resolves the tool-result\n * CONTENT string (or a denial note). Returns the final text.\n */\nexport async function chatTurn({ messages, tools, execTool, onRound }) {\n  for (let round = 0; round < MAX_ROUNDS; round++) {\n    const r = await store.chatLlm(messages, tools);\n    if (r.status !== \"ok\") throw new Error(r.msg ?? \"chat_llm failed\");\n    const d = r.data ?? {};\n    if (d.kind !== \"tool_calls\") {\n      return d.content ?? \"\";\n    }\n    messages.push(d.assistant_message);\n    onRound?.(d.tool_calls);\n    for (const call of d.tool_calls) {\n      const content = await execTool(call);\n      messages.push({ role: \"tool\", tool_call_id: call.id, content });\n    }\n  }\n  return \"(stopped: the agent used its whole tool budget without a final answer)\";\n}\n","old":"// agentloop.js — the client side of \"chat with the code\", the way the\n// owner called it (2026-07-25): skip control_query AND tool_loop, call\n// agent.llm.chat_llm directly. This module owns what control_query used to\n// do server-side — the system prompt, the context fences, the history —\n// plus the tool loop the notebook can actually SHOW: chat_llm returns\n// normalized tool_calls; the session renders each as a cell and gates\n// mutating ones behind its typed confirm before the loop continues.\n//\n// chat_llm contract (agent.llm, JSONObject envelope — payload under data):\n//   in:  messages (system/user/assistant/tool), tools (OpenAI function defs)\n//   out: {kind:\"text\", content, assistant_message}\n//      | {kind:\"tool_calls\", tool_calls:[{id,name,arguments(JSON string)}],\n//         assistant_message}   — assistant_message goes back on the list.\n// MCP tools (agent.plugin.list_tools) arrive as {name:\"lib-ctl-cmd\",\n// description, inputSchema} — inputSchema is a real JSON Schema.\n\nimport { store } from \"./store.js\";\n\n// Generous: the autonomous developer workflow legitimately spends rounds\n// on compile-debug cycles.\nexport const MAX_ROUNDS = 10;\n\n// The system prompt lives client-side now — the cost of skipping\n// control_query, and the point: the bench knows what the model is looking\n// at and how its replies get used. The platform-knowledge sections are\n// the owner's, folded in from agent.llm.tool_loop's prompt (his call:\n// without them the model can't write ndata-correct code or reason about\n// the platform). Model-agnostic (Qwen or Claude).\n// The platform-knowledge CORE lives in the store (dev.prompts `prompt`\n// facet — docs/prompting.md): ONE journaled curriculum shared with the\n// agent app's tool_loop. SYSTEM_PROMPT here is the notebook's SHELL —\n// venue, fences, memory, answer style; session assembles core + shell +\n// TOOLS_PROMPT + the owner addendum at ask time.\nexport const SYSTEM_PROMPT = `Right now you are speaking through the session notebook of Bench, the Newbound IDE — the user sees every tool call you make as a notebook cell.\n\nWHAT YOU SEE\n- Current code arrives in fenced blocks labeled lib:ctl.facet (facets) or lib:ctl:command.lang (command bodies; the first line may be a synthesized signature comment). A flow body arrives as JSON labeled lib:ctl:command.flow.\n- \"Recent notebook activity\" lists commands run in this session, with OUTPUTS TRUNCATED. Never conclude that something is absent because you don't see it in a truncated result — rerun the command via a tool when you need the full output.\n\nLONG-TERM MEMORY (the kb library — docs/memory.md)\n- The kb library is your long-term memory. Controls are knowledge DOMAINS — reference domains (platform-api, workflow, …) plus dated MONTH BUCKETS (m2026-07) for episodic history; the type rides naming and tags, not structure. Each memory control's \\`memory\\` facet is a JSON array of entries {claim, detail?, tags, source?, confidence, time}. When memory exists, your context includes a memory:index fence listing the domains.\n- RECALL: when a task touches a domain, read its entries — dev-code-read_control_facet {lib:\"kb\", ctl:\"<domain>\", facet:\"memory\"} (a read; runs without confirmation). Entries carry POINTERS ({lib, ctl, facet, hash} or {doc}) rather than copies: read the referent for current truth, and if the referent's hash no longer matches source.hash, the entry may be STALE — say so. The index fence marks domains carrying such entries (\"N stale?\").\n- REMEMBER: when the user asks you to remember something, or you learn a durable non-obvious fact worth keeping, route by type — durable knowledge to the fitting reference domain, session events and lessons to the current month bucket — and call dev-code-remember {lib:\"kb\", domain:\"<domain>\", entry:{claim, detail?, tags, source?, confidence?}}. ONE atomic claim per call. The command validates the entry, stamps \\`time\\` for you, auto-stamps a facet-pointer source's hash ({lib, ctl, facet} — omit the hash), refuses to touch a malformed facet, and journals the write. Every memory write stops at the user's confirm. Create a new domain (dev-code-add_control) only when none fits; promoting a month-bucket lesson into a reference domain is a deliberate copy — say so when you do it.\n- Keep entries ATOMIC (one claim each), tag them with the libs/controls they concern, and never copy what canon already says — record the why/gotcha/technique, with a pointer.\n\nHOW TO ANSWER\n- Be concise and concrete. If you are unsure, say so plainly rather than inventing platform behavior.\n- To propose a facet change, reply with ONE complete fenced block per changed facet, tagged html, css, or js. The user can apply such a block as a journaled whole-facet patch in one click — a partial snippet cannot be applied, so always return the full facet.\n- Command-body suggestions: a fenced block tagged rust or python containing the full body.`;\n\n// Fetched lazily at ask time (the connection exists by then; module\n// install happens before it does). Cached per page; a failed fetch\n// clears the cache so the next ask retries, and the error is LOUD —\n// a silently thin prompt regresses the confabulation lesson.\nlet corePromise = null;\nexport function corePrompt() {\n  if (!corePromise) {\n    corePromise = (async () => {\n      const r = await store.readFacet(\"dev\", \"prompts\", \"prompt\");\n      if (r?.status === \"ok\" && r.exists && (r.source ?? \"\").trim()) {\n        return r.source.trim();\n      }\n      corePromise = null;\n      throw new Error(\"dev.prompts `prompt` facet unavailable — the platform curriculum is store-resident (docs/prompting.md)\");\n    })();\n  }\n  return corePromise;\n}\n\n// The tool model (owner's design, 2026-07-25): DISCOVERY is always on,\n// ATTACHMENT is a context optimization, AUTHORIZATION happens per call.\nexport const TOOLS_PROMPT = `\n\nTOOLS\nNewbound commands are your tools, named library-control-command (e.g. dev-code-read_command). Call them through the tool-calling interface — never by describing a call in text. Every call you make appears as a cell in the user's notebook.\n\n- A core set is attached with full schemas. EVERY other platform command is also available: use find_tools(query) to discover commands for a task, describe_tool(name) for a full schema, and call_command(name, args) to invoke anything not directly attached. Never claim a capability is missing until find_tools says so.\n- Authorization is per call, not per tool: read-only commands run immediately; anything mutating or potentially destructive stops for the user's typed confirmation first — so do not hesitate to REQUEST a powerful command when the task needs it; the user decides at the moment of use. A denial is the user's decision: respect it and continue without that action.\n- Read before you write. For edits prefer the journaled, revertible commands: dev-code-patch_control_facet for UI facets, dev-code-patch_command_body / upsert_command for command bodies, dev-code-read_flow_body / write_flow_body for flow commands.`;\n\n// Newbound meta types -> Rust types, for synthesized signatures (ported\n// from control_query's lookup_rust_api_data_type).\nconst RUST_TYPE = {\n  FLAT: \"DataObject\", JSONObject: \"DataObject\", JSONArray: \"DataArray\",\n  InputStream: \"DataBytes\", float: \"f64\", Integer: \"i64\", Boolean: \"bool\",\n  Any: \"Data\", NULL: \"DNull\",\n};\nconst rustType = (t) => RUST_TYPE[t] ?? \"String\";\n\n/** The context prompt, ported from control_query: an editing preamble plus\n    one fence per included piece, tagged the way the backend prompt always\n    tagged them. `included` = chatctx snapshot entries the user checked. */\nexport function contextBlock(included) {\n  const merged = {};\n  for (const p of included) Object.assign(merged, p.fields);\n  const { lib, ctl } = merged;\n  let out = \"\";\n  if (lib || ctl) {\n    out = `We are editing the ${ctl} control in the ${lib} library.`;\n    for (const facet of [\"html\", \"css\", \"js\"]) {\n      const v = (merged[facet] ?? \"\").trim?.() ?? \"\";\n      if (v) out += `\\n\\n\\`\\`\\`${lib}:${ctl}.${facet}\\n${v}\\n\\`\\`\\``;\n    }\n    if (merged.code && merged.cmdname) {\n      const lang = merged.lang === \"rust\" ? \"rs\" : merged.lang === \"python\" ? \"py\" : merged.lang;\n      let signature = \"\";\n      if (merged.cmdparams) {\n        const ps = merged.cmdparams.map((p) =>\n          lang === \"rs\" ? `${p.name}: ${rustType(p.type)}` : p.name).join(\", \");\n        signature = lang === \"rs\"\n          ? `// fn ${merged.cmdname}(${ps}) -> ${rustType(merged.cmdreturn ?? \"String\")}\\n`\n          : `# def ${merged.cmdname}(${ps}):\\n`;\n      }\n      out += `\\n\\n\\`\\`\\`${lib}:${ctl}:${merged.cmdname}.${lang}\\n${signature}${merged.code.trim()}\\n\\`\\`\\``;\n    }\n    if (merged.flow && merged.cmdname) {\n      out += `\\n\\n\\`\\`\\`${lib}:${ctl}:${merged.cmdname}.flow\\n${merged.flow}\\n\\`\\`\\``;\n    }\n  }\n  if (merged.memory) {\n    out += (out ? \"\\n\\n\" : \"\") + `\\`\\`\\`memory:index\\n${merged.memory.trim()}\\n\\`\\`\\``;\n  }\n  return out;\n}\n\nexport const clamp = (s, n = 2000) =>\n  s.length <= n ? s : s.slice(0, n) + `\\n…[${s.length - n} chars clipped]`;\n\n/** MCP tool descriptors -> OpenAI function defs, enabled names only. */\nexport function toolDefs(mcpTools, enabledNames) {\n  const on = new Set(enabledNames);\n  return (mcpTools ?? [])\n    .filter((t) => on.has(t.name))\n    .map((t) => ({\n      type: \"function\",\n      function: {\n        name: t.name,\n        description: t.description ?? \"\",\n        parameters: t.inputSchema ?? { type: \"object\", properties: {} },\n      },\n    }));\n}\n\n// ── the tool model: defaults, meta-tools, gating ──────────\n\n// Attached-by-default: the read family (auto-run) plus the journaled\n// workhorses (each call confirm-gated). Everything else stays reachable\n// through call_command. The picker stores per-user add/remove overrides.\nexport const DEFAULT_TOOLS = [\n  \"dev-code-read_control_facet\", \"dev-code-read_command\",\n  \"dev-code-read_flow_body\", \"dev-code-list_control_patches\",\n  \"dev-code-list_libraries\", \"dev-code-list_controls\",\n  \"dev-code-list_commands\", \"dev-code-list_assets\",\n  \"dev-code-search_commands\",\n  \"dev-code-patch_control_facet\", \"dev-code-patch_command_body\",\n  \"dev-code-upsert_command\", \"dev-code-write_flow_body\",\n  \"dev-code-invoke_command\", \"dev-code-evaluate_rust\",\n  \"dev-code-remember\",\n];\n\n/** The always-attached meta-tools: discovery + the gateway. */\nexport const META_TOOL_DEFS = [\n  {\n    type: \"function\",\n    function: {\n      name: \"find_tools\",\n      description: \"Search ALL platform commands (not just attached tools) by keywords against their names and descriptions. Returns matches with their authorization tier. Use this before concluding a capability is missing.\",\n      parameters: { type: \"object\",\n        properties: { query: { type: \"string\", description: \"Keywords, e.g. 'delete asset' or 'rust eval'\" } },\n        required: [\"query\"] },\n    },\n  },\n  {\n    type: \"function\",\n    function: {\n      name: \"describe_tool\",\n      description: \"Full descriptor (description + JSON Schema of arguments) for one platform command by its lib-ctl-cmd name. Use before call_command when unsure of the arguments.\",\n      parameters: { type: \"object\",\n        properties: { name: { type: \"string\", description: \"Tool name, e.g. dev-code-delete_asset\" } },\n        required: [\"name\"] },\n    },\n  },\n  {\n    type: \"function\",\n    function: {\n      name: \"call_command\",\n      description: \"Invoke ANY platform command by its lib-ctl-cmd name — attached or not. Arguments are validated against the command's schema; mutating commands stop for the user's confirmation like every other call.\",\n      parameters: { type: \"object\",\n        properties: {\n          name: { type: \"string\", description: \"Tool name, e.g. dev-code-delete_asset\" },\n          args: { type: \"object\", description: \"The command's arguments, per its schema\" },\n        },\n        required: [\"name\", \"args\"] },\n    },\n  },\n];\n\nconst READ_TOOL_PREFIXES = [\n  \"list\", \"read\", \"get\", \"search\", \"describe\", \"lib_info\", \"check\",\n  \"current\", \"info\", \"peers\", \"libs\", \"apps\", \"assets\", \"asset\", \"lookup\",\n];\n\n/** Authorization tier for a catalog entry: \"auto\" (read — runs directly)\n    or \"confirm\" (stops at the typed confirm). The TAGS convention wins\n    when present — a command whose MCP tags include agent-auto or\n    agent-confirm is classified by the owner (describe() forwards the\n    record's `tags` field once the flowlang side does; empty today,\n    forward-compatible). `groups` is SECURITY groups and is never read\n    for gating. Fallback: the read-prefix heuristic on the command name. */\nexport function gateFor(name, entry) {\n  const tags = entry?.tags ?? [];\n  if (tags.includes(\"agent-confirm\")) return \"confirm\";\n  if (tags.includes(\"agent-auto\")) return \"auto\";\n  const cmd = parseToolName(name)?.cmd ?? name;\n  return READ_TOOL_PREFIXES.some((p) => cmd === p || cmd.startsWith(p + \"_\"))\n    ? \"auto\" : \"confirm\";\n}\n\n/** Light client-side check of args against a catalog inputSchema — enough\n    for the model to self-correct: missing required keys and gross type\n    mismatches, reported together. Null = fine. */\nexport function schemaComplaints(schema, args) {\n  if (!schema || typeof args !== \"object\" || args === null) return null;\n  const problems = [];\n  for (const req of schema.required ?? []) {\n    if (!(req in args)) problems.push(`missing required argument '${req}'`);\n  }\n  const kinds = { string: \"string\", number: \"number\", boolean: \"boolean\" };\n  for (const [k, v] of Object.entries(args)) {\n    const spec = schema.properties?.[k];\n    if (!spec) continue;\n    if (spec.type in kinds && typeof v !== kinds[spec.type]) {\n      problems.push(`argument '${k}' should be a ${spec.type}`);\n    } else if (spec.type === \"object\" && (typeof v !== \"object\" || v === null || Array.isArray(v))) {\n      problems.push(`argument '${k}' should be an object`);\n    } else if (spec.type === \"array\" && !Array.isArray(v)) {\n      problems.push(`argument '${k}' should be an array`);\n    }\n  }\n  return problems.length ? problems.join(\"; \") : null;\n}\n\n/** Search the catalog for find_tools: token match on name + description. */\nexport function searchCatalog(catalog, query, limit = 12) {\n  const tokens = (query ?? \"\").toLowerCase().split(/[^a-z0-9_]+/).filter(Boolean);\n  if (!tokens.length) return [];\n  const scored = [];\n  for (const t of catalog) {\n    const hay = (t.name + \" \" + (t.description ?? \"\")).toLowerCase();\n    const hits = tokens.filter((tok) => hay.includes(tok)).length;\n    if (hits) scored.push([hits, t]);\n  }\n  scored.sort((a, b) => b[0] - a[0]);\n  return scored.slice(0, limit).map(([, t]) => ({\n    name: t.name,\n    gate: gateFor(t.name, t),\n    summary: (t.summary ?? t.description ?? \"\").slice(0, 180),\n  }));\n}\n\n/** \"lib-ctl-cmd\" -> {lib, ctl, cmd} (ctl may itself contain dashes). */\nexport function parseToolName(name) {\n  const parts = name.split(\"-\");\n  if (parts.length < 3) return null;\n  return { lib: parts[0], ctl: parts.slice(1, -1).join(\"-\"), cmd: parts.at(-1) };\n}\n\n/**\n * One conversational turn, driving chat_llm until it answers in text.\n * execTool(call) is the session's: it renders the cell, gates mutating\n * commands behind the typed confirm, runs it, and resolves the tool-result\n * CONTENT string (or a denial note). Returns the final text.\n */\nexport async function chatTurn({ messages, tools, execTool, onRound }) {\n  for (let round = 0; round < MAX_ROUNDS; round++) {\n    const r = await store.chatLlm(messages, tools);\n    if (r.status !== \"ok\") throw new Error(r.msg ?? \"chat_llm failed\");\n    const d = r.data ?? {};\n    if (d.kind !== \"tool_calls\") {\n      return d.content ?? \"\";\n    }\n    messages.push(d.assistant_message);\n    onRound?.(d.tool_calls);\n    for (const call of d.tool_calls) {\n      const content = await execTool(call);\n      messages.push({ role: \"tool\", tool_call_id: call.id, content });\n    }\n  }\n  return \"(stopped: the agent used its whole tool budget without a final answer)\";\n}\n","patch_id":"p22","time":1785515591693},{"author":"claude","facet":"js","label":"chatTurn: surface inner chat_llm errors","new":"    if (r.status !== \"ok\") throw new Error(r.msg ?? \"chat_llm failed\");\n    const d = r.data ?? {};\n    // Failures ride INSIDE the FLAT envelope: wrapper-caught panics as\n    // {status:\"err\", msg} (a missing VLLM config lands here), chat_llm's own\n    // transport/parse errors as {kind:\"error\", content}. Returning either as\n    // a normal reply renders an EMPTY or misleading cell — throw, so every\n    // venue's catch shows an honest error.\n    if (d.status === \"err\") throw new Error(d.msg ?? \"chat_llm failed\");\n    if (d.kind === \"error\") throw new Error(d.content ?? \"chat_llm failed\");\n    if (d.kind !== \"tool_calls\") {","old":"    if (r.status !== \"ok\") throw new Error(r.msg ?? \"chat_llm failed\");\n    const d = r.data ?? {};\n    if (d.kind !== \"tool_calls\") {","patch_id":"p23","time":1785518941533},{"author":"claude","facet":"js","label":"chatTurn: elide duplicate tool results","new":"    onRound?.(d.tool_calls);\n    for (const call of d.tool_calls) {\n      const content = await execTool(call);\n      // Context guard: if an identical result already sits in this turn's\n      // messages (the model re-read an unchanged facet — a real trace showed\n      // the same 26KB source pulled three times), elide the duplicate TEXT.\n      // The tool still ran; only the repeated payload is replaced.\n      const prior = seen.get(content);\n      messages.push({ role: \"tool\", tool_call_id: call.id,\n        content: prior !== undefined && content.length > 500\n          ? `[result identical to the ${prior} call earlier this turn — unchanged; reuse that output]`\n          : content });\n      if (prior === undefined) seen.set(content, call.function?.name ?? \"tool\");\n    }","old":"    onRound?.(d.tool_calls);\n    for (const call of d.tool_calls) {\n      const content = await execTool(call);\n      messages.push({ role: \"tool\", tool_call_id: call.id, content });\n    }","patch_id":"p24","time":1785580650232},{"author":"claude","facet":"js","label":"chatTurn: elide duplicate tool results","new":"export async function chatTurn({ messages, tools, execTool, onRound }) {\n  const seen = new Map();   // result text -> tool name, for the context guard\n  for (let round = 0; round < MAX_ROUNDS; round++) {","old":"export async function chatTurn({ messages, tools, execTool, onRound }) {\n  for (let round = 0; round < MAX_ROUNDS; round++) {","patch_id":"p25","time":1785580650236},{"author":"claude","facet":"js","label":"find_tools: name hits outrank description hits","new":"  const scored = [];\n  for (const t of catalog) {\n    const name = t.name.toLowerCase();\n    const desc = (t.description ?? \"\").toLowerCase();\n    // Name hits must dominate: nearly every DESCRIPTION contains \"newbound\"\n    // or \"command\", so flat token counting degenerated to catalog order (a\n    // real trace ranked close_stream above read_command for \"read command\").\n    let score = 0;\n    let matched = 0;\n    for (const tok of tokens) {\n      if (name.includes(tok)) { score += 5; matched++; }\n      else if (desc.includes(tok)) { score += 1; matched++; }\n    }\n    if (!matched) continue;\n    if (matched === tokens.length) score += 3;\n    scored.push([score, t]);\n  }\n  scored.sort((a, b) => b[0] - a[0]);","old":"  const scored = [];\n  for (const t of catalog) {\n    const hay = (t.name + \" \" + (t.description ?? \"\")).toLowerCase();\n    const hits = tokens.filter((tok) => hay.includes(tok)).length;\n    if (hits) scored.push([hits, t]);\n  }\n  scored.sort((a, b) => b[0] - a[0]);","patch_id":"p26","time":1785584383405},{"author":"claude","facet":"js","label":"chatTurn: toolless wrap-up on budget exhaustion","new":"  // Budget exhausted. A bare stop misreports turns whose LAST round did the\n  // work (a real trace: the successful whole-facet patch landed on round 10\n  // and the user was told the agent stopped without an answer). Force one\n  // toolless wrap-up call; fall back to the honest stop line if it fails.\n  messages.push({ role: \"user\", content:\n    \"[Tool budget exhausted. Do not request tools. Answer NOW from what you already have: what you completed, what succeeded or failed (name any patch ids), and what remains undone.]\" });\n  try {\n    const r = await store.chatLlm(messages, []);\n    if (r.status === \"ok\") {\n      const d = r.data ?? {};\n      if (d.status !== \"err\" && d.kind !== \"error\" && d.kind !== \"tool_calls\"\n          && (d.content ?? \"\").trim()) {\n        return d.content;\n      }\n    }\n  } catch { /* fall through to the honest stop */ }\n  return \"(stopped: the agent used its whole tool budget without a final answer)\";\n}","old":"  return \"(stopped: the agent used its whole tool budget without a final answer)\";\n}","patch_id":"p27","time":1785586550767},{"author":"claude","facet":"js","label":"chatTurn injects the mode pack","new":"export async function chatTurn({ messages, tools, execTool, onRound }) {\n  const seen = new Map();   // result text -> tool name, for the context guard\n  // Recall layer 4: the mode-keyed pack for THIS ask, injected as a\n  // second system message on every provider call but never written into\n  // the venue's own messages array — the conversation the venue keeps\n  // must not accumulate a pack per turn. Rebuilt per call because the\n  // loop pushes tool exchanges into `messages` as it goes. One\n  // implementation covers every venue that speaks through chatTurn.\n  let pack = null;\n  try {\n    await memoryReady;\n    const ask = [...messages].reverse().find((m) => m.role === \"user\")?.content ?? \"\";\n    pack = packFor(ask);\n  } catch { pack = null; }\n  globalThis.__nbAgentPack = pack\n    ? { modes: pack.modes, count: pack.count, total: pack.total, at: Date.now() }\n    : null;   // instrumentation: a failed turn should show what was injected\n  const convo = () => {\n    if (!pack) return messages;\n    const p = { role: \"system\", content: pack.block };\n    return messages[0]?.role === \"system\"\n      ? [messages[0], p, ...messages.slice(1)] : [p, ...messages];\n  };\n  for (let round = 0; round < MAX_ROUNDS; round++) {\n    const r = await store.chatLlm(convo(), tools);","old":"export async function chatTurn({ messages, tools, execTool, onRound }) {\n  const seen = new Map();   // result text -> tool name, for the context guard\n  for (let round = 0; round < MAX_ROUNDS; round++) {\n    const r = await store.chatLlm(messages, tools);","patch_id":"p28","time":1785938988622},{"author":"claude","facet":"js","label":"the wrap-up call carries the pack","new":"    const r = await store.chatLlm(convo(), []);","old":"    const r = await store.chatLlm(messages, []);","patch_id":"p29","time":1785938988627},{"author":"claude","facet":"js","label":"agentloop imports the pack builder","new":"import { store } from \"./store.js\";\nimport { packFor, ready as memoryReady } from \"./memory.js\";","old":"import { store } from \"./store.js\";","patch_id":"p30","time":1785939004335},{"author":"claude","facet":"js","label":"one system message: append the pack","new":"  // Recall layer 4: the mode-keyed pack for THIS ask, APPENDED to the one\n  // system message on every provider call but never written into the\n  // venue's own messages array — the conversation the venue keeps must\n  // not accumulate a pack per turn. Rebuilt per call because the loop\n  // pushes tool exchanges into `messages` as it goes. One implementation\n  // covers every venue that speaks through chatTurn.","old":"  // Recall layer 4: the mode-keyed pack for THIS ask, injected as a\n  // second system message on every provider call but never written into\n  // the venue's own messages array — the conversation the venue keeps\n  // must not accumulate a pack per turn. Rebuilt per call because the\n  // loop pushes tool exchanges into `messages` as it goes. One\n  // implementation covers every venue that speaks through chatTurn.","patch_id":"p31","time":1785964059810},{"author":"claude","facet":"js","label":"one system message: append the pack","new":"  // ONE system message, at index 0. vLLM's chat template raises\n  // \"System message must be at the beginning.\" for a system message at\n  // any later index — including index 1, ahead of every user turn — so\n  // the pack is APPENDED to the existing system prompt rather than\n  // inserted beside it. Appending (not prepending) also keeps the core\n  // curriculum as a stable prefix, so server-side prefix caching still\n  // hits. The venue's own array is never mutated; the merged copy lives\n  // only for the duration of the call.\n  const convo = () => {\n    if (!pack) return messages;\n    if (messages[0]?.role === \"system\") {\n      return [{ ...messages[0], content: messages[0].content + \"\\n\\n\" + pack.block },\n              ...messages.slice(1)];\n    }\n    return [{ role: \"system\", content: pack.block }, ...messages];\n  };","old":"  const convo = () => {\n    if (!pack) return messages;\n    const p = { role: \"system\", content: pack.block };\n    return messages[0]?.role === \"system\"\n      ? [messages[0], p, ...messages.slice(1)] : [p, ...messages];\n  };","patch_id":"p32","time":1785964059814},{"author":"Claude","facet":"js","label":"agentloop: own the library wire (store drops add-on API)","new":"import { store } from \"./store.js\";\nimport { packFor, ready as memoryReady } from \"./memory.js\";\n\n// ── this library's own wire ─────────────────────────────────\n// The platform's store carries no add-on-specific API: the provider owns\n// its library name and command names. Everything below rides the store's\n// generic by-name surface; resolution failures come back as err envelopes\n// (never throws), so callers branch on status only.\nasync function agentCall(ctl, cmd, args) {\n  const r = await store.invoke(\"agent\", ctl, cmd, args);\n  return r instanceof Error ? { status: \"err\", msg: r.message } : r.envelope;\n}\n\n/** The MCP tool list (agent.plugin.list_tools, FLAT {tools:[...]}). */\nexport function listTools() {\n  return agentCall(\"plugin\", \"list_tools\", {});\n}\n\n/** One chat_llm completion (agent.llm — the OpenAI-compatible bridge to\n    the instance's configured model). JSONObject envelope: the payload\n    ({kind, content | tool_calls, assistant_message}) sits under data.\n    chatTurn below drives this directly — control_query and tool_loop are\n    deliberately not called. */\nexport function chatLlm(messages, tools) {\n  return agentCall(\"llm\", \"chat_llm\", { messages, tools });\n}\n\n/** Draft a command description (agent.plugin.describe_command, String). */\nexport function describeCommand(args) {\n  return agentCall(\"plugin\", \"describe_command\", args);\n}\n\n/** The archivist's intake (docs/memory.md) — the session fires and\n    forgets; resolution failures resolve to Error values, not rejections. */\nexport function logTurn(entry) {\n  return store.invoke(\"agent\", \"archivist\", \"log_turn\", entry);\n}\n\n/** chat_llm reads system.apps.agent.runtime (VLLM_URL/VLLM_MODEL) —\n    absent when the agent app isn't exposed. The session shows whatever\n    non-empty string this returns beneath a failed ask. */\nexport function errorHint(msg) {\n  if (/Key '(agent|VLLM_URL|VLLM_MODEL)' not found/.test(msg ?? \"\")) {\n    return \"This instance's agent app isn't configured: add `agent` to \" +\n      \"config.properties apps=, put VLLM_URL and VLLM_MODEL in \" +\n      \"runtime/agent/botd.properties, and restart \" +\n      \"(tools/scratch-instance.md has the recipe).\";\n  }\n  return \"\";\n}","old":"import { store } from \"./store.js\";\nimport { packFor, ready as memoryReady } from \"./memory.js\";","patch_id":"p33","time":1786366301226},{"author":"Claude","facet":"js","label":"agentloop: own the library wire (store drops add-on API)","new":"    const r = await chatLlm(convo(), tools);","old":"    const r = await store.chatLlm(convo(), tools);","patch_id":"p34","time":1786366301235},{"author":"Claude","facet":"js","label":"agentloop: own the library wire (store drops add-on API)","new":"    const r = await chatLlm(convo(), []);","old":"    const r = await store.chatLlm(convo(), []);","patch_id":"p35","time":1786366301243},{"author":"Claude","facet":"js","label":"the curriculum lives with the agent now","new":"      const r = await store.readFacet(\"agent\", \"prompts\", \"prompt\");","old":"      const r = await store.readFacet(\"dev\", \"prompts\", \"prompt\");","patch_id":"p36","time":1786375242605},{"author":"Claude","facet":"js","label":"viewctx","new":"    tagged them. `included` = viewctx snapshot entries the user checked. */","old":"    tagged them. `included` = chatctx snapshot entries the user checked. */","patch_id":"p37","time":1786375562175},{"author":"claude","facet":"js","label":"collapse: library control / api.js primitives (module layer retired)","new":"// agentloop.js — the client side of \"chat with the code\", the way the\n// owner called it (2026-07-25): skip control_query AND tool_loop, call\n// agent.llm.chat_llm directly. This module owns what control_query used to\n// do server-side — the system prompt, the context fences, the history —\n// plus the tool loop the notebook can actually SHOW: chat_llm returns\n// normalized tool_calls; the session renders each as a cell and gates\n// mutating ones behind its typed confirm before the loop continues.\n//\n// chat_llm contract (agent.llm, JSONObject envelope — payload under data):\n//   in:  messages (system/user/assistant/tool), tools (OpenAI function defs)\n//   out: {kind:\"text\", content, assistant_message}\n//      | {kind:\"tool_calls\", tool_calls:[{id,name,arguments(JSON string)}],\n//         assistant_message}   — assistant_message goes back on the list.\n// MCP tools (agent.plugin.list_tools) arrive as {name:\"lib-ctl-cmd\",\n// description, inputSchema} — inputSchema is a real JSON Schema.\n\n\n// ── this library's own wire ─────────────────────────────────\n// The platform's store carries no add-on-specific API: the provider owns\n// its library name and command names. Everything below rides the store's\n// generic by-name surface; resolution failures come back as err envelopes\n// (never throws), so callers branch on status only.\n//\n// LIBRARY control — headless: defines window.NB_AGENTLOOP once (idempotent across\n// installs). Consumers list this control as a hidden data-control child\n// div and use the global from their ready.\n\nvar me = this;\nvar ME = document.getElementById(me.UUID);\n\nme.ready = function () {\n  if (window.NB_AGENTLOOP) return;\n  const { packFor, ready: memoryReady } = window.NB_MEMORY;\n  const jsonP = (c2, v2) => new Promise((res2) => json(c2, v2, res2));\n  const invokeP = (l2, c2, m2, a2) => new Promise((res2) => invokeCommand(l2, c2, m2, a2, res2));\n  const invoke = async (l2, c2, m2, a2) => {\n    const t0 = performance.now();\n    const envelope = await invokeP(l2, c2, m2, a2);\n    return { envelope, ms: Math.round(performance.now() - t0) };\n  };\n  const code = (m2, a2) => invokeP(\"dev\", \"code\", m2, a2);\n  const readFacet = (l2, c2, f2) => code(\"read_control_facet\", { lib: l2, ctl: c2, facet: f2 });\n  window.NB_AGENTLOOP = (function () {\n\nasync function agentCall(ctl, cmd, args) {\n  const r = await invoke(\"agent\", ctl, cmd, args);\n  return r.envelope;\n}\n\n/** The MCP tool list (agent.plugin.list_tools, FLAT {tools:[...]}). */\nfunction listTools() {\n  return agentCall(\"plugin\", \"list_tools\", {});\n}\n\n/** One chat_llm completion (agent.llm — the OpenAI-compatible bridge to\n    the instance's configured model). JSONObject envelope: the payload\n    ({kind, content | tool_calls, assistant_message}) sits under data.\n    chatTurn below drives this directly — control_query and tool_loop are\n    deliberately not called. */\nfunction chatLlm(messages, tools) {\n  return agentCall(\"llm\", \"chat_llm\", { messages, tools });\n}\n\n/** Draft a command description (agent.plugin.describe_command, String). */\nfunction describeCommand(args) {\n  return agentCall(\"plugin\", \"describe_command\", args);\n}\n\n/** The archivist's intake (docs/memory.md) — the session fires and\n    forgets; resolution failures resolve to Error values, not rejections. */\nfunction logTurn(entry) {\n  return invoke(\"agent\", \"archivist\", \"log_turn\", entry);\n}\n\n/** chat_llm reads system.apps.agent.runtime (VLLM_URL/VLLM_MODEL) —\n    absent when the agent app isn't exposed. The session shows whatever\n    non-empty string this returns beneath a failed ask. */\nfunction errorHint(msg) {\n  if (/Key '(agent|VLLM_URL|VLLM_MODEL)' not found/.test(msg ?? \"\")) {\n    return \"This instance's agent app isn't configured: add `agent` to \" +\n      \"config.properties apps=, put VLLM_URL and VLLM_MODEL in \" +\n      \"runtime/agent/botd.properties, and restart \" +\n      \"(tools/scratch-instance.md has the recipe).\";\n  }\n  return \"\";\n}\n\n// Generous: the autonomous developer workflow legitimately spends rounds\n// on compile-debug cycles.\nconst MAX_ROUNDS = 10;\n\n// The system prompt lives client-side now — the cost of skipping\n// control_query, and the point: the bench knows what the model is looking\n// at and how its replies get used. The platform-knowledge sections are\n// the owner's, folded in from agent.llm.tool_loop's prompt (his call:\n// without them the model can't write ndata-correct code or reason about\n// the platform). Model-agnostic (Qwen or Claude).\n// The platform-knowledge CORE lives in the store (dev.prompts `prompt`\n// facet — docs/prompting.md): ONE journaled curriculum shared with the\n// agent app's tool_loop. SYSTEM_PROMPT here is the notebook's SHELL —\n// venue, fences, memory, answer style; session assembles core + shell +\n// TOOLS_PROMPT + the owner addendum at ask time.\nconst SYSTEM_PROMPT = `Right now you are speaking through the session notebook of Bench, the Newbound IDE — the user sees every tool call you make as a notebook cell.\n\nWHAT YOU SEE\n- Current code arrives in fenced blocks labeled lib:ctl.facet (facets) or lib:ctl:command.lang (command bodies; the first line may be a synthesized signature comment). A flow body arrives as JSON labeled lib:ctl:command.flow.\n- \"Recent notebook activity\" lists commands run in this session, with OUTPUTS TRUNCATED. Never conclude that something is absent because you don't see it in a truncated result — rerun the command via a tool when you need the full output.\n\nMEMORY IN THIS NOTEBOOK\n- Your context includes a memory:index fence: every kb domain with entry counts, tags, staleness marks (\"N stale?\" — re-read those referents before trusting the marked entries), and entries pushed for the surface open on the bench (\"Relevant now\").\n- Memory FORMATION is automatic here: the archivist reviews completed sessions in the background and files what is durable (journaled, tagged unreviewed). Do not file memories on your own initiative. When the user explicitly asks you to remember something, call dev-code-remember — it runs without confirmation, because the request itself is the authorization.\n\nHOW TO ANSWER\n- Be concise and concrete. If you are unsure, say so plainly rather than inventing platform behavior.\n- To propose a facet change, reply with ONE complete fenced block per changed facet, tagged html, css, or js. The user can apply such a block as a journaled whole-facet patch in one click — a partial snippet cannot be applied, so always return the full facet.\n- Command-body suggestions: a fenced block tagged rust or python containing the full body.`;\n\n// Fetched lazily at ask time (the connection exists by then; module\n// install happens before it does). Cached per page; a failed fetch\n// clears the cache so the next ask retries, and the error is LOUD —\n// a silently thin prompt regresses the confabulation lesson.\nlet corePromise = null;\nfunction corePrompt() {\n  if (!corePromise) {\n    corePromise = (async () => {\n      const r = await readFacet(\"agent\", \"prompts\", \"prompt\");\n      if (r?.status === \"ok\" && r.exists && (r.source ?? \"\").trim()) {\n        return r.source.trim();\n      }\n      corePromise = null;\n      throw new Error(\"dev.prompts `prompt` facet unavailable — the platform curriculum is store-resident (docs/prompting.md)\");\n    })();\n  }\n  return corePromise;\n}\n\n// The tool model (owner's design, 2026-07-25): DISCOVERY is always on,\n// ATTACHMENT is a context optimization, AUTHORIZATION happens per call.\nconst TOOLS_PROMPT = `\n\nTOOLS\nNewbound commands are your tools, named library-control-command (e.g. dev-code-read_command). Call them through the tool-calling interface — never by describing a call in text. Every call you make appears as a cell in the user's notebook.\n\n- A core set is attached with full schemas. EVERY other platform command is also available: use find_tools(query) to discover commands for a task, describe_tool(name) for a full schema, and call_command(name, args) to invoke anything not directly attached. Never claim a capability is missing until find_tools says so.\n- Authorization is per call, not per tool: read-only commands run immediately; anything mutating or potentially destructive stops for the user's typed confirmation first — so do not hesitate to REQUEST a powerful command when the task needs it; the user decides at the moment of use. A denial is the user's decision: respect it and continue without that action.\n- Read before you write. For edits prefer the journaled, revertible commands: dev-code-patch_control_facet for UI facets, dev-code-patch_command_body / upsert_command for command bodies, dev-code-read_flow_body / write_flow_body for flow commands.`;\n\n// Newbound meta types -> Rust types, for synthesized signatures (ported\n// from control_query's lookup_rust_api_data_type).\nconst RUST_TYPE = {\n  FLAT: \"DataObject\", JSONObject: \"DataObject\", JSONArray: \"DataArray\",\n  InputStream: \"DataBytes\", float: \"f64\", Integer: \"i64\", Boolean: \"bool\",\n  Any: \"Data\", NULL: \"DNull\",\n};\nconst rustType = (t) => RUST_TYPE[t] ?? \"String\";\n\n/** The context prompt, ported from control_query: an editing preamble plus\n    one fence per included piece, tagged the way the backend prompt always\n    tagged them. `included` = viewctx snapshot entries the user checked. */\nfunction contextBlock(included) {\n  const merged = {};\n  for (const p of included) Object.assign(merged, p.fields);\n  const { lib, ctl } = merged;\n  let out = \"\";\n  if (lib || ctl) {\n    out = `We are editing the ${ctl} control in the ${lib} library.`;\n    for (const facet of [\"html\", \"css\", \"js\"]) {\n      const v = (merged[facet] ?? \"\").trim?.() ?? \"\";\n      if (v) out += `\\n\\n\\`\\`\\`${lib}:${ctl}.${facet}\\n${v}\\n\\`\\`\\``;\n    }\n    if (merged.code && merged.cmdname) {\n      const lang = merged.lang === \"rust\" ? \"rs\" : merged.lang === \"python\" ? \"py\" : merged.lang;\n      let signature = \"\";\n      if (merged.cmdparams) {\n        const ps = merged.cmdparams.map((p) =>\n          lang === \"rs\" ? `${p.name}: ${rustType(p.type)}` : p.name).join(\", \");\n        signature = lang === \"rs\"\n          ? `// fn ${merged.cmdname}(${ps}) -> ${rustType(merged.cmdreturn ?? \"String\")}\\n`\n          : `# def ${merged.cmdname}(${ps}):\\n`;\n      }\n      out += `\\n\\n\\`\\`\\`${lib}:${ctl}:${merged.cmdname}.${lang}\\n${signature}${merged.code.trim()}\\n\\`\\`\\``;\n    }\n    if (merged.flow && merged.cmdname) {\n      out += `\\n\\n\\`\\`\\`${lib}:${ctl}:${merged.cmdname}.flow\\n${merged.flow}\\n\\`\\`\\``;\n    }\n  }\n  if (merged.memory) {\n    out += (out ? \"\\n\\n\" : \"\") + `\\`\\`\\`memory:index\\n${merged.memory.trim()}\\n\\`\\`\\``;\n  }\n  return out;\n}\n\nconst clamp = (s, n = 2000) =>\n  s.length <= n ? s : s.slice(0, n) + `\\n…[${s.length - n} chars clipped]`;\n\n/** MCP tool descriptors -> OpenAI function defs, enabled names only. */\nfunction toolDefs(mcpTools, enabledNames) {\n  const on = new Set(enabledNames);\n  return (mcpTools ?? [])\n    .filter((t) => on.has(t.name))\n    .map((t) => ({\n      type: \"function\",\n      function: {\n        name: t.name,\n        description: t.description ?? \"\",\n        parameters: t.inputSchema ?? { type: \"object\", properties: {} },\n      },\n    }));\n}\n\n// ── the tool model: defaults, meta-tools, gating ──────────\n\n// Attached-by-default: the read family (auto-run) plus the journaled\n// workhorses (each call confirm-gated). Everything else stays reachable\n// through call_command. The picker stores per-user add/remove overrides.\nconst DEFAULT_TOOLS = [\n  \"dev-code-read_control_facet\", \"dev-code-read_command\",\n  \"dev-code-read_flow_body\", \"dev-code-list_control_patches\",\n  \"dev-code-list_libraries\", \"dev-code-list_controls\",\n  \"dev-code-list_commands\", \"dev-code-list_assets\",\n  \"dev-code-search_commands\",\n  \"dev-code-patch_control_facet\", \"dev-code-patch_command_body\",\n  \"dev-code-upsert_command\", \"dev-code-write_flow_body\",\n  \"dev-code-invoke_command\", \"dev-code-evaluate_rust\",\n  \"dev-code-remember\",\n];\n\n/** The always-attached meta-tools: discovery + the gateway. */\nconst META_TOOL_DEFS = [\n  {\n    type: \"function\",\n    function: {\n      name: \"find_tools\",\n      description: \"Search ALL platform commands (not just attached tools) by keywords against their names and descriptions. Returns matches with their authorization tier. Use this before concluding a capability is missing.\",\n      parameters: { type: \"object\",\n        properties: { query: { type: \"string\", description: \"Keywords, e.g. 'delete asset' or 'rust eval'\" } },\n        required: [\"query\"] },\n    },\n  },\n  {\n    type: \"function\",\n    function: {\n      name: \"describe_tool\",\n      description: \"Full descriptor (description + JSON Schema of arguments) for one platform command by its lib-ctl-cmd name. Use before call_command when unsure of the arguments.\",\n      parameters: { type: \"object\",\n        properties: { name: { type: \"string\", description: \"Tool name, e.g. dev-code-delete_asset\" } },\n        required: [\"name\"] },\n    },\n  },\n  {\n    type: \"function\",\n    function: {\n      name: \"call_command\",\n      description: \"Invoke ANY platform command by its lib-ctl-cmd name — attached or not. Arguments are validated against the command's schema; mutating commands stop for the user's confirmation like every other call.\",\n      parameters: { type: \"object\",\n        properties: {\n          name: { type: \"string\", description: \"Tool name, e.g. dev-code-delete_asset\" },\n          args: { type: \"object\", description: \"The command's arguments, per its schema\" },\n        },\n        required: [\"name\", \"args\"] },\n    },\n  },\n];\n\nconst READ_TOOL_PREFIXES = [\n  \"list\", \"read\", \"get\", \"search\", \"describe\", \"lib_info\", \"check\",\n  \"current\", \"info\", \"peers\", \"libs\", \"apps\", \"assets\", \"asset\", \"lookup\",\n];\n\n/** Authorization tier for a catalog entry: \"auto\" (read — runs directly)\n    or \"confirm\" (stops at the typed confirm). The TAGS convention wins\n    when present — a command whose MCP tags include agent-auto or\n    agent-confirm is classified by the owner (describe() forwards the\n    record's `tags` field once the flowlang side does; empty today,\n    forward-compatible). `groups` is SECURITY groups and is never read\n    for gating. Fallback: the read-prefix heuristic on the command name. */\n// remember runs WITHOUT confirmation on explicit user request — the ask\n// itself is the authorization (docs/memory.md); autonomous memory\n// formation belongs to the archivist, and the prompt says so.\nconst AUTO_OVERRIDES = new Set([\"dev-code-remember\"]);\n\nfunction gateFor(name, entry) {\n  if (AUTO_OVERRIDES.has(name)) return \"auto\";\n  const tags = entry?.tags ?? [];\n  if (tags.includes(\"agent-confirm\")) return \"confirm\";\n  if (tags.includes(\"agent-auto\")) return \"auto\";\n  const cmd = parseToolName(name)?.cmd ?? name;\n  return READ_TOOL_PREFIXES.some((p) => cmd === p || cmd.startsWith(p + \"_\"))\n    ? \"auto\" : \"confirm\";\n}\n\n/** Light client-side check of args against a catalog inputSchema — enough\n    for the model to self-correct: missing required keys and gross type\n    mismatches, reported together. Null = fine. */\nfunction schemaComplaints(schema, args) {\n  if (!schema || typeof args !== \"object\" || args === null) return null;\n  const problems = [];\n  for (const req of schema.required ?? []) {\n    if (!(req in args)) problems.push(`missing required argument '${req}'`);\n  }\n  const kinds = { string: \"string\", number: \"number\", boolean: \"boolean\" };\n  for (const [k, v] of Object.entries(args)) {\n    const spec = schema.properties?.[k];\n    if (!spec) continue;\n    if (spec.type in kinds && typeof v !== kinds[spec.type]) {\n      problems.push(`argument '${k}' should be a ${spec.type}`);\n    } else if (spec.type === \"object\" && (typeof v !== \"object\" || v === null || Array.isArray(v))) {\n      problems.push(`argument '${k}' should be an object`);\n    } else if (spec.type === \"array\" && !Array.isArray(v)) {\n      problems.push(`argument '${k}' should be an array`);\n    }\n  }\n  return problems.length ? problems.join(\"; \") : null;\n}\n\n/** Search the catalog for find_tools: token match on name + description. */\nfunction searchCatalog(catalog, query, limit = 12) {\n  const tokens = (query ?? \"\").toLowerCase().split(/[^a-z0-9_]+/).filter(Boolean);\n  if (!tokens.length) return [];\n  const scored = [];\n  for (const t of catalog) {\n    const name = t.name.toLowerCase();\n    const desc = (t.description ?? \"\").toLowerCase();\n    // Name hits must dominate: nearly every DESCRIPTION contains \"newbound\"\n    // or \"command\", so flat token counting degenerated to catalog order (a\n    // real trace ranked close_stream above read_command for \"read command\").\n    let score = 0;\n    let matched = 0;\n    for (const tok of tokens) {\n      if (name.includes(tok)) { score += 5; matched++; }\n      else if (desc.includes(tok)) { score += 1; matched++; }\n    }\n    if (!matched) continue;\n    if (matched === tokens.length) score += 3;\n    scored.push([score, t]);\n  }\n  scored.sort((a, b) => b[0] - a[0]);\n  return scored.slice(0, limit).map(([, t]) => ({\n    name: t.name,\n    gate: gateFor(t.name, t),\n    summary: (t.summary ?? t.description ?? \"\").slice(0, 180),\n  }));\n}\n\n/** \"lib-ctl-cmd\" -> {lib, ctl, cmd} (ctl may itself contain dashes). */\nfunction parseToolName(name) {\n  const parts = name.split(\"-\");\n  if (parts.length < 3) return null;\n  return { lib: parts[0], ctl: parts.slice(1, -1).join(\"-\"), cmd: parts.at(-1) };\n}\n\n/**\n * One conversational turn, driving chat_llm until it answers in text.\n * execTool(call) is the session's: it renders the cell, gates mutating\n * commands behind the typed confirm, runs it, and resolves the tool-result\n * CONTENT string (or a denial note). Returns the final text.\n */\nasync function chatTurn({ messages, tools, execTool, onRound }) {\n  const seen = new Map();   // result text -> tool name, for the context guard\n  // Recall layer 4: the mode-keyed pack for THIS ask, APPENDED to the one\n  // system message on every provider call but never written into the\n  // venue's own messages array — the conversation the venue keeps must\n  // not accumulate a pack per turn. Rebuilt per call because the loop\n  // pushes tool exchanges into `messages` as it goes. One implementation\n  // covers every venue that speaks through chatTurn.\n  let pack = null;\n  try {\n    await memoryReady;\n    const ask = [...messages].reverse().find((m) => m.role === \"user\")?.content ?? \"\";\n    pack = packFor(ask);\n  } catch { pack = null; }\n  globalThis.__nbAgentPack = pack\n    ? { modes: pack.modes, count: pack.count, total: pack.total, at: Date.now() }\n    : null;   // instrumentation: a failed turn should show what was injected\n  // ONE system message, at index 0. vLLM's chat template raises\n  // \"System message must be at the beginning.\" for a system message at\n  // any later index — including index 1, ahead of every user turn — so\n  // the pack is APPENDED to the existing system prompt rather than\n  // inserted beside it. Appending (not prepending) also keeps the core\n  // curriculum as a stable prefix, so server-side prefix caching still\n  // hits. The venue's own array is never mutated; the merged copy lives\n  // only for the duration of the call.\n  const convo = () => {\n    if (!pack) return messages;\n    if (messages[0]?.role === \"system\") {\n      return [{ ...messages[0], content: messages[0].content + \"\\n\\n\" + pack.block },\n              ...messages.slice(1)];\n    }\n    return [{ role: \"system\", content: pack.block }, ...messages];\n  };\n  for (let round = 0; round < MAX_ROUNDS; round++) {\n    const r = await chatLlm(convo(), tools);\n    if (r.status !== \"ok\") throw new Error(r.msg ?? \"chat_llm failed\");\n    const d = r.data ?? {};\n    // Failures ride INSIDE the FLAT envelope: wrapper-caught panics as\n    // {status:\"err\", msg} (a missing VLLM config lands here), chat_llm's own\n    // transport/parse errors as {kind:\"error\", content}. Returning either as\n    // a normal reply renders an EMPTY or misleading cell — throw, so every\n    // venue's catch shows an honest error.\n    if (d.status === \"err\") throw new Error(d.msg ?? \"chat_llm failed\");\n    if (d.kind === \"error\") throw new Error(d.content ?? \"chat_llm failed\");\n    if (d.kind !== \"tool_calls\") {\n      return d.content ?? \"\";\n    }\n    messages.push(d.assistant_message);\n    onRound?.(d.tool_calls);\n    for (const call of d.tool_calls) {\n      const content = await execTool(call);\n      // Context guard: if an identical result already sits in this turn's\n      // messages (the model re-read an unchanged facet — a real trace showed\n      // the same 26KB source pulled three times), elide the duplicate TEXT.\n      // The tool still ran; only the repeated payload is replaced.\n      const prior = seen.get(content);\n      messages.push({ role: \"tool\", tool_call_id: call.id,\n        content: prior !== undefined && content.length > 500\n          ? `[result identical to the ${prior} call earlier this turn — unchanged; reuse that output]`\n          : content });\n      if (prior === undefined) seen.set(content, call.function?.name ?? \"tool\");\n    }\n  }\n  // Budget exhausted. A bare stop misreports turns whose LAST round did the\n  // work (a real trace: the successful whole-facet patch landed on round 10\n  // and the user was told the agent stopped without an answer). Force one\n  // toolless wrap-up call; fall back to the honest stop line if it fails.\n  messages.push({ role: \"user\", content:\n    \"[Tool budget exhausted. Do not request tools. Answer NOW from what you already have: what you completed, what succeeded or failed (name any patch ids), and what remains undone.]\" });\n  try {\n    const r = await chatLlm(convo(), []);\n    if (r.status === \"ok\") {\n      const d = r.data ?? {};\n      if (d.status !== \"err\" && d.kind !== \"error\" && d.kind !== \"tool_calls\"\n          && (d.content ?? \"\").trim()) {\n        return d.content;\n      }\n    }\n  } catch { /* fall through to the honest stop */ }\n  return \"(stopped: the agent used its whole tool budget without a final answer)\";\n}\n\n    return { listTools, chatLlm, describeCommand, logTurn, errorHint, MAX_ROUNDS, SYSTEM_PROMPT, corePrompt, TOOLS_PROMPT, contextBlock, clamp, toolDefs, DEFAULT_TOOLS, META_TOOL_DEFS, gateFor, schemaComplaints, searchCatalog, parseToolName, chatTurn };\n  })();\n};\n","old":"// agentloop.js — the client side of \"chat with the code\", the way the\n// owner called it (2026-07-25): skip control_query AND tool_loop, call\n// agent.llm.chat_llm directly. This module owns what control_query used to\n// do server-side — the system prompt, the context fences, the history —\n// plus the tool loop the notebook can actually SHOW: chat_llm returns\n// normalized tool_calls; the session renders each as a cell and gates\n// mutating ones behind its typed confirm before the loop continues.\n//\n// chat_llm contract (agent.llm, JSONObject envelope — payload under data):\n//   in:  messages (system/user/assistant/tool), tools (OpenAI function defs)\n//   out: {kind:\"text\", content, assistant_message}\n//      | {kind:\"tool_calls\", tool_calls:[{id,name,arguments(JSON string)}],\n//         assistant_message}   — assistant_message goes back on the list.\n// MCP tools (agent.plugin.list_tools) arrive as {name:\"lib-ctl-cmd\",\n// description, inputSchema} — inputSchema is a real JSON Schema.\n\nimport { store } from \"./store.js\";\nimport { packFor, ready as memoryReady } from \"./memory.js\";\n\n// ── this library's own wire ─────────────────────────────────\n// The platform's store carries no add-on-specific API: the provider owns\n// its library name and command names. Everything below rides the store's\n// generic by-name surface; resolution failures come back as err envelopes\n// (never throws), so callers branch on status only.\nasync function agentCall(ctl, cmd, args) {\n  const r = await store.invoke(\"agent\", ctl, cmd, args);\n  return r instanceof Error ? { status: \"err\", msg: r.message } : r.envelope;\n}\n\n/** The MCP tool list (agent.plugin.list_tools, FLAT {tools:[...]}). */\nexport function listTools() {\n  return agentCall(\"plugin\", \"list_tools\", {});\n}\n\n/** One chat_llm completion (agent.llm — the OpenAI-compatible bridge to\n    the instance's configured model). JSONObject envelope: the payload\n    ({kind, content | tool_calls, assistant_message}) sits under data.\n    chatTurn below drives this directly — control_query and tool_loop are\n    deliberately not called. */\nexport function chatLlm(messages, tools) {\n  return agentCall(\"llm\", \"chat_llm\", { messages, tools });\n}\n\n/** Draft a command description (agent.plugin.describe_command, String). */\nexport function describeCommand(args) {\n  return agentCall(\"plugin\", \"describe_command\", args);\n}\n\n/** The archivist's intake (docs/memory.md) — the session fires and\n    forgets; resolution failures resolve to Error values, not rejections. */\nexport function logTurn(entry) {\n  return store.invoke(\"agent\", \"archivist\", \"log_turn\", entry);\n}\n\n/** chat_llm reads system.apps.agent.runtime (VLLM_URL/VLLM_MODEL) —\n    absent when the agent app isn't exposed. The session shows whatever\n    non-empty string this returns beneath a failed ask. */\nexport function errorHint(msg) {\n  if (/Key '(agent|VLLM_URL|VLLM_MODEL)' not found/.test(msg ?? \"\")) {\n    return \"This instance's agent app isn't configured: add `agent` to \" +\n      \"config.properties apps=, put VLLM_URL and VLLM_MODEL in \" +\n      \"runtime/agent/botd.properties, and restart \" +\n      \"(tools/scratch-instance.md has the recipe).\";\n  }\n  return \"\";\n}\n\n// Generous: the autonomous developer workflow legitimately spends rounds\n// on compile-debug cycles.\nexport const MAX_ROUNDS = 10;\n\n// The system prompt lives client-side now — the cost of skipping\n// control_query, and the point: the bench knows what the model is looking\n// at and how its replies get used. The platform-knowledge sections are\n// the owner's, folded in from agent.llm.tool_loop's prompt (his call:\n// without them the model can't write ndata-correct code or reason about\n// the platform). Model-agnostic (Qwen or Claude).\n// The platform-knowledge CORE lives in the store (dev.prompts `prompt`\n// facet — docs/prompting.md): ONE journaled curriculum shared with the\n// agent app's tool_loop. SYSTEM_PROMPT here is the notebook's SHELL —\n// venue, fences, memory, answer style; session assembles core + shell +\n// TOOLS_PROMPT + the owner addendum at ask time.\nexport const SYSTEM_PROMPT = `Right now you are speaking through the session notebook of Bench, the Newbound IDE — the user sees every tool call you make as a notebook cell.\n\nWHAT YOU SEE\n- Current code arrives in fenced blocks labeled lib:ctl.facet (facets) or lib:ctl:command.lang (command bodies; the first line may be a synthesized signature comment). A flow body arrives as JSON labeled lib:ctl:command.flow.\n- \"Recent notebook activity\" lists commands run in this session, with OUTPUTS TRUNCATED. Never conclude that something is absent because you don't see it in a truncated result — rerun the command via a tool when you need the full output.\n\nMEMORY IN THIS NOTEBOOK\n- Your context includes a memory:index fence: every kb domain with entry counts, tags, staleness marks (\"N stale?\" — re-read those referents before trusting the marked entries), and entries pushed for the surface open on the bench (\"Relevant now\").\n- Memory FORMATION is automatic here: the archivist reviews completed sessions in the background and files what is durable (journaled, tagged unreviewed). Do not file memories on your own initiative. When the user explicitly asks you to remember something, call dev-code-remember — it runs without confirmation, because the request itself is the authorization.\n\nHOW TO ANSWER\n- Be concise and concrete. If you are unsure, say so plainly rather than inventing platform behavior.\n- To propose a facet change, reply with ONE complete fenced block per changed facet, tagged html, css, or js. The user can apply such a block as a journaled whole-facet patch in one click — a partial snippet cannot be applied, so always return the full facet.\n- Command-body suggestions: a fenced block tagged rust or python containing the full body.`;\n\n// Fetched lazily at ask time (the connection exists by then; module\n// install happens before it does). Cached per page; a failed fetch\n// clears the cache so the next ask retries, and the error is LOUD —\n// a silently thin prompt regresses the confabulation lesson.\nlet corePromise = null;\nexport function corePrompt() {\n  if (!corePromise) {\n    corePromise = (async () => {\n      const r = await store.readFacet(\"agent\", \"prompts\", \"prompt\");\n      if (r?.status === \"ok\" && r.exists && (r.source ?? \"\").trim()) {\n        return r.source.trim();\n      }\n      corePromise = null;\n      throw new Error(\"dev.prompts `prompt` facet unavailable — the platform curriculum is store-resident (docs/prompting.md)\");\n    })();\n  }\n  return corePromise;\n}\n\n// The tool model (owner's design, 2026-07-25): DISCOVERY is always on,\n// ATTACHMENT is a context optimization, AUTHORIZATION happens per call.\nexport const TOOLS_PROMPT = `\n\nTOOLS\nNewbound commands are your tools, named library-control-command (e.g. dev-code-read_command). Call them through the tool-calling interface — never by describing a call in text. Every call you make appears as a cell in the user's notebook.\n\n- A core set is attached with full schemas. EVERY other platform command is also available: use find_tools(query) to discover commands for a task, describe_tool(name) for a full schema, and call_command(name, args) to invoke anything not directly attached. Never claim a capability is missing until find_tools says so.\n- Authorization is per call, not per tool: read-only commands run immediately; anything mutating or potentially destructive stops for the user's typed confirmation first — so do not hesitate to REQUEST a powerful command when the task needs it; the user decides at the moment of use. A denial is the user's decision: respect it and continue without that action.\n- Read before you write. For edits prefer the journaled, revertible commands: dev-code-patch_control_facet for UI facets, dev-code-patch_command_body / upsert_command for command bodies, dev-code-read_flow_body / write_flow_body for flow commands.`;\n\n// Newbound meta types -> Rust types, for synthesized signatures (ported\n// from control_query's lookup_rust_api_data_type).\nconst RUST_TYPE = {\n  FLAT: \"DataObject\", JSONObject: \"DataObject\", JSONArray: \"DataArray\",\n  InputStream: \"DataBytes\", float: \"f64\", Integer: \"i64\", Boolean: \"bool\",\n  Any: \"Data\", NULL: \"DNull\",\n};\nconst rustType = (t) => RUST_TYPE[t] ?? \"String\";\n\n/** The context prompt, ported from control_query: an editing preamble plus\n    one fence per included piece, tagged the way the backend prompt always\n    tagged them. `included` = viewctx snapshot entries the user checked. */\nexport function contextBlock(included) {\n  const merged = {};\n  for (const p of included) Object.assign(merged, p.fields);\n  const { lib, ctl } = merged;\n  let out = \"\";\n  if (lib || ctl) {\n    out = `We are editing the ${ctl} control in the ${lib} library.`;\n    for (const facet of [\"html\", \"css\", \"js\"]) {\n      const v = (merged[facet] ?? \"\").trim?.() ?? \"\";\n      if (v) out += `\\n\\n\\`\\`\\`${lib}:${ctl}.${facet}\\n${v}\\n\\`\\`\\``;\n    }\n    if (merged.code && merged.cmdname) {\n      const lang = merged.lang === \"rust\" ? \"rs\" : merged.lang === \"python\" ? \"py\" : merged.lang;\n      let signature = \"\";\n      if (merged.cmdparams) {\n        const ps = merged.cmdparams.map((p) =>\n          lang === \"rs\" ? `${p.name}: ${rustType(p.type)}` : p.name).join(\", \");\n        signature = lang === \"rs\"\n          ? `// fn ${merged.cmdname}(${ps}) -> ${rustType(merged.cmdreturn ?? \"String\")}\\n`\n          : `# def ${merged.cmdname}(${ps}):\\n`;\n      }\n      out += `\\n\\n\\`\\`\\`${lib}:${ctl}:${merged.cmdname}.${lang}\\n${signature}${merged.code.trim()}\\n\\`\\`\\``;\n    }\n    if (merged.flow && merged.cmdname) {\n      out += `\\n\\n\\`\\`\\`${lib}:${ctl}:${merged.cmdname}.flow\\n${merged.flow}\\n\\`\\`\\``;\n    }\n  }\n  if (merged.memory) {\n    out += (out ? \"\\n\\n\" : \"\") + `\\`\\`\\`memory:index\\n${merged.memory.trim()}\\n\\`\\`\\``;\n  }\n  return out;\n}\n\nexport const clamp = (s, n = 2000) =>\n  s.length <= n ? s : s.slice(0, n) + `\\n…[${s.length - n} chars clipped]`;\n\n/** MCP tool descriptors -> OpenAI function defs, enabled names only. */\nexport function toolDefs(mcpTools, enabledNames) {\n  const on = new Set(enabledNames);\n  return (mcpTools ?? [])\n    .filter((t) => on.has(t.name))\n    .map((t) => ({\n      type: \"function\",\n      function: {\n        name: t.name,\n        description: t.description ?? \"\",\n        parameters: t.inputSchema ?? { type: \"object\", properties: {} },\n      },\n    }));\n}\n\n// ── the tool model: defaults, meta-tools, gating ──────────\n\n// Attached-by-default: the read family (auto-run) plus the journaled\n// workhorses (each call confirm-gated). Everything else stays reachable\n// through call_command. The picker stores per-user add/remove overrides.\nexport const DEFAULT_TOOLS = [\n  \"dev-code-read_control_facet\", \"dev-code-read_command\",\n  \"dev-code-read_flow_body\", \"dev-code-list_control_patches\",\n  \"dev-code-list_libraries\", \"dev-code-list_controls\",\n  \"dev-code-list_commands\", \"dev-code-list_assets\",\n  \"dev-code-search_commands\",\n  \"dev-code-patch_control_facet\", \"dev-code-patch_command_body\",\n  \"dev-code-upsert_command\", \"dev-code-write_flow_body\",\n  \"dev-code-invoke_command\", \"dev-code-evaluate_rust\",\n  \"dev-code-remember\",\n];\n\n/** The always-attached meta-tools: discovery + the gateway. */\nexport const META_TOOL_DEFS = [\n  {\n    type: \"function\",\n    function: {\n      name: \"find_tools\",\n      description: \"Search ALL platform commands (not just attached tools) by keywords against their names and descriptions. Returns matches with their authorization tier. Use this before concluding a capability is missing.\",\n      parameters: { type: \"object\",\n        properties: { query: { type: \"string\", description: \"Keywords, e.g. 'delete asset' or 'rust eval'\" } },\n        required: [\"query\"] },\n    },\n  },\n  {\n    type: \"function\",\n    function: {\n      name: \"describe_tool\",\n      description: \"Full descriptor (description + JSON Schema of arguments) for one platform command by its lib-ctl-cmd name. Use before call_command when unsure of the arguments.\",\n      parameters: { type: \"object\",\n        properties: { name: { type: \"string\", description: \"Tool name, e.g. dev-code-delete_asset\" } },\n        required: [\"name\"] },\n    },\n  },\n  {\n    type: \"function\",\n    function: {\n      name: \"call_command\",\n      description: \"Invoke ANY platform command by its lib-ctl-cmd name — attached or not. Arguments are validated against the command's schema; mutating commands stop for the user's confirmation like every other call.\",\n      parameters: { type: \"object\",\n        properties: {\n          name: { type: \"string\", description: \"Tool name, e.g. dev-code-delete_asset\" },\n          args: { type: \"object\", description: \"The command's arguments, per its schema\" },\n        },\n        required: [\"name\", \"args\"] },\n    },\n  },\n];\n\nconst READ_TOOL_PREFIXES = [\n  \"list\", \"read\", \"get\", \"search\", \"describe\", \"lib_info\", \"check\",\n  \"current\", \"info\", \"peers\", \"libs\", \"apps\", \"assets\", \"asset\", \"lookup\",\n];\n\n/** Authorization tier for a catalog entry: \"auto\" (read — runs directly)\n    or \"confirm\" (stops at the typed confirm). The TAGS convention wins\n    when present — a command whose MCP tags include agent-auto or\n    agent-confirm is classified by the owner (describe() forwards the\n    record's `tags` field once the flowlang side does; empty today,\n    forward-compatible). `groups` is SECURITY groups and is never read\n    for gating. Fallback: the read-prefix heuristic on the command name. */\n// remember runs WITHOUT confirmation on explicit user request — the ask\n// itself is the authorization (docs/memory.md); autonomous memory\n// formation belongs to the archivist, and the prompt says so.\nconst AUTO_OVERRIDES = new Set([\"dev-code-remember\"]);\n\nexport function gateFor(name, entry) {\n  if (AUTO_OVERRIDES.has(name)) return \"auto\";\n  const tags = entry?.tags ?? [];\n  if (tags.includes(\"agent-confirm\")) return \"confirm\";\n  if (tags.includes(\"agent-auto\")) return \"auto\";\n  const cmd = parseToolName(name)?.cmd ?? name;\n  return READ_TOOL_PREFIXES.some((p) => cmd === p || cmd.startsWith(p + \"_\"))\n    ? \"auto\" : \"confirm\";\n}\n\n/** Light client-side check of args against a catalog inputSchema — enough\n    for the model to self-correct: missing required keys and gross type\n    mismatches, reported together. Null = fine. */\nexport function schemaComplaints(schema, args) {\n  if (!schema || typeof args !== \"object\" || args === null) return null;\n  const problems = [];\n  for (const req of schema.required ?? []) {\n    if (!(req in args)) problems.push(`missing required argument '${req}'`);\n  }\n  const kinds = { string: \"string\", number: \"number\", boolean: \"boolean\" };\n  for (const [k, v] of Object.entries(args)) {\n    const spec = schema.properties?.[k];\n    if (!spec) continue;\n    if (spec.type in kinds && typeof v !== kinds[spec.type]) {\n      problems.push(`argument '${k}' should be a ${spec.type}`);\n    } else if (spec.type === \"object\" && (typeof v !== \"object\" || v === null || Array.isArray(v))) {\n      problems.push(`argument '${k}' should be an object`);\n    } else if (spec.type === \"array\" && !Array.isArray(v)) {\n      problems.push(`argument '${k}' should be an array`);\n    }\n  }\n  return problems.length ? problems.join(\"; \") : null;\n}\n\n/** Search the catalog for find_tools: token match on name + description. */\nexport function searchCatalog(catalog, query, limit = 12) {\n  const tokens = (query ?? \"\").toLowerCase().split(/[^a-z0-9_]+/).filter(Boolean);\n  if (!tokens.length) return [];\n  const scored = [];\n  for (const t of catalog) {\n    const name = t.name.toLowerCase();\n    const desc = (t.description ?? \"\").toLowerCase();\n    // Name hits must dominate: nearly every DESCRIPTION contains \"newbound\"\n    // or \"command\", so flat token counting degenerated to catalog order (a\n    // real trace ranked close_stream above read_command for \"read command\").\n    let score = 0;\n    let matched = 0;\n    for (const tok of tokens) {\n      if (name.includes(tok)) { score += 5; matched++; }\n      else if (desc.includes(tok)) { score += 1; matched++; }\n    }\n    if (!matched) continue;\n    if (matched === tokens.length) score += 3;\n    scored.push([score, t]);\n  }\n  scored.sort((a, b) => b[0] - a[0]);\n  return scored.slice(0, limit).map(([, t]) => ({\n    name: t.name,\n    gate: gateFor(t.name, t),\n    summary: (t.summary ?? t.description ?? \"\").slice(0, 180),\n  }));\n}\n\n/** \"lib-ctl-cmd\" -> {lib, ctl, cmd} (ctl may itself contain dashes). */\nexport function parseToolName(name) {\n  const parts = name.split(\"-\");\n  if (parts.length < 3) return null;\n  return { lib: parts[0], ctl: parts.slice(1, -1).join(\"-\"), cmd: parts.at(-1) };\n}\n\n/**\n * One conversational turn, driving chat_llm until it answers in text.\n * execTool(call) is the session's: it renders the cell, gates mutating\n * commands behind the typed confirm, runs it, and resolves the tool-result\n * CONTENT string (or a denial note). Returns the final text.\n */\nexport async function chatTurn({ messages, tools, execTool, onRound }) {\n  const seen = new Map();   // result text -> tool name, for the context guard\n  // Recall layer 4: the mode-keyed pack for THIS ask, APPENDED to the one\n  // system message on every provider call but never written into the\n  // venue's own messages array — the conversation the venue keeps must\n  // not accumulate a pack per turn. Rebuilt per call because the loop\n  // pushes tool exchanges into `messages` as it goes. One implementation\n  // covers every venue that speaks through chatTurn.\n  let pack = null;\n  try {\n    await memoryReady;\n    const ask = [...messages].reverse().find((m) => m.role === \"user\")?.content ?? \"\";\n    pack = packFor(ask);\n  } catch { pack = null; }\n  globalThis.__nbAgentPack = pack\n    ? { modes: pack.modes, count: pack.count, total: pack.total, at: Date.now() }\n    : null;   // instrumentation: a failed turn should show what was injected\n  // ONE system message, at index 0. vLLM's chat template raises\n  // \"System message must be at the beginning.\" for a system message at\n  // any later index — including index 1, ahead of every user turn — so\n  // the pack is APPENDED to the existing system prompt rather than\n  // inserted beside it. Appending (not prepending) also keeps the core\n  // curriculum as a stable prefix, so server-side prefix caching still\n  // hits. The venue's own array is never mutated; the merged copy lives\n  // only for the duration of the call.\n  const convo = () => {\n    if (!pack) return messages;\n    if (messages[0]?.role === \"system\") {\n      return [{ ...messages[0], content: messages[0].content + \"\\n\\n\" + pack.block },\n              ...messages.slice(1)];\n    }\n    return [{ role: \"system\", content: pack.block }, ...messages];\n  };\n  for (let round = 0; round < MAX_ROUNDS; round++) {\n    const r = await chatLlm(convo(), tools);\n    if (r.status !== \"ok\") throw new Error(r.msg ?? \"chat_llm failed\");\n    const d = r.data ?? {};\n    // Failures ride INSIDE the FLAT envelope: wrapper-caught panics as\n    // {status:\"err\", msg} (a missing VLLM config lands here), chat_llm's own\n    // transport/parse errors as {kind:\"error\", content}. Returning either as\n    // a normal reply renders an EMPTY or misleading cell — throw, so every\n    // venue's catch shows an honest error.\n    if (d.status === \"err\") throw new Error(d.msg ?? \"chat_llm failed\");\n    if (d.kind === \"error\") throw new Error(d.content ?? \"chat_llm failed\");\n    if (d.kind !== \"tool_calls\") {\n      return d.content ?? \"\";\n    }\n    messages.push(d.assistant_message);\n    onRound?.(d.tool_calls);\n    for (const call of d.tool_calls) {\n      const content = await execTool(call);\n      // Context guard: if an identical result already sits in this turn's\n      // messages (the model re-read an unchanged facet — a real trace showed\n      // the same 26KB source pulled three times), elide the duplicate TEXT.\n      // The tool still ran; only the repeated payload is replaced.\n      const prior = seen.get(content);\n      messages.push({ role: \"tool\", tool_call_id: call.id,\n        content: prior !== undefined && content.length > 500\n          ? `[result identical to the ${prior} call earlier this turn — unchanged; reuse that output]`\n          : content });\n      if (prior === undefined) seen.set(content, call.function?.name ?? \"tool\");\n    }\n  }\n  // Budget exhausted. A bare stop misreports turns whose LAST round did the\n  // work (a real trace: the successful whole-facet patch landed on round 10\n  // and the user was told the agent stopped without an answer). Force one\n  // toolless wrap-up call; fall back to the honest stop line if it fails.\n  messages.push({ role: \"user\", content:\n    \"[Tool budget exhausted. Do not request tools. Answer NOW from what you already have: what you completed, what succeeded or failed (name any patch ids), and what remains undone.]\" });\n  try {\n    const r = await chatLlm(convo(), []);\n    if (r.status === \"ok\") {\n      const d = r.data ?? {};\n      if (d.status !== \"err\" && d.kind !== \"error\" && d.kind !== \"tool_calls\"\n          && (d.content ?? \"\").trim()) {\n        return d.content;\n      }\n    }\n  } catch { /* fall through to the honest stop */ }\n  return \"(stopped: the agent used its whole tool budget without a final answer)\";\n}\n","patch_id":"p38","time":1786403598078},{"author":"claude","facet":"html","label":"collapse: library control / api.js primitives (module layer retired)","new":"<div class=\"data-control\" data-control=\"agent:memory\" hidden></div>\n","old":"","patch_id":"p39","time":1786403598124},{"author":"claude","facet":"js","label":"sweep stale comments (the bench world is gone)","new":"// agentloop.js — the client side of \"chat with the code\", the way the\n// owner called it (2026-07-25): skip control_query AND tool_loop, call\n// agent.llm.chat_llm directly. This module owns what control_query used to\n// do server-side — the system prompt, the context fences, the history —\n// plus the tool loop the notebook can actually SHOW: chat_llm returns\n// normalized tool_calls; the session renders each as a cell and gates\n// mutating ones behind its typed confirm before the loop continues.\n//\n// chat_llm contract (agent.llm, JSONObject envelope — payload under data):\n//   in:  messages (system/user/assistant/tool), tools (OpenAI function defs)\n//   out: {kind:\"text\", content, assistant_message}\n//      | {kind:\"tool_calls\", tool_calls:[{id,name,arguments(JSON string)}],\n//         assistant_message}   — assistant_message goes back on the list.\n// MCP tools (agent.plugin.list_tools) arrive as {name:\"lib-ctl-cmd\",\n// description, inputSchema} — inputSchema is a real JSON Schema.\n\n\n// ── this library's own wire ─────────────────────────────────\n// The platform carries no add-on-specific API: the provider owns its\n// library name and command names. Everything below rides invokeCommand's\n// generic by-name surface; resolution failures come back as err envelopes\n// (never throws), so callers branch on status only.\n//\n// LIBRARY control — headless: defines window.NB_AGENTLOOP once (idempotent across\n// installs). Consumers list this control as a hidden data-control child\n// div and use the global from their ready.\n\nvar me = this;\nvar ME = document.getElementById(me.UUID);\n\nme.ready = function () {\n  if (window.NB_AGENTLOOP) return;\n  const { packFor, ready: memoryReady } = window.NB_MEMORY;\n  const jsonP = (c2, v2) => new Promise((res2) => json(c2, v2, res2));\n  const invokeP = (l2, c2, m2, a2) => new Promise((res2) => invokeCommand(l2, c2, m2, a2, res2));\n  const invoke = async (l2, c2, m2, a2) => {\n    const t0 = performance.now();\n    const envelope = await invokeP(l2, c2, m2, a2);\n    return { envelope, ms: Math.round(performance.now() - t0) };\n  };\n  const code = (m2, a2) => invokeP(\"dev\", \"code\", m2, a2);\n  const readFacet = (l2, c2, f2) => code(\"read_control_facet\", { lib: l2, ctl: c2, facet: f2 });\n  window.NB_AGENTLOOP = (function () {\n\nasync function agentCall(ctl, cmd, args) {\n  const r = await invoke(\"agent\", ctl, cmd, args);\n  return r.envelope;\n}\n\n/** The MCP tool list (agent.plugin.list_tools, FLAT {tools:[...]}). */\nfunction listTools() {\n  return agentCall(\"plugin\", \"list_tools\", {});\n}\n\n/** One chat_llm completion (agent.llm — the OpenAI-compatible bridge to\n    the instance's configured model). JSONObject envelope: the payload\n    ({kind, content | tool_calls, assistant_message}) sits under data.\n    chatTurn below drives this directly — control_query and tool_loop are\n    deliberately not called. */\nfunction chatLlm(messages, tools) {\n  return agentCall(\"llm\", \"chat_llm\", { messages, tools });\n}\n\n/** Draft a command description (agent.plugin.describe_command, String). */\nfunction describeCommand(args) {\n  return agentCall(\"plugin\", \"describe_command\", args);\n}\n\n/** The archivist's intake (docs/memory.md) — the session fires and\n    forgets; resolution failures resolve to Error values, not rejections. */\nfunction logTurn(entry) {\n  return invoke(\"agent\", \"archivist\", \"log_turn\", entry);\n}\n\n/** chat_llm reads system.apps.agent.runtime (VLLM_URL/VLLM_MODEL) —\n    absent when the agent app isn't exposed. The session shows whatever\n    non-empty string this returns beneath a failed ask. */\nfunction errorHint(msg) {\n  if (/Key '(agent|VLLM_URL|VLLM_MODEL)' not found/.test(msg ?? \"\")) {\n    return \"This instance's agent app isn't configured: add `agent` to \" +\n      \"config.properties apps=, put VLLM_URL and VLLM_MODEL in \" +\n      \"runtime/agent/botd.properties, and restart \" +\n      \"(tools/scratch-instance.md has the recipe).\";\n  }\n  return \"\";\n}\n\n// Generous: the autonomous developer workflow legitimately spends rounds\n// on compile-debug cycles.\nconst MAX_ROUNDS = 10;\n\n// The system prompt lives client-side now — the cost of skipping\n// control_query, and the point: the notebook knows what the model is looking\n// at and how its replies get used. The platform-knowledge sections are\n// the owner's, folded in from agent.llm.tool_loop's prompt (his call:\n// without them the model can't write ndata-correct code or reason about\n// the platform). Model-agnostic (Qwen or Claude).\n// The platform-knowledge CORE lives in the store (dev.prompts `prompt`\n// facet — docs/prompting.md): ONE journaled curriculum shared with the\n// agent app's tool_loop. SYSTEM_PROMPT here is the notebook's SHELL —\n// venue, fences, memory, answer style; session assembles core + shell +\n// TOOLS_PROMPT + the owner addendum at ask time.\nconst SYSTEM_PROMPT = `Right now you are speaking through the session notebook of Bench, the Newbound IDE — the user sees every tool call you make as a notebook cell.\n\nWHAT YOU SEE\n- Current code arrives in fenced blocks labeled lib:ctl.facet (facets) or lib:ctl:command.lang (command bodies; the first line may be a synthesized signature comment). A flow body arrives as JSON labeled lib:ctl:command.flow.\n- \"Recent notebook activity\" lists commands run in this session, with OUTPUTS TRUNCATED. Never conclude that something is absent because you don't see it in a truncated result — rerun the command via a tool when you need the full output.\n\nMEMORY IN THIS NOTEBOOK\n- Your context includes a memory:index fence: every kb domain with entry counts, tags, staleness marks (\"N stale?\" — re-read those referents before trusting the marked entries), and entries pushed for the surface open on the bench (\"Relevant now\").\n- Memory FORMATION is automatic here: the archivist reviews completed sessions in the background and files what is durable (journaled, tagged unreviewed). Do not file memories on your own initiative. When the user explicitly asks you to remember something, call dev-code-remember — it runs without confirmation, because the request itself is the authorization.\n\nHOW TO ANSWER\n- Be concise and concrete. If you are unsure, say so plainly rather than inventing platform behavior.\n- To propose a facet change, reply with ONE complete fenced block per changed facet, tagged html, css, or js. The user can apply such a block as a journaled whole-facet patch in one click — a partial snippet cannot be applied, so always return the full facet.\n- Command-body suggestions: a fenced block tagged rust or python containing the full body.`;\n\n// Fetched lazily at ask time (the connection exists by then; module\n// install happens before it does). Cached per page; a failed fetch\n// clears the cache so the next ask retries, and the error is LOUD —\n// a silently thin prompt regresses the confabulation lesson.\nlet corePromise = null;\nfunction corePrompt() {\n  if (!corePromise) {\n    corePromise = (async () => {\n      const r = await readFacet(\"agent\", \"prompts\", \"prompt\");\n      if (r?.status === \"ok\" && r.exists && (r.source ?? \"\").trim()) {\n        return r.source.trim();\n      }\n      corePromise = null;\n      throw new Error(\"dev.prompts `prompt` facet unavailable — the platform curriculum is store-resident (docs/prompting.md)\");\n    })();\n  }\n  return corePromise;\n}\n\n// The tool model (owner's design, 2026-07-25): DISCOVERY is always on,\n// ATTACHMENT is a context optimization, AUTHORIZATION happens per call.\nconst TOOLS_PROMPT = `\n\nTOOLS\nNewbound commands are your tools, named library-control-command (e.g. dev-code-read_command). Call them through the tool-calling interface — never by describing a call in text. Every call you make appears as a cell in the user's notebook.\n\n- A core set is attached with full schemas. EVERY other platform command is also available: use find_tools(query) to discover commands for a task, describe_tool(name) for a full schema, and call_command(name, args) to invoke anything not directly attached. Never claim a capability is missing until find_tools says so.\n- Authorization is per call, not per tool: read-only commands run immediately; anything mutating or potentially destructive stops for the user's typed confirmation first — so do not hesitate to REQUEST a powerful command when the task needs it; the user decides at the moment of use. A denial is the user's decision: respect it and continue without that action.\n- Read before you write. For edits prefer the journaled, revertible commands: dev-code-patch_control_facet for UI facets, dev-code-patch_command_body / upsert_command for command bodies, dev-code-read_flow_body / write_flow_body for flow commands.`;\n\n// Newbound meta types -> Rust types, for synthesized signatures (ported\n// from control_query's lookup_rust_api_data_type).\nconst RUST_TYPE = {\n  FLAT: \"DataObject\", JSONObject: \"DataObject\", JSONArray: \"DataArray\",\n  InputStream: \"DataBytes\", float: \"f64\", Integer: \"i64\", Boolean: \"bool\",\n  Any: \"Data\", NULL: \"DNull\",\n};\nconst rustType = (t) => RUST_TYPE[t] ?? \"String\";\n\n/** The context prompt, ported from control_query: an editing preamble plus\n    one fence per included piece, tagged the way the backend prompt always\n    tagged them. `included` = viewctx snapshot entries the user checked. */\nfunction contextBlock(included) {\n  const merged = {};\n  for (const p of included) Object.assign(merged, p.fields);\n  const { lib, ctl } = merged;\n  let out = \"\";\n  if (lib || ctl) {\n    out = `We are editing the ${ctl} control in the ${lib} library.`;\n    for (const facet of [\"html\", \"css\", \"js\"]) {\n      const v = (merged[facet] ?? \"\").trim?.() ?? \"\";\n      if (v) out += `\\n\\n\\`\\`\\`${lib}:${ctl}.${facet}\\n${v}\\n\\`\\`\\``;\n    }\n    if (merged.code && merged.cmdname) {\n      const lang = merged.lang === \"rust\" ? \"rs\" : merged.lang === \"python\" ? \"py\" : merged.lang;\n      let signature = \"\";\n      if (merged.cmdparams) {\n        const ps = merged.cmdparams.map((p) =>\n          lang === \"rs\" ? `${p.name}: ${rustType(p.type)}` : p.name).join(\", \");\n        signature = lang === \"rs\"\n          ? `// fn ${merged.cmdname}(${ps}) -> ${rustType(merged.cmdreturn ?? \"String\")}\\n`\n          : `# def ${merged.cmdname}(${ps}):\\n`;\n      }\n      out += `\\n\\n\\`\\`\\`${lib}:${ctl}:${merged.cmdname}.${lang}\\n${signature}${merged.code.trim()}\\n\\`\\`\\``;\n    }\n    if (merged.flow && merged.cmdname) {\n      out += `\\n\\n\\`\\`\\`${lib}:${ctl}:${merged.cmdname}.flow\\n${merged.flow}\\n\\`\\`\\``;\n    }\n  }\n  if (merged.memory) {\n    out += (out ? \"\\n\\n\" : \"\") + `\\`\\`\\`memory:index\\n${merged.memory.trim()}\\n\\`\\`\\``;\n  }\n  return out;\n}\n\nconst clamp = (s, n = 2000) =>\n  s.length <= n ? s : s.slice(0, n) + `\\n…[${s.length - n} chars clipped]`;\n\n/** MCP tool descriptors -> OpenAI function defs, enabled names only. */\nfunction toolDefs(mcpTools, enabledNames) {\n  const on = new Set(enabledNames);\n  return (mcpTools ?? [])\n    .filter((t) => on.has(t.name))\n    .map((t) => ({\n      type: \"function\",\n      function: {\n        name: t.name,\n        description: t.description ?? \"\",\n        parameters: t.inputSchema ?? { type: \"object\", properties: {} },\n      },\n    }));\n}\n\n// ── the tool model: defaults, meta-tools, gating ──────────\n\n// Attached-by-default: the read family (auto-run) plus the journaled\n// workhorses (each call confirm-gated). Everything else stays reachable\n// through call_command. The picker stores per-user add/remove overrides.\nconst DEFAULT_TOOLS = [\n  \"dev-code-read_control_facet\", \"dev-code-read_command\",\n  \"dev-code-read_flow_body\", \"dev-code-list_control_patches\",\n  \"dev-code-list_libraries\", \"dev-code-list_controls\",\n  \"dev-code-list_commands\", \"dev-code-list_assets\",\n  \"dev-code-search_commands\",\n  \"dev-code-patch_control_facet\", \"dev-code-patch_command_body\",\n  \"dev-code-upsert_command\", \"dev-code-write_flow_body\",\n  \"dev-code-invoke_command\", \"dev-code-evaluate_rust\",\n  \"dev-code-remember\",\n];\n\n/** The always-attached meta-tools: discovery + the gateway. */\nconst META_TOOL_DEFS = [\n  {\n    type: \"function\",\n    function: {\n      name: \"find_tools\",\n      description: \"Search ALL platform commands (not just attached tools) by keywords against their names and descriptions. Returns matches with their authorization tier. Use this before concluding a capability is missing.\",\n      parameters: { type: \"object\",\n        properties: { query: { type: \"string\", description: \"Keywords, e.g. 'delete asset' or 'rust eval'\" } },\n        required: [\"query\"] },\n    },\n  },\n  {\n    type: \"function\",\n    function: {\n      name: \"describe_tool\",\n      description: \"Full descriptor (description + JSON Schema of arguments) for one platform command by its lib-ctl-cmd name. Use before call_command when unsure of the arguments.\",\n      parameters: { type: \"object\",\n        properties: { name: { type: \"string\", description: \"Tool name, e.g. dev-code-delete_asset\" } },\n        required: [\"name\"] },\n    },\n  },\n  {\n    type: \"function\",\n    function: {\n      name: \"call_command\",\n      description: \"Invoke ANY platform command by its lib-ctl-cmd name — attached or not. Arguments are validated against the command's schema; mutating commands stop for the user's confirmation like every other call.\",\n      parameters: { type: \"object\",\n        properties: {\n          name: { type: \"string\", description: \"Tool name, e.g. dev-code-delete_asset\" },\n          args: { type: \"object\", description: \"The command's arguments, per its schema\" },\n        },\n        required: [\"name\", \"args\"] },\n    },\n  },\n];\n\nconst READ_TOOL_PREFIXES = [\n  \"list\", \"read\", \"get\", \"search\", \"describe\", \"lib_info\", \"check\",\n  \"current\", \"info\", \"peers\", \"libs\", \"apps\", \"assets\", \"asset\", \"lookup\",\n];\n\n/** Authorization tier for a catalog entry: \"auto\" (read — runs directly)\n    or \"confirm\" (stops at the typed confirm). The TAGS convention wins\n    when present — a command whose MCP tags include agent-auto or\n    agent-confirm is classified by the owner (describe() forwards the\n    record's `tags` field once the flowlang side does; empty today,\n    forward-compatible). `groups` is SECURITY groups and is never read\n    for gating. Fallback: the read-prefix heuristic on the command name. */\n// remember runs WITHOUT confirmation on explicit user request — the ask\n// itself is the authorization (docs/memory.md); autonomous memory\n// formation belongs to the archivist, and the prompt says so.\nconst AUTO_OVERRIDES = new Set([\"dev-code-remember\"]);\n\nfunction gateFor(name, entry) {\n  if (AUTO_OVERRIDES.has(name)) return \"auto\";\n  const tags = entry?.tags ?? [];\n  if (tags.includes(\"agent-confirm\")) return \"confirm\";\n  if (tags.includes(\"agent-auto\")) return \"auto\";\n  const cmd = parseToolName(name)?.cmd ?? name;\n  return READ_TOOL_PREFIXES.some((p) => cmd === p || cmd.startsWith(p + \"_\"))\n    ? \"auto\" : \"confirm\";\n}\n\n/** Light client-side check of args against a catalog inputSchema — enough\n    for the model to self-correct: missing required keys and gross type\n    mismatches, reported together. Null = fine. */\nfunction schemaComplaints(schema, args) {\n  if (!schema || typeof args !== \"object\" || args === null) return null;\n  const problems = [];\n  for (const req of schema.required ?? []) {\n    if (!(req in args)) problems.push(`missing required argument '${req}'`);\n  }\n  const kinds = { string: \"string\", number: \"number\", boolean: \"boolean\" };\n  for (const [k, v] of Object.entries(args)) {\n    const spec = schema.properties?.[k];\n    if (!spec) continue;\n    if (spec.type in kinds && typeof v !== kinds[spec.type]) {\n      problems.push(`argument '${k}' should be a ${spec.type}`);\n    } else if (spec.type === \"object\" && (typeof v !== \"object\" || v === null || Array.isArray(v))) {\n      problems.push(`argument '${k}' should be an object`);\n    } else if (spec.type === \"array\" && !Array.isArray(v)) {\n      problems.push(`argument '${k}' should be an array`);\n    }\n  }\n  return problems.length ? problems.join(\"; \") : null;\n}\n\n/** Search the catalog for find_tools: token match on name + description. */\nfunction searchCatalog(catalog, query, limit = 12) {\n  const tokens = (query ?? \"\").toLowerCase().split(/[^a-z0-9_]+/).filter(Boolean);\n  if (!tokens.length) return [];\n  const scored = [];\n  for (const t of catalog) {\n    const name = t.name.toLowerCase();\n    const desc = (t.description ?? \"\").toLowerCase();\n    // Name hits must dominate: nearly every DESCRIPTION contains \"newbound\"\n    // or \"command\", so flat token counting degenerated to catalog order (a\n    // real trace ranked close_stream above read_command for \"read command\").\n    let score = 0;\n    let matched = 0;\n    for (const tok of tokens) {\n      if (name.includes(tok)) { score += 5; matched++; }\n      else if (desc.includes(tok)) { score += 1; matched++; }\n    }\n    if (!matched) continue;\n    if (matched === tokens.length) score += 3;\n    scored.push([score, t]);\n  }\n  scored.sort((a, b) => b[0] - a[0]);\n  return scored.slice(0, limit).map(([, t]) => ({\n    name: t.name,\n    gate: gateFor(t.name, t),\n    summary: (t.summary ?? t.description ?? \"\").slice(0, 180),\n  }));\n}\n\n/** \"lib-ctl-cmd\" -> {lib, ctl, cmd} (ctl may itself contain dashes). */\nfunction parseToolName(name) {\n  const parts = name.split(\"-\");\n  if (parts.length < 3) return null;\n  return { lib: parts[0], ctl: parts.slice(1, -1).join(\"-\"), cmd: parts.at(-1) };\n}\n\n/**\n * One conversational turn, driving chat_llm until it answers in text.\n * execTool(call) is the session's: it renders the cell, gates mutating\n * commands behind the typed confirm, runs it, and resolves the tool-result\n * CONTENT string (or a denial note). Returns the final text.\n */\nasync function chatTurn({ messages, tools, execTool, onRound }) {\n  const seen = new Map();   // result text -> tool name, for the context guard\n  // Recall layer 4: the mode-keyed pack for THIS ask, APPENDED to the one\n  // system message on every provider call but never written into the\n  // venue's own messages array — the conversation the venue keeps must\n  // not accumulate a pack per turn. Rebuilt per call because the loop\n  // pushes tool exchanges into `messages` as it goes. One implementation\n  // covers every venue that speaks through chatTurn.\n  let pack = null;\n  try {\n    await memoryReady;\n    const ask = [...messages].reverse().find((m) => m.role === \"user\")?.content ?? \"\";\n    pack = packFor(ask);\n  } catch { pack = null; }\n  globalThis.__nbAgentPack = pack\n    ? { modes: pack.modes, count: pack.count, total: pack.total, at: Date.now() }\n    : null;   // instrumentation: a failed turn should show what was injected\n  // ONE system message, at index 0. vLLM's chat template raises\n  // \"System message must be at the beginning.\" for a system message at\n  // any later index — including index 1, ahead of every user turn — so\n  // the pack is APPENDED to the existing system prompt rather than\n  // inserted beside it. Appending (not prepending) also keeps the core\n  // curriculum as a stable prefix, so server-side prefix caching still\n  // hits. The venue's own array is never mutated; the merged copy lives\n  // only for the duration of the call.\n  const convo = () => {\n    if (!pack) return messages;\n    if (messages[0]?.role === \"system\") {\n      return [{ ...messages[0], content: messages[0].content + \"\\n\\n\" + pack.block },\n              ...messages.slice(1)];\n    }\n    return [{ role: \"system\", content: pack.block }, ...messages];\n  };\n  for (let round = 0; round < MAX_ROUNDS; round++) {\n    const r = await chatLlm(convo(), tools);\n    if (r.status !== \"ok\") throw new Error(r.msg ?? \"chat_llm failed\");\n    const d = r.data ?? {};\n    // Failures ride INSIDE the FLAT envelope: wrapper-caught panics as\n    // {status:\"err\", msg} (a missing VLLM config lands here), chat_llm's own\n    // transport/parse errors as {kind:\"error\", content}. Returning either as\n    // a normal reply renders an EMPTY or misleading cell — throw, so every\n    // venue's catch shows an honest error.\n    if (d.status === \"err\") throw new Error(d.msg ?? \"chat_llm failed\");\n    if (d.kind === \"error\") throw new Error(d.content ?? \"chat_llm failed\");\n    if (d.kind !== \"tool_calls\") {\n      return d.content ?? \"\";\n    }\n    messages.push(d.assistant_message);\n    onRound?.(d.tool_calls);\n    for (const call of d.tool_calls) {\n      const content = await execTool(call);\n      // Context guard: if an identical result already sits in this turn's\n      // messages (the model re-read an unchanged facet — a real trace showed\n      // the same 26KB source pulled three times), elide the duplicate TEXT.\n      // The tool still ran; only the repeated payload is replaced.\n      const prior = seen.get(content);\n      messages.push({ role: \"tool\", tool_call_id: call.id,\n        content: prior !== undefined && content.length > 500\n          ? `[result identical to the ${prior} call earlier this turn — unchanged; reuse that output]`\n          : content });\n      if (prior === undefined) seen.set(content, call.function?.name ?? \"tool\");\n    }\n  }\n  // Budget exhausted. A bare stop misreports turns whose LAST round did the\n  // work (a real trace: the successful whole-facet patch landed on round 10\n  // and the user was told the agent stopped without an answer). Force one\n  // toolless wrap-up call; fall back to the honest stop line if it fails.\n  messages.push({ role: \"user\", content:\n    \"[Tool budget exhausted. Do not request tools. Answer NOW from what you already have: what you completed, what succeeded or failed (name any patch ids), and what remains undone.]\" });\n  try {\n    const r = await chatLlm(convo(), []);\n    if (r.status === \"ok\") {\n      const d = r.data ?? {};\n      if (d.status !== \"err\" && d.kind !== \"error\" && d.kind !== \"tool_calls\"\n          && (d.content ?? \"\").trim()) {\n        return d.content;\n      }\n    }\n  } catch { /* fall through to the honest stop */ }\n  return \"(stopped: the agent used its whole tool budget without a final answer)\";\n}\n\n    return { listTools, chatLlm, describeCommand, logTurn, errorHint, MAX_ROUNDS, SYSTEM_PROMPT, corePrompt, TOOLS_PROMPT, contextBlock, clamp, toolDefs, DEFAULT_TOOLS, META_TOOL_DEFS, gateFor, schemaComplaints, searchCatalog, parseToolName, chatTurn };\n  })();\n};\n","old":"// agentloop.js — the client side of \"chat with the code\", the way the\n// owner called it (2026-07-25): skip control_query AND tool_loop, call\n// agent.llm.chat_llm directly. This module owns what control_query used to\n// do server-side — the system prompt, the context fences, the history —\n// plus the tool loop the notebook can actually SHOW: chat_llm returns\n// normalized tool_calls; the session renders each as a cell and gates\n// mutating ones behind its typed confirm before the loop continues.\n//\n// chat_llm contract (agent.llm, JSONObject envelope — payload under data):\n//   in:  messages (system/user/assistant/tool), tools (OpenAI function defs)\n//   out: {kind:\"text\", content, assistant_message}\n//      | {kind:\"tool_calls\", tool_calls:[{id,name,arguments(JSON string)}],\n//         assistant_message}   — assistant_message goes back on the list.\n// MCP tools (agent.plugin.list_tools) arrive as {name:\"lib-ctl-cmd\",\n// description, inputSchema} — inputSchema is a real JSON Schema.\n\n\n// ── this library's own wire ─────────────────────────────────\n// The platform's store carries no add-on-specific API: the provider owns\n// its library name and command names. Everything below rides the store's\n// generic by-name surface; resolution failures come back as err envelopes\n// (never throws), so callers branch on status only.\n//\n// LIBRARY control — headless: defines window.NB_AGENTLOOP once (idempotent across\n// installs). Consumers list this control as a hidden data-control child\n// div and use the global from their ready.\n\nvar me = this;\nvar ME = document.getElementById(me.UUID);\n\nme.ready = function () {\n  if (window.NB_AGENTLOOP) return;\n  const { packFor, ready: memoryReady } = window.NB_MEMORY;\n  const jsonP = (c2, v2) => new Promise((res2) => json(c2, v2, res2));\n  const invokeP = (l2, c2, m2, a2) => new Promise((res2) => invokeCommand(l2, c2, m2, a2, res2));\n  const invoke = async (l2, c2, m2, a2) => {\n    const t0 = performance.now();\n    const envelope = await invokeP(l2, c2, m2, a2);\n    return { envelope, ms: Math.round(performance.now() - t0) };\n  };\n  const code = (m2, a2) => invokeP(\"dev\", \"code\", m2, a2);\n  const readFacet = (l2, c2, f2) => code(\"read_control_facet\", { lib: l2, ctl: c2, facet: f2 });\n  window.NB_AGENTLOOP = (function () {\n\nasync function agentCall(ctl, cmd, args) {\n  const r = await invoke(\"agent\", ctl, cmd, args);\n  return r.envelope;\n}\n\n/** The MCP tool list (agent.plugin.list_tools, FLAT {tools:[...]}). */\nfunction listTools() {\n  return agentCall(\"plugin\", \"list_tools\", {});\n}\n\n/** One chat_llm completion (agent.llm — the OpenAI-compatible bridge to\n    the instance's configured model). JSONObject envelope: the payload\n    ({kind, content | tool_calls, assistant_message}) sits under data.\n    chatTurn below drives this directly — control_query and tool_loop are\n    deliberately not called. */\nfunction chatLlm(messages, tools) {\n  return agentCall(\"llm\", \"chat_llm\", { messages, tools });\n}\n\n/** Draft a command description (agent.plugin.describe_command, String). */\nfunction describeCommand(args) {\n  return agentCall(\"plugin\", \"describe_command\", args);\n}\n\n/** The archivist's intake (docs/memory.md) — the session fires and\n    forgets; resolution failures resolve to Error values, not rejections. */\nfunction logTurn(entry) {\n  return invoke(\"agent\", \"archivist\", \"log_turn\", entry);\n}\n\n/** chat_llm reads system.apps.agent.runtime (VLLM_URL/VLLM_MODEL) —\n    absent when the agent app isn't exposed. The session shows whatever\n    non-empty string this returns beneath a failed ask. */\nfunction errorHint(msg) {\n  if (/Key '(agent|VLLM_URL|VLLM_MODEL)' not found/.test(msg ?? \"\")) {\n    return \"This instance's agent app isn't configured: add `agent` to \" +\n      \"config.properties apps=, put VLLM_URL and VLLM_MODEL in \" +\n      \"runtime/agent/botd.properties, and restart \" +\n      \"(tools/scratch-instance.md has the recipe).\";\n  }\n  return \"\";\n}\n\n// Generous: the autonomous developer workflow legitimately spends rounds\n// on compile-debug cycles.\nconst MAX_ROUNDS = 10;\n\n// The system prompt lives client-side now — the cost of skipping\n// control_query, and the point: the bench knows what the model is looking\n// at and how its replies get used. The platform-knowledge sections are\n// the owner's, folded in from agent.llm.tool_loop's prompt (his call:\n// without them the model can't write ndata-correct code or reason about\n// the platform). Model-agnostic (Qwen or Claude).\n// The platform-knowledge CORE lives in the store (dev.prompts `prompt`\n// facet — docs/prompting.md): ONE journaled curriculum shared with the\n// agent app's tool_loop. SYSTEM_PROMPT here is the notebook's SHELL —\n// venue, fences, memory, answer style; session assembles core + shell +\n// TOOLS_PROMPT + the owner addendum at ask time.\nconst SYSTEM_PROMPT = `Right now you are speaking through the session notebook of Bench, the Newbound IDE — the user sees every tool call you make as a notebook cell.\n\nWHAT YOU SEE\n- Current code arrives in fenced blocks labeled lib:ctl.facet (facets) or lib:ctl:command.lang (command bodies; the first line may be a synthesized signature comment). A flow body arrives as JSON labeled lib:ctl:command.flow.\n- \"Recent notebook activity\" lists commands run in this session, with OUTPUTS TRUNCATED. Never conclude that something is absent because you don't see it in a truncated result — rerun the command via a tool when you need the full output.\n\nMEMORY IN THIS NOTEBOOK\n- Your context includes a memory:index fence: every kb domain with entry counts, tags, staleness marks (\"N stale?\" — re-read those referents before trusting the marked entries), and entries pushed for the surface open on the bench (\"Relevant now\").\n- Memory FORMATION is automatic here: the archivist reviews completed sessions in the background and files what is durable (journaled, tagged unreviewed). Do not file memories on your own initiative. When the user explicitly asks you to remember something, call dev-code-remember — it runs without confirmation, because the request itself is the authorization.\n\nHOW TO ANSWER\n- Be concise and concrete. If you are unsure, say so plainly rather than inventing platform behavior.\n- To propose a facet change, reply with ONE complete fenced block per changed facet, tagged html, css, or js. The user can apply such a block as a journaled whole-facet patch in one click — a partial snippet cannot be applied, so always return the full facet.\n- Command-body suggestions: a fenced block tagged rust or python containing the full body.`;\n\n// Fetched lazily at ask time (the connection exists by then; module\n// install happens before it does). Cached per page; a failed fetch\n// clears the cache so the next ask retries, and the error is LOUD —\n// a silently thin prompt regresses the confabulation lesson.\nlet corePromise = null;\nfunction corePrompt() {\n  if (!corePromise) {\n    corePromise = (async () => {\n      const r = await readFacet(\"agent\", \"prompts\", \"prompt\");\n      if (r?.status === \"ok\" && r.exists && (r.source ?? \"\").trim()) {\n        return r.source.trim();\n      }\n      corePromise = null;\n      throw new Error(\"dev.prompts `prompt` facet unavailable — the platform curriculum is store-resident (docs/prompting.md)\");\n    })();\n  }\n  return corePromise;\n}\n\n// The tool model (owner's design, 2026-07-25): DISCOVERY is always on,\n// ATTACHMENT is a context optimization, AUTHORIZATION happens per call.\nconst TOOLS_PROMPT = `\n\nTOOLS\nNewbound commands are your tools, named library-control-command (e.g. dev-code-read_command). Call them through the tool-calling interface — never by describing a call in text. Every call you make appears as a cell in the user's notebook.\n\n- A core set is attached with full schemas. EVERY other platform command is also available: use find_tools(query) to discover commands for a task, describe_tool(name) for a full schema, and call_command(name, args) to invoke anything not directly attached. Never claim a capability is missing until find_tools says so.\n- Authorization is per call, not per tool: read-only commands run immediately; anything mutating or potentially destructive stops for the user's typed confirmation first — so do not hesitate to REQUEST a powerful command when the task needs it; the user decides at the moment of use. A denial is the user's decision: respect it and continue without that action.\n- Read before you write. For edits prefer the journaled, revertible commands: dev-code-patch_control_facet for UI facets, dev-code-patch_command_body / upsert_command for command bodies, dev-code-read_flow_body / write_flow_body for flow commands.`;\n\n// Newbound meta types -> Rust types, for synthesized signatures (ported\n// from control_query's lookup_rust_api_data_type).\nconst RUST_TYPE = {\n  FLAT: \"DataObject\", JSONObject: \"DataObject\", JSONArray: \"DataArray\",\n  InputStream: \"DataBytes\", float: \"f64\", Integer: \"i64\", Boolean: \"bool\",\n  Any: \"Data\", NULL: \"DNull\",\n};\nconst rustType = (t) => RUST_TYPE[t] ?? \"String\";\n\n/** The context prompt, ported from control_query: an editing preamble plus\n    one fence per included piece, tagged the way the backend prompt always\n    tagged them. `included` = viewctx snapshot entries the user checked. */\nfunction contextBlock(included) {\n  const merged = {};\n  for (const p of included) Object.assign(merged, p.fields);\n  const { lib, ctl } = merged;\n  let out = \"\";\n  if (lib || ctl) {\n    out = `We are editing the ${ctl} control in the ${lib} library.`;\n    for (const facet of [\"html\", \"css\", \"js\"]) {\n      const v = (merged[facet] ?? \"\").trim?.() ?? \"\";\n      if (v) out += `\\n\\n\\`\\`\\`${lib}:${ctl}.${facet}\\n${v}\\n\\`\\`\\``;\n    }\n    if (merged.code && merged.cmdname) {\n      const lang = merged.lang === \"rust\" ? \"rs\" : merged.lang === \"python\" ? \"py\" : merged.lang;\n      let signature = \"\";\n      if (merged.cmdparams) {\n        const ps = merged.cmdparams.map((p) =>\n          lang === \"rs\" ? `${p.name}: ${rustType(p.type)}` : p.name).join(\", \");\n        signature = lang === \"rs\"\n          ? `// fn ${merged.cmdname}(${ps}) -> ${rustType(merged.cmdreturn ?? \"String\")}\\n`\n          : `# def ${merged.cmdname}(${ps}):\\n`;\n      }\n      out += `\\n\\n\\`\\`\\`${lib}:${ctl}:${merged.cmdname}.${lang}\\n${signature}${merged.code.trim()}\\n\\`\\`\\``;\n    }\n    if (merged.flow && merged.cmdname) {\n      out += `\\n\\n\\`\\`\\`${lib}:${ctl}:${merged.cmdname}.flow\\n${merged.flow}\\n\\`\\`\\``;\n    }\n  }\n  if (merged.memory) {\n    out += (out ? \"\\n\\n\" : \"\") + `\\`\\`\\`memory:index\\n${merged.memory.trim()}\\n\\`\\`\\``;\n  }\n  return out;\n}\n\nconst clamp = (s, n = 2000) =>\n  s.length <= n ? s : s.slice(0, n) + `\\n…[${s.length - n} chars clipped]`;\n\n/** MCP tool descriptors -> OpenAI function defs, enabled names only. */\nfunction toolDefs(mcpTools, enabledNames) {\n  const on = new Set(enabledNames);\n  return (mcpTools ?? [])\n    .filter((t) => on.has(t.name))\n    .map((t) => ({\n      type: \"function\",\n      function: {\n        name: t.name,\n        description: t.description ?? \"\",\n        parameters: t.inputSchema ?? { type: \"object\", properties: {} },\n      },\n    }));\n}\n\n// ── the tool model: defaults, meta-tools, gating ──────────\n\n// Attached-by-default: the read family (auto-run) plus the journaled\n// workhorses (each call confirm-gated). Everything else stays reachable\n// through call_command. The picker stores per-user add/remove overrides.\nconst DEFAULT_TOOLS = [\n  \"dev-code-read_control_facet\", \"dev-code-read_command\",\n  \"dev-code-read_flow_body\", \"dev-code-list_control_patches\",\n  \"dev-code-list_libraries\", \"dev-code-list_controls\",\n  \"dev-code-list_commands\", \"dev-code-list_assets\",\n  \"dev-code-search_commands\",\n  \"dev-code-patch_control_facet\", \"dev-code-patch_command_body\",\n  \"dev-code-upsert_command\", \"dev-code-write_flow_body\",\n  \"dev-code-invoke_command\", \"dev-code-evaluate_rust\",\n  \"dev-code-remember\",\n];\n\n/** The always-attached meta-tools: discovery + the gateway. */\nconst META_TOOL_DEFS = [\n  {\n    type: \"function\",\n    function: {\n      name: \"find_tools\",\n      description: \"Search ALL platform commands (not just attached tools) by keywords against their names and descriptions. Returns matches with their authorization tier. Use this before concluding a capability is missing.\",\n      parameters: { type: \"object\",\n        properties: { query: { type: \"string\", description: \"Keywords, e.g. 'delete asset' or 'rust eval'\" } },\n        required: [\"query\"] },\n    },\n  },\n  {\n    type: \"function\",\n    function: {\n      name: \"describe_tool\",\n      description: \"Full descriptor (description + JSON Schema of arguments) for one platform command by its lib-ctl-cmd name. Use before call_command when unsure of the arguments.\",\n      parameters: { type: \"object\",\n        properties: { name: { type: \"string\", description: \"Tool name, e.g. dev-code-delete_asset\" } },\n        required: [\"name\"] },\n    },\n  },\n  {\n    type: \"function\",\n    function: {\n      name: \"call_command\",\n      description: \"Invoke ANY platform command by its lib-ctl-cmd name — attached or not. Arguments are validated against the command's schema; mutating commands stop for the user's confirmation like every other call.\",\n      parameters: { type: \"object\",\n        properties: {\n          name: { type: \"string\", description: \"Tool name, e.g. dev-code-delete_asset\" },\n          args: { type: \"object\", description: \"The command's arguments, per its schema\" },\n        },\n        required: [\"name\", \"args\"] },\n    },\n  },\n];\n\nconst READ_TOOL_PREFIXES = [\n  \"list\", \"read\", \"get\", \"search\", \"describe\", \"lib_info\", \"check\",\n  \"current\", \"info\", \"peers\", \"libs\", \"apps\", \"assets\", \"asset\", \"lookup\",\n];\n\n/** Authorization tier for a catalog entry: \"auto\" (read — runs directly)\n    or \"confirm\" (stops at the typed confirm). The TAGS convention wins\n    when present — a command whose MCP tags include agent-auto or\n    agent-confirm is classified by the owner (describe() forwards the\n    record's `tags` field once the flowlang side does; empty today,\n    forward-compatible). `groups` is SECURITY groups and is never read\n    for gating. Fallback: the read-prefix heuristic on the command name. */\n// remember runs WITHOUT confirmation on explicit user request — the ask\n// itself is the authorization (docs/memory.md); autonomous memory\n// formation belongs to the archivist, and the prompt says so.\nconst AUTO_OVERRIDES = new Set([\"dev-code-remember\"]);\n\nfunction gateFor(name, entry) {\n  if (AUTO_OVERRIDES.has(name)) return \"auto\";\n  const tags = entry?.tags ?? [];\n  if (tags.includes(\"agent-confirm\")) return \"confirm\";\n  if (tags.includes(\"agent-auto\")) return \"auto\";\n  const cmd = parseToolName(name)?.cmd ?? name;\n  return READ_TOOL_PREFIXES.some((p) => cmd === p || cmd.startsWith(p + \"_\"))\n    ? \"auto\" : \"confirm\";\n}\n\n/** Light client-side check of args against a catalog inputSchema — enough\n    for the model to self-correct: missing required keys and gross type\n    mismatches, reported together. Null = fine. */\nfunction schemaComplaints(schema, args) {\n  if (!schema || typeof args !== \"object\" || args === null) return null;\n  const problems = [];\n  for (const req of schema.required ?? []) {\n    if (!(req in args)) problems.push(`missing required argument '${req}'`);\n  }\n  const kinds = { string: \"string\", number: \"number\", boolean: \"boolean\" };\n  for (const [k, v] of Object.entries(args)) {\n    const spec = schema.properties?.[k];\n    if (!spec) continue;\n    if (spec.type in kinds && typeof v !== kinds[spec.type]) {\n      problems.push(`argument '${k}' should be a ${spec.type}`);\n    } else if (spec.type === \"object\" && (typeof v !== \"object\" || v === null || Array.isArray(v))) {\n      problems.push(`argument '${k}' should be an object`);\n    } else if (spec.type === \"array\" && !Array.isArray(v)) {\n      problems.push(`argument '${k}' should be an array`);\n    }\n  }\n  return problems.length ? problems.join(\"; \") : null;\n}\n\n/** Search the catalog for find_tools: token match on name + description. */\nfunction searchCatalog(catalog, query, limit = 12) {\n  const tokens = (query ?? \"\").toLowerCase().split(/[^a-z0-9_]+/).filter(Boolean);\n  if (!tokens.length) return [];\n  const scored = [];\n  for (const t of catalog) {\n    const name = t.name.toLowerCase();\n    const desc = (t.description ?? \"\").toLowerCase();\n    // Name hits must dominate: nearly every DESCRIPTION contains \"newbound\"\n    // or \"command\", so flat token counting degenerated to catalog order (a\n    // real trace ranked close_stream above read_command for \"read command\").\n    let score = 0;\n    let matched = 0;\n    for (const tok of tokens) {\n      if (name.includes(tok)) { score += 5; matched++; }\n      else if (desc.includes(tok)) { score += 1; matched++; }\n    }\n    if (!matched) continue;\n    if (matched === tokens.length) score += 3;\n    scored.push([score, t]);\n  }\n  scored.sort((a, b) => b[0] - a[0]);\n  return scored.slice(0, limit).map(([, t]) => ({\n    name: t.name,\n    gate: gateFor(t.name, t),\n    summary: (t.summary ?? t.description ?? \"\").slice(0, 180),\n  }));\n}\n\n/** \"lib-ctl-cmd\" -> {lib, ctl, cmd} (ctl may itself contain dashes). */\nfunction parseToolName(name) {\n  const parts = name.split(\"-\");\n  if (parts.length < 3) return null;\n  return { lib: parts[0], ctl: parts.slice(1, -1).join(\"-\"), cmd: parts.at(-1) };\n}\n\n/**\n * One conversational turn, driving chat_llm until it answers in text.\n * execTool(call) is the session's: it renders the cell, gates mutating\n * commands behind the typed confirm, runs it, and resolves the tool-result\n * CONTENT string (or a denial note). Returns the final text.\n */\nasync function chatTurn({ messages, tools, execTool, onRound }) {\n  const seen = new Map();   // result text -> tool name, for the context guard\n  // Recall layer 4: the mode-keyed pack for THIS ask, APPENDED to the one\n  // system message on every provider call but never written into the\n  // venue's own messages array — the conversation the venue keeps must\n  // not accumulate a pack per turn. Rebuilt per call because the loop\n  // pushes tool exchanges into `messages` as it goes. One implementation\n  // covers every venue that speaks through chatTurn.\n  let pack = null;\n  try {\n    await memoryReady;\n    const ask = [...messages].reverse().find((m) => m.role === \"user\")?.content ?? \"\";\n    pack = packFor(ask);\n  } catch { pack = null; }\n  globalThis.__nbAgentPack = pack\n    ? { modes: pack.modes, count: pack.count, total: pack.total, at: Date.now() }\n    : null;   // instrumentation: a failed turn should show what was injected\n  // ONE system message, at index 0. vLLM's chat template raises\n  // \"System message must be at the beginning.\" for a system message at\n  // any later index — including index 1, ahead of every user turn — so\n  // the pack is APPENDED to the existing system prompt rather than\n  // inserted beside it. Appending (not prepending) also keeps the core\n  // curriculum as a stable prefix, so server-side prefix caching still\n  // hits. The venue's own array is never mutated; the merged copy lives\n  // only for the duration of the call.\n  const convo = () => {\n    if (!pack) return messages;\n    if (messages[0]?.role === \"system\") {\n      return [{ ...messages[0], content: messages[0].content + \"\\n\\n\" + pack.block },\n              ...messages.slice(1)];\n    }\n    return [{ role: \"system\", content: pack.block }, ...messages];\n  };\n  for (let round = 0; round < MAX_ROUNDS; round++) {\n    const r = await chatLlm(convo(), tools);\n    if (r.status !== \"ok\") throw new Error(r.msg ?? \"chat_llm failed\");\n    const d = r.data ?? {};\n    // Failures ride INSIDE the FLAT envelope: wrapper-caught panics as\n    // {status:\"err\", msg} (a missing VLLM config lands here), chat_llm's own\n    // transport/parse errors as {kind:\"error\", content}. Returning either as\n    // a normal reply renders an EMPTY or misleading cell — throw, so every\n    // venue's catch shows an honest error.\n    if (d.status === \"err\") throw new Error(d.msg ?? \"chat_llm failed\");\n    if (d.kind === \"error\") throw new Error(d.content ?? \"chat_llm failed\");\n    if (d.kind !== \"tool_calls\") {\n      return d.content ?? \"\";\n    }\n    messages.push(d.assistant_message);\n    onRound?.(d.tool_calls);\n    for (const call of d.tool_calls) {\n      const content = await execTool(call);\n      // Context guard: if an identical result already sits in this turn's\n      // messages (the model re-read an unchanged facet — a real trace showed\n      // the same 26KB source pulled three times), elide the duplicate TEXT.\n      // The tool still ran; only the repeated payload is replaced.\n      const prior = seen.get(content);\n      messages.push({ role: \"tool\", tool_call_id: call.id,\n        content: prior !== undefined && content.length > 500\n          ? `[result identical to the ${prior} call earlier this turn — unchanged; reuse that output]`\n          : content });\n      if (prior === undefined) seen.set(content, call.function?.name ?? \"tool\");\n    }\n  }\n  // Budget exhausted. A bare stop misreports turns whose LAST round did the\n  // work (a real trace: the successful whole-facet patch landed on round 10\n  // and the user was told the agent stopped without an answer). Force one\n  // toolless wrap-up call; fall back to the honest stop line if it fails.\n  messages.push({ role: \"user\", content:\n    \"[Tool budget exhausted. Do not request tools. Answer NOW from what you already have: what you completed, what succeeded or failed (name any patch ids), and what remains undone.]\" });\n  try {\n    const r = await chatLlm(convo(), []);\n    if (r.status === \"ok\") {\n      const d = r.data ?? {};\n      if (d.status !== \"err\" && d.kind !== \"error\" && d.kind !== \"tool_calls\"\n          && (d.content ?? \"\").trim()) {\n        return d.content;\n      }\n    }\n  } catch { /* fall through to the honest stop */ }\n  return \"(stopped: the agent used its whole tool budget without a final answer)\";\n}\n\n    return { listTools, chatLlm, describeCommand, logTurn, errorHint, MAX_ROUNDS, SYSTEM_PROMPT, corePrompt, TOOLS_PROMPT, contextBlock, clamp, toolDefs, DEFAULT_TOOLS, META_TOOL_DEFS, gateFor, schemaComplaints, searchCatalog, parseToolName, chatTurn };\n  })();\n};\n","patch_id":"p40","time":1786407258492},{"author":"claude","facet":"js","label":"the prompts control lives in agent, not dev","new":"// agentloop.js — the client side of \"chat with the code\", the way the\n// owner called it (2026-07-25): skip control_query AND tool_loop, call\n// agent.llm.chat_llm directly. This module owns what control_query used to\n// do server-side — the system prompt, the context fences, the history —\n// plus the tool loop the notebook can actually SHOW: chat_llm returns\n// normalized tool_calls; the session renders each as a cell and gates\n// mutating ones behind its typed confirm before the loop continues.\n//\n// chat_llm contract (agent.llm, JSONObject envelope — payload under data):\n//   in:  messages (system/user/assistant/tool), tools (OpenAI function defs)\n//   out: {kind:\"text\", content, assistant_message}\n//      | {kind:\"tool_calls\", tool_calls:[{id,name,arguments(JSON string)}],\n//         assistant_message}   — assistant_message goes back on the list.\n// MCP tools (agent.plugin.list_tools) arrive as {name:\"lib-ctl-cmd\",\n// description, inputSchema} — inputSchema is a real JSON Schema.\n\n\n// ── this library's own wire ─────────────────────────────────\n// The platform carries no add-on-specific API: the provider owns its\n// library name and command names. Everything below rides invokeCommand's\n// generic by-name surface; resolution failures come back as err envelopes\n// (never throws), so callers branch on status only.\n//\n// LIBRARY control — headless: defines window.NB_AGENTLOOP once (idempotent across\n// installs). Consumers list this control as a hidden data-control child\n// div and use the global from their ready.\n\nvar me = this;\nvar ME = document.getElementById(me.UUID);\n\nme.ready = function () {\n  if (window.NB_AGENTLOOP) return;\n  const { packFor, ready: memoryReady } = window.NB_MEMORY;\n  const jsonP = (c2, v2) => new Promise((res2) => json(c2, v2, res2));\n  const invokeP = (l2, c2, m2, a2) => new Promise((res2) => invokeCommand(l2, c2, m2, a2, res2));\n  const invoke = async (l2, c2, m2, a2) => {\n    const t0 = performance.now();\n    const envelope = await invokeP(l2, c2, m2, a2);\n    return { envelope, ms: Math.round(performance.now() - t0) };\n  };\n  const code = (m2, a2) => invokeP(\"dev\", \"code\", m2, a2);\n  const readFacet = (l2, c2, f2) => code(\"read_control_facet\", { lib: l2, ctl: c2, facet: f2 });\n  window.NB_AGENTLOOP = (function () {\n\nasync function agentCall(ctl, cmd, args) {\n  const r = await invoke(\"agent\", ctl, cmd, args);\n  return r.envelope;\n}\n\n/** The MCP tool list (agent.plugin.list_tools, FLAT {tools:[...]}). */\nfunction listTools() {\n  return agentCall(\"plugin\", \"list_tools\", {});\n}\n\n/** One chat_llm completion (agent.llm — the OpenAI-compatible bridge to\n    the instance's configured model). JSONObject envelope: the payload\n    ({kind, content | tool_calls, assistant_message}) sits under data.\n    chatTurn below drives this directly — control_query and tool_loop are\n    deliberately not called. */\nfunction chatLlm(messages, tools) {\n  return agentCall(\"llm\", \"chat_llm\", { messages, tools });\n}\n\n/** Draft a command description (agent.plugin.describe_command, String). */\nfunction describeCommand(args) {\n  return agentCall(\"plugin\", \"describe_command\", args);\n}\n\n/** The archivist's intake (docs/memory.md) — the session fires and\n    forgets; resolution failures resolve to Error values, not rejections. */\nfunction logTurn(entry) {\n  return invoke(\"agent\", \"archivist\", \"log_turn\", entry);\n}\n\n/** chat_llm reads system.apps.agent.runtime (VLLM_URL/VLLM_MODEL) —\n    absent when the agent app isn't exposed. The session shows whatever\n    non-empty string this returns beneath a failed ask. */\nfunction errorHint(msg) {\n  if (/Key '(agent|VLLM_URL|VLLM_MODEL)' not found/.test(msg ?? \"\")) {\n    return \"This instance's agent app isn't configured: add `agent` to \" +\n      \"config.properties apps=, put VLLM_URL and VLLM_MODEL in \" +\n      \"runtime/agent/botd.properties, and restart \" +\n      \"(tools/scratch-instance.md has the recipe).\";\n  }\n  return \"\";\n}\n\n// Generous: the autonomous developer workflow legitimately spends rounds\n// on compile-debug cycles.\nconst MAX_ROUNDS = 10;\n\n// The system prompt lives client-side now — the cost of skipping\n// control_query, and the point: the notebook knows what the model is looking\n// at and how its replies get used. The platform-knowledge sections are\n// the owner's, folded in from agent.llm.tool_loop's prompt (his call:\n// without them the model can't write ndata-correct code or reason about\n// the platform). Model-agnostic (Qwen or Claude).\n// The platform-knowledge CORE lives in the store (agent.prompts `prompt`\n// facet — docs/prompting.md): ONE journaled curriculum shared with the\n// agent app's tool_loop. SYSTEM_PROMPT here is the notebook's SHELL —\n// venue, fences, memory, answer style; session assembles core + shell +\n// TOOLS_PROMPT + the owner addendum at ask time.\nconst SYSTEM_PROMPT = `Right now you are speaking through the session notebook of Bench, the Newbound IDE — the user sees every tool call you make as a notebook cell.\n\nWHAT YOU SEE\n- Current code arrives in fenced blocks labeled lib:ctl.facet (facets) or lib:ctl:command.lang (command bodies; the first line may be a synthesized signature comment). A flow body arrives as JSON labeled lib:ctl:command.flow.\n- \"Recent notebook activity\" lists commands run in this session, with OUTPUTS TRUNCATED. Never conclude that something is absent because you don't see it in a truncated result — rerun the command via a tool when you need the full output.\n\nMEMORY IN THIS NOTEBOOK\n- Your context includes a memory:index fence: every kb domain with entry counts, tags, staleness marks (\"N stale?\" — re-read those referents before trusting the marked entries), and entries pushed for the surface open on the bench (\"Relevant now\").\n- Memory FORMATION is automatic here: the archivist reviews completed sessions in the background and files what is durable (journaled, tagged unreviewed). Do not file memories on your own initiative. When the user explicitly asks you to remember something, call dev-code-remember — it runs without confirmation, because the request itself is the authorization.\n\nHOW TO ANSWER\n- Be concise and concrete. If you are unsure, say so plainly rather than inventing platform behavior.\n- To propose a facet change, reply with ONE complete fenced block per changed facet, tagged html, css, or js. The user can apply such a block as a journaled whole-facet patch in one click — a partial snippet cannot be applied, so always return the full facet.\n- Command-body suggestions: a fenced block tagged rust or python containing the full body.`;\n\n// Fetched lazily at ask time (the connection exists by then; module\n// install happens before it does). Cached per page; a failed fetch\n// clears the cache so the next ask retries, and the error is LOUD —\n// a silently thin prompt regresses the confabulation lesson.\nlet corePromise = null;\nfunction corePrompt() {\n  if (!corePromise) {\n    corePromise = (async () => {\n      const r = await readFacet(\"agent\", \"prompts\", \"prompt\");\n      if (r?.status === \"ok\" && r.exists && (r.source ?? \"\").trim()) {\n        return r.source.trim();\n      }\n      corePromise = null;\n      throw new Error(\"agent.prompts `prompt` facet unavailable — the platform curriculum is store-resident (docs/prompting.md)\");\n    })();\n  }\n  return corePromise;\n}\n\n// The tool model (owner's design, 2026-07-25): DISCOVERY is always on,\n// ATTACHMENT is a context optimization, AUTHORIZATION happens per call.\nconst TOOLS_PROMPT = `\n\nTOOLS\nNewbound commands are your tools, named library-control-command (e.g. dev-code-read_command). Call them through the tool-calling interface — never by describing a call in text. Every call you make appears as a cell in the user's notebook.\n\n- A core set is attached with full schemas. EVERY other platform command is also available: use find_tools(query) to discover commands for a task, describe_tool(name) for a full schema, and call_command(name, args) to invoke anything not directly attached. Never claim a capability is missing until find_tools says so.\n- Authorization is per call, not per tool: read-only commands run immediately; anything mutating or potentially destructive stops for the user's typed confirmation first — so do not hesitate to REQUEST a powerful command when the task needs it; the user decides at the moment of use. A denial is the user's decision: respect it and continue without that action.\n- Read before you write. For edits prefer the journaled, revertible commands: dev-code-patch_control_facet for UI facets, dev-code-patch_command_body / upsert_command for command bodies, dev-code-read_flow_body / write_flow_body for flow commands.`;\n\n// Newbound meta types -> Rust types, for synthesized signatures (ported\n// from control_query's lookup_rust_api_data_type).\nconst RUST_TYPE = {\n  FLAT: \"DataObject\", JSONObject: \"DataObject\", JSONArray: \"DataArray\",\n  InputStream: \"DataBytes\", float: \"f64\", Integer: \"i64\", Boolean: \"bool\",\n  Any: \"Data\", NULL: \"DNull\",\n};\nconst rustType = (t) => RUST_TYPE[t] ?? \"String\";\n\n/** The context prompt, ported from control_query: an editing preamble plus\n    one fence per included piece, tagged the way the backend prompt always\n    tagged them. `included` = viewctx snapshot entries the user checked. */\nfunction contextBlock(included) {\n  const merged = {};\n  for (const p of included) Object.assign(merged, p.fields);\n  const { lib, ctl } = merged;\n  let out = \"\";\n  if (lib || ctl) {\n    out = `We are editing the ${ctl} control in the ${lib} library.`;\n    for (const facet of [\"html\", \"css\", \"js\"]) {\n      const v = (merged[facet] ?? \"\").trim?.() ?? \"\";\n      if (v) out += `\\n\\n\\`\\`\\`${lib}:${ctl}.${facet}\\n${v}\\n\\`\\`\\``;\n    }\n    if (merged.code && merged.cmdname) {\n      const lang = merged.lang === \"rust\" ? \"rs\" : merged.lang === \"python\" ? \"py\" : merged.lang;\n      let signature = \"\";\n      if (merged.cmdparams) {\n        const ps = merged.cmdparams.map((p) =>\n          lang === \"rs\" ? `${p.name}: ${rustType(p.type)}` : p.name).join(\", \");\n        signature = lang === \"rs\"\n          ? `// fn ${merged.cmdname}(${ps}) -> ${rustType(merged.cmdreturn ?? \"String\")}\\n`\n          : `# def ${merged.cmdname}(${ps}):\\n`;\n      }\n      out += `\\n\\n\\`\\`\\`${lib}:${ctl}:${merged.cmdname}.${lang}\\n${signature}${merged.code.trim()}\\n\\`\\`\\``;\n    }\n    if (merged.flow && merged.cmdname) {\n      out += `\\n\\n\\`\\`\\`${lib}:${ctl}:${merged.cmdname}.flow\\n${merged.flow}\\n\\`\\`\\``;\n    }\n  }\n  if (merged.memory) {\n    out += (out ? \"\\n\\n\" : \"\") + `\\`\\`\\`memory:index\\n${merged.memory.trim()}\\n\\`\\`\\``;\n  }\n  return out;\n}\n\nconst clamp = (s, n = 2000) =>\n  s.length <= n ? s : s.slice(0, n) + `\\n…[${s.length - n} chars clipped]`;\n\n/** MCP tool descriptors -> OpenAI function defs, enabled names only. */\nfunction toolDefs(mcpTools, enabledNames) {\n  const on = new Set(enabledNames);\n  return (mcpTools ?? [])\n    .filter((t) => on.has(t.name))\n    .map((t) => ({\n      type: \"function\",\n      function: {\n        name: t.name,\n        description: t.description ?? \"\",\n        parameters: t.inputSchema ?? { type: \"object\", properties: {} },\n      },\n    }));\n}\n\n// ── the tool model: defaults, meta-tools, gating ──────────\n\n// Attached-by-default: the read family (auto-run) plus the journaled\n// workhorses (each call confirm-gated). Everything else stays reachable\n// through call_command. The picker stores per-user add/remove overrides.\nconst DEFAULT_TOOLS = [\n  \"dev-code-read_control_facet\", \"dev-code-read_command\",\n  \"dev-code-read_flow_body\", \"dev-code-list_control_patches\",\n  \"dev-code-list_libraries\", \"dev-code-list_controls\",\n  \"dev-code-list_commands\", \"dev-code-list_assets\",\n  \"dev-code-search_commands\",\n  \"dev-code-patch_control_facet\", \"dev-code-patch_command_body\",\n  \"dev-code-upsert_command\", \"dev-code-write_flow_body\",\n  \"dev-code-invoke_command\", \"dev-code-evaluate_rust\",\n  \"dev-code-remember\",\n];\n\n/** The always-attached meta-tools: discovery + the gateway. */\nconst META_TOOL_DEFS = [\n  {\n    type: \"function\",\n    function: {\n      name: \"find_tools\",\n      description: \"Search ALL platform commands (not just attached tools) by keywords against their names and descriptions. Returns matches with their authorization tier. Use this before concluding a capability is missing.\",\n      parameters: { type: \"object\",\n        properties: { query: { type: \"string\", description: \"Keywords, e.g. 'delete asset' or 'rust eval'\" } },\n        required: [\"query\"] },\n    },\n  },\n  {\n    type: \"function\",\n    function: {\n      name: \"describe_tool\",\n      description: \"Full descriptor (description + JSON Schema of arguments) for one platform command by its lib-ctl-cmd name. Use before call_command when unsure of the arguments.\",\n      parameters: { type: \"object\",\n        properties: { name: { type: \"string\", description: \"Tool name, e.g. dev-code-delete_asset\" } },\n        required: [\"name\"] },\n    },\n  },\n  {\n    type: \"function\",\n    function: {\n      name: \"call_command\",\n      description: \"Invoke ANY platform command by its lib-ctl-cmd name — attached or not. Arguments are validated against the command's schema; mutating commands stop for the user's confirmation like every other call.\",\n      parameters: { type: \"object\",\n        properties: {\n          name: { type: \"string\", description: \"Tool name, e.g. dev-code-delete_asset\" },\n          args: { type: \"object\", description: \"The command's arguments, per its schema\" },\n        },\n        required: [\"name\", \"args\"] },\n    },\n  },\n];\n\nconst READ_TOOL_PREFIXES = [\n  \"list\", \"read\", \"get\", \"search\", \"describe\", \"lib_info\", \"check\",\n  \"current\", \"info\", \"peers\", \"libs\", \"apps\", \"assets\", \"asset\", \"lookup\",\n];\n\n/** Authorization tier for a catalog entry: \"auto\" (read — runs directly)\n    or \"confirm\" (stops at the typed confirm). The TAGS convention wins\n    when present — a command whose MCP tags include agent-auto or\n    agent-confirm is classified by the owner (describe() forwards the\n    record's `tags` field once the flowlang side does; empty today,\n    forward-compatible). `groups` is SECURITY groups and is never read\n    for gating. Fallback: the read-prefix heuristic on the command name. */\n// remember runs WITHOUT confirmation on explicit user request — the ask\n// itself is the authorization (docs/memory.md); autonomous memory\n// formation belongs to the archivist, and the prompt says so.\nconst AUTO_OVERRIDES = new Set([\"dev-code-remember\"]);\n\nfunction gateFor(name, entry) {\n  if (AUTO_OVERRIDES.has(name)) return \"auto\";\n  const tags = entry?.tags ?? [];\n  if (tags.includes(\"agent-confirm\")) return \"confirm\";\n  if (tags.includes(\"agent-auto\")) return \"auto\";\n  const cmd = parseToolName(name)?.cmd ?? name;\n  return READ_TOOL_PREFIXES.some((p) => cmd === p || cmd.startsWith(p + \"_\"))\n    ? \"auto\" : \"confirm\";\n}\n\n/** Light client-side check of args against a catalog inputSchema — enough\n    for the model to self-correct: missing required keys and gross type\n    mismatches, reported together. Null = fine. */\nfunction schemaComplaints(schema, args) {\n  if (!schema || typeof args !== \"object\" || args === null) return null;\n  const problems = [];\n  for (const req of schema.required ?? []) {\n    if (!(req in args)) problems.push(`missing required argument '${req}'`);\n  }\n  const kinds = { string: \"string\", number: \"number\", boolean: \"boolean\" };\n  for (const [k, v] of Object.entries(args)) {\n    const spec = schema.properties?.[k];\n    if (!spec) continue;\n    if (spec.type in kinds && typeof v !== kinds[spec.type]) {\n      problems.push(`argument '${k}' should be a ${spec.type}`);\n    } else if (spec.type === \"object\" && (typeof v !== \"object\" || v === null || Array.isArray(v))) {\n      problems.push(`argument '${k}' should be an object`);\n    } else if (spec.type === \"array\" && !Array.isArray(v)) {\n      problems.push(`argument '${k}' should be an array`);\n    }\n  }\n  return problems.length ? problems.join(\"; \") : null;\n}\n\n/** Search the catalog for find_tools: token match on name + description. */\nfunction searchCatalog(catalog, query, limit = 12) {\n  const tokens = (query ?? \"\").toLowerCase().split(/[^a-z0-9_]+/).filter(Boolean);\n  if (!tokens.length) return [];\n  const scored = [];\n  for (const t of catalog) {\n    const name = t.name.toLowerCase();\n    const desc = (t.description ?? \"\").toLowerCase();\n    // Name hits must dominate: nearly every DESCRIPTION contains \"newbound\"\n    // or \"command\", so flat token counting degenerated to catalog order (a\n    // real trace ranked close_stream above read_command for \"read command\").\n    let score = 0;\n    let matched = 0;\n    for (const tok of tokens) {\n      if (name.includes(tok)) { score += 5; matched++; }\n      else if (desc.includes(tok)) { score += 1; matched++; }\n    }\n    if (!matched) continue;\n    if (matched === tokens.length) score += 3;\n    scored.push([score, t]);\n  }\n  scored.sort((a, b) => b[0] - a[0]);\n  return scored.slice(0, limit).map(([, t]) => ({\n    name: t.name,\n    gate: gateFor(t.name, t),\n    summary: (t.summary ?? t.description ?? \"\").slice(0, 180),\n  }));\n}\n\n/** \"lib-ctl-cmd\" -> {lib, ctl, cmd} (ctl may itself contain dashes). */\nfunction parseToolName(name) {\n  const parts = name.split(\"-\");\n  if (parts.length < 3) return null;\n  return { lib: parts[0], ctl: parts.slice(1, -1).join(\"-\"), cmd: parts.at(-1) };\n}\n\n/**\n * One conversational turn, driving chat_llm until it answers in text.\n * execTool(call) is the session's: it renders the cell, gates mutating\n * commands behind the typed confirm, runs it, and resolves the tool-result\n * CONTENT string (or a denial note). Returns the final text.\n */\nasync function chatTurn({ messages, tools, execTool, onRound }) {\n  const seen = new Map();   // result text -> tool name, for the context guard\n  // Recall layer 4: the mode-keyed pack for THIS ask, APPENDED to the one\n  // system message on every provider call but never written into the\n  // venue's own messages array — the conversation the venue keeps must\n  // not accumulate a pack per turn. Rebuilt per call because the loop\n  // pushes tool exchanges into `messages` as it goes. One implementation\n  // covers every venue that speaks through chatTurn.\n  let pack = null;\n  try {\n    await memoryReady;\n    const ask = [...messages].reverse().find((m) => m.role === \"user\")?.content ?? \"\";\n    pack = packFor(ask);\n  } catch { pack = null; }\n  globalThis.__nbAgentPack = pack\n    ? { modes: pack.modes, count: pack.count, total: pack.total, at: Date.now() }\n    : null;   // instrumentation: a failed turn should show what was injected\n  // ONE system message, at index 0. vLLM's chat template raises\n  // \"System message must be at the beginning.\" for a system message at\n  // any later index — including index 1, ahead of every user turn — so\n  // the pack is APPENDED to the existing system prompt rather than\n  // inserted beside it. Appending (not prepending) also keeps the core\n  // curriculum as a stable prefix, so server-side prefix caching still\n  // hits. The venue's own array is never mutated; the merged copy lives\n  // only for the duration of the call.\n  const convo = () => {\n    if (!pack) return messages;\n    if (messages[0]?.role === \"system\") {\n      return [{ ...messages[0], content: messages[0].content + \"\\n\\n\" + pack.block },\n              ...messages.slice(1)];\n    }\n    return [{ role: \"system\", content: pack.block }, ...messages];\n  };\n  for (let round = 0; round < MAX_ROUNDS; round++) {\n    const r = await chatLlm(convo(), tools);\n    if (r.status !== \"ok\") throw new Error(r.msg ?? \"chat_llm failed\");\n    const d = r.data ?? {};\n    // Failures ride INSIDE the FLAT envelope: wrapper-caught panics as\n    // {status:\"err\", msg} (a missing VLLM config lands here), chat_llm's own\n    // transport/parse errors as {kind:\"error\", content}. Returning either as\n    // a normal reply renders an EMPTY or misleading cell — throw, so every\n    // venue's catch shows an honest error.\n    if (d.status === \"err\") throw new Error(d.msg ?? \"chat_llm failed\");\n    if (d.kind === \"error\") throw new Error(d.content ?? \"chat_llm failed\");\n    if (d.kind !== \"tool_calls\") {\n      return d.content ?? \"\";\n    }\n    messages.push(d.assistant_message);\n    onRound?.(d.tool_calls);\n    for (const call of d.tool_calls) {\n      const content = await execTool(call);\n      // Context guard: if an identical result already sits in this turn's\n      // messages (the model re-read an unchanged facet — a real trace showed\n      // the same 26KB source pulled three times), elide the duplicate TEXT.\n      // The tool still ran; only the repeated payload is replaced.\n      const prior = seen.get(content);\n      messages.push({ role: \"tool\", tool_call_id: call.id,\n        content: prior !== undefined && content.length > 500\n          ? `[result identical to the ${prior} call earlier this turn — unchanged; reuse that output]`\n          : content });\n      if (prior === undefined) seen.set(content, call.function?.name ?? \"tool\");\n    }\n  }\n  // Budget exhausted. A bare stop misreports turns whose LAST round did the\n  // work (a real trace: the successful whole-facet patch landed on round 10\n  // and the user was told the agent stopped without an answer). Force one\n  // toolless wrap-up call; fall back to the honest stop line if it fails.\n  messages.push({ role: \"user\", content:\n    \"[Tool budget exhausted. Do not request tools. Answer NOW from what you already have: what you completed, what succeeded or failed (name any patch ids), and what remains undone.]\" });\n  try {\n    const r = await chatLlm(convo(), []);\n    if (r.status === \"ok\") {\n      const d = r.data ?? {};\n      if (d.status !== \"err\" && d.kind !== \"error\" && d.kind !== \"tool_calls\"\n          && (d.content ?? \"\").trim()) {\n        return d.content;\n      }\n    }\n  } catch { /* fall through to the honest stop */ }\n  return \"(stopped: the agent used its whole tool budget without a final answer)\";\n}\n\n    return { listTools, chatLlm, describeCommand, logTurn, errorHint, MAX_ROUNDS, SYSTEM_PROMPT, corePrompt, TOOLS_PROMPT, contextBlock, clamp, toolDefs, DEFAULT_TOOLS, META_TOOL_DEFS, gateFor, schemaComplaints, searchCatalog, parseToolName, chatTurn };\n  })();\n};\n","old":"// agentloop.js — the client side of \"chat with the code\", the way the\n// owner called it (2026-07-25): skip control_query AND tool_loop, call\n// agent.llm.chat_llm directly. This module owns what control_query used to\n// do server-side — the system prompt, the context fences, the history —\n// plus the tool loop the notebook can actually SHOW: chat_llm returns\n// normalized tool_calls; the session renders each as a cell and gates\n// mutating ones behind its typed confirm before the loop continues.\n//\n// chat_llm contract (agent.llm, JSONObject envelope — payload under data):\n//   in:  messages (system/user/assistant/tool), tools (OpenAI function defs)\n//   out: {kind:\"text\", content, assistant_message}\n//      | {kind:\"tool_calls\", tool_calls:[{id,name,arguments(JSON string)}],\n//         assistant_message}   — assistant_message goes back on the list.\n// MCP tools (agent.plugin.list_tools) arrive as {name:\"lib-ctl-cmd\",\n// description, inputSchema} — inputSchema is a real JSON Schema.\n\n\n// ── this library's own wire ─────────────────────────────────\n// The platform carries no add-on-specific API: the provider owns its\n// library name and command names. Everything below rides invokeCommand's\n// generic by-name surface; resolution failures come back as err envelopes\n// (never throws), so callers branch on status only.\n//\n// LIBRARY control — headless: defines window.NB_AGENTLOOP once (idempotent across\n// installs). Consumers list this control as a hidden data-control child\n// div and use the global from their ready.\n\nvar me = this;\nvar ME = document.getElementById(me.UUID);\n\nme.ready = function () {\n  if (window.NB_AGENTLOOP) return;\n  const { packFor, ready: memoryReady } = window.NB_MEMORY;\n  const jsonP = (c2, v2) => new Promise((res2) => json(c2, v2, res2));\n  const invokeP = (l2, c2, m2, a2) => new Promise((res2) => invokeCommand(l2, c2, m2, a2, res2));\n  const invoke = async (l2, c2, m2, a2) => {\n    const t0 = performance.now();\n    const envelope = await invokeP(l2, c2, m2, a2);\n    return { envelope, ms: Math.round(performance.now() - t0) };\n  };\n  const code = (m2, a2) => invokeP(\"dev\", \"code\", m2, a2);\n  const readFacet = (l2, c2, f2) => code(\"read_control_facet\", { lib: l2, ctl: c2, facet: f2 });\n  window.NB_AGENTLOOP = (function () {\n\nasync function agentCall(ctl, cmd, args) {\n  const r = await invoke(\"agent\", ctl, cmd, args);\n  return r.envelope;\n}\n\n/** The MCP tool list (agent.plugin.list_tools, FLAT {tools:[...]}). */\nfunction listTools() {\n  return agentCall(\"plugin\", \"list_tools\", {});\n}\n\n/** One chat_llm completion (agent.llm — the OpenAI-compatible bridge to\n    the instance's configured model). JSONObject envelope: the payload\n    ({kind, content | tool_calls, assistant_message}) sits under data.\n    chatTurn below drives this directly — control_query and tool_loop are\n    deliberately not called. */\nfunction chatLlm(messages, tools) {\n  return agentCall(\"llm\", \"chat_llm\", { messages, tools });\n}\n\n/** Draft a command description (agent.plugin.describe_command, String). */\nfunction describeCommand(args) {\n  return agentCall(\"plugin\", \"describe_command\", args);\n}\n\n/** The archivist's intake (docs/memory.md) — the session fires and\n    forgets; resolution failures resolve to Error values, not rejections. */\nfunction logTurn(entry) {\n  return invoke(\"agent\", \"archivist\", \"log_turn\", entry);\n}\n\n/** chat_llm reads system.apps.agent.runtime (VLLM_URL/VLLM_MODEL) —\n    absent when the agent app isn't exposed. The session shows whatever\n    non-empty string this returns beneath a failed ask. */\nfunction errorHint(msg) {\n  if (/Key '(agent|VLLM_URL|VLLM_MODEL)' not found/.test(msg ?? \"\")) {\n    return \"This instance's agent app isn't configured: add `agent` to \" +\n      \"config.properties apps=, put VLLM_URL and VLLM_MODEL in \" +\n      \"runtime/agent/botd.properties, and restart \" +\n      \"(tools/scratch-instance.md has the recipe).\";\n  }\n  return \"\";\n}\n\n// Generous: the autonomous developer workflow legitimately spends rounds\n// on compile-debug cycles.\nconst MAX_ROUNDS = 10;\n\n// The system prompt lives client-side now — the cost of skipping\n// control_query, and the point: the notebook knows what the model is looking\n// at and how its replies get used. The platform-knowledge sections are\n// the owner's, folded in from agent.llm.tool_loop's prompt (his call:\n// without them the model can't write ndata-correct code or reason about\n// the platform). Model-agnostic (Qwen or Claude).\n// The platform-knowledge CORE lives in the store (dev.prompts `prompt`\n// facet — docs/prompting.md): ONE journaled curriculum shared with the\n// agent app's tool_loop. SYSTEM_PROMPT here is the notebook's SHELL —\n// venue, fences, memory, answer style; session assembles core + shell +\n// TOOLS_PROMPT + the owner addendum at ask time.\nconst SYSTEM_PROMPT = `Right now you are speaking through the session notebook of Bench, the Newbound IDE — the user sees every tool call you make as a notebook cell.\n\nWHAT YOU SEE\n- Current code arrives in fenced blocks labeled lib:ctl.facet (facets) or lib:ctl:command.lang (command bodies; the first line may be a synthesized signature comment). A flow body arrives as JSON labeled lib:ctl:command.flow.\n- \"Recent notebook activity\" lists commands run in this session, with OUTPUTS TRUNCATED. Never conclude that something is absent because you don't see it in a truncated result — rerun the command via a tool when you need the full output.\n\nMEMORY IN THIS NOTEBOOK\n- Your context includes a memory:index fence: every kb domain with entry counts, tags, staleness marks (\"N stale?\" — re-read those referents before trusting the marked entries), and entries pushed for the surface open on the bench (\"Relevant now\").\n- Memory FORMATION is automatic here: the archivist reviews completed sessions in the background and files what is durable (journaled, tagged unreviewed). Do not file memories on your own initiative. When the user explicitly asks you to remember something, call dev-code-remember — it runs without confirmation, because the request itself is the authorization.\n\nHOW TO ANSWER\n- Be concise and concrete. If you are unsure, say so plainly rather than inventing platform behavior.\n- To propose a facet change, reply with ONE complete fenced block per changed facet, tagged html, css, or js. The user can apply such a block as a journaled whole-facet patch in one click — a partial snippet cannot be applied, so always return the full facet.\n- Command-body suggestions: a fenced block tagged rust or python containing the full body.`;\n\n// Fetched lazily at ask time (the connection exists by then; module\n// install happens before it does). Cached per page; a failed fetch\n// clears the cache so the next ask retries, and the error is LOUD —\n// a silently thin prompt regresses the confabulation lesson.\nlet corePromise = null;\nfunction corePrompt() {\n  if (!corePromise) {\n    corePromise = (async () => {\n      const r = await readFacet(\"agent\", \"prompts\", \"prompt\");\n      if (r?.status === \"ok\" && r.exists && (r.source ?? \"\").trim()) {\n        return r.source.trim();\n      }\n      corePromise = null;\n      throw new Error(\"dev.prompts `prompt` facet unavailable — the platform curriculum is store-resident (docs/prompting.md)\");\n    })();\n  }\n  return corePromise;\n}\n\n// The tool model (owner's design, 2026-07-25): DISCOVERY is always on,\n// ATTACHMENT is a context optimization, AUTHORIZATION happens per call.\nconst TOOLS_PROMPT = `\n\nTOOLS\nNewbound commands are your tools, named library-control-command (e.g. dev-code-read_command). Call them through the tool-calling interface — never by describing a call in text. Every call you make appears as a cell in the user's notebook.\n\n- A core set is attached with full schemas. EVERY other platform command is also available: use find_tools(query) to discover commands for a task, describe_tool(name) for a full schema, and call_command(name, args) to invoke anything not directly attached. Never claim a capability is missing until find_tools says so.\n- Authorization is per call, not per tool: read-only commands run immediately; anything mutating or potentially destructive stops for the user's typed confirmation first — so do not hesitate to REQUEST a powerful command when the task needs it; the user decides at the moment of use. A denial is the user's decision: respect it and continue without that action.\n- Read before you write. For edits prefer the journaled, revertible commands: dev-code-patch_control_facet for UI facets, dev-code-patch_command_body / upsert_command for command bodies, dev-code-read_flow_body / write_flow_body for flow commands.`;\n\n// Newbound meta types -> Rust types, for synthesized signatures (ported\n// from control_query's lookup_rust_api_data_type).\nconst RUST_TYPE = {\n  FLAT: \"DataObject\", JSONObject: \"DataObject\", JSONArray: \"DataArray\",\n  InputStream: \"DataBytes\", float: \"f64\", Integer: \"i64\", Boolean: \"bool\",\n  Any: \"Data\", NULL: \"DNull\",\n};\nconst rustType = (t) => RUST_TYPE[t] ?? \"String\";\n\n/** The context prompt, ported from control_query: an editing preamble plus\n    one fence per included piece, tagged the way the backend prompt always\n    tagged them. `included` = viewctx snapshot entries the user checked. */\nfunction contextBlock(included) {\n  const merged = {};\n  for (const p of included) Object.assign(merged, p.fields);\n  const { lib, ctl } = merged;\n  let out = \"\";\n  if (lib || ctl) {\n    out = `We are editing the ${ctl} control in the ${lib} library.`;\n    for (const facet of [\"html\", \"css\", \"js\"]) {\n      const v = (merged[facet] ?? \"\").trim?.() ?? \"\";\n      if (v) out += `\\n\\n\\`\\`\\`${lib}:${ctl}.${facet}\\n${v}\\n\\`\\`\\``;\n    }\n    if (merged.code && merged.cmdname) {\n      const lang = merged.lang === \"rust\" ? \"rs\" : merged.lang === \"python\" ? \"py\" : merged.lang;\n      let signature = \"\";\n      if (merged.cmdparams) {\n        const ps = merged.cmdparams.map((p) =>\n          lang === \"rs\" ? `${p.name}: ${rustType(p.type)}` : p.name).join(\", \");\n        signature = lang === \"rs\"\n          ? `// fn ${merged.cmdname}(${ps}) -> ${rustType(merged.cmdreturn ?? \"String\")}\\n`\n          : `# def ${merged.cmdname}(${ps}):\\n`;\n      }\n      out += `\\n\\n\\`\\`\\`${lib}:${ctl}:${merged.cmdname}.${lang}\\n${signature}${merged.code.trim()}\\n\\`\\`\\``;\n    }\n    if (merged.flow && merged.cmdname) {\n      out += `\\n\\n\\`\\`\\`${lib}:${ctl}:${merged.cmdname}.flow\\n${merged.flow}\\n\\`\\`\\``;\n    }\n  }\n  if (merged.memory) {\n    out += (out ? \"\\n\\n\" : \"\") + `\\`\\`\\`memory:index\\n${merged.memory.trim()}\\n\\`\\`\\``;\n  }\n  return out;\n}\n\nconst clamp = (s, n = 2000) =>\n  s.length <= n ? s : s.slice(0, n) + `\\n…[${s.length - n} chars clipped]`;\n\n/** MCP tool descriptors -> OpenAI function defs, enabled names only. */\nfunction toolDefs(mcpTools, enabledNames) {\n  const on = new Set(enabledNames);\n  return (mcpTools ?? [])\n    .filter((t) => on.has(t.name))\n    .map((t) => ({\n      type: \"function\",\n      function: {\n        name: t.name,\n        description: t.description ?? \"\",\n        parameters: t.inputSchema ?? { type: \"object\", properties: {} },\n      },\n    }));\n}\n\n// ── the tool model: defaults, meta-tools, gating ──────────\n\n// Attached-by-default: the read family (auto-run) plus the journaled\n// workhorses (each call confirm-gated). Everything else stays reachable\n// through call_command. The picker stores per-user add/remove overrides.\nconst DEFAULT_TOOLS = [\n  \"dev-code-read_control_facet\", \"dev-code-read_command\",\n  \"dev-code-read_flow_body\", \"dev-code-list_control_patches\",\n  \"dev-code-list_libraries\", \"dev-code-list_controls\",\n  \"dev-code-list_commands\", \"dev-code-list_assets\",\n  \"dev-code-search_commands\",\n  \"dev-code-patch_control_facet\", \"dev-code-patch_command_body\",\n  \"dev-code-upsert_command\", \"dev-code-write_flow_body\",\n  \"dev-code-invoke_command\", \"dev-code-evaluate_rust\",\n  \"dev-code-remember\",\n];\n\n/** The always-attached meta-tools: discovery + the gateway. */\nconst META_TOOL_DEFS = [\n  {\n    type: \"function\",\n    function: {\n      name: \"find_tools\",\n      description: \"Search ALL platform commands (not just attached tools) by keywords against their names and descriptions. Returns matches with their authorization tier. Use this before concluding a capability is missing.\",\n      parameters: { type: \"object\",\n        properties: { query: { type: \"string\", description: \"Keywords, e.g. 'delete asset' or 'rust eval'\" } },\n        required: [\"query\"] },\n    },\n  },\n  {\n    type: \"function\",\n    function: {\n      name: \"describe_tool\",\n      description: \"Full descriptor (description + JSON Schema of arguments) for one platform command by its lib-ctl-cmd name. Use before call_command when unsure of the arguments.\",\n      parameters: { type: \"object\",\n        properties: { name: { type: \"string\", description: \"Tool name, e.g. dev-code-delete_asset\" } },\n        required: [\"name\"] },\n    },\n  },\n  {\n    type: \"function\",\n    function: {\n      name: \"call_command\",\n      description: \"Invoke ANY platform command by its lib-ctl-cmd name — attached or not. Arguments are validated against the command's schema; mutating commands stop for the user's confirmation like every other call.\",\n      parameters: { type: \"object\",\n        properties: {\n          name: { type: \"string\", description: \"Tool name, e.g. dev-code-delete_asset\" },\n          args: { type: \"object\", description: \"The command's arguments, per its schema\" },\n        },\n        required: [\"name\", \"args\"] },\n    },\n  },\n];\n\nconst READ_TOOL_PREFIXES = [\n  \"list\", \"read\", \"get\", \"search\", \"describe\", \"lib_info\", \"check\",\n  \"current\", \"info\", \"peers\", \"libs\", \"apps\", \"assets\", \"asset\", \"lookup\",\n];\n\n/** Authorization tier for a catalog entry: \"auto\" (read — runs directly)\n    or \"confirm\" (stops at the typed confirm). The TAGS convention wins\n    when present — a command whose MCP tags include agent-auto or\n    agent-confirm is classified by the owner (describe() forwards the\n    record's `tags` field once the flowlang side does; empty today,\n    forward-compatible). `groups` is SECURITY groups and is never read\n    for gating. Fallback: the read-prefix heuristic on the command name. */\n// remember runs WITHOUT confirmation on explicit user request — the ask\n// itself is the authorization (docs/memory.md); autonomous memory\n// formation belongs to the archivist, and the prompt says so.\nconst AUTO_OVERRIDES = new Set([\"dev-code-remember\"]);\n\nfunction gateFor(name, entry) {\n  if (AUTO_OVERRIDES.has(name)) return \"auto\";\n  const tags = entry?.tags ?? [];\n  if (tags.includes(\"agent-confirm\")) return \"confirm\";\n  if (tags.includes(\"agent-auto\")) return \"auto\";\n  const cmd = parseToolName(name)?.cmd ?? name;\n  return READ_TOOL_PREFIXES.some((p) => cmd === p || cmd.startsWith(p + \"_\"))\n    ? \"auto\" : \"confirm\";\n}\n\n/** Light client-side check of args against a catalog inputSchema — enough\n    for the model to self-correct: missing required keys and gross type\n    mismatches, reported together. Null = fine. */\nfunction schemaComplaints(schema, args) {\n  if (!schema || typeof args !== \"object\" || args === null) return null;\n  const problems = [];\n  for (const req of schema.required ?? []) {\n    if (!(req in args)) problems.push(`missing required argument '${req}'`);\n  }\n  const kinds = { string: \"string\", number: \"number\", boolean: \"boolean\" };\n  for (const [k, v] of Object.entries(args)) {\n    const spec = schema.properties?.[k];\n    if (!spec) continue;\n    if (spec.type in kinds && typeof v !== kinds[spec.type]) {\n      problems.push(`argument '${k}' should be a ${spec.type}`);\n    } else if (spec.type === \"object\" && (typeof v !== \"object\" || v === null || Array.isArray(v))) {\n      problems.push(`argument '${k}' should be an object`);\n    } else if (spec.type === \"array\" && !Array.isArray(v)) {\n      problems.push(`argument '${k}' should be an array`);\n    }\n  }\n  return problems.length ? problems.join(\"; \") : null;\n}\n\n/** Search the catalog for find_tools: token match on name + description. */\nfunction searchCatalog(catalog, query, limit = 12) {\n  const tokens = (query ?? \"\").toLowerCase().split(/[^a-z0-9_]+/).filter(Boolean);\n  if (!tokens.length) return [];\n  const scored = [];\n  for (const t of catalog) {\n    const name = t.name.toLowerCase();\n    const desc = (t.description ?? \"\").toLowerCase();\n    // Name hits must dominate: nearly every DESCRIPTION contains \"newbound\"\n    // or \"command\", so flat token counting degenerated to catalog order (a\n    // real trace ranked close_stream above read_command for \"read command\").\n    let score = 0;\n    let matched = 0;\n    for (const tok of tokens) {\n      if (name.includes(tok)) { score += 5; matched++; }\n      else if (desc.includes(tok)) { score += 1; matched++; }\n    }\n    if (!matched) continue;\n    if (matched === tokens.length) score += 3;\n    scored.push([score, t]);\n  }\n  scored.sort((a, b) => b[0] - a[0]);\n  return scored.slice(0, limit).map(([, t]) => ({\n    name: t.name,\n    gate: gateFor(t.name, t),\n    summary: (t.summary ?? t.description ?? \"\").slice(0, 180),\n  }));\n}\n\n/** \"lib-ctl-cmd\" -> {lib, ctl, cmd} (ctl may itself contain dashes). */\nfunction parseToolName(name) {\n  const parts = name.split(\"-\");\n  if (parts.length < 3) return null;\n  return { lib: parts[0], ctl: parts.slice(1, -1).join(\"-\"), cmd: parts.at(-1) };\n}\n\n/**\n * One conversational turn, driving chat_llm until it answers in text.\n * execTool(call) is the session's: it renders the cell, gates mutating\n * commands behind the typed confirm, runs it, and resolves the tool-result\n * CONTENT string (or a denial note). Returns the final text.\n */\nasync function chatTurn({ messages, tools, execTool, onRound }) {\n  const seen = new Map();   // result text -> tool name, for the context guard\n  // Recall layer 4: the mode-keyed pack for THIS ask, APPENDED to the one\n  // system message on every provider call but never written into the\n  // venue's own messages array — the conversation the venue keeps must\n  // not accumulate a pack per turn. Rebuilt per call because the loop\n  // pushes tool exchanges into `messages` as it goes. One implementation\n  // covers every venue that speaks through chatTurn.\n  let pack = null;\n  try {\n    await memoryReady;\n    const ask = [...messages].reverse().find((m) => m.role === \"user\")?.content ?? \"\";\n    pack = packFor(ask);\n  } catch { pack = null; }\n  globalThis.__nbAgentPack = pack\n    ? { modes: pack.modes, count: pack.count, total: pack.total, at: Date.now() }\n    : null;   // instrumentation: a failed turn should show what was injected\n  // ONE system message, at index 0. vLLM's chat template raises\n  // \"System message must be at the beginning.\" for a system message at\n  // any later index — including index 1, ahead of every user turn — so\n  // the pack is APPENDED to the existing system prompt rather than\n  // inserted beside it. Appending (not prepending) also keeps the core\n  // curriculum as a stable prefix, so server-side prefix caching still\n  // hits. The venue's own array is never mutated; the merged copy lives\n  // only for the duration of the call.\n  const convo = () => {\n    if (!pack) return messages;\n    if (messages[0]?.role === \"system\") {\n      return [{ ...messages[0], content: messages[0].content + \"\\n\\n\" + pack.block },\n              ...messages.slice(1)];\n    }\n    return [{ role: \"system\", content: pack.block }, ...messages];\n  };\n  for (let round = 0; round < MAX_ROUNDS; round++) {\n    const r = await chatLlm(convo(), tools);\n    if (r.status !== \"ok\") throw new Error(r.msg ?? \"chat_llm failed\");\n    const d = r.data ?? {};\n    // Failures ride INSIDE the FLAT envelope: wrapper-caught panics as\n    // {status:\"err\", msg} (a missing VLLM config lands here), chat_llm's own\n    // transport/parse errors as {kind:\"error\", content}. Returning either as\n    // a normal reply renders an EMPTY or misleading cell — throw, so every\n    // venue's catch shows an honest error.\n    if (d.status === \"err\") throw new Error(d.msg ?? \"chat_llm failed\");\n    if (d.kind === \"error\") throw new Error(d.content ?? \"chat_llm failed\");\n    if (d.kind !== \"tool_calls\") {\n      return d.content ?? \"\";\n    }\n    messages.push(d.assistant_message);\n    onRound?.(d.tool_calls);\n    for (const call of d.tool_calls) {\n      const content = await execTool(call);\n      // Context guard: if an identical result already sits in this turn's\n      // messages (the model re-read an unchanged facet — a real trace showed\n      // the same 26KB source pulled three times), elide the duplicate TEXT.\n      // The tool still ran; only the repeated payload is replaced.\n      const prior = seen.get(content);\n      messages.push({ role: \"tool\", tool_call_id: call.id,\n        content: prior !== undefined && content.length > 500\n          ? `[result identical to the ${prior} call earlier this turn — unchanged; reuse that output]`\n          : content });\n      if (prior === undefined) seen.set(content, call.function?.name ?? \"tool\");\n    }\n  }\n  // Budget exhausted. A bare stop misreports turns whose LAST round did the\n  // work (a real trace: the successful whole-facet patch landed on round 10\n  // and the user was told the agent stopped without an answer). Force one\n  // toolless wrap-up call; fall back to the honest stop line if it fails.\n  messages.push({ role: \"user\", content:\n    \"[Tool budget exhausted. Do not request tools. Answer NOW from what you already have: what you completed, what succeeded or failed (name any patch ids), and what remains undone.]\" });\n  try {\n    const r = await chatLlm(convo(), []);\n    if (r.status === \"ok\") {\n      const d = r.data ?? {};\n      if (d.status !== \"err\" && d.kind !== \"error\" && d.kind !== \"tool_calls\"\n          && (d.content ?? \"\").trim()) {\n        return d.content;\n      }\n    }\n  } catch { /* fall through to the honest stop */ }\n  return \"(stopped: the agent used its whole tool budget without a final answer)\";\n}\n\n    return { listTools, chatLlm, describeCommand, logTurn, errorHint, MAX_ROUNDS, SYSTEM_PROMPT, corePrompt, TOOLS_PROMPT, contextBlock, clamp, toolDefs, DEFAULT_TOOLS, META_TOOL_DEFS, gateFor, schemaComplaints, searchCatalog, parseToolName, chatTurn };\n  })();\n};\n","patch_id":"p41","time":1786410431691},{"author":"agent","facet":"js","label":"zero-globals: agentloop reads memory off its own child mount","new":"// LIBRARY control — headless: the api rides this control's own element\n// (zero-globals doctrine; el.api IS this constructor object). Consumers\n// mount it as a hidden data-control child div and read the element's api\n// from their ready. Dependencies are child mounts — memory rides this\n// control's own html, ready before this ready fires.\n\nvar me = this;\nvar ME = document.getElementById(me.UUID);\n\nme.ready = function () {\n  const { packFor, indexReady: memoryReady } =\n    ME.querySelector('[data-control=\"agent:memory\"]').api;","old":"// LIBRARY control — headless: defines window.NB_AGENTLOOP once (idempotent across\n// installs). Consumers list this control as a hidden data-control child\n// div and use the global from their ready.\n\nvar me = this;\nvar ME = document.getElementById(me.UUID);\n\nme.ready = function () {\n  if (window.NB_AGENTLOOP) return;\n  const { packFor, ready: memoryReady } = window.NB_MEMORY;","patch_id":"p42","time":1788784375049},{"author":"agent","facet":"js","label":"zero-globals: drop the NB_AGENTLOOP IIFE wrapper","new":"\nasync function agentCall(ctl, cmd, args) {","old":"  window.NB_AGENTLOOP = (function () {\n\nasync function agentCall(ctl, cmd, args) {","patch_id":"p43","time":1788784379874},{"author":"agent","facet":"js","label":"zero-globals: agentloop api on its own element","new":"  Object.assign(me, { listTools, chatLlm, describeCommand, logTurn, errorHint, MAX_ROUNDS, SYSTEM_PROMPT, corePrompt, TOOLS_PROMPT, contextBlock, clamp, toolDefs, DEFAULT_TOOLS, META_TOOL_DEFS, gateFor, schemaComplaints, searchCatalog, parseToolName, chatTurn });\n};","old":"    return { listTools, chatLlm, describeCommand, logTurn, errorHint, MAX_ROUNDS, SYSTEM_PROMPT, corePrompt, TOOLS_PROMPT, contextBlock, clamp, toolDefs, DEFAULT_TOOLS, META_TOOL_DEFS, gateFor, schemaComplaints, searchCatalog, parseToolName, chatTurn };\n  })();\n};","patch_id":"p44","time":1788784386220}]},"id":"yzxyjg19f9bc55ecak2_patches","readers":[],"time":1788784386220,"username":"system","writers":[]}