[
{"claim": "Phase 5b prebuild landed (claude/phase5b-prebuild): agent.model is the resident service doorway - salience (SALIENCE_CTL filler, POST /salience, 1.5s timeout, err degrades like an unset seam), service_status, and curriculum_export (salience pairs + curation traces + live claims to one JSONL batch, deliberate like seed_export). tools/model-service/service.py is trainer+server in one process: stdlib-only stub mode, double-buffered live pointer with /promote, trainer skeleton draining runtime/model/ingest with a 5-deep checkpoint ring. NanochatScorer is the single class the GPU day fills in; docs/runbook-5b.md is the owner script.", "tags": "phase5b", "confidence": "high", "time": 1786838926380},
{"claim": "Owner simplification 2026-08-16: NO salience seam - SALIENCE_CTL is gone. The subsystem is on/off via SALIENCE=on in botd.properties; the executive calls agent.model.salience directly (in-crate) and fires agent.model.bootstrap once per start when the service is not answering. bootstrap is the owner-idiom oneshot: donefile-guarded nanochat env under runtime/model/deps (only when MODEL_CHECKPOINT is a real path), service script rewritten from the compiled-in asset data/agent/_ASSETS/service.py whenever it differs (so glue edits go in the ASSET, never the runtime copy), launch when /status is down. Settings: SALIENCE, MODEL_CHECKPOINT, MODEL_SERVICE_PORT, NANOCHAT_REPO. This supersedes the SALIENCE_CTL claims.", "tags": "phase5b", "confidence": "high", "time": 1786840042087},
{"claim": "Phase 5b fixes from the owner box (2026-08-16): (1) nanochat cannot pip install -e . (flat layout: dev/, runs/, nanochat/ top-level - setuptools refuses); bootstrap instead installs only the pyproject [project] dependencies (tomllib extraction) and launches the service with PYTHONPATH at the clone. (2) Install success sentinel env_ready is written by the bash script itself as its last act under set -e - a rust-side venv-exists check stamped broken envs as installed. Sentinel rename also self-heals boxes that hit the old bug (reinstall from clean). (3) MODEL_CHECKPOINT is a nanochat BASE DIR (tokenizer/ + *_checkpoints/, the ~/.cache/nanochat layout); NanochatScorer glue is shipped in the asset: NANOCHAT_BASE_DIR env before import, load_model rl->sft->base, Engine.generate_batch, render_for_completion, JSON parse with number salvage. (4) The model dir is runtime/agent/model - the runtime/ root belongs to apps (owner rule).", "tags": "phase5b", "confidence": "high", "time": 1786842592406},
{"claim": "Model service load is ASYNC (2026-08-16): the port binds first, the scorer loads in a background thread - a real checkpoint takes long enough (torch import + weights) that loading before serving made every honest launch look dead. /status reports mode loading|error|<scorer> plus boot_error; /salience answers 503 during both (executive degrades). bootstrap polls the bind up to 20s and passes service_mode + boot_error through its report, so agent-model-service_status is the diagnostic for any launch problem - no log digging needed for the common cases.", "tags": "phase5b", "confidence": "high", "time": 1786843175049},
{"claim": "Bootstrap trains the model too (owner, 2026-08-16: an empty MODEL_CHECKPOINT dir means BUILD it, not error). Stage 1.6: if the checkpoint dir has no base/chatsft/chatrl_checkpoints and no train_done, bootstrap writes train.sh from the library asset (data/agent/_ASSETS/train.sh - nanochat speedrun pipeline: dataset, tok_train, base_train, chat_sft, torchrun on nvidia-smi GPU count) and nohups it with a pidfile guard; knobs via NANOCHAT_TRAIN_ARGS (default --depth=20; speedrun scale is --depth=24 --device-batch-size=16 --fp8). The service load-retries every 60s in mode waiting, so verdicts start on their own when base weights land; sft lands later - a service restart upgrades the loaded source. boot_error only fails the bootstrap report when training is not_needed - waiting-while-training is ok.", "tags": "phase5b", "confidence": "high", "time": 1786843815148},
{"claim": "Bootstrap converges stale services (2026-08-16): /status reports pid and stale_script (file mtime newer than process start). When bootstrap finds a service answering but stale, it kills that pid and relaunches -> service: relaunched. Without this, a repo update left an old process holding the port reporting already_running while running last weeks code - bit the owner twice. One manual kill is needed only for services started before this shipped (they report neither field).", "tags": "phase5b", "confidence": "high", "time": 1786844579176},
{"claim": "First real training run (owner 5090, 2026-08-16): pipeline worked end to end (dataset, tokenizer trained into the base dir, model built) then OOMed on step one - base_train device-batch-size default 32 is an 80GB-card number; and without FlashAttention 3 the SDPA fallback cannot do sliding windows so SSSL runs terribly (nanochat warns both). NANOCHAT_TRAIN_ARGS default is now --depth=20 --device-batch-size=8 --window-pattern=L (consumer ~32GB); drop batch to 4 if still OOM - grad accum keeps the total batch identical, and chat_sft inherits device_batch_size from pretrain meta so one setting sizes both stages. train.sh exports PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True. Because train args are read from botd at bootstrap time, a failed run is retuned by editing botd.properties and rerunning bootstrap - no pull needed.", "tags": "phase5b", "confidence": "high", "time": 1786844940379},
{"claim": "Phase 6 landed (2026-08-16): the resident service trains for real. trainer_real in the service asset: candidate deepcopy of the live model, AdamW at MODEL_TRAIN_LR, mixed mini-batches per MODEL_MIX (fresh curriculum from ingest via render_sample, replay reservoir capped 4096 in replay.jsonl, standard data from the base dirs parquet shards or standard.txt fallback), 1-in-10 fresh docs held out (heldout.jsonl, never trained). Gate every MODEL_GATE every= steps: cand must not regress on held-out standard beyond regress= AND must be <= live on held-out curriculum; pass -> ring checkpoint (cpt-<step>, keep 5) + promote via NanochatScorer.from_parts through the double buffer; fail streak of fails= -> candidate resets to live weights. All settings ride the bootstrap launch line so a service relaunch picks up botd changes. Verified on a fabricated tiny nanochat base (CPU): promote/hold/reset all exercised; serving uninterrupted through 40+ promotions. Owner calls (replay ratio MODEL_MIX, gate thresholds MODEL_GATE) shipped as proposed defaults pending veto.", "tags": "phase6", "confidence": "high", "time": 1786892344268},
{"claim": "Process lesson (bit three times, 2026-08-16): killing processes by pattern match kills your own shell - the bash -c command text contains the pattern. And `cd X && nohup python ... &` backgrounds the whole list, so $! is the SUBSHELL pid, not pythons - pid files written that way lie, and kill $(cat pidfile) silently no-ops after the subshell exits. Reliable: scan /proc matching the executable path in cmdline[0] plus an argument, excluding os.getpid(); or write the pidfile from INSIDE the nohup-ed script ($$).", "tags": "process", "confidence": "high", "time": 1786892344869},
{"claim": "NANOCHAT_DIST (2026-08-16): multi-node base training through the same self-bootstrap. Setting nnodes=2,rank=<node>,master=IP:PORT,iface=<NCCL iface> rides the train.sh launch line as arg 4; empty = single-node --standalone unchanged. Rank 0 trains the tokenizer and writes train_done; other ranks download data (filelock-deduped on shared dirs), wait for tokenizer.pkl+token_bytes.pt, and join the torchrun rendezvous; iface exports NCCL_SOCKET_IFNAME. The base dir must hold identical data on all nodes (NFS or pre-sync). Malformed specs (missing port) fail fast with a FATAL line. Built for the owners DGX Spark pair over ConnectX-7; ray is not involved - nanochat is plain torchrun DDP. Verified by dry-run harness (fake nvidia-smi/torchrun/python): correct torchrun args in single, rank-0, rank-1 modes; rank-1 tokenizer wait and train_done guard; bad-spec rejection.", "tags": "phase6", "confidence": "high", "time": 1786894731163},
{"claim": "Phase 6b (2026-08-16): the gate measures the job, and the metrics are real. Gate: once >=4 held-out salience pairs exist, both candidate and live GENERATE verdicts on agree_n held-out pairs with THE serving prompt (salience_prompt/parse_salience shared between scorer and gate); agreement = 1 - mean|gen - frontier target|; promote needs cand_agree >= live_agree - agree_slack (MODEL_GATE gains agree_slack=0.05, agree_n=8) plus the standard-loss forgetting guard; under 4 pairs the loss proxy remains. Metrics: the service journals every served verdict, loss every 10 steps, and every gate row to runtime/agent/model/metrics.jsonl (self-capped at ~8k lines); agent-model-metrics returns downsampled loss series, gate history + pass count, and a 10-bucket verdict histogram; the mind tab renders them as sparkline/histogram/gatebar. Verified on the tiny base (agreement arithmetic exact: 0.5-default gen vs 0.75 targets = 0.75 both sides) and in headless Chromium.", "tags": "phase6b", "confidence": "high", "time": 1786900393518},
{"claim": "GPU release/resume (2026-08-17): the agents whole GPU footprint is the one service process. POST /shutdown (agent-model-service_stop; mind tab release GPU button) exits cleanly - ring/replay/heldout/metrics survive, only unpromoted candidate steps are lost. Resume via bootstrap: make_scorer now prefers the NEWEST RING CHECKPOINT (NanochatScorer.from_ring: build_model on the cpt dir, tokenizer from the base dir) over the birth checkpoint, so promoted CPT progress carries across restarts - this also fixed /promote in real mode, which wrongly constructed NanochatScorer(ring_dir) as if a ring dir were a base dir. Ring dirs are now time-named (cpt-<ts>-<steps>) so steps-counter resets cannot collide. SALIENCE=off (live key) keeps the service down across executive restarts.", "tags": "gpu", "confidence": "high", "time": 1786963748247},
{"claim": "Phase 8a (2026-08-17): the pointer is split. The salience pointer stays the fast permissive lane; the USER-FACING pointer serves the service's /chat and advances only through the stricter USER_GATE (mode=manual|auto, soak_s + verdicts served AS the salience pointer, agree floor from the trainer's last gate, regress slack vs the last user promotion's held-out standard loss, check_s cadence). mode=manual parks the candidate as READY-with-evals for agent-model-user_promote (the mind tab button); a READY candidate is CLEARED the moment a newer promotion takes the fast lane - it qualifies only while it IS the canary. A watchdog re-audits the serving user pointer against the growing held-out pair set and auto-rolls-back to last_good on agreement decay; agent-model-user_rollback is the manual twin (one step, not a stack). User checkpoints are ring-pruning-protected; the pointer persists in user_pointer.json; soak clocks reset on restart. LLM=LOCAL in botd routes agent-app chat to it (text-only, no tools; 503 until promoted).", "detail": "Verified 12/12 in real mode on the fabricated tiny base (staged battery: train-on phases for ring production, train-off phases for stable soak) plus the full endpoint surface in stub mode and platform integration (bootstrap passes --user-gate, commands, LOCAL arm, mind tab) on a disposable.", "tags": "model,user-pointer,phase8a,gate", "confidence": "high", "time": 1786986028419},
{"claim": "The service's /promote endpoint was unreachable from the moment /shutdown landed (GPU-release phase) until Phase 8a: the /shutdown insertion accidentally replaced the 'if self.path == /promote:' line, leaving the whole promote body as dead code after a return - every promote_pointer call 404ed. Restored in Phase 8a. Lesson: after inserting an early-return path into a chained handler, re-read the whole chain - the battery that validated /shutdown did not re-test its neighbors.", "detail": "Found by reading, not by a failure report - the owner's promote-pointer screenshot predated the /shutdown change.", "tags": "model,service,regression", "confidence": "high", "time": 1786986028499},
{"claim": "Phase 8b (2026-08-17): the personality adapter. A standing LoRA rides the USER-FACING pointer only (never the salience lane), derived inside the service from persona/persona.jsonl (rows {user,assistant} or {messages}; every 5th held out) via forward-hook LoRA on nanochat attention linears with masked assistant-token loss (render_conversation mask; ignore_index -1). USER_LORA=mode,rank,alpha,lr,steps,slack,min_gain,guard,targets (targets DOT-separated because parse_kv splits on commas). The first derivation fires automatically once a user pointer serves and the corpus has rows; after that the probe (held-out persona loss of the SERVING model, watchdog cadence) triggers background re-derivation when it slips past slack. Derivation gate: min_gain improvement on held-out persona loss AND standard-LM guard; rejection leaves serving untouched. adapter.pt persists and merges into fresh weights at every promotion/rollback/restart; the serving name grows a +lora suffix.", "detail": "Verified 12/12 real-mode on the tiny base: auto first derivation (6.32->5.67 held-out), restart re-apply, probe-slip re-derivation after an owner corpus rewrite (reason=probe, 6.19->5.49), impossible-gate manual rejection with serving untouched, salience lane bare throughout.", "tags": "model,lora,persona,phase8b", "confidence": "high", "time": 1786994899759},
{"claim": "The persona corpus has an owner-facing editor in the mind tab (persona card): agent-model-persona_read returns the corpus as parsed rows - or, when nothing is saved, the SHIPPED DEFAULT SEED (data/agent/_ASSETS/persona-seed.jsonl, 28 exchanges in the resident model's voice, nanochat-identity style adapted to this platform) flagged seed:true; adoption is a deliberate save, never a side effect. agent-model-persona_write validates every JSONL line ({user,assistant} or {messages}) and rejects the WHOLE write with the offending line number - a typo can never truncate the corpus - then writes atomically (tmp+rename). The editor loads once per session (the 5s status poll never stomps edits), renders pair rows as two textareas and messages rows as raw JSON, and the service picks saves up on its own at the next gate tick.", "detail": "Verified on a disposable with Playwright: 28 seed rows render with the seed label, edit+delete+add+save round-trips to disk with the held-out count, reload reads from disk, a bad line rejects with 'line 2' and leaves the corpus untouched.", "tags": "model,persona,editor,ui", "confidence": "high", "time": 1786995859353},
{"claim": "Unparseable-escalates (2026-08-17): a local verdict the parser cannot read is UNCERTAINTY, not a verdict. The service marks it (parse_salience returns a parsed flag; /salience responses and verdict metrics carry parsed:false for salvaged numbers and total failures); the executive then refuses to steer on it (the salvaged number would route everything fast and starve the correction loop), counts it (unparsed_verdicts, on status and the mind tab loop card), and ALWAYS escalates it under the normal 5s cooldown - each one lands a disagreement pair. Root cause also fixed: render_sample was training salience pairs in a PLAIN-TEXT dialect (PERCEPTION:/SALIENCE VERDICT:/REASONING:) while serving demanded JSON - the flywheel was teaching the parsers failure mode. Curriculum now renders the serving prompt + the JSON answer, so training, serving, and the agreement gate speak one dialect.", "detail": "Found live on the owners box: cpt-450s first two real verdicts salvaged to 0, both fast-steered, zero escalations - the drift routed around its own correction. Verified: tinybase /salience reports parsed:false and replay.jsonl carries the serving-format doc; executive half with a pinned fake (salient 0.9, parsed false): no steer, counted, escalated, pair landed - 0.9 would otherwise deep-steer and never escalate, so the test discriminates.", "tags": "model,salience,format-drift,escalation", "confidence": "high", "time": 1787014898796},
{"claim": "The spectrum cycle is chartered (docs/spectrum-cycle.md, 2026-08-18, owner review pending): the model subsystem must scale as a continuous spectrum - a nanochat d10 on one 3090 up to frontier-scale open weights on a cluster - and scale is a measured quantity, never a branch: no mechanism may test model family, name, or size class; only the posture solver reads the resource map and the model footprint, and only its published output varies.", "detail": "Companion to harvest-cycle.md (harvest grows the food; spectrum grows the organism). Structure: the backend seam and the engine seam, candidate = base + delta, the recipe as the unit of composition. Standing rule 5: compatibility with the shipped 5b-8b loop is the FIRST acceptance gate of every phase. Phases S1-S8; ten owner calls collected at the end of the doc.", "tags": "spectrum,charter,doctrine", "source": {"doc": "docs/spectrum-cycle.md @ 79d73b6 (claude/agent-resident-model-subsystem-evpfgw)"}, "confidence": "high", "time": 1787093843802},
{"claim": "General open-source weights cannot drop into the nanochat stack, and never will via conversion - the mismatch is architectural, not file-format (NanochatScorer constructs nanochat's own GPT class and pickled tokenizer); the route to open models is a second backend (HFScorer) behind the existing scorer seam. Checkpoints already in nanochat's base-dir layout DO drop in today: point MODEL_CHECKPOINT at the directory, honored as-is.", "detail": "The nanochat weld is ~250 of service.py's 1648 lines plus bootstrap's env stage: load/serve (NanochatScorer:257, from_ring:301), dialect render (render_for_completion in score/gen_agreement/render_sample), loss forward model(x,y), ring save_checkpoint:1238, lora_target_paths:759 transformer.h.* names, render_conversation persona masking, sequence_len meta, base_data parquet standard docs:998, the three pool literals:1162, the venv build. Everything above the scorer seam - gates, double buffer, pointers, soak, persona probe - is already model-agnostic.", "tags": "spectrum,nanochat,backend", "source": {"doc": "docs/spectrum-cycle.md weld inventory; verified against data/agent/_ASSETS/service.py 2026-08-18"}, "confidence": "high", "time": 1787093857826},
{"claim": "The CPT trainer's unwritten ceiling: the candidate is a full deepcopy of the live model with fp32 AdamW moments beside it, so serve-and-train-on-one-card holds only while the model fits roughly twice (params x ~20 bytes; a d20 is under 12GB all-in). The spectrum charter removes the ceiling by making the candidate base + delta, with a posture solver walking full -> full-sharded -> lora -> qlora -> frozen and taking the top rung that fits, arithmetic published.", "detail": "frozen is a posture, not a failure: banking and measurement continue. The split pointer lets the two lanes sit at different spectrum points - a d20 salience judge training full beside an imported 8B user pointer serving frozen on the same card is the intended shape. Full posture's ring layout is unchanged on disk, so a live instance upgrades in place.", "tags": "spectrum,trainer,cpt", "source": {"doc": "docs/spectrum-cycle.md fidelity ladder; trainer_real in data/agent/_ASSETS/service.py"}, "confidence": "high", "time": 1787093857888},
{"claim": "Owner direction 2026-08-18: the subsystem is also the lab, and the agent feeds it. The feed contract: every capture channel, live or chartered by the harvest cycle, terminates in a named managed dataset - no orphan banks; synthetic generation is a dataset operation (dataset_derive) with lineage recorded (source datasets + revisions, generator as a provenance tag never a branch, transform recipe); recipes snap base/datasets/posture/adapters/evals together and the bench answers data-vs-architecture through the one-brick discipline, scored with the gates' own instruments on frozen eval snapshots.", "detail": "Streams vs snapshots: live channels feed append-only streams the standing CPT loop may ride (today's behavior); experiments and SFT runs pin frozen hashed snapshots for reproducibility. Purpose-built LoRAs on demand generalize derive_adapter (corpus -> LoRA -> gate -> apply) from persona special case to named adapter records - chartered as S4, early, because the pattern is already proven in service.py:870. Model-driven derivation spends tokens: deliberate command or drive budget, never ambient.", "tags": "spectrum,datasets,bench,adapters", "source": {"doc": "docs/spectrum-cycle.md S2 feed contract + S4 + S6, owner direction in session 2026-08-18"}, "confidence": "high", "time": 1787093867455},
{"claim": "All ten spectrum-cycle owner calls are ruled (2026-08-18/19) and docs/spectrum-cycle.md is plan of record. Load-bearing rulings: registry records live in the runtime library and bytes in the runtime folder (user data, never shipped); anchors resolve by lineage (born-here models anchor to their training datasets, imports mint a self-sampled anchor at the door); adapters stack gated as a unit; two residents allowed with residency as a second solver-walked ladder (resident/quantized/offload/on-demand/external, fast lane priority, nothing refuses for RAM - it degrades visibly); derivation governance is uniform across generators, deliberate or drive-budgeted, never ambient.", "detail": "Also ruled: solver fits = free VRAM minus computed serving reservation minus 15% margin, forced postures that do not fit refuse at launch with arithmetic shown; ring prunes by byte budget (100GB default) with two floors (protected set, newest entry); bench yardstick pinned as frozen snapshot at run start, one-brick violations warn never refuse; external engine installs only when configured, promotions there move adapters via hot-swap; acquisition only through the two deliberate doors, revision-pinned, HF_TOKEN only for gated models.", "tags": "spectrum,charter,rulings", "source": {"doc": "docs/spectrum-cycle.md owner calls 1-10, ruled in session 2026-08-18/19"}, "confidence": "high", "time": 1787098886170},
{"claim": "S1 landed (claude/spectrum-s1s2-registry): the registry exists. agent-model-import is the deliberate door (local paths; hf: sources refuse until the fetch stage; backend-shape validation at the door; FNV fingerprint over names+sizes; footprint from metadata), records in the runtime library models record (list-shaped, salience_log idiom), registry.json rendered on every write as the service's store-blind window (mtime pickup, the persona.jsonl pattern). agent-model-models lists; agent-model-model_remove refuses while MODEL= names the record and purges bytes only inside the managed weights dir. Bootstrap resolves MODEL= to path+backend ahead of the MODEL_CHECKPOINT alias (absent MODEL= is byte-for-byte the old path); hf records stage but refuse to serve until S3. The resource map rides /status (torch view, computed outside the scoring lock) with agent-model-resources proxying and an nvidia-smi fallback; /mint_anchor self-samples an import's anchor in a background thread (own scorer, never the serving slots), refusing in stub mode - import records mint_pending and never blocks on it.", "detail": "Battery on stubs passed: R1 (stub + bare MODEL_CHECKPOINT unchanged, stale-script convergence relaunched the new asset), import/list/remove round trip both backends, dup + hub + hf-serve + missing-MODEL refusals, purge guard left out-of-weights files alone, resources sane on a CPU-only box (empty gpus, real disk), stub mint 409, import mint deferral recorded. GPU-box items for the real battery: actual mint generation, MODEL= serving a real checkpoint.", "tags": "spectrum,s1,registry", "source": {"doc": "docs/spectrum-s1s2.md S1; battery run in session 2026-08-19"}, "confidence": "high", "time": 1787100343894},
{"claim": "S2 landed (claude/spectrum-s1s2-registry): the dataset manager exists. Six commands - dataset_add (in-place registration, FNV content hash, read-time holdout_every policy so user files are never rewritten; anchor-kind registration flips anchor_status on waiting models), dataset_list, dataset_inspect (peek + re-verify; snapshot hash mismatch is a tamper alarm, stream hash is rolling by design), dataset_snapshot (frozen hashed cut copied into the managed dir, lineage snapshot:<source>@<rows>), dataset_derive (render_dialect runs IN THE SERVICE via /derive so the dialect keeps one home; lineage derived:<transform>:<source>), dataset_remove (refuses while MODEL_MIX names it; purge only inside the managed dir). The feed contract runs: curriculum_export sweeps into the salience-pairs and memory streams line-deduped (idempotent - second export appends zero), self-registering, legacy ingest untouched. The trainer builds pools from registry.json datasets joined with MODEL_MIX weights by name (built-ins unchanged when nothing is configured), reports per-dataset held-out losses at gate time without gating, and reads a registered model's anchor through the manager (unregistered checkpoint = legacy split, byte-for-byte).", "detail": "Battery passed on stubs: add/inspect/verify round trip with tamper detection, export idempotence (212 swept, 0 on repeat), snapshot + derive with correct lineage, loader harness (191/21 split at every=10, jsonl streams render through render_sample, anchor resolves checkpoint->model->dataset->30 docs), R1 stub status healthy throughout. Bug found and fixed in-battery: the service may run with a relative --data-dir, so /derive's reply path only resolves from its cwd - registry records must always be written with absolute paths computed from the store root; dataset_derive now does. GPU-box items: pools actually training under mix weights, per-dataset gate losses on a real candidate.", "tags": "spectrum,s2,datasets", "source": {"doc": "docs/spectrum-s1s2.md S2; battery run in session 2026-08-19"}, "confidence": "high", "time": 1787101226212},
{"claim": "S3 landed (claude/spectrum-s3-backend): the backend seam is real. Every scorer carries generate_text(messages); score_via builds THE dialect once for all backends with unparseable-escalates unchanged; /chat, the user watchdog, and minting route through it. HFScorer serves an HF dir via transformers (chat template or honest concatenation; bf16 on cuda else fp32; posture frozen and published until S5). The backends' structural loss difference - nanochat pre-shifted (x,y) with -1 ignore vs HF aligned labels with -100 and INTERNAL shift - lives in two seam helpers (masked_lm_loss_t, plain_lm_loss_t); getting this wrong is an off-by-one that silently corrupts every loss. Persona hook-LoRA is backend-blind (one target vocabulary onto nanochat paths or HF q_proj/v_proj leaves; no PEFT, per the no-third-party doctrine). An hf resident soaks as base:hf:<name>, so the STANDARD user gate covers it with zero special cases. Bootstrap: hf records get a donefile-guarded env stage (CPU wheels when no GPU, PyPI fallback where the pytorch CDN is denied) and --backend rides the launch line.", "detail": "CPU battery passed end-to-end on a fabricated 147k-param model: served the salience lane (random-model gibberish exercised unparseable-escalates: 0.5, parsed:false), soak->READY->manual promote->/chat with native system messages, mint 40 rows through the seam, anchor registration flipped anchor_status, persona derivation on HF targets accepted (heldout 6.22->5.65) and merged (+lora serving), R1 restored to stub byte-for-byte. Two fixes found in-battery: newer transformers return BatchEncoding from apply_chat_template (normalize at the seam - the failure is an empty AttributeError deep in generate), and the pytorch CDN can be proxy-denied while PyPI is allowed (the env script falls back). Direct agent-crate builds need --features=serde_support,python_runtime (the upsert path always uses them). Owner-box battery still owed: a real open-weight model end to end.", "tags": "spectrum,s3,backend,hf", "source": {"doc": "docs/spectrum-s3.md; battery run in session 2026-08-19"}, "confidence": "high", "time": 1787103684021},
{"claim": "S4 landed (claude/spectrum-s4-adapters): adapters on demand. agent-model-adapter_derive derives a named adapter from any registered dataset against the serving pointer's base or a registered model - hook-LoRA in the service, persona's gate (min_gain on subject held-out, standard-loss guard), knobs from USER_LORA, blob in the managed adapters dir, record with the gate report verbatim in the runtime library; deriving never applies. adapter_apply changes the user pointer's stack under ruling 3: rebuild from FRESH base + persona + every member (additive deltas commute - no order; rebuild beats subtracting in bf16), gate the COMBINATION (per-member subject probes with the full stack, one std guard); pass serves and persists across promotions/restarts, fail leaves serving untouched and names the member that broke. The watchdog probes each member's drift on cadence - surfaced, never silently acted on. Persona stays the standing first skin beneath the stack.", "detail": "CPU battery passed: derive accept 6.21->5.95 on a 12-row dataset (holdout policy split 10/2); stack of one then two, both members re-clearing the unit gate on fresh ground; delete refused while applied; gated unapply; restart re-applied the stack. Bug found in-battery: the +lora/+adp name markers were endswith-checked and broke once stacked - all three sites now use containment. Owner-box battery owed: real model, real corpora, stack probe across a real promotion. Persona-into-records unification deferred to the S5 refactor.", "tags": "spectrum,s4,adapters", "source": {"doc": "docs/spectrum-s4.md; battery run in session 2026-08-19"}, "confidence": "high", "time": 1787104308929},
{"claim": "S5 landed (claude/spectrum-s5-delta-trainer): the candidate is base + delta and the posture solver rules how it trains. solve_posture publishes fits arithmetic per ruling 4 (free memory measured from GPU or /proc/meminfo minus MODEL_HEADROOM percent margin; ladder full -> lora -> frozen on this branch; MODEL_POSTURE= forces a rung and a misfit refuses training loudly with the numbers, serving untouched). The lora rung trains the SERVING model zero-copy: hook-LoRA gated per-thread (TRAIN_TLS) so serving threads never see the candidate; same drain/mix/pools/gates as full; promotion merges the delta into the serving weights and rings the blob with its base ref (kilobytes); restart resume and pointer keys re-merge the delta ring chronologically - reconstruction is exact because each delta trained relative to base plus every earlier merge. CPT on an hf resident is real. Ring prunes by MODEL_RING_GB byte budget (ruling 5) with both floors; the full rung is the shipped deepcopy path byte-for-byte.", "detail": "CPU battery: solver chose lora on meminfo (500MB need + 2245MB margin vs 14973MB free, published), trainer stepped the tiny hf resident on the memory stream, five merge-promotions to hf:tiny-hf:cpt-30 with 19KB deltas, stop/relaunch resumed at cpt-42, MODEL_POSTURE=full refused with reason, R1 restored. Deferred deliberately: USER_MODEL= two-residents (own branch), full-sharded (S7), qlora (quantization dependency needs a doctrine ruling), full hf saves (ring format ruling). Owner-box battery owed: real model, nanochat full-rung regression, budget-sized prune observed.", "tags": "spectrum,s5,solver,delta", "source": {"doc": "docs/spectrum-s5.md; battery run in session 2026-08-19"}, "confidence": "high", "time": 1787104910469},
{"claim": "S6 landed (claude/spectrum-s6-bench): the lab is real. Recipes are runtime-library records - base x mix x posture x budget x lr x evals, validated against the registry at authoring, cloned with an edits object so one-brick variants are one edit. agent-model-experiment diffs the bricks (notes is free text, never a brick), WARNS with the list when more than one moved (never refuses, per ruling 7), and hands RESOLVED recipes to the service - the store-blind seam holds. Each arm: fresh base load, bare measure, bounded always-on hook-LoRA on the arm mix, with-delta measure, weights freed; instruments are the gates own (per-dataset holdout, anchor/standard, agreement); eval material pinned at run start with hashes in the report (a stream says rolling, a snapshot names its hash). Both trainer rungs skip candidate steps while the bench borrows (published as trainer.borrowed_by); serving never pauses. Reports append to experiments.jsonl; agent-model-experiments reads live + last ten.", "detail": "CPU battery: one-brick mix experiment diffed exactly [mix], pinned hashes recorded, both arms ran fresh-base; the two eval sets scored identically - CORRECT and a real bench finding, since memory-docs is the render-dialect derivation of memory (same holdout text). Two-brick clone warned with the moved bricks and recorded honestly; unregistered mix dataset refused at authoring; R1 restored. Nit for backlog: a slow first hf load can outlive bootstrap 20s probe and report launch_failed for a service that comes up seconds later. Owner-box owed: real-scale experiment with genuinely diverging arms, borrow observed over a long run.", "tags": "spectrum,s6,bench,recipes", "source": {"doc": "docs/spectrum-s6.md; battery run in session 2026-08-19"}, "confidence": "high", "time": 1787128593601},
{"claim": "S7 landed (claude/spectrum-s7-placement): placement and the external rung. MODEL_PLACEMENT= names GPU roles (serve=/train=), published with honest fallback in /status.placement; the placed train device carries the training-side fresh loads (bench arms, adapter derivations, mints) while the full-rung candidate split and multi-node CPT wait for owner-box proof. MODEL_ENGINE_URL= puts an OpenAI-compatible engine behind the scorer surface: ExternalScorer only transports - score_via builds THE dialect and parses THE answer, so unparseable-escalates applies to remote verdicts unchanged (ruling 8, verified through the wire). The engine holds the weights: solver answers frozen with the hot-swap note, derive/stack/persona paths refuse with reasons, /promote explains that this rung promotes by adapter hot-swap on the engine. No engine install stage: seam now, dependency when a box needs it.", "detail": "CPU battery with a stdlib mock OpenAI server: external:mock-70b served the salience lane (model id discovered from /v1/models), parsed:true on good JSON and 0.5/parsed:false on deliberate garbage, /chat through the engine at 1ms, frozen posture with reason, /promote hot-swap message, degenerate placement published. R1 restored. Nits: a restored user pointer NAME keeps its persisted +lora suffix even when the serving scorer is bare (display drift only); pkill -f matching its own command line kills the invoking shell - use the [b]racket pattern. Owner-box owed: real vLLM behind the URL, split placement on two GPUs, hot-swap wiring.", "tags": "spectrum,s7,placement,engine", "source": {"doc": "docs/spectrum-s7.md; battery run in session 2026-08-19"}, "confidence": "high", "time": 1787129123639},
{"claim": "S8 landed (claude/spectrum-s8-sft) and the spectrum cycle's chartered phases are all built. The SFT gate design (docs/spectrum-s8.md, written before the code per the harvest deferral rule): the SFT gate is the bench's instruments guarding the ENTRY to the ring - subject gain on held-out conversations, the forgetting guard through the anchor, agreement non-regression on held-out pairs (unmeasurable instruments report as such, never silently pass as measured) - and everything after the ring is shipped machinery. The sft-* ring namespace is the safety: cpt-* deltas are by definition already merged into serving and resume re-merges them; sft-* candidates are NOT, resume never touches them, and only the deliberate sft_promote (promote-by-key through the double buffer) puts one on the fast lane for its soak. From there the standard user gate qualifies READY, user_promote advances /chat, persona and the stack re-apply on top.", "detail": "CPU battery: 24-conversation fabricated bank (kind sft, holdout every=5), sft_run rank 8 x 80 steps -> accept (subject 6.20->5.95; std and agree None on this box, reported not-measured), candidate sft-<ts>-voice-1 ringed; restart did NOT auto-merge it; sft_promote flipped to B:hf:tiny-hf+sft-*, soaked under the sft key, standard gate READY, user pointer advanced, /chat served it with persona re-applied (+lora). Reject path: 2-step run failed min_gain, no ring entry. R1 restored. sft_run refuses non-conversation dataset kinds at the command; runs borrow the time-share alongside bench runs. Owner-box owed: the real H1 chat bank on a capable imported base - the quality jump the charter predicted.", "tags": "spectrum,s8,sft", "source": {"doc": "docs/spectrum-s8.md; battery run in session 2026-08-19"}, "confidence": "high", "time": 1787129643863},
{"claim": "dataset_feed IS the feed contract as code (H1c): channels hand JSONL lines; the feeder owns line-hash dedup, append, stream registration, row counts, and the registry.json re-render, and refuses to re-kind an existing bank. curriculum_export's salience-pairs/memory/chat sweeps all call it; H3's why-harvest must too - never inline the plumbing again", "detail": "chat-bank registers as kind sft, holdout_every 5 (ten conversations = the 8/2 corpus sft_run demands). Text replies only; tool_calls transcripts are not SFT-trainable conversations yet.", "tags": "dataset_feed,feed-contract,curriculum_export", "confidence": "high", "time": 1787139770973},
{"claim": "H3 landed: why_harvest (procedural - patches journals + git history into code-why cpt, trailers stripped, idempotent; 1,026 rows on first sweep) and dataset_derive's distill_why (model-driven, 1-25 row spend cap per deliberate run, done-set prevents re-spend, H2 coding context per row, QA rows into code-why-qa sft with generator/src-pin/row-hash provenance, claims via adjudicate for patch rows). The generator-address rule holds: render_dialect dispatches to the service, distill_why runs in the agent library behind ask_llm - where a generator EXECUTES picks the path; which model answers is only a tag", "detail": "Distill quality verified live on the CLAUDECODE arm (4 rows, 0 unparseable, ~12s/row). The adjudicate claim path is code-complete but not yet exercised live: the newest source rows are commit rows (no lib.ctl home), and claims fire only for patch rows - the first deep run will cross them. H5's rumination act is the drive-budgeted caller.", "tags": "harvest,h3,code-why", "confidence": "high", "time": 1787143707461},
{"claim": "H6 landed, closing the harvest cycle: agent-model-harvest_report (claims by domain in-window; memory writes by author FROM THE JOURNALS - entries do not carry authorship, the _patches journal does; act traces; bank rows; capture volume; messages by venue; notions pending; wonderings; context token averages per purpose = the syspack gauge) and the mind tab's harvest card rendering it, verified in the running app via browser. All six phases of docs/harvest-cycle.md are live: the message store, the assembler, the why-harvest + distill, the system sensor + room consolidation + hollis vocabulary, the rumination repertoire, and the gauge cluster", "tags": "harvest,h6,cycle-complete", "confidence": "high", "time": 1787146872644},
{"claim": "The resource map reports gpus:[] on NVIDIA GB10 unified-memory boxes, and host free-RAM there is not the headroom it appears to be.", "detail": "On a GB10 (Ubuntu 24.04, aarch64, driver 580.159.03, CUDA 13.0), agent-model-resources answered gpus:[] via its nvidia-smi fallback while the GPU was present and resident. GB10 answers `Not Supported` for Memory-Usage, so the per-GPU memory parse yields nothing and the whole GPU list comes back empty - the posture solver and ring byte-budget warnings are blind to the only accelerator on the box. Separately, unified memory means GPU allocations come out of the same host pool: a resident vLLM/ray worker held ~105GB visible only in nvidia-smi, so /proc reported 4.5GB available of 124.5GB while summed process RSS was 8.5GB. Free RAM and summed RSS disagreeing by ~100GB is the signature, not a leak.", "tags": "agent.model,resources,gpu,gb10,unified-memory,posture-solver,unreviewed", "confidence": "high", "time": 1787255993905},
{"claim": "The system sensor's disk band is blinded by turning the resident model off: resources() probes df at runtime/agent/model, a directory only agent-model-bootstrap creates.", "detail": "Traced empirically 2026-08-21 with SALIENCE unset and the service down: `df` on /newbound/runtime/agent/model answers 'No such file or directory', so agent-sensor-system_sense emits only mem_avail_pct, load_per_cpu and service_alive - 3 of its 5 band-crossing metrics. Disk headroom is a whole-host fact probed at a model-subsystem path, so a subsystem that is off by default silently removes a host-level sense. Fix direction: probe a path that always exists (the checkout root or runtime/) rather than the bootstrap-created model dir.", "tags": "agent.model,agent.sensor,resources,system_sense,disk,coupling,bug,unreviewed", "confidence": "high", "time": 1787308193380},
{"end": true}]
